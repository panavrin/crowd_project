//////////////////////////////////////////////////////////////////////////
//                                                                      //
// This is a generated file. You can view the original                  //
// source in your browser if your browser supports source maps.         //
// Source maps are supported by all recent versions of Chrome, Safari,  //
// and Firefox, and by Internet Explorer 11.                            //
//                                                                      //
//////////////////////////////////////////////////////////////////////////


(function () {

/* Imports */
var Meteor = Package.meteor.Meteor;
var Template = Package.templating.Template;
var ShareJSConnector = Package['mizzao:sharejs'].ShareJSConnector;
var Blaze = Package.blaze.Blaze;
var UI = Package.blaze.UI;
var Handlebars = Package.blaze.Handlebars;
var Spacebars = Package.spacebars.Spacebars;
var HTML = Package.htmljs.HTML;

/* Package-scope variables */
var __coffeescriptShare;

(function(){

//////////////////////////////////////////////////////////////////////////////////////////////////
//                                                                                              //
// packages/mizzao_sharejs-codemirror/packages/mizzao_sharejs-codemirror.js                     //
//                                                                                              //
//////////////////////////////////////////////////////////////////////////////////////////////////
                                                                                                //
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//                                                                                                                     //
// packages/mizzao:sharejs-codemirror/codemirror/lib/codemirror.js                                                     //
// This file is in bare mode and is not in its own closure.                                                            //
//                                                                                                                     //
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
                                                                                                                       //
// CodeMirror, copyright (c) by Marijn Haverbeke and others                                                            // 1
// Distributed under an MIT license: http://codemirror.net/LICENSE                                                     // 2
                                                                                                                       // 3
// This is CodeMirror (http://codemirror.net), a code editor                                                           // 4
// implemented in JavaScript on top of the browser's DOM.                                                              // 5
//                                                                                                                     // 6
// You can find some technical background for some of the code below                                                   // 7
// at http://marijnhaverbeke.nl/blog/#cm-internals .                                                                   // 8
                                                                                                                       // 9
(function(mod) {                                                                                                       // 10
  if (typeof exports == "object" && typeof module == "object") // CommonJS                                             // 11
    module.exports = mod();                                                                                            // 12
  else if (typeof define == "function" && define.amd) // AMD                                                           // 13
    return define([], mod);                                                                                            // 14
  else // Plain browser env                                                                                            // 15
    this.CodeMirror = mod();                                                                                           // 16
})(function() {                                                                                                        // 17
  "use strict";                                                                                                        // 18
                                                                                                                       // 19
  // BROWSER SNIFFING                                                                                                  // 20
                                                                                                                       // 21
  // Kludges for bugs and behavior differences that can't be feature                                                   // 22
  // detected are enabled based on userAgent etc sniffing.                                                             // 23
                                                                                                                       // 24
  var gecko = /gecko\/\d/i.test(navigator.userAgent);                                                                  // 25
  // ie_uptoN means Internet Explorer version N or lower                                                               // 26
  var ie_upto10 = /MSIE \d/.test(navigator.userAgent);                                                                 // 27
  var ie_11up = /Trident\/(?:[7-9]|\d{2,})\..*rv:(\d+)/.exec(navigator.userAgent);                                     // 28
  var ie = ie_upto10 || ie_11up;                                                                                       // 29
  var ie_version = ie && (ie_upto10 ? document.documentMode || 6 : ie_11up[1]);                                        // 30
  var webkit = /WebKit\//.test(navigator.userAgent);                                                                   // 31
  var qtwebkit = webkit && /Qt\/\d+\.\d+/.test(navigator.userAgent);                                                   // 32
  var chrome = /Chrome\//.test(navigator.userAgent);                                                                   // 33
  var presto = /Opera\//.test(navigator.userAgent);                                                                    // 34
  var safari = /Apple Computer/.test(navigator.vendor);                                                                // 35
  var khtml = /KHTML\//.test(navigator.userAgent);                                                                     // 36
  var mac_geMountainLion = /Mac OS X 1\d\D([8-9]|\d\d)\D/.test(navigator.userAgent);                                   // 37
  var phantom = /PhantomJS/.test(navigator.userAgent);                                                                 // 38
                                                                                                                       // 39
  var ios = /AppleWebKit/.test(navigator.userAgent) && /Mobile\/\w+/.test(navigator.userAgent);                        // 40
  // This is woefully incomplete. Suggestions for alternative methods welcome.                                         // 41
  var mobile = ios || /Android|webOS|BlackBerry|Opera Mini|Opera Mobi|IEMobile/i.test(navigator.userAgent);            // 42
  var mac = ios || /Mac/.test(navigator.platform);                                                                     // 43
  var windows = /win/i.test(navigator.platform);                                                                       // 44
                                                                                                                       // 45
  var presto_version = presto && navigator.userAgent.match(/Version\/(\d*\.\d*)/);                                     // 46
  if (presto_version) presto_version = Number(presto_version[1]);                                                      // 47
  if (presto_version && presto_version >= 15) { presto = false; webkit = true; }                                       // 48
  // Some browsers use the wrong event properties to signal cmd/ctrl on OS X                                           // 49
  var flipCtrlCmd = mac && (qtwebkit || presto && (presto_version == null || presto_version < 12.11));                 // 50
  var captureRightClick = gecko || (ie && ie_version >= 9);                                                            // 51
                                                                                                                       // 52
  // Optimize some code when these features are not used.                                                              // 53
  var sawReadOnlySpans = false, sawCollapsedSpans = false;                                                             // 54
                                                                                                                       // 55
  // EDITOR CONSTRUCTOR                                                                                                // 56
                                                                                                                       // 57
  // A CodeMirror instance represents an editor. This is the object                                                    // 58
  // that user code is usually dealing with.                                                                           // 59
                                                                                                                       // 60
  function CodeMirror(place, options) {                                                                                // 61
    if (!(this instanceof CodeMirror)) return new CodeMirror(place, options);                                          // 62
                                                                                                                       // 63
    this.options = options = options ? copyObj(options) : {};                                                          // 64
    // Determine effective options based on given values and defaults.                                                 // 65
    copyObj(defaults, options, false);                                                                                 // 66
    setGuttersForLineNumbers(options);                                                                                 // 67
                                                                                                                       // 68
    var doc = options.value;                                                                                           // 69
    if (typeof doc == "string") doc = new Doc(doc, options.mode);                                                      // 70
    this.doc = doc;                                                                                                    // 71
                                                                                                                       // 72
    var display = this.display = new Display(place, doc);                                                              // 73
    display.wrapper.CodeMirror = this;                                                                                 // 74
    updateGutters(this);                                                                                               // 75
    themeChanged(this);                                                                                                // 76
    if (options.lineWrapping)                                                                                          // 77
      this.display.wrapper.className += " CodeMirror-wrap";                                                            // 78
    if (options.autofocus && !mobile) focusInput(this);                                                                // 79
    initScrollbars(this);                                                                                              // 80
                                                                                                                       // 81
    this.state = {                                                                                                     // 82
      keyMaps: [],  // stores maps added by addKeyMap                                                                  // 83
      overlays: [], // highlighting overlays, as added by addOverlay                                                   // 84
      modeGen: 0,   // bumped when mode/overlay changes, used to invalidate highlighting info                          // 85
      overwrite: false, focused: false,                                                                                // 86
      suppressEdits: false, // used to disable editing during key handlers when in readOnly mode                       // 87
      pasteIncoming: false, cutIncoming: false, // help recognize paste/cut edits in readInput                         // 88
      draggingText: false,                                                                                             // 89
      highlight: new Delayed(), // stores highlight worker timeout                                                     // 90
      keySeq: null  // Unfinished key sequence                                                                         // 91
    };                                                                                                                 // 92
                                                                                                                       // 93
    // Override magic textarea content restore that IE sometimes does                                                  // 94
    // on our hidden textarea on reload                                                                                // 95
    if (ie && ie_version < 11) setTimeout(bind(resetInput, this, true), 20);                                           // 96
                                                                                                                       // 97
    registerEventHandlers(this);                                                                                       // 98
    ensureGlobalHandlers();                                                                                            // 99
                                                                                                                       // 100
    startOperation(this);                                                                                              // 101
    this.curOp.forceUpdate = true;                                                                                     // 102
    attachDoc(this, doc);                                                                                              // 103
                                                                                                                       // 104
    if ((options.autofocus && !mobile) || activeElt() == display.input)                                                // 105
      setTimeout(bind(onFocus, this), 20);                                                                             // 106
    else                                                                                                               // 107
      onBlur(this);                                                                                                    // 108
                                                                                                                       // 109
    for (var opt in optionHandlers) if (optionHandlers.hasOwnProperty(opt))                                            // 110
      optionHandlers[opt](this, options[opt], Init);                                                                   // 111
    maybeUpdateLineNumberWidth(this);                                                                                  // 112
    for (var i = 0; i < initHooks.length; ++i) initHooks[i](this);                                                     // 113
    endOperation(this);                                                                                                // 114
    // Suppress optimizelegibility in Webkit, since it breaks text                                                     // 115
    // measuring on line wrapping boundaries.                                                                          // 116
    if (webkit && options.lineWrapping &&                                                                              // 117
        getComputedStyle(display.lineDiv).textRendering == "optimizelegibility")                                       // 118
      display.lineDiv.style.textRendering = "auto";                                                                    // 119
  }                                                                                                                    // 120
                                                                                                                       // 121
  // DISPLAY CONSTRUCTOR                                                                                               // 122
                                                                                                                       // 123
  // The display handles the DOM integration, both for input reading                                                   // 124
  // and content drawing. It holds references to DOM nodes and                                                         // 125
  // display-related state.                                                                                            // 126
                                                                                                                       // 127
  function Display(place, doc) {                                                                                       // 128
    var d = this;                                                                                                      // 129
                                                                                                                       // 130
    // The semihidden textarea that is focused when the editor is                                                      // 131
    // focused, and receives input.                                                                                    // 132
    var input = d.input = elt("textarea", null, null, "position: absolute; padding: 0; width: 1px; height: 1em; outline: none");
    // The textarea is kept positioned near the cursor to prevent the                                                  // 134
    // fact that it'll be scrolled into view on input from scrolling                                                   // 135
    // our fake cursor out of view. On webkit, when wrap=off, paste is                                                 // 136
    // very slow. So make the area wide instead.                                                                       // 137
    if (webkit) input.style.width = "1000px";                                                                          // 138
    else input.setAttribute("wrap", "off");                                                                            // 139
    // If border: 0; -- iOS fails to open keyboard (issue #1287)                                                       // 140
    if (ios) input.style.border = "1px solid black";                                                                   // 141
    input.setAttribute("autocorrect", "off"); input.setAttribute("autocapitalize", "off"); input.setAttribute("spellcheck", "false");
                                                                                                                       // 143
    // Wraps and hides input textarea                                                                                  // 144
    d.inputDiv = elt("div", [input], null, "overflow: hidden; position: relative; width: 3px; height: 0px;");          // 145
    // Covers bottom-right square when both scrollbars are present.                                                    // 146
    d.scrollbarFiller = elt("div", null, "CodeMirror-scrollbar-filler");                                               // 147
    d.scrollbarFiller.setAttribute("not-content", "true");                                                             // 148
    // Covers bottom of gutter when coverGutterNextToScrollbar is on                                                   // 149
    // and h scrollbar is present.                                                                                     // 150
    d.gutterFiller = elt("div", null, "CodeMirror-gutter-filler");                                                     // 151
    d.gutterFiller.setAttribute("not-content", "true");                                                                // 152
    // Will contain the actual code, positioned to cover the viewport.                                                 // 153
    d.lineDiv = elt("div", null, "CodeMirror-code");                                                                   // 154
    // Elements are added to these to represent selection and cursors.                                                 // 155
    d.selectionDiv = elt("div", null, null, "position: relative; z-index: 1");                                         // 156
    d.cursorDiv = elt("div", null, "CodeMirror-cursors");                                                              // 157
    // A visibility: hidden element used to find the size of things.                                                   // 158
    d.measure = elt("div", null, "CodeMirror-measure");                                                                // 159
    // When lines outside of the viewport are measured, they are drawn in this.                                        // 160
    d.lineMeasure = elt("div", null, "CodeMirror-measure");                                                            // 161
    // Wraps everything that needs to exist inside the vertically-padded coordinate system                             // 162
    d.lineSpace = elt("div", [d.measure, d.lineMeasure, d.selectionDiv, d.cursorDiv, d.lineDiv],                       // 163
                      null, "position: relative; outline: none");                                                      // 164
    // Moved around its parent to cover visible view.                                                                  // 165
    d.mover = elt("div", [elt("div", [d.lineSpace], "CodeMirror-lines")], null, "position: relative");                 // 166
    // Set to the height of the document, allowing scrolling.                                                          // 167
    d.sizer = elt("div", [d.mover], "CodeMirror-sizer");                                                               // 168
    d.sizerWidth = null;                                                                                               // 169
    // Behavior of elts with overflow: auto and padding is                                                             // 170
    // inconsistent across browsers. This is used to ensure the                                                        // 171
    // scrollable area is big enough.                                                                                  // 172
    d.heightForcer = elt("div", null, null, "position: absolute; height: " + scrollerGap + "px; width: 1px;");         // 173
    // Will contain the gutters, if any.                                                                               // 174
    d.gutters = elt("div", null, "CodeMirror-gutters");                                                                // 175
    d.lineGutter = null;                                                                                               // 176
    // Actual scrollable element.                                                                                      // 177
    d.scroller = elt("div", [d.sizer, d.heightForcer, d.gutters], "CodeMirror-scroll");                                // 178
    d.scroller.setAttribute("tabIndex", "-1");                                                                         // 179
    // The element in which the editor lives.                                                                          // 180
    d.wrapper = elt("div", [d.inputDiv, d.scrollbarFiller, d.gutterFiller, d.scroller], "CodeMirror");                 // 181
                                                                                                                       // 182
    // Work around IE7 z-index bug (not perfect, hence IE7 not really being supported)                                 // 183
    if (ie && ie_version < 8) { d.gutters.style.zIndex = -1; d.scroller.style.paddingRight = 0; }                      // 184
    // Needed to hide big blue blinking cursor on Mobile Safari                                                        // 185
    if (ios) input.style.width = "0px";                                                                                // 186
    if (!webkit) d.scroller.draggable = true;                                                                          // 187
    // Needed to handle Tab key in KHTML                                                                               // 188
    if (khtml) { d.inputDiv.style.height = "1px"; d.inputDiv.style.position = "absolute"; }                            // 189
                                                                                                                       // 190
    if (place) {                                                                                                       // 191
      if (place.appendChild) place.appendChild(d.wrapper);                                                             // 192
      else place(d.wrapper);                                                                                           // 193
    }                                                                                                                  // 194
                                                                                                                       // 195
    // Current rendered range (may be bigger than the view window).                                                    // 196
    d.viewFrom = d.viewTo = doc.first;                                                                                 // 197
    d.reportedViewFrom = d.reportedViewTo = doc.first;                                                                 // 198
    // Information about the rendered lines.                                                                           // 199
    d.view = [];                                                                                                       // 200
    d.renderedView = null;                                                                                             // 201
    // Holds info about a single rendered line when it was rendered                                                    // 202
    // for measurement, while not in view.                                                                             // 203
    d.externalMeasured = null;                                                                                         // 204
    // Empty space (in pixels) above the view                                                                          // 205
    d.viewOffset = 0;                                                                                                  // 206
    d.lastWrapHeight = d.lastWrapWidth = 0;                                                                            // 207
    d.updateLineNumbers = null;                                                                                        // 208
                                                                                                                       // 209
    d.nativeBarWidth = d.barHeight = d.barWidth = 0;                                                                   // 210
    d.scrollbarsClipped = false;                                                                                       // 211
                                                                                                                       // 212
    // Used to only resize the line number gutter when necessary (when                                                 // 213
    // the amount of lines crosses a boundary that makes its width change)                                             // 214
    d.lineNumWidth = d.lineNumInnerWidth = d.lineNumChars = null;                                                      // 215
    // See readInput and resetInput                                                                                    // 216
    d.prevInput = "";                                                                                                  // 217
    // Set to true when a non-horizontal-scrolling line widget is                                                      // 218
    // added. As an optimization, line widget aligning is skipped when                                                 // 219
    // this is false.                                                                                                  // 220
    d.alignWidgets = false;                                                                                            // 221
    // Flag that indicates whether we expect input to appear real soon                                                 // 222
    // now (after some event like 'keypress' or 'input') and are                                                       // 223
    // polling intensively.                                                                                            // 224
    d.pollingFast = false;                                                                                             // 225
    // Self-resetting timeout for the poller                                                                           // 226
    d.poll = new Delayed();                                                                                            // 227
                                                                                                                       // 228
    d.cachedCharWidth = d.cachedTextHeight = d.cachedPaddingH = null;                                                  // 229
                                                                                                                       // 230
    // Tracks when resetInput has punted to just putting a short                                                       // 231
    // string into the textarea instead of the full selection.                                                         // 232
    d.inaccurateSelection = false;                                                                                     // 233
                                                                                                                       // 234
    // Tracks the maximum line length so that the horizontal scrollbar                                                 // 235
    // can be kept static when scrolling.                                                                              // 236
    d.maxLine = null;                                                                                                  // 237
    d.maxLineLength = 0;                                                                                               // 238
    d.maxLineChanged = false;                                                                                          // 239
                                                                                                                       // 240
    // Used for measuring wheel scrolling granularity                                                                  // 241
    d.wheelDX = d.wheelDY = d.wheelStartX = d.wheelStartY = null;                                                      // 242
                                                                                                                       // 243
    // True when shift is held down.                                                                                   // 244
    d.shift = false;                                                                                                   // 245
                                                                                                                       // 246
    // Used to track whether anything happened since the context menu                                                  // 247
    // was opened.                                                                                                     // 248
    d.selForContextMenu = null;                                                                                        // 249
  }                                                                                                                    // 250
                                                                                                                       // 251
  // STATE UPDATES                                                                                                     // 252
                                                                                                                       // 253
  // Used to get the editor into a consistent state again when options change.                                         // 254
                                                                                                                       // 255
  function loadMode(cm) {                                                                                              // 256
    cm.doc.mode = CodeMirror.getMode(cm.options, cm.doc.modeOption);                                                   // 257
    resetModeState(cm);                                                                                                // 258
  }                                                                                                                    // 259
                                                                                                                       // 260
  function resetModeState(cm) {                                                                                        // 261
    cm.doc.iter(function(line) {                                                                                       // 262
      if (line.stateAfter) line.stateAfter = null;                                                                     // 263
      if (line.styles) line.styles = null;                                                                             // 264
    });                                                                                                                // 265
    cm.doc.frontier = cm.doc.first;                                                                                    // 266
    startWorker(cm, 100);                                                                                              // 267
    cm.state.modeGen++;                                                                                                // 268
    if (cm.curOp) regChange(cm);                                                                                       // 269
  }                                                                                                                    // 270
                                                                                                                       // 271
  function wrappingChanged(cm) {                                                                                       // 272
    if (cm.options.lineWrapping) {                                                                                     // 273
      addClass(cm.display.wrapper, "CodeMirror-wrap");                                                                 // 274
      cm.display.sizer.style.minWidth = "";                                                                            // 275
      cm.display.sizerWidth = null;                                                                                    // 276
    } else {                                                                                                           // 277
      rmClass(cm.display.wrapper, "CodeMirror-wrap");                                                                  // 278
      findMaxLine(cm);                                                                                                 // 279
    }                                                                                                                  // 280
    estimateLineHeights(cm);                                                                                           // 281
    regChange(cm);                                                                                                     // 282
    clearCaches(cm);                                                                                                   // 283
    setTimeout(function(){updateScrollbars(cm);}, 100);                                                                // 284
  }                                                                                                                    // 285
                                                                                                                       // 286
  // Returns a function that estimates the height of a line, to use as                                                 // 287
  // first approximation until the line becomes visible (and is thus                                                   // 288
  // properly measurable).                                                                                             // 289
  function estimateHeight(cm) {                                                                                        // 290
    var th = textHeight(cm.display), wrapping = cm.options.lineWrapping;                                               // 291
    var perLine = wrapping && Math.max(5, cm.display.scroller.clientWidth / charWidth(cm.display) - 3);                // 292
    return function(line) {                                                                                            // 293
      if (lineIsHidden(cm.doc, line)) return 0;                                                                        // 294
                                                                                                                       // 295
      var widgetsHeight = 0;                                                                                           // 296
      if (line.widgets) for (var i = 0; i < line.widgets.length; i++) {                                                // 297
        if (line.widgets[i].height) widgetsHeight += line.widgets[i].height;                                           // 298
      }                                                                                                                // 299
                                                                                                                       // 300
      if (wrapping)                                                                                                    // 301
        return widgetsHeight + (Math.ceil(line.text.length / perLine) || 1) * th;                                      // 302
      else                                                                                                             // 303
        return widgetsHeight + th;                                                                                     // 304
    };                                                                                                                 // 305
  }                                                                                                                    // 306
                                                                                                                       // 307
  function estimateLineHeights(cm) {                                                                                   // 308
    var doc = cm.doc, est = estimateHeight(cm);                                                                        // 309
    doc.iter(function(line) {                                                                                          // 310
      var estHeight = est(line);                                                                                       // 311
      if (estHeight != line.height) updateLineHeight(line, estHeight);                                                 // 312
    });                                                                                                                // 313
  }                                                                                                                    // 314
                                                                                                                       // 315
  function themeChanged(cm) {                                                                                          // 316
    cm.display.wrapper.className = cm.display.wrapper.className.replace(/\s*cm-s-\S+/g, "") +                          // 317
      cm.options.theme.replace(/(^|\s)\s*/g, " cm-s-");                                                                // 318
    clearCaches(cm);                                                                                                   // 319
  }                                                                                                                    // 320
                                                                                                                       // 321
  function guttersChanged(cm) {                                                                                        // 322
    updateGutters(cm);                                                                                                 // 323
    regChange(cm);                                                                                                     // 324
    setTimeout(function(){alignHorizontally(cm);}, 20);                                                                // 325
  }                                                                                                                    // 326
                                                                                                                       // 327
  // Rebuild the gutter elements, ensure the margin to the left of the                                                 // 328
  // code matches their width.                                                                                         // 329
  function updateGutters(cm) {                                                                                         // 330
    var gutters = cm.display.gutters, specs = cm.options.gutters;                                                      // 331
    removeChildren(gutters);                                                                                           // 332
    for (var i = 0; i < specs.length; ++i) {                                                                           // 333
      var gutterClass = specs[i];                                                                                      // 334
      var gElt = gutters.appendChild(elt("div", null, "CodeMirror-gutter " + gutterClass));                            // 335
      if (gutterClass == "CodeMirror-linenumbers") {                                                                   // 336
        cm.display.lineGutter = gElt;                                                                                  // 337
        gElt.style.width = (cm.display.lineNumWidth || 1) + "px";                                                      // 338
      }                                                                                                                // 339
    }                                                                                                                  // 340
    gutters.style.display = i ? "" : "none";                                                                           // 341
    updateGutterSpace(cm);                                                                                             // 342
  }                                                                                                                    // 343
                                                                                                                       // 344
  function updateGutterSpace(cm) {                                                                                     // 345
    var width = cm.display.gutters.offsetWidth;                                                                        // 346
    cm.display.sizer.style.marginLeft = width + "px";                                                                  // 347
  }                                                                                                                    // 348
                                                                                                                       // 349
  // Compute the character length of a line, taking into account                                                       // 350
  // collapsed ranges (see markText) that might hide parts, and join                                                   // 351
  // other lines onto it.                                                                                              // 352
  function lineLength(line) {                                                                                          // 353
    if (line.height == 0) return 0;                                                                                    // 354
    var len = line.text.length, merged, cur = line;                                                                    // 355
    while (merged = collapsedSpanAtStart(cur)) {                                                                       // 356
      var found = merged.find(0, true);                                                                                // 357
      cur = found.from.line;                                                                                           // 358
      len += found.from.ch - found.to.ch;                                                                              // 359
    }                                                                                                                  // 360
    cur = line;                                                                                                        // 361
    while (merged = collapsedSpanAtEnd(cur)) {                                                                         // 362
      var found = merged.find(0, true);                                                                                // 363
      len -= cur.text.length - found.from.ch;                                                                          // 364
      cur = found.to.line;                                                                                             // 365
      len += cur.text.length - found.to.ch;                                                                            // 366
    }                                                                                                                  // 367
    return len;                                                                                                        // 368
  }                                                                                                                    // 369
                                                                                                                       // 370
  // Find the longest line in the document.                                                                            // 371
  function findMaxLine(cm) {                                                                                           // 372
    var d = cm.display, doc = cm.doc;                                                                                  // 373
    d.maxLine = getLine(doc, doc.first);                                                                               // 374
    d.maxLineLength = lineLength(d.maxLine);                                                                           // 375
    d.maxLineChanged = true;                                                                                           // 376
    doc.iter(function(line) {                                                                                          // 377
      var len = lineLength(line);                                                                                      // 378
      if (len > d.maxLineLength) {                                                                                     // 379
        d.maxLineLength = len;                                                                                         // 380
        d.maxLine = line;                                                                                              // 381
      }                                                                                                                // 382
    });                                                                                                                // 383
  }                                                                                                                    // 384
                                                                                                                       // 385
  // Make sure the gutters options contains the element                                                                // 386
  // "CodeMirror-linenumbers" when the lineNumbers option is true.                                                     // 387
  function setGuttersForLineNumbers(options) {                                                                         // 388
    var found = indexOf(options.gutters, "CodeMirror-linenumbers");                                                    // 389
    if (found == -1 && options.lineNumbers) {                                                                          // 390
      options.gutters = options.gutters.concat(["CodeMirror-linenumbers"]);                                            // 391
    } else if (found > -1 && !options.lineNumbers) {                                                                   // 392
      options.gutters = options.gutters.slice(0);                                                                      // 393
      options.gutters.splice(found, 1);                                                                                // 394
    }                                                                                                                  // 395
  }                                                                                                                    // 396
                                                                                                                       // 397
  // SCROLLBARS                                                                                                        // 398
                                                                                                                       // 399
  // Prepare DOM reads needed to update the scrollbars. Done in one                                                    // 400
  // shot to minimize update/measure roundtrips.                                                                       // 401
  function measureForScrollbars(cm) {                                                                                  // 402
    var d = cm.display, gutterW = d.gutters.offsetWidth;                                                               // 403
    var docH = Math.round(cm.doc.height + paddingVert(cm.display));                                                    // 404
    return {                                                                                                           // 405
      clientHeight: d.scroller.clientHeight,                                                                           // 406
      viewHeight: d.wrapper.clientHeight,                                                                              // 407
      scrollWidth: d.scroller.scrollWidth, clientWidth: d.scroller.clientWidth,                                        // 408
      viewWidth: d.wrapper.clientWidth,                                                                                // 409
      barLeft: cm.options.fixedGutter ? gutterW : 0,                                                                   // 410
      docHeight: docH,                                                                                                 // 411
      scrollHeight: docH + scrollGap(cm) + d.barHeight,                                                                // 412
      nativeBarWidth: d.nativeBarWidth,                                                                                // 413
      gutterWidth: gutterW                                                                                             // 414
    };                                                                                                                 // 415
  }                                                                                                                    // 416
                                                                                                                       // 417
  function NativeScrollbars(place, scroll, cm) {                                                                       // 418
    this.cm = cm;                                                                                                      // 419
    var vert = this.vert = elt("div", [elt("div", null, null, "min-width: 1px")], "CodeMirror-vscrollbar");            // 420
    var horiz = this.horiz = elt("div", [elt("div", null, null, "height: 100%; min-height: 1px")], "CodeMirror-hscrollbar");
    place(vert); place(horiz);                                                                                         // 422
                                                                                                                       // 423
    on(vert, "scroll", function() {                                                                                    // 424
      if (vert.clientHeight) scroll(vert.scrollTop, "vertical");                                                       // 425
    });                                                                                                                // 426
    on(horiz, "scroll", function() {                                                                                   // 427
      if (horiz.clientWidth) scroll(horiz.scrollLeft, "horizontal");                                                   // 428
    });                                                                                                                // 429
                                                                                                                       // 430
    this.checkedOverlay = false;                                                                                       // 431
    // Need to set a minimum width to see the scrollbar on IE7 (but must not set it on IE8).                           // 432
    if (ie && ie_version < 8) this.horiz.style.minHeight = this.vert.style.minWidth = "18px";                          // 433
  }                                                                                                                    // 434
                                                                                                                       // 435
  NativeScrollbars.prototype = copyObj({                                                                               // 436
    update: function(measure) {                                                                                        // 437
      var needsH = measure.scrollWidth > measure.clientWidth + 1;                                                      // 438
      var needsV = measure.scrollHeight > measure.clientHeight + 1;                                                    // 439
      var sWidth = measure.nativeBarWidth;                                                                             // 440
                                                                                                                       // 441
      if (needsV) {                                                                                                    // 442
        this.vert.style.display = "block";                                                                             // 443
        this.vert.style.bottom = needsH ? sWidth + "px" : "0";                                                         // 444
        var totalHeight = measure.viewHeight - (needsH ? sWidth : 0);                                                  // 445
        // A bug in IE8 can cause this value to be negative, so guard it.                                              // 446
        this.vert.firstChild.style.height =                                                                            // 447
          Math.max(0, measure.scrollHeight - measure.clientHeight + totalHeight) + "px";                               // 448
      } else {                                                                                                         // 449
        this.vert.style.display = "";                                                                                  // 450
        this.vert.firstChild.style.height = "0";                                                                       // 451
      }                                                                                                                // 452
                                                                                                                       // 453
      if (needsH) {                                                                                                    // 454
        this.horiz.style.display = "block";                                                                            // 455
        this.horiz.style.right = needsV ? sWidth + "px" : "0";                                                         // 456
        this.horiz.style.left = measure.barLeft + "px";                                                                // 457
        var totalWidth = measure.viewWidth - measure.barLeft - (needsV ? sWidth : 0);                                  // 458
        this.horiz.firstChild.style.width =                                                                            // 459
          (measure.scrollWidth - measure.clientWidth + totalWidth) + "px";                                             // 460
      } else {                                                                                                         // 461
        this.horiz.style.display = "";                                                                                 // 462
        this.horiz.firstChild.style.width = "0";                                                                       // 463
      }                                                                                                                // 464
                                                                                                                       // 465
      if (!this.checkedOverlay && measure.clientHeight > 0) {                                                          // 466
        if (sWidth == 0) this.overlayHack();                                                                           // 467
        this.checkedOverlay = true;                                                                                    // 468
      }                                                                                                                // 469
                                                                                                                       // 470
      return {right: needsV ? sWidth : 0, bottom: needsH ? sWidth : 0};                                                // 471
    },                                                                                                                 // 472
    setScrollLeft: function(pos) {                                                                                     // 473
      if (this.horiz.scrollLeft != pos) this.horiz.scrollLeft = pos;                                                   // 474
    },                                                                                                                 // 475
    setScrollTop: function(pos) {                                                                                      // 476
      if (this.vert.scrollTop != pos) this.vert.scrollTop = pos;                                                       // 477
    },                                                                                                                 // 478
    overlayHack: function() {                                                                                          // 479
      var w = mac && !mac_geMountainLion ? "12px" : "18px";                                                            // 480
      this.horiz.style.minHeight = this.vert.style.minWidth = w;                                                       // 481
      var self = this;                                                                                                 // 482
      var barMouseDown = function(e) {                                                                                 // 483
        if (e_target(e) != self.vert && e_target(e) != self.horiz)                                                     // 484
          operation(self.cm, onMouseDown)(e);                                                                          // 485
      };                                                                                                               // 486
      on(this.vert, "mousedown", barMouseDown);                                                                        // 487
      on(this.horiz, "mousedown", barMouseDown);                                                                       // 488
    },                                                                                                                 // 489
    clear: function() {                                                                                                // 490
      var parent = this.horiz.parentNode;                                                                              // 491
      parent.removeChild(this.horiz);                                                                                  // 492
      parent.removeChild(this.vert);                                                                                   // 493
    }                                                                                                                  // 494
  }, NativeScrollbars.prototype);                                                                                      // 495
                                                                                                                       // 496
  function NullScrollbars() {}                                                                                         // 497
                                                                                                                       // 498
  NullScrollbars.prototype = copyObj({                                                                                 // 499
    update: function() { return {bottom: 0, right: 0}; },                                                              // 500
    setScrollLeft: function() {},                                                                                      // 501
    setScrollTop: function() {},                                                                                       // 502
    clear: function() {}                                                                                               // 503
  }, NullScrollbars.prototype);                                                                                        // 504
                                                                                                                       // 505
  CodeMirror.scrollbarModel = {"native": NativeScrollbars, "null": NullScrollbars};                                    // 506
                                                                                                                       // 507
  function initScrollbars(cm) {                                                                                        // 508
    if (cm.display.scrollbars) {                                                                                       // 509
      cm.display.scrollbars.clear();                                                                                   // 510
      if (cm.display.scrollbars.addClass)                                                                              // 511
        rmClass(cm.display.wrapper, cm.display.scrollbars.addClass);                                                   // 512
    }                                                                                                                  // 513
                                                                                                                       // 514
    cm.display.scrollbars = new CodeMirror.scrollbarModel[cm.options.scrollbarStyle](function(node) {                  // 515
      cm.display.wrapper.insertBefore(node, cm.display.scrollbarFiller);                                               // 516
      on(node, "mousedown", function() {                                                                               // 517
        if (cm.state.focused) setTimeout(bind(focusInput, cm), 0);                                                     // 518
      });                                                                                                              // 519
      node.setAttribute("not-content", "true");                                                                        // 520
    }, function(pos, axis) {                                                                                           // 521
      if (axis == "horizontal") setScrollLeft(cm, pos);                                                                // 522
      else setScrollTop(cm, pos);                                                                                      // 523
    }, cm);                                                                                                            // 524
    if (cm.display.scrollbars.addClass)                                                                                // 525
      addClass(cm.display.wrapper, cm.display.scrollbars.addClass);                                                    // 526
  }                                                                                                                    // 527
                                                                                                                       // 528
  function updateScrollbars(cm, measure) {                                                                             // 529
    if (!measure) measure = measureForScrollbars(cm);                                                                  // 530
    var startWidth = cm.display.barWidth, startHeight = cm.display.barHeight;                                          // 531
    updateScrollbarsInner(cm, measure);                                                                                // 532
    for (var i = 0; i < 4 && startWidth != cm.display.barWidth || startHeight != cm.display.barHeight; i++) {          // 533
      if (startWidth != cm.display.barWidth && cm.options.lineWrapping)                                                // 534
        updateHeightsInViewport(cm);                                                                                   // 535
      updateScrollbarsInner(cm, measureForScrollbars(cm));                                                             // 536
      startWidth = cm.display.barWidth; startHeight = cm.display.barHeight;                                            // 537
    }                                                                                                                  // 538
  }                                                                                                                    // 539
                                                                                                                       // 540
  // Re-synchronize the fake scrollbars with the actual size of the                                                    // 541
  // content.                                                                                                          // 542
  function updateScrollbarsInner(cm, measure) {                                                                        // 543
    var d = cm.display;                                                                                                // 544
    var sizes = d.scrollbars.update(measure);                                                                          // 545
                                                                                                                       // 546
    d.sizer.style.paddingRight = (d.barWidth = sizes.right) + "px";                                                    // 547
    d.sizer.style.paddingBottom = (d.barHeight = sizes.bottom) + "px";                                                 // 548
                                                                                                                       // 549
    if (sizes.right && sizes.bottom) {                                                                                 // 550
      d.scrollbarFiller.style.display = "block";                                                                       // 551
      d.scrollbarFiller.style.height = sizes.bottom + "px";                                                            // 552
      d.scrollbarFiller.style.width = sizes.right + "px";                                                              // 553
    } else d.scrollbarFiller.style.display = "";                                                                       // 554
    if (sizes.bottom && cm.options.coverGutterNextToScrollbar && cm.options.fixedGutter) {                             // 555
      d.gutterFiller.style.display = "block";                                                                          // 556
      d.gutterFiller.style.height = sizes.bottom + "px";                                                               // 557
      d.gutterFiller.style.width = measure.gutterWidth + "px";                                                         // 558
    } else d.gutterFiller.style.display = "";                                                                          // 559
  }                                                                                                                    // 560
                                                                                                                       // 561
  // Compute the lines that are visible in a given viewport (defaults                                                  // 562
  // the the current scroll position). viewport may contain top,                                                       // 563
  // height, and ensure (see op.scrollToPos) properties.                                                               // 564
  function visibleLines(display, doc, viewport) {                                                                      // 565
    var top = viewport && viewport.top != null ? Math.max(0, viewport.top) : display.scroller.scrollTop;               // 566
    top = Math.floor(top - paddingTop(display));                                                                       // 567
    var bottom = viewport && viewport.bottom != null ? viewport.bottom : top + display.wrapper.clientHeight;           // 568
                                                                                                                       // 569
    var from = lineAtHeight(doc, top), to = lineAtHeight(doc, bottom);                                                 // 570
    // Ensure is a {from: {line, ch}, to: {line, ch}} object, and                                                      // 571
    // forces those lines into the viewport (if possible).                                                             // 572
    if (viewport && viewport.ensure) {                                                                                 // 573
      var ensureFrom = viewport.ensure.from.line, ensureTo = viewport.ensure.to.line;                                  // 574
      if (ensureFrom < from) {                                                                                         // 575
        from = ensureFrom;                                                                                             // 576
        to = lineAtHeight(doc, heightAtLine(getLine(doc, ensureFrom)) + display.wrapper.clientHeight);                 // 577
      } else if (Math.min(ensureTo, doc.lastLine()) >= to) {                                                           // 578
        from = lineAtHeight(doc, heightAtLine(getLine(doc, ensureTo)) - display.wrapper.clientHeight);                 // 579
        to = ensureTo;                                                                                                 // 580
      }                                                                                                                // 581
    }                                                                                                                  // 582
    return {from: from, to: Math.max(to, from + 1)};                                                                   // 583
  }                                                                                                                    // 584
                                                                                                                       // 585
  // LINE NUMBERS                                                                                                      // 586
                                                                                                                       // 587
  // Re-align line numbers and gutter marks to compensate for                                                          // 588
  // horizontal scrolling.                                                                                             // 589
  function alignHorizontally(cm) {                                                                                     // 590
    var display = cm.display, view = display.view;                                                                     // 591
    if (!display.alignWidgets && (!display.gutters.firstChild || !cm.options.fixedGutter)) return;                     // 592
    var comp = compensateForHScroll(display) - display.scroller.scrollLeft + cm.doc.scrollLeft;                        // 593
    var gutterW = display.gutters.offsetWidth, left = comp + "px";                                                     // 594
    for (var i = 0; i < view.length; i++) if (!view[i].hidden) {                                                       // 595
      if (cm.options.fixedGutter && view[i].gutter)                                                                    // 596
        view[i].gutter.style.left = left;                                                                              // 597
      var align = view[i].alignable;                                                                                   // 598
      if (align) for (var j = 0; j < align.length; j++)                                                                // 599
        align[j].style.left = left;                                                                                    // 600
    }                                                                                                                  // 601
    if (cm.options.fixedGutter)                                                                                        // 602
      display.gutters.style.left = (comp + gutterW) + "px";                                                            // 603
  }                                                                                                                    // 604
                                                                                                                       // 605
  // Used to ensure that the line number gutter is still the right                                                     // 606
  // size for the current document size. Returns true when an update                                                   // 607
  // is needed.                                                                                                        // 608
  function maybeUpdateLineNumberWidth(cm) {                                                                            // 609
    if (!cm.options.lineNumbers) return false;                                                                         // 610
    var doc = cm.doc, last = lineNumberFor(cm.options, doc.first + doc.size - 1), display = cm.display;                // 611
    if (last.length != display.lineNumChars) {                                                                         // 612
      var test = display.measure.appendChild(elt("div", [elt("div", last)],                                            // 613
                                                 "CodeMirror-linenumber CodeMirror-gutter-elt"));                      // 614
      var innerW = test.firstChild.offsetWidth, padding = test.offsetWidth - innerW;                                   // 615
      display.lineGutter.style.width = "";                                                                             // 616
      display.lineNumInnerWidth = Math.max(innerW, display.lineGutter.offsetWidth - padding);                          // 617
      display.lineNumWidth = display.lineNumInnerWidth + padding;                                                      // 618
      display.lineNumChars = display.lineNumInnerWidth ? last.length : -1;                                             // 619
      display.lineGutter.style.width = display.lineNumWidth + "px";                                                    // 620
      updateGutterSpace(cm);                                                                                           // 621
      return true;                                                                                                     // 622
    }                                                                                                                  // 623
    return false;                                                                                                      // 624
  }                                                                                                                    // 625
                                                                                                                       // 626
  function lineNumberFor(options, i) {                                                                                 // 627
    return String(options.lineNumberFormatter(i + options.firstLineNumber));                                           // 628
  }                                                                                                                    // 629
                                                                                                                       // 630
  // Computes display.scroller.scrollLeft + display.gutters.offsetWidth,                                               // 631
  // but using getBoundingClientRect to get a sub-pixel-accurate                                                       // 632
  // result.                                                                                                           // 633
  function compensateForHScroll(display) {                                                                             // 634
    return display.scroller.getBoundingClientRect().left - display.sizer.getBoundingClientRect().left;                 // 635
  }                                                                                                                    // 636
                                                                                                                       // 637
  // DISPLAY DRAWING                                                                                                   // 638
                                                                                                                       // 639
  function DisplayUpdate(cm, viewport, force) {                                                                        // 640
    var display = cm.display;                                                                                          // 641
                                                                                                                       // 642
    this.viewport = viewport;                                                                                          // 643
    // Store some values that we'll need later (but don't want to force a relayout for)                                // 644
    this.visible = visibleLines(display, cm.doc, viewport);                                                            // 645
    this.editorIsHidden = !display.wrapper.offsetWidth;                                                                // 646
    this.wrapperHeight = display.wrapper.clientHeight;                                                                 // 647
    this.wrapperWidth = display.wrapper.clientWidth;                                                                   // 648
    this.oldDisplayWidth = displayWidth(cm);                                                                           // 649
    this.force = force;                                                                                                // 650
    this.dims = getDimensions(cm);                                                                                     // 651
  }                                                                                                                    // 652
                                                                                                                       // 653
  function maybeClipScrollbars(cm) {                                                                                   // 654
    var display = cm.display;                                                                                          // 655
    if (!display.scrollbarsClipped && display.scroller.offsetWidth) {                                                  // 656
      display.nativeBarWidth = display.scroller.offsetWidth - display.scroller.clientWidth;                            // 657
      display.heightForcer.style.height = scrollGap(cm) + "px";                                                        // 658
      display.sizer.style.marginBottom = -display.nativeBarWidth + "px";                                               // 659
      display.sizer.style.borderRightWidth = scrollGap(cm) + "px";                                                     // 660
      display.scrollbarsClipped = true;                                                                                // 661
    }                                                                                                                  // 662
  }                                                                                                                    // 663
                                                                                                                       // 664
  // Does the actual updating of the line display. Bails out                                                           // 665
  // (returning false) when there is nothing to be done and forced is                                                  // 666
  // false.                                                                                                            // 667
  function updateDisplayIfNeeded(cm, update) {                                                                         // 668
    var display = cm.display, doc = cm.doc;                                                                            // 669
                                                                                                                       // 670
    if (update.editorIsHidden) {                                                                                       // 671
      resetView(cm);                                                                                                   // 672
      return false;                                                                                                    // 673
    }                                                                                                                  // 674
                                                                                                                       // 675
    // Bail out if the visible area is already rendered and nothing changed.                                           // 676
    if (!update.force &&                                                                                               // 677
        update.visible.from >= display.viewFrom && update.visible.to <= display.viewTo &&                              // 678
        (display.updateLineNumbers == null || display.updateLineNumbers >= display.viewTo) &&                          // 679
        display.renderedView == display.view && countDirtyView(cm) == 0)                                               // 680
      return false;                                                                                                    // 681
                                                                                                                       // 682
    if (maybeUpdateLineNumberWidth(cm)) {                                                                              // 683
      resetView(cm);                                                                                                   // 684
      update.dims = getDimensions(cm);                                                                                 // 685
    }                                                                                                                  // 686
                                                                                                                       // 687
    // Compute a suitable new viewport (from & to)                                                                     // 688
    var end = doc.first + doc.size;                                                                                    // 689
    var from = Math.max(update.visible.from - cm.options.viewportMargin, doc.first);                                   // 690
    var to = Math.min(end, update.visible.to + cm.options.viewportMargin);                                             // 691
    if (display.viewFrom < from && from - display.viewFrom < 20) from = Math.max(doc.first, display.viewFrom);         // 692
    if (display.viewTo > to && display.viewTo - to < 20) to = Math.min(end, display.viewTo);                           // 693
    if (sawCollapsedSpans) {                                                                                           // 694
      from = visualLineNo(cm.doc, from);                                                                               // 695
      to = visualLineEndNo(cm.doc, to);                                                                                // 696
    }                                                                                                                  // 697
                                                                                                                       // 698
    var different = from != display.viewFrom || to != display.viewTo ||                                                // 699
      display.lastWrapHeight != update.wrapperHeight || display.lastWrapWidth != update.wrapperWidth;                  // 700
    adjustView(cm, from, to);                                                                                          // 701
                                                                                                                       // 702
    display.viewOffset = heightAtLine(getLine(cm.doc, display.viewFrom));                                              // 703
    // Position the mover div to align with the current scroll position                                                // 704
    cm.display.mover.style.top = display.viewOffset + "px";                                                            // 705
                                                                                                                       // 706
    var toUpdate = countDirtyView(cm);                                                                                 // 707
    if (!different && toUpdate == 0 && !update.force && display.renderedView == display.view &&                        // 708
        (display.updateLineNumbers == null || display.updateLineNumbers >= display.viewTo))                            // 709
      return false;                                                                                                    // 710
                                                                                                                       // 711
    // For big changes, we hide the enclosing element during the                                                       // 712
    // update, since that speeds up the operations on most browsers.                                                   // 713
    var focused = activeElt();                                                                                         // 714
    if (toUpdate > 4) display.lineDiv.style.display = "none";                                                          // 715
    patchDisplay(cm, display.updateLineNumbers, update.dims);                                                          // 716
    if (toUpdate > 4) display.lineDiv.style.display = "";                                                              // 717
    display.renderedView = display.view;                                                                               // 718
    // There might have been a widget with a focused element that got                                                  // 719
    // hidden or updated, if so re-focus it.                                                                           // 720
    if (focused && activeElt() != focused && focused.offsetHeight) focused.focus();                                    // 721
                                                                                                                       // 722
    // Prevent selection and cursors from interfering with the scroll                                                  // 723
    // width and height.                                                                                               // 724
    removeChildren(display.cursorDiv);                                                                                 // 725
    removeChildren(display.selectionDiv);                                                                              // 726
    display.gutters.style.height = 0;                                                                                  // 727
                                                                                                                       // 728
    if (different) {                                                                                                   // 729
      display.lastWrapHeight = update.wrapperHeight;                                                                   // 730
      display.lastWrapWidth = update.wrapperWidth;                                                                     // 731
      startWorker(cm, 400);                                                                                            // 732
    }                                                                                                                  // 733
                                                                                                                       // 734
    display.updateLineNumbers = null;                                                                                  // 735
                                                                                                                       // 736
    return true;                                                                                                       // 737
  }                                                                                                                    // 738
                                                                                                                       // 739
  function postUpdateDisplay(cm, update) {                                                                             // 740
    var force = update.force, viewport = update.viewport;                                                              // 741
    for (var first = true;; first = false) {                                                                           // 742
      if (first && cm.options.lineWrapping && update.oldDisplayWidth != displayWidth(cm)) {                            // 743
        force = true;                                                                                                  // 744
      } else {                                                                                                         // 745
        force = false;                                                                                                 // 746
        // Clip forced viewport to actual scrollable area.                                                             // 747
        if (viewport && viewport.top != null)                                                                          // 748
          viewport = {top: Math.min(cm.doc.height + paddingVert(cm.display) - displayHeight(cm), viewport.top)};       // 749
        // Updated line heights might result in the drawn area not                                                     // 750
        // actually covering the viewport. Keep looping until it does.                                                 // 751
        update.visible = visibleLines(cm.display, cm.doc, viewport);                                                   // 752
        if (update.visible.from >= cm.display.viewFrom && update.visible.to <= cm.display.viewTo)                      // 753
          break;                                                                                                       // 754
      }                                                                                                                // 755
      if (!updateDisplayIfNeeded(cm, update)) break;                                                                   // 756
      updateHeightsInViewport(cm);                                                                                     // 757
      var barMeasure = measureForScrollbars(cm);                                                                       // 758
      updateSelection(cm);                                                                                             // 759
      setDocumentHeight(cm, barMeasure);                                                                               // 760
      updateScrollbars(cm, barMeasure);                                                                                // 761
    }                                                                                                                  // 762
                                                                                                                       // 763
    signalLater(cm, "update", cm);                                                                                     // 764
    if (cm.display.viewFrom != cm.display.reportedViewFrom || cm.display.viewTo != cm.display.reportedViewTo) {        // 765
      signalLater(cm, "viewportChange", cm, cm.display.viewFrom, cm.display.viewTo);                                   // 766
      cm.display.reportedViewFrom = cm.display.viewFrom; cm.display.reportedViewTo = cm.display.viewTo;                // 767
    }                                                                                                                  // 768
  }                                                                                                                    // 769
                                                                                                                       // 770
  function updateDisplaySimple(cm, viewport) {                                                                         // 771
    var update = new DisplayUpdate(cm, viewport);                                                                      // 772
    if (updateDisplayIfNeeded(cm, update)) {                                                                           // 773
      updateHeightsInViewport(cm);                                                                                     // 774
      postUpdateDisplay(cm, update);                                                                                   // 775
      var barMeasure = measureForScrollbars(cm);                                                                       // 776
      updateSelection(cm);                                                                                             // 777
      setDocumentHeight(cm, barMeasure);                                                                               // 778
      updateScrollbars(cm, barMeasure);                                                                                // 779
    }                                                                                                                  // 780
  }                                                                                                                    // 781
                                                                                                                       // 782
  function setDocumentHeight(cm, measure) {                                                                            // 783
    cm.display.sizer.style.minHeight = measure.docHeight + "px";                                                       // 784
    var total = measure.docHeight + cm.display.barHeight;                                                              // 785
    cm.display.heightForcer.style.top = total + "px";                                                                  // 786
    cm.display.gutters.style.height = Math.max(total + scrollGap(cm), measure.clientHeight) + "px";                    // 787
  }                                                                                                                    // 788
                                                                                                                       // 789
  // Read the actual heights of the rendered lines, and update their                                                   // 790
  // stored heights to match.                                                                                          // 791
  function updateHeightsInViewport(cm) {                                                                               // 792
    var display = cm.display;                                                                                          // 793
    var prevBottom = display.lineDiv.offsetTop;                                                                        // 794
    for (var i = 0; i < display.view.length; i++) {                                                                    // 795
      var cur = display.view[i], height;                                                                               // 796
      if (cur.hidden) continue;                                                                                        // 797
      if (ie && ie_version < 8) {                                                                                      // 798
        var bot = cur.node.offsetTop + cur.node.offsetHeight;                                                          // 799
        height = bot - prevBottom;                                                                                     // 800
        prevBottom = bot;                                                                                              // 801
      } else {                                                                                                         // 802
        var box = cur.node.getBoundingClientRect();                                                                    // 803
        height = box.bottom - box.top;                                                                                 // 804
      }                                                                                                                // 805
      var diff = cur.line.height - height;                                                                             // 806
      if (height < 2) height = textHeight(display);                                                                    // 807
      if (diff > .001 || diff < -.001) {                                                                               // 808
        updateLineHeight(cur.line, height);                                                                            // 809
        updateWidgetHeight(cur.line);                                                                                  // 810
        if (cur.rest) for (var j = 0; j < cur.rest.length; j++)                                                        // 811
          updateWidgetHeight(cur.rest[j]);                                                                             // 812
      }                                                                                                                // 813
    }                                                                                                                  // 814
  }                                                                                                                    // 815
                                                                                                                       // 816
  // Read and store the height of line widgets associated with the                                                     // 817
  // given line.                                                                                                       // 818
  function updateWidgetHeight(line) {                                                                                  // 819
    if (line.widgets) for (var i = 0; i < line.widgets.length; ++i)                                                    // 820
      line.widgets[i].height = line.widgets[i].node.offsetHeight;                                                      // 821
  }                                                                                                                    // 822
                                                                                                                       // 823
  // Do a bulk-read of the DOM positions and sizes needed to draw the                                                  // 824
  // view, so that we don't interleave reading and writing to the DOM.                                                 // 825
  function getDimensions(cm) {                                                                                         // 826
    var d = cm.display, left = {}, width = {};                                                                         // 827
    var gutterLeft = d.gutters.clientLeft;                                                                             // 828
    for (var n = d.gutters.firstChild, i = 0; n; n = n.nextSibling, ++i) {                                             // 829
      left[cm.options.gutters[i]] = n.offsetLeft + n.clientLeft + gutterLeft;                                          // 830
      width[cm.options.gutters[i]] = n.clientWidth;                                                                    // 831
    }                                                                                                                  // 832
    return {fixedPos: compensateForHScroll(d),                                                                         // 833
            gutterTotalWidth: d.gutters.offsetWidth,                                                                   // 834
            gutterLeft: left,                                                                                          // 835
            gutterWidth: width,                                                                                        // 836
            wrapperWidth: d.wrapper.clientWidth};                                                                      // 837
  }                                                                                                                    // 838
                                                                                                                       // 839
  // Sync the actual display DOM structure with display.view, removing                                                 // 840
  // nodes for lines that are no longer in view, and creating the ones                                                 // 841
  // that are not there yet, and updating the ones that are out of                                                     // 842
  // date.                                                                                                             // 843
  function patchDisplay(cm, updateNumbersFrom, dims) {                                                                 // 844
    var display = cm.display, lineNumbers = cm.options.lineNumbers;                                                    // 845
    var container = display.lineDiv, cur = container.firstChild;                                                       // 846
                                                                                                                       // 847
    function rm(node) {                                                                                                // 848
      var next = node.nextSibling;                                                                                     // 849
      // Works around a throw-scroll bug in OS X Webkit                                                                // 850
      if (webkit && mac && cm.display.currentWheelTarget == node)                                                      // 851
        node.style.display = "none";                                                                                   // 852
      else                                                                                                             // 853
        node.parentNode.removeChild(node);                                                                             // 854
      return next;                                                                                                     // 855
    }                                                                                                                  // 856
                                                                                                                       // 857
    var view = display.view, lineN = display.viewFrom;                                                                 // 858
    // Loop over the elements in the view, syncing cur (the DOM nodes                                                  // 859
    // in display.lineDiv) with the view as we go.                                                                     // 860
    for (var i = 0; i < view.length; i++) {                                                                            // 861
      var lineView = view[i];                                                                                          // 862
      if (lineView.hidden) {                                                                                           // 863
      } else if (!lineView.node) { // Not drawn yet                                                                    // 864
        var node = buildLineElement(cm, lineView, lineN, dims);                                                        // 865
        container.insertBefore(node, cur);                                                                             // 866
      } else { // Already drawn                                                                                        // 867
        while (cur != lineView.node) cur = rm(cur);                                                                    // 868
        var updateNumber = lineNumbers && updateNumbersFrom != null &&                                                 // 869
          updateNumbersFrom <= lineN && lineView.lineNumber;                                                           // 870
        if (lineView.changes) {                                                                                        // 871
          if (indexOf(lineView.changes, "gutter") > -1) updateNumber = false;                                          // 872
          updateLineForChanges(cm, lineView, lineN, dims);                                                             // 873
        }                                                                                                              // 874
        if (updateNumber) {                                                                                            // 875
          removeChildren(lineView.lineNumber);                                                                         // 876
          lineView.lineNumber.appendChild(document.createTextNode(lineNumberFor(cm.options, lineN)));                  // 877
        }                                                                                                              // 878
        cur = lineView.node.nextSibling;                                                                               // 879
      }                                                                                                                // 880
      lineN += lineView.size;                                                                                          // 881
    }                                                                                                                  // 882
    while (cur) cur = rm(cur);                                                                                         // 883
  }                                                                                                                    // 884
                                                                                                                       // 885
  // When an aspect of a line changes, a string is added to                                                            // 886
  // lineView.changes. This updates the relevant part of the line's                                                    // 887
  // DOM structure.                                                                                                    // 888
  function updateLineForChanges(cm, lineView, lineN, dims) {                                                           // 889
    for (var j = 0; j < lineView.changes.length; j++) {                                                                // 890
      var type = lineView.changes[j];                                                                                  // 891
      if (type == "text") updateLineText(cm, lineView);                                                                // 892
      else if (type == "gutter") updateLineGutter(cm, lineView, lineN, dims);                                          // 893
      else if (type == "class") updateLineClasses(lineView);                                                           // 894
      else if (type == "widget") updateLineWidgets(lineView, dims);                                                    // 895
    }                                                                                                                  // 896
    lineView.changes = null;                                                                                           // 897
  }                                                                                                                    // 898
                                                                                                                       // 899
  // Lines with gutter elements, widgets or a background class need to                                                 // 900
  // be wrapped, and have the extra elements added to the wrapper div                                                  // 901
  function ensureLineWrapped(lineView) {                                                                               // 902
    if (lineView.node == lineView.text) {                                                                              // 903
      lineView.node = elt("div", null, null, "position: relative");                                                    // 904
      if (lineView.text.parentNode)                                                                                    // 905
        lineView.text.parentNode.replaceChild(lineView.node, lineView.text);                                           // 906
      lineView.node.appendChild(lineView.text);                                                                        // 907
      if (ie && ie_version < 8) lineView.node.style.zIndex = 2;                                                        // 908
    }                                                                                                                  // 909
    return lineView.node;                                                                                              // 910
  }                                                                                                                    // 911
                                                                                                                       // 912
  function updateLineBackground(lineView) {                                                                            // 913
    var cls = lineView.bgClass ? lineView.bgClass + " " + (lineView.line.bgClass || "") : lineView.line.bgClass;       // 914
    if (cls) cls += " CodeMirror-linebackground";                                                                      // 915
    if (lineView.background) {                                                                                         // 916
      if (cls) lineView.background.className = cls;                                                                    // 917
      else { lineView.background.parentNode.removeChild(lineView.background); lineView.background = null; }            // 918
    } else if (cls) {                                                                                                  // 919
      var wrap = ensureLineWrapped(lineView);                                                                          // 920
      lineView.background = wrap.insertBefore(elt("div", null, cls), wrap.firstChild);                                 // 921
    }                                                                                                                  // 922
  }                                                                                                                    // 923
                                                                                                                       // 924
  // Wrapper around buildLineContent which will reuse the structure                                                    // 925
  // in display.externalMeasured when possible.                                                                        // 926
  function getLineContent(cm, lineView) {                                                                              // 927
    var ext = cm.display.externalMeasured;                                                                             // 928
    if (ext && ext.line == lineView.line) {                                                                            // 929
      cm.display.externalMeasured = null;                                                                              // 930
      lineView.measure = ext.measure;                                                                                  // 931
      return ext.built;                                                                                                // 932
    }                                                                                                                  // 933
    return buildLineContent(cm, lineView);                                                                             // 934
  }                                                                                                                    // 935
                                                                                                                       // 936
  // Redraw the line's text. Interacts with the background and text                                                    // 937
  // classes because the mode may output tokens that influence these                                                   // 938
  // classes.                                                                                                          // 939
  function updateLineText(cm, lineView) {                                                                              // 940
    var cls = lineView.text.className;                                                                                 // 941
    var built = getLineContent(cm, lineView);                                                                          // 942
    if (lineView.text == lineView.node) lineView.node = built.pre;                                                     // 943
    lineView.text.parentNode.replaceChild(built.pre, lineView.text);                                                   // 944
    lineView.text = built.pre;                                                                                         // 945
    if (built.bgClass != lineView.bgClass || built.textClass != lineView.textClass) {                                  // 946
      lineView.bgClass = built.bgClass;                                                                                // 947
      lineView.textClass = built.textClass;                                                                            // 948
      updateLineClasses(lineView);                                                                                     // 949
    } else if (cls) {                                                                                                  // 950
      lineView.text.className = cls;                                                                                   // 951
    }                                                                                                                  // 952
  }                                                                                                                    // 953
                                                                                                                       // 954
  function updateLineClasses(lineView) {                                                                               // 955
    updateLineBackground(lineView);                                                                                    // 956
    if (lineView.line.wrapClass)                                                                                       // 957
      ensureLineWrapped(lineView).className = lineView.line.wrapClass;                                                 // 958
    else if (lineView.node != lineView.text)                                                                           // 959
      lineView.node.className = "";                                                                                    // 960
    var textClass = lineView.textClass ? lineView.textClass + " " + (lineView.line.textClass || "") : lineView.line.textClass;
    lineView.text.className = textClass || "";                                                                         // 962
  }                                                                                                                    // 963
                                                                                                                       // 964
  function updateLineGutter(cm, lineView, lineN, dims) {                                                               // 965
    if (lineView.gutter) {                                                                                             // 966
      lineView.node.removeChild(lineView.gutter);                                                                      // 967
      lineView.gutter = null;                                                                                          // 968
    }                                                                                                                  // 969
    var markers = lineView.line.gutterMarkers;                                                                         // 970
    if (cm.options.lineNumbers || markers) {                                                                           // 971
      var wrap = ensureLineWrapped(lineView);                                                                          // 972
      var gutterWrap = lineView.gutter =                                                                               // 973
        wrap.insertBefore(elt("div", null, "CodeMirror-gutter-wrapper", "left: " +                                     // 974
                              (cm.options.fixedGutter ? dims.fixedPos : -dims.gutterTotalWidth) +                      // 975
                              "px; width: " + dims.gutterTotalWidth + "px"),                                           // 976
                          lineView.text);                                                                              // 977
      if (lineView.line.gutterClass)                                                                                   // 978
        gutterWrap.className += " " + lineView.line.gutterClass;                                                       // 979
      if (cm.options.lineNumbers && (!markers || !markers["CodeMirror-linenumbers"]))                                  // 980
        lineView.lineNumber = gutterWrap.appendChild(                                                                  // 981
          elt("div", lineNumberFor(cm.options, lineN),                                                                 // 982
              "CodeMirror-linenumber CodeMirror-gutter-elt",                                                           // 983
              "left: " + dims.gutterLeft["CodeMirror-linenumbers"] + "px; width: "                                     // 984
              + cm.display.lineNumInnerWidth + "px"));                                                                 // 985
      if (markers) for (var k = 0; k < cm.options.gutters.length; ++k) {                                               // 986
        var id = cm.options.gutters[k], found = markers.hasOwnProperty(id) && markers[id];                             // 987
        if (found)                                                                                                     // 988
          gutterWrap.appendChild(elt("div", [found], "CodeMirror-gutter-elt", "left: " +                               // 989
                                     dims.gutterLeft[id] + "px; width: " + dims.gutterWidth[id] + "px"));              // 990
      }                                                                                                                // 991
    }                                                                                                                  // 992
  }                                                                                                                    // 993
                                                                                                                       // 994
  function updateLineWidgets(lineView, dims) {                                                                         // 995
    if (lineView.alignable) lineView.alignable = null;                                                                 // 996
    for (var node = lineView.node.firstChild, next; node; node = next) {                                               // 997
      var next = node.nextSibling;                                                                                     // 998
      if (node.className == "CodeMirror-linewidget")                                                                   // 999
        lineView.node.removeChild(node);                                                                               // 1000
    }                                                                                                                  // 1001
    insertLineWidgets(lineView, dims);                                                                                 // 1002
  }                                                                                                                    // 1003
                                                                                                                       // 1004
  // Build a line's DOM representation from scratch                                                                    // 1005
  function buildLineElement(cm, lineView, lineN, dims) {                                                               // 1006
    var built = getLineContent(cm, lineView);                                                                          // 1007
    lineView.text = lineView.node = built.pre;                                                                         // 1008
    if (built.bgClass) lineView.bgClass = built.bgClass;                                                               // 1009
    if (built.textClass) lineView.textClass = built.textClass;                                                         // 1010
                                                                                                                       // 1011
    updateLineClasses(lineView);                                                                                       // 1012
    updateLineGutter(cm, lineView, lineN, dims);                                                                       // 1013
    insertLineWidgets(lineView, dims);                                                                                 // 1014
    return lineView.node;                                                                                              // 1015
  }                                                                                                                    // 1016
                                                                                                                       // 1017
  // A lineView may contain multiple logical lines (when merged by                                                     // 1018
  // collapsed spans). The widgets for all of them need to be drawn.                                                   // 1019
  function insertLineWidgets(lineView, dims) {                                                                         // 1020
    insertLineWidgetsFor(lineView.line, lineView, dims, true);                                                         // 1021
    if (lineView.rest) for (var i = 0; i < lineView.rest.length; i++)                                                  // 1022
      insertLineWidgetsFor(lineView.rest[i], lineView, dims, false);                                                   // 1023
  }                                                                                                                    // 1024
                                                                                                                       // 1025
  function insertLineWidgetsFor(line, lineView, dims, allowAbove) {                                                    // 1026
    if (!line.widgets) return;                                                                                         // 1027
    var wrap = ensureLineWrapped(lineView);                                                                            // 1028
    for (var i = 0, ws = line.widgets; i < ws.length; ++i) {                                                           // 1029
      var widget = ws[i], node = elt("div", [widget.node], "CodeMirror-linewidget");                                   // 1030
      if (!widget.handleMouseEvents) node.setAttribute("cm-ignore-events", "true");                                    // 1031
      positionLineWidget(widget, node, lineView, dims);                                                                // 1032
      if (allowAbove && widget.above)                                                                                  // 1033
        wrap.insertBefore(node, lineView.gutter || lineView.text);                                                     // 1034
      else                                                                                                             // 1035
        wrap.appendChild(node);                                                                                        // 1036
      signalLater(widget, "redraw");                                                                                   // 1037
    }                                                                                                                  // 1038
  }                                                                                                                    // 1039
                                                                                                                       // 1040
  function positionLineWidget(widget, node, lineView, dims) {                                                          // 1041
    if (widget.noHScroll) {                                                                                            // 1042
      (lineView.alignable || (lineView.alignable = [])).push(node);                                                    // 1043
      var width = dims.wrapperWidth;                                                                                   // 1044
      node.style.left = dims.fixedPos + "px";                                                                          // 1045
      if (!widget.coverGutter) {                                                                                       // 1046
        width -= dims.gutterTotalWidth;                                                                                // 1047
        node.style.paddingLeft = dims.gutterTotalWidth + "px";                                                         // 1048
      }                                                                                                                // 1049
      node.style.width = width + "px";                                                                                 // 1050
    }                                                                                                                  // 1051
    if (widget.coverGutter) {                                                                                          // 1052
      node.style.zIndex = 5;                                                                                           // 1053
      node.style.position = "relative";                                                                                // 1054
      if (!widget.noHScroll) node.style.marginLeft = -dims.gutterTotalWidth + "px";                                    // 1055
    }                                                                                                                  // 1056
  }                                                                                                                    // 1057
                                                                                                                       // 1058
  // POSITION OBJECT                                                                                                   // 1059
                                                                                                                       // 1060
  // A Pos instance represents a position within the text.                                                             // 1061
  var Pos = CodeMirror.Pos = function(line, ch) {                                                                      // 1062
    if (!(this instanceof Pos)) return new Pos(line, ch);                                                              // 1063
    this.line = line; this.ch = ch;                                                                                    // 1064
  };                                                                                                                   // 1065
                                                                                                                       // 1066
  // Compare two positions, return 0 if they are the same, a negative                                                  // 1067
  // number when a is less, and a positive number otherwise.                                                           // 1068
  var cmp = CodeMirror.cmpPos = function(a, b) { return a.line - b.line || a.ch - b.ch; };                             // 1069
                                                                                                                       // 1070
  function copyPos(x) {return Pos(x.line, x.ch);}                                                                      // 1071
  function maxPos(a, b) { return cmp(a, b) < 0 ? b : a; }                                                              // 1072
  function minPos(a, b) { return cmp(a, b) < 0 ? a : b; }                                                              // 1073
                                                                                                                       // 1074
  // SELECTION / CURSOR                                                                                                // 1075
                                                                                                                       // 1076
  // Selection objects are immutable. A new one is created every time                                                  // 1077
  // the selection changes. A selection is one or more non-overlapping                                                 // 1078
  // (and non-touching) ranges, sorted, and an integer that indicates                                                  // 1079
  // which one is the primary selection (the one that's scrolled into                                                  // 1080
  // view, that getCursor returns, etc).                                                                               // 1081
  function Selection(ranges, primIndex) {                                                                              // 1082
    this.ranges = ranges;                                                                                              // 1083
    this.primIndex = primIndex;                                                                                        // 1084
  }                                                                                                                    // 1085
                                                                                                                       // 1086
  Selection.prototype = {                                                                                              // 1087
    primary: function() { return this.ranges[this.primIndex]; },                                                       // 1088
    equals: function(other) {                                                                                          // 1089
      if (other == this) return true;                                                                                  // 1090
      if (other.primIndex != this.primIndex || other.ranges.length != this.ranges.length) return false;                // 1091
      for (var i = 0; i < this.ranges.length; i++) {                                                                   // 1092
        var here = this.ranges[i], there = other.ranges[i];                                                            // 1093
        if (cmp(here.anchor, there.anchor) != 0 || cmp(here.head, there.head) != 0) return false;                      // 1094
      }                                                                                                                // 1095
      return true;                                                                                                     // 1096
    },                                                                                                                 // 1097
    deepCopy: function() {                                                                                             // 1098
      for (var out = [], i = 0; i < this.ranges.length; i++)                                                           // 1099
        out[i] = new Range(copyPos(this.ranges[i].anchor), copyPos(this.ranges[i].head));                              // 1100
      return new Selection(out, this.primIndex);                                                                       // 1101
    },                                                                                                                 // 1102
    somethingSelected: function() {                                                                                    // 1103
      for (var i = 0; i < this.ranges.length; i++)                                                                     // 1104
        if (!this.ranges[i].empty()) return true;                                                                      // 1105
      return false;                                                                                                    // 1106
    },                                                                                                                 // 1107
    contains: function(pos, end) {                                                                                     // 1108
      if (!end) end = pos;                                                                                             // 1109
      for (var i = 0; i < this.ranges.length; i++) {                                                                   // 1110
        var range = this.ranges[i];                                                                                    // 1111
        if (cmp(end, range.from()) >= 0 && cmp(pos, range.to()) <= 0)                                                  // 1112
          return i;                                                                                                    // 1113
      }                                                                                                                // 1114
      return -1;                                                                                                       // 1115
    }                                                                                                                  // 1116
  };                                                                                                                   // 1117
                                                                                                                       // 1118
  function Range(anchor, head) {                                                                                       // 1119
    this.anchor = anchor; this.head = head;                                                                            // 1120
  }                                                                                                                    // 1121
                                                                                                                       // 1122
  Range.prototype = {                                                                                                  // 1123
    from: function() { return minPos(this.anchor, this.head); },                                                       // 1124
    to: function() { return maxPos(this.anchor, this.head); },                                                         // 1125
    empty: function() {                                                                                                // 1126
      return this.head.line == this.anchor.line && this.head.ch == this.anchor.ch;                                     // 1127
    }                                                                                                                  // 1128
  };                                                                                                                   // 1129
                                                                                                                       // 1130
  // Take an unsorted, potentially overlapping set of ranges, and                                                      // 1131
  // build a selection out of it. 'Consumes' ranges array (modifying                                                   // 1132
  // it).                                                                                                              // 1133
  function normalizeSelection(ranges, primIndex) {                                                                     // 1134
    var prim = ranges[primIndex];                                                                                      // 1135
    ranges.sort(function(a, b) { return cmp(a.from(), b.from()); });                                                   // 1136
    primIndex = indexOf(ranges, prim);                                                                                 // 1137
    for (var i = 1; i < ranges.length; i++) {                                                                          // 1138
      var cur = ranges[i], prev = ranges[i - 1];                                                                       // 1139
      if (cmp(prev.to(), cur.from()) >= 0) {                                                                           // 1140
        var from = minPos(prev.from(), cur.from()), to = maxPos(prev.to(), cur.to());                                  // 1141
        var inv = prev.empty() ? cur.from() == cur.head : prev.from() == prev.head;                                    // 1142
        if (i <= primIndex) --primIndex;                                                                               // 1143
        ranges.splice(--i, 2, new Range(inv ? to : from, inv ? from : to));                                            // 1144
      }                                                                                                                // 1145
    }                                                                                                                  // 1146
    return new Selection(ranges, primIndex);                                                                           // 1147
  }                                                                                                                    // 1148
                                                                                                                       // 1149
  function simpleSelection(anchor, head) {                                                                             // 1150
    return new Selection([new Range(anchor, head || anchor)], 0);                                                      // 1151
  }                                                                                                                    // 1152
                                                                                                                       // 1153
  // Most of the external API clips given positions to make sure they                                                  // 1154
  // actually exist within the document.                                                                               // 1155
  function clipLine(doc, n) {return Math.max(doc.first, Math.min(n, doc.first + doc.size - 1));}                       // 1156
  function clipPos(doc, pos) {                                                                                         // 1157
    if (pos.line < doc.first) return Pos(doc.first, 0);                                                                // 1158
    var last = doc.first + doc.size - 1;                                                                               // 1159
    if (pos.line > last) return Pos(last, getLine(doc, last).text.length);                                             // 1160
    return clipToLen(pos, getLine(doc, pos.line).text.length);                                                         // 1161
  }                                                                                                                    // 1162
  function clipToLen(pos, linelen) {                                                                                   // 1163
    var ch = pos.ch;                                                                                                   // 1164
    if (ch == null || ch > linelen) return Pos(pos.line, linelen);                                                     // 1165
    else if (ch < 0) return Pos(pos.line, 0);                                                                          // 1166
    else return pos;                                                                                                   // 1167
  }                                                                                                                    // 1168
  function isLine(doc, l) {return l >= doc.first && l < doc.first + doc.size;}                                         // 1169
  function clipPosArray(doc, array) {                                                                                  // 1170
    for (var out = [], i = 0; i < array.length; i++) out[i] = clipPos(doc, array[i]);                                  // 1171
    return out;                                                                                                        // 1172
  }                                                                                                                    // 1173
                                                                                                                       // 1174
  // SELECTION UPDATES                                                                                                 // 1175
                                                                                                                       // 1176
  // The 'scroll' parameter given to many of these indicated whether                                                   // 1177
  // the new cursor position should be scrolled into view after                                                        // 1178
  // modifying the selection.                                                                                          // 1179
                                                                                                                       // 1180
  // If shift is held or the extend flag is set, extends a range to                                                    // 1181
  // include a given position (and optionally a second position).                                                      // 1182
  // Otherwise, simply returns the range between the given positions.                                                  // 1183
  // Used for cursor motion and such.                                                                                  // 1184
  function extendRange(doc, range, head, other) {                                                                      // 1185
    if (doc.cm && doc.cm.display.shift || doc.extend) {                                                                // 1186
      var anchor = range.anchor;                                                                                       // 1187
      if (other) {                                                                                                     // 1188
        var posBefore = cmp(head, anchor) < 0;                                                                         // 1189
        if (posBefore != (cmp(other, anchor) < 0)) {                                                                   // 1190
          anchor = head;                                                                                               // 1191
          head = other;                                                                                                // 1192
        } else if (posBefore != (cmp(head, other) < 0)) {                                                              // 1193
          head = other;                                                                                                // 1194
        }                                                                                                              // 1195
      }                                                                                                                // 1196
      return new Range(anchor, head);                                                                                  // 1197
    } else {                                                                                                           // 1198
      return new Range(other || head, head);                                                                           // 1199
    }                                                                                                                  // 1200
  }                                                                                                                    // 1201
                                                                                                                       // 1202
  // Extend the primary selection range, discard the rest.                                                             // 1203
  function extendSelection(doc, head, other, options) {                                                                // 1204
    setSelection(doc, new Selection([extendRange(doc, doc.sel.primary(), head, other)], 0), options);                  // 1205
  }                                                                                                                    // 1206
                                                                                                                       // 1207
  // Extend all selections (pos is an array of selections with length                                                  // 1208
  // equal the number of selections)                                                                                   // 1209
  function extendSelections(doc, heads, options) {                                                                     // 1210
    for (var out = [], i = 0; i < doc.sel.ranges.length; i++)                                                          // 1211
      out[i] = extendRange(doc, doc.sel.ranges[i], heads[i], null);                                                    // 1212
    var newSel = normalizeSelection(out, doc.sel.primIndex);                                                           // 1213
    setSelection(doc, newSel, options);                                                                                // 1214
  }                                                                                                                    // 1215
                                                                                                                       // 1216
  // Updates a single range in the selection.                                                                          // 1217
  function replaceOneSelection(doc, i, range, options) {                                                               // 1218
    var ranges = doc.sel.ranges.slice(0);                                                                              // 1219
    ranges[i] = range;                                                                                                 // 1220
    setSelection(doc, normalizeSelection(ranges, doc.sel.primIndex), options);                                         // 1221
  }                                                                                                                    // 1222
                                                                                                                       // 1223
  // Reset the selection to a single range.                                                                            // 1224
  function setSimpleSelection(doc, anchor, head, options) {                                                            // 1225
    setSelection(doc, simpleSelection(anchor, head), options);                                                         // 1226
  }                                                                                                                    // 1227
                                                                                                                       // 1228
  // Give beforeSelectionChange handlers a change to influence a                                                       // 1229
  // selection update.                                                                                                 // 1230
  function filterSelectionChange(doc, sel) {                                                                           // 1231
    var obj = {                                                                                                        // 1232
      ranges: sel.ranges,                                                                                              // 1233
      update: function(ranges) {                                                                                       // 1234
        this.ranges = [];                                                                                              // 1235
        for (var i = 0; i < ranges.length; i++)                                                                        // 1236
          this.ranges[i] = new Range(clipPos(doc, ranges[i].anchor),                                                   // 1237
                                     clipPos(doc, ranges[i].head));                                                    // 1238
      }                                                                                                                // 1239
    };                                                                                                                 // 1240
    signal(doc, "beforeSelectionChange", doc, obj);                                                                    // 1241
    if (doc.cm) signal(doc.cm, "beforeSelectionChange", doc.cm, obj);                                                  // 1242
    if (obj.ranges != sel.ranges) return normalizeSelection(obj.ranges, obj.ranges.length - 1);                        // 1243
    else return sel;                                                                                                   // 1244
  }                                                                                                                    // 1245
                                                                                                                       // 1246
  function setSelectionReplaceHistory(doc, sel, options) {                                                             // 1247
    var done = doc.history.done, last = lst(done);                                                                     // 1248
    if (last && last.ranges) {                                                                                         // 1249
      done[done.length - 1] = sel;                                                                                     // 1250
      setSelectionNoUndo(doc, sel, options);                                                                           // 1251
    } else {                                                                                                           // 1252
      setSelection(doc, sel, options);                                                                                 // 1253
    }                                                                                                                  // 1254
  }                                                                                                                    // 1255
                                                                                                                       // 1256
  // Set a new selection.                                                                                              // 1257
  function setSelection(doc, sel, options) {                                                                           // 1258
    setSelectionNoUndo(doc, sel, options);                                                                             // 1259
    addSelectionToHistory(doc, doc.sel, doc.cm ? doc.cm.curOp.id : NaN, options);                                      // 1260
  }                                                                                                                    // 1261
                                                                                                                       // 1262
  function setSelectionNoUndo(doc, sel, options) {                                                                     // 1263
    if (hasHandler(doc, "beforeSelectionChange") || doc.cm && hasHandler(doc.cm, "beforeSelectionChange"))             // 1264
      sel = filterSelectionChange(doc, sel);                                                                           // 1265
                                                                                                                       // 1266
    var bias = options && options.bias ||                                                                              // 1267
      (cmp(sel.primary().head, doc.sel.primary().head) < 0 ? -1 : 1);                                                  // 1268
    setSelectionInner(doc, skipAtomicInSelection(doc, sel, bias, true));                                               // 1269
                                                                                                                       // 1270
    if (!(options && options.scroll === false) && doc.cm)                                                              // 1271
      ensureCursorVisible(doc.cm);                                                                                     // 1272
  }                                                                                                                    // 1273
                                                                                                                       // 1274
  function setSelectionInner(doc, sel) {                                                                               // 1275
    if (sel.equals(doc.sel)) return;                                                                                   // 1276
                                                                                                                       // 1277
    doc.sel = sel;                                                                                                     // 1278
                                                                                                                       // 1279
    if (doc.cm) {                                                                                                      // 1280
      doc.cm.curOp.updateInput = doc.cm.curOp.selectionChanged = true;                                                 // 1281
      signalCursorActivity(doc.cm);                                                                                    // 1282
    }                                                                                                                  // 1283
    signalLater(doc, "cursorActivity", doc);                                                                           // 1284
  }                                                                                                                    // 1285
                                                                                                                       // 1286
  // Verify that the selection does not partially select any atomic                                                    // 1287
  // marked ranges.                                                                                                    // 1288
  function reCheckSelection(doc) {                                                                                     // 1289
    setSelectionInner(doc, skipAtomicInSelection(doc, doc.sel, null, false), sel_dontScroll);                          // 1290
  }                                                                                                                    // 1291
                                                                                                                       // 1292
  // Return a selection that does not partially select any atomic                                                      // 1293
  // ranges.                                                                                                           // 1294
  function skipAtomicInSelection(doc, sel, bias, mayClear) {                                                           // 1295
    var out;                                                                                                           // 1296
    for (var i = 0; i < sel.ranges.length; i++) {                                                                      // 1297
      var range = sel.ranges[i];                                                                                       // 1298
      var newAnchor = skipAtomic(doc, range.anchor, bias, mayClear);                                                   // 1299
      var newHead = skipAtomic(doc, range.head, bias, mayClear);                                                       // 1300
      if (out || newAnchor != range.anchor || newHead != range.head) {                                                 // 1301
        if (!out) out = sel.ranges.slice(0, i);                                                                        // 1302
        out[i] = new Range(newAnchor, newHead);                                                                        // 1303
      }                                                                                                                // 1304
    }                                                                                                                  // 1305
    return out ? normalizeSelection(out, sel.primIndex) : sel;                                                         // 1306
  }                                                                                                                    // 1307
                                                                                                                       // 1308
  // Ensure a given position is not inside an atomic range.                                                            // 1309
  function skipAtomic(doc, pos, bias, mayClear) {                                                                      // 1310
    var flipped = false, curPos = pos;                                                                                 // 1311
    var dir = bias || 1;                                                                                               // 1312
    doc.cantEdit = false;                                                                                              // 1313
    search: for (;;) {                                                                                                 // 1314
      var line = getLine(doc, curPos.line);                                                                            // 1315
      if (line.markedSpans) {                                                                                          // 1316
        for (var i = 0; i < line.markedSpans.length; ++i) {                                                            // 1317
          var sp = line.markedSpans[i], m = sp.marker;                                                                 // 1318
          if ((sp.from == null || (m.inclusiveLeft ? sp.from <= curPos.ch : sp.from < curPos.ch)) &&                   // 1319
              (sp.to == null || (m.inclusiveRight ? sp.to >= curPos.ch : sp.to > curPos.ch))) {                        // 1320
            if (mayClear) {                                                                                            // 1321
              signal(m, "beforeCursorEnter");                                                                          // 1322
              if (m.explicitlyCleared) {                                                                               // 1323
                if (!line.markedSpans) break;                                                                          // 1324
                else {--i; continue;}                                                                                  // 1325
              }                                                                                                        // 1326
            }                                                                                                          // 1327
            if (!m.atomic) continue;                                                                                   // 1328
            var newPos = m.find(dir < 0 ? -1 : 1);                                                                     // 1329
            if (cmp(newPos, curPos) == 0) {                                                                            // 1330
              newPos.ch += dir;                                                                                        // 1331
              if (newPos.ch < 0) {                                                                                     // 1332
                if (newPos.line > doc.first) newPos = clipPos(doc, Pos(newPos.line - 1));                              // 1333
                else newPos = null;                                                                                    // 1334
              } else if (newPos.ch > line.text.length) {                                                               // 1335
                if (newPos.line < doc.first + doc.size - 1) newPos = Pos(newPos.line + 1, 0);                          // 1336
                else newPos = null;                                                                                    // 1337
              }                                                                                                        // 1338
              if (!newPos) {                                                                                           // 1339
                if (flipped) {                                                                                         // 1340
                  // Driven in a corner -- no valid cursor position found at all                                       // 1341
                  // -- try again *with* clearing, if we didn't already                                                // 1342
                  if (!mayClear) return skipAtomic(doc, pos, bias, true);                                              // 1343
                  // Otherwise, turn off editing until further notice, and return the start of the doc                 // 1344
                  doc.cantEdit = true;                                                                                 // 1345
                  return Pos(doc.first, 0);                                                                            // 1346
                }                                                                                                      // 1347
                flipped = true; newPos = pos; dir = -dir;                                                              // 1348
              }                                                                                                        // 1349
            }                                                                                                          // 1350
            curPos = newPos;                                                                                           // 1351
            continue search;                                                                                           // 1352
          }                                                                                                            // 1353
        }                                                                                                              // 1354
      }                                                                                                                // 1355
      return curPos;                                                                                                   // 1356
    }                                                                                                                  // 1357
  }                                                                                                                    // 1358
                                                                                                                       // 1359
  // SELECTION DRAWING                                                                                                 // 1360
                                                                                                                       // 1361
  // Redraw the selection and/or cursor                                                                                // 1362
  function drawSelection(cm) {                                                                                         // 1363
    var display = cm.display, doc = cm.doc, result = {};                                                               // 1364
    var curFragment = result.cursors = document.createDocumentFragment();                                              // 1365
    var selFragment = result.selection = document.createDocumentFragment();                                            // 1366
                                                                                                                       // 1367
    for (var i = 0; i < doc.sel.ranges.length; i++) {                                                                  // 1368
      var range = doc.sel.ranges[i];                                                                                   // 1369
      var collapsed = range.empty();                                                                                   // 1370
      if (collapsed || cm.options.showCursorWhenSelecting)                                                             // 1371
        drawSelectionCursor(cm, range, curFragment);                                                                   // 1372
      if (!collapsed)                                                                                                  // 1373
        drawSelectionRange(cm, range, selFragment);                                                                    // 1374
    }                                                                                                                  // 1375
                                                                                                                       // 1376
    // Move the hidden textarea near the cursor to prevent scrolling artifacts                                         // 1377
    if (cm.options.moveInputWithCursor) {                                                                              // 1378
      var headPos = cursorCoords(cm, doc.sel.primary().head, "div");                                                   // 1379
      var wrapOff = display.wrapper.getBoundingClientRect(), lineOff = display.lineDiv.getBoundingClientRect();        // 1380
      result.teTop = Math.max(0, Math.min(display.wrapper.clientHeight - 10,                                           // 1381
                                          headPos.top + lineOff.top - wrapOff.top));                                   // 1382
      result.teLeft = Math.max(0, Math.min(display.wrapper.clientWidth - 10,                                           // 1383
                                           headPos.left + lineOff.left - wrapOff.left));                               // 1384
    }                                                                                                                  // 1385
                                                                                                                       // 1386
    return result;                                                                                                     // 1387
  }                                                                                                                    // 1388
                                                                                                                       // 1389
  function showSelection(cm, drawn) {                                                                                  // 1390
    removeChildrenAndAdd(cm.display.cursorDiv, drawn.cursors);                                                         // 1391
    removeChildrenAndAdd(cm.display.selectionDiv, drawn.selection);                                                    // 1392
    if (drawn.teTop != null) {                                                                                         // 1393
      cm.display.inputDiv.style.top = drawn.teTop + "px";                                                              // 1394
      cm.display.inputDiv.style.left = drawn.teLeft + "px";                                                            // 1395
    }                                                                                                                  // 1396
  }                                                                                                                    // 1397
                                                                                                                       // 1398
  function updateSelection(cm) {                                                                                       // 1399
    showSelection(cm, drawSelection(cm));                                                                              // 1400
  }                                                                                                                    // 1401
                                                                                                                       // 1402
  // Draws a cursor for the given range                                                                                // 1403
  function drawSelectionCursor(cm, range, output) {                                                                    // 1404
    var pos = cursorCoords(cm, range.head, "div", null, null, !cm.options.singleCursorHeightPerLine);                  // 1405
                                                                                                                       // 1406
    var cursor = output.appendChild(elt("div", "\u00a0", "CodeMirror-cursor"));                                        // 1407
    cursor.style.left = pos.left + "px";                                                                               // 1408
    cursor.style.top = pos.top + "px";                                                                                 // 1409
    cursor.style.height = Math.max(0, pos.bottom - pos.top) * cm.options.cursorHeight + "px";                          // 1410
                                                                                                                       // 1411
    if (pos.other) {                                                                                                   // 1412
      // Secondary cursor, shown when on a 'jump' in bi-directional text                                               // 1413
      var otherCursor = output.appendChild(elt("div", "\u00a0", "CodeMirror-cursor CodeMirror-secondarycursor"));      // 1414
      otherCursor.style.display = "";                                                                                  // 1415
      otherCursor.style.left = pos.other.left + "px";                                                                  // 1416
      otherCursor.style.top = pos.other.top + "px";                                                                    // 1417
      otherCursor.style.height = (pos.other.bottom - pos.other.top) * .85 + "px";                                      // 1418
    }                                                                                                                  // 1419
  }                                                                                                                    // 1420
                                                                                                                       // 1421
  // Draws the given range as a highlighted selection                                                                  // 1422
  function drawSelectionRange(cm, range, output) {                                                                     // 1423
    var display = cm.display, doc = cm.doc;                                                                            // 1424
    var fragment = document.createDocumentFragment();                                                                  // 1425
    var padding = paddingH(cm.display), leftSide = padding.left;                                                       // 1426
    var rightSide = Math.max(display.sizerWidth, displayWidth(cm) - display.sizer.offsetLeft) - padding.right;         // 1427
                                                                                                                       // 1428
    function add(left, top, width, bottom) {                                                                           // 1429
      if (top < 0) top = 0;                                                                                            // 1430
      top = Math.round(top);                                                                                           // 1431
      bottom = Math.round(bottom);                                                                                     // 1432
      fragment.appendChild(elt("div", null, "CodeMirror-selected", "position: absolute; left: " + left +               // 1433
                               "px; top: " + top + "px; width: " + (width == null ? rightSide - left : width) +        // 1434
                               "px; height: " + (bottom - top) + "px"));                                               // 1435
    }                                                                                                                  // 1436
                                                                                                                       // 1437
    function drawForLine(line, fromArg, toArg) {                                                                       // 1438
      var lineObj = getLine(doc, line);                                                                                // 1439
      var lineLen = lineObj.text.length;                                                                               // 1440
      var start, end;                                                                                                  // 1441
      function coords(ch, bias) {                                                                                      // 1442
        return charCoords(cm, Pos(line, ch), "div", lineObj, bias);                                                    // 1443
      }                                                                                                                // 1444
                                                                                                                       // 1445
      iterateBidiSections(getOrder(lineObj), fromArg || 0, toArg == null ? lineLen : toArg, function(from, to, dir) {  // 1446
        var leftPos = coords(from, "left"), rightPos, left, right;                                                     // 1447
        if (from == to) {                                                                                              // 1448
          rightPos = leftPos;                                                                                          // 1449
          left = right = leftPos.left;                                                                                 // 1450
        } else {                                                                                                       // 1451
          rightPos = coords(to - 1, "right");                                                                          // 1452
          if (dir == "rtl") { var tmp = leftPos; leftPos = rightPos; rightPos = tmp; }                                 // 1453
          left = leftPos.left;                                                                                         // 1454
          right = rightPos.right;                                                                                      // 1455
        }                                                                                                              // 1456
        if (fromArg == null && from == 0) left = leftSide;                                                             // 1457
        if (rightPos.top - leftPos.top > 3) { // Different lines, draw top part                                        // 1458
          add(left, leftPos.top, null, leftPos.bottom);                                                                // 1459
          left = leftSide;                                                                                             // 1460
          if (leftPos.bottom < rightPos.top) add(left, leftPos.bottom, null, rightPos.top);                            // 1461
        }                                                                                                              // 1462
        if (toArg == null && to == lineLen) right = rightSide;                                                         // 1463
        if (!start || leftPos.top < start.top || leftPos.top == start.top && leftPos.left < start.left)                // 1464
          start = leftPos;                                                                                             // 1465
        if (!end || rightPos.bottom > end.bottom || rightPos.bottom == end.bottom && rightPos.right > end.right)       // 1466
          end = rightPos;                                                                                              // 1467
        if (left < leftSide + 1) left = leftSide;                                                                      // 1468
        add(left, rightPos.top, right - left, rightPos.bottom);                                                        // 1469
      });                                                                                                              // 1470
      return {start: start, end: end};                                                                                 // 1471
    }                                                                                                                  // 1472
                                                                                                                       // 1473
    var sFrom = range.from(), sTo = range.to();                                                                        // 1474
    if (sFrom.line == sTo.line) {                                                                                      // 1475
      drawForLine(sFrom.line, sFrom.ch, sTo.ch);                                                                       // 1476
    } else {                                                                                                           // 1477
      var fromLine = getLine(doc, sFrom.line), toLine = getLine(doc, sTo.line);                                        // 1478
      var singleVLine = visualLine(fromLine) == visualLine(toLine);                                                    // 1479
      var leftEnd = drawForLine(sFrom.line, sFrom.ch, singleVLine ? fromLine.text.length + 1 : null).end;              // 1480
      var rightStart = drawForLine(sTo.line, singleVLine ? 0 : null, sTo.ch).start;                                    // 1481
      if (singleVLine) {                                                                                               // 1482
        if (leftEnd.top < rightStart.top - 2) {                                                                        // 1483
          add(leftEnd.right, leftEnd.top, null, leftEnd.bottom);                                                       // 1484
          add(leftSide, rightStart.top, rightStart.left, rightStart.bottom);                                           // 1485
        } else {                                                                                                       // 1486
          add(leftEnd.right, leftEnd.top, rightStart.left - leftEnd.right, leftEnd.bottom);                            // 1487
        }                                                                                                              // 1488
      }                                                                                                                // 1489
      if (leftEnd.bottom < rightStart.top)                                                                             // 1490
        add(leftSide, leftEnd.bottom, null, rightStart.top);                                                           // 1491
    }                                                                                                                  // 1492
                                                                                                                       // 1493
    output.appendChild(fragment);                                                                                      // 1494
  }                                                                                                                    // 1495
                                                                                                                       // 1496
  // Cursor-blinking                                                                                                   // 1497
  function restartBlink(cm) {                                                                                          // 1498
    if (!cm.state.focused) return;                                                                                     // 1499
    var display = cm.display;                                                                                          // 1500
    clearInterval(display.blinker);                                                                                    // 1501
    var on = true;                                                                                                     // 1502
    display.cursorDiv.style.visibility = "";                                                                           // 1503
    if (cm.options.cursorBlinkRate > 0)                                                                                // 1504
      display.blinker = setInterval(function() {                                                                       // 1505
        display.cursorDiv.style.visibility = (on = !on) ? "" : "hidden";                                               // 1506
      }, cm.options.cursorBlinkRate);                                                                                  // 1507
    else if (cm.options.cursorBlinkRate < 0)                                                                           // 1508
      display.cursorDiv.style.visibility = "hidden";                                                                   // 1509
  }                                                                                                                    // 1510
                                                                                                                       // 1511
  // HIGHLIGHT WORKER                                                                                                  // 1512
                                                                                                                       // 1513
  function startWorker(cm, time) {                                                                                     // 1514
    if (cm.doc.mode.startState && cm.doc.frontier < cm.display.viewTo)                                                 // 1515
      cm.state.highlight.set(time, bind(highlightWorker, cm));                                                         // 1516
  }                                                                                                                    // 1517
                                                                                                                       // 1518
  function highlightWorker(cm) {                                                                                       // 1519
    var doc = cm.doc;                                                                                                  // 1520
    if (doc.frontier < doc.first) doc.frontier = doc.first;                                                            // 1521
    if (doc.frontier >= cm.display.viewTo) return;                                                                     // 1522
    var end = +new Date + cm.options.workTime;                                                                         // 1523
    var state = copyState(doc.mode, getStateBefore(cm, doc.frontier));                                                 // 1524
    var changedLines = [];                                                                                             // 1525
                                                                                                                       // 1526
    doc.iter(doc.frontier, Math.min(doc.first + doc.size, cm.display.viewTo + 500), function(line) {                   // 1527
      if (doc.frontier >= cm.display.viewFrom) { // Visible                                                            // 1528
        var oldStyles = line.styles;                                                                                   // 1529
        var highlighted = highlightLine(cm, line, state, true);                                                        // 1530
        line.styles = highlighted.styles;                                                                              // 1531
        var oldCls = line.styleClasses, newCls = highlighted.classes;                                                  // 1532
        if (newCls) line.styleClasses = newCls;                                                                        // 1533
        else if (oldCls) line.styleClasses = null;                                                                     // 1534
        var ischange = !oldStyles || oldStyles.length != line.styles.length ||                                         // 1535
          oldCls != newCls && (!oldCls || !newCls || oldCls.bgClass != newCls.bgClass || oldCls.textClass != newCls.textClass);
        for (var i = 0; !ischange && i < oldStyles.length; ++i) ischange = oldStyles[i] != line.styles[i];             // 1537
        if (ischange) changedLines.push(doc.frontier);                                                                 // 1538
        line.stateAfter = copyState(doc.mode, state);                                                                  // 1539
      } else {                                                                                                         // 1540
        processLine(cm, line.text, state);                                                                             // 1541
        line.stateAfter = doc.frontier % 5 == 0 ? copyState(doc.mode, state) : null;                                   // 1542
      }                                                                                                                // 1543
      ++doc.frontier;                                                                                                  // 1544
      if (+new Date > end) {                                                                                           // 1545
        startWorker(cm, cm.options.workDelay);                                                                         // 1546
        return true;                                                                                                   // 1547
      }                                                                                                                // 1548
    });                                                                                                                // 1549
    if (changedLines.length) runInOp(cm, function() {                                                                  // 1550
      for (var i = 0; i < changedLines.length; i++)                                                                    // 1551
        regLineChange(cm, changedLines[i], "text");                                                                    // 1552
    });                                                                                                                // 1553
  }                                                                                                                    // 1554
                                                                                                                       // 1555
  // Finds the line to start with when starting a parse. Tries to                                                      // 1556
  // find a line with a stateAfter, so that it can start with a                                                        // 1557
  // valid state. If that fails, it returns the line with the                                                          // 1558
  // smallest indentation, which tends to need the least context to                                                    // 1559
  // parse correctly.                                                                                                  // 1560
  function findStartLine(cm, n, precise) {                                                                             // 1561
    var minindent, minline, doc = cm.doc;                                                                              // 1562
    var lim = precise ? -1 : n - (cm.doc.mode.innerMode ? 1000 : 100);                                                 // 1563
    for (var search = n; search > lim; --search) {                                                                     // 1564
      if (search <= doc.first) return doc.first;                                                                       // 1565
      var line = getLine(doc, search - 1);                                                                             // 1566
      if (line.stateAfter && (!precise || search <= doc.frontier)) return search;                                      // 1567
      var indented = countColumn(line.text, null, cm.options.tabSize);                                                 // 1568
      if (minline == null || minindent > indented) {                                                                   // 1569
        minline = search - 1;                                                                                          // 1570
        minindent = indented;                                                                                          // 1571
      }                                                                                                                // 1572
    }                                                                                                                  // 1573
    return minline;                                                                                                    // 1574
  }                                                                                                                    // 1575
                                                                                                                       // 1576
  function getStateBefore(cm, n, precise) {                                                                            // 1577
    var doc = cm.doc, display = cm.display;                                                                            // 1578
    if (!doc.mode.startState) return true;                                                                             // 1579
    var pos = findStartLine(cm, n, precise), state = pos > doc.first && getLine(doc, pos-1).stateAfter;                // 1580
    if (!state) state = startState(doc.mode);                                                                          // 1581
    else state = copyState(doc.mode, state);                                                                           // 1582
    doc.iter(pos, n, function(line) {                                                                                  // 1583
      processLine(cm, line.text, state);                                                                               // 1584
      var save = pos == n - 1 || pos % 5 == 0 || pos >= display.viewFrom && pos < display.viewTo;                      // 1585
      line.stateAfter = save ? copyState(doc.mode, state) : null;                                                      // 1586
      ++pos;                                                                                                           // 1587
    });                                                                                                                // 1588
    if (precise) doc.frontier = pos;                                                                                   // 1589
    return state;                                                                                                      // 1590
  }                                                                                                                    // 1591
                                                                                                                       // 1592
  // POSITION MEASUREMENT                                                                                              // 1593
                                                                                                                       // 1594
  function paddingTop(display) {return display.lineSpace.offsetTop;}                                                   // 1595
  function paddingVert(display) {return display.mover.offsetHeight - display.lineSpace.offsetHeight;}                  // 1596
  function paddingH(display) {                                                                                         // 1597
    if (display.cachedPaddingH) return display.cachedPaddingH;                                                         // 1598
    var e = removeChildrenAndAdd(display.measure, elt("pre", "x"));                                                    // 1599
    var style = window.getComputedStyle ? window.getComputedStyle(e) : e.currentStyle;                                 // 1600
    var data = {left: parseInt(style.paddingLeft), right: parseInt(style.paddingRight)};                               // 1601
    if (!isNaN(data.left) && !isNaN(data.right)) display.cachedPaddingH = data;                                        // 1602
    return data;                                                                                                       // 1603
  }                                                                                                                    // 1604
                                                                                                                       // 1605
  function scrollGap(cm) { return scrollerGap - cm.display.nativeBarWidth; }                                           // 1606
  function displayWidth(cm) {                                                                                          // 1607
    return cm.display.scroller.clientWidth - scrollGap(cm) - cm.display.barWidth;                                      // 1608
  }                                                                                                                    // 1609
  function displayHeight(cm) {                                                                                         // 1610
    return cm.display.scroller.clientHeight - scrollGap(cm) - cm.display.barHeight;                                    // 1611
  }                                                                                                                    // 1612
                                                                                                                       // 1613
  // Ensure the lineView.wrapping.heights array is populated. This is                                                  // 1614
  // an array of bottom offsets for the lines that make up a drawn                                                     // 1615
  // line. When lineWrapping is on, there might be more than one                                                       // 1616
  // height.                                                                                                           // 1617
  function ensureLineHeights(cm, lineView, rect) {                                                                     // 1618
    var wrapping = cm.options.lineWrapping;                                                                            // 1619
    var curWidth = wrapping && displayWidth(cm);                                                                       // 1620
    if (!lineView.measure.heights || wrapping && lineView.measure.width != curWidth) {                                 // 1621
      var heights = lineView.measure.heights = [];                                                                     // 1622
      if (wrapping) {                                                                                                  // 1623
        lineView.measure.width = curWidth;                                                                             // 1624
        var rects = lineView.text.firstChild.getClientRects();                                                         // 1625
        for (var i = 0; i < rects.length - 1; i++) {                                                                   // 1626
          var cur = rects[i], next = rects[i + 1];                                                                     // 1627
          if (Math.abs(cur.bottom - next.bottom) > 2)                                                                  // 1628
            heights.push((cur.bottom + next.top) / 2 - rect.top);                                                      // 1629
        }                                                                                                              // 1630
      }                                                                                                                // 1631
      heights.push(rect.bottom - rect.top);                                                                            // 1632
    }                                                                                                                  // 1633
  }                                                                                                                    // 1634
                                                                                                                       // 1635
  // Find a line map (mapping character offsets to text nodes) and a                                                   // 1636
  // measurement cache for the given line number. (A line view might                                                   // 1637
  // contain multiple lines when collapsed ranges are present.)                                                        // 1638
  function mapFromLineView(lineView, line, lineN) {                                                                    // 1639
    if (lineView.line == line)                                                                                         // 1640
      return {map: lineView.measure.map, cache: lineView.measure.cache};                                               // 1641
    for (var i = 0; i < lineView.rest.length; i++)                                                                     // 1642
      if (lineView.rest[i] == line)                                                                                    // 1643
        return {map: lineView.measure.maps[i], cache: lineView.measure.caches[i]};                                     // 1644
    for (var i = 0; i < lineView.rest.length; i++)                                                                     // 1645
      if (lineNo(lineView.rest[i]) > lineN)                                                                            // 1646
        return {map: lineView.measure.maps[i], cache: lineView.measure.caches[i], before: true};                       // 1647
  }                                                                                                                    // 1648
                                                                                                                       // 1649
  // Render a line into the hidden node display.externalMeasured. Used                                                 // 1650
  // when measurement is needed for a line that's not in the viewport.                                                 // 1651
  function updateExternalMeasurement(cm, line) {                                                                       // 1652
    line = visualLine(line);                                                                                           // 1653
    var lineN = lineNo(line);                                                                                          // 1654
    var view = cm.display.externalMeasured = new LineView(cm.doc, line, lineN);                                        // 1655
    view.lineN = lineN;                                                                                                // 1656
    var built = view.built = buildLineContent(cm, view);                                                               // 1657
    view.text = built.pre;                                                                                             // 1658
    removeChildrenAndAdd(cm.display.lineMeasure, built.pre);                                                           // 1659
    return view;                                                                                                       // 1660
  }                                                                                                                    // 1661
                                                                                                                       // 1662
  // Get a {top, bottom, left, right} box (in line-local coordinates)                                                  // 1663
  // for a given character.                                                                                            // 1664
  function measureChar(cm, line, ch, bias) {                                                                           // 1665
    return measureCharPrepared(cm, prepareMeasureForLine(cm, line), ch, bias);                                         // 1666
  }                                                                                                                    // 1667
                                                                                                                       // 1668
  // Find a line view that corresponds to the given line number.                                                       // 1669
  function findViewForLine(cm, lineN) {                                                                                // 1670
    if (lineN >= cm.display.viewFrom && lineN < cm.display.viewTo)                                                     // 1671
      return cm.display.view[findViewIndex(cm, lineN)];                                                                // 1672
    var ext = cm.display.externalMeasured;                                                                             // 1673
    if (ext && lineN >= ext.lineN && lineN < ext.lineN + ext.size)                                                     // 1674
      return ext;                                                                                                      // 1675
  }                                                                                                                    // 1676
                                                                                                                       // 1677
  // Measurement can be split in two steps, the set-up work that                                                       // 1678
  // applies to the whole line, and the measurement of the actual                                                      // 1679
  // character. Functions like coordsChar, that need to do a lot of                                                    // 1680
  // measurements in a row, can thus ensure that the set-up work is                                                    // 1681
  // only done once.                                                                                                   // 1682
  function prepareMeasureForLine(cm, line) {                                                                           // 1683
    var lineN = lineNo(line);                                                                                          // 1684
    var view = findViewForLine(cm, lineN);                                                                             // 1685
    if (view && !view.text)                                                                                            // 1686
      view = null;                                                                                                     // 1687
    else if (view && view.changes)                                                                                     // 1688
      updateLineForChanges(cm, view, lineN, getDimensions(cm));                                                        // 1689
    if (!view)                                                                                                         // 1690
      view = updateExternalMeasurement(cm, line);                                                                      // 1691
                                                                                                                       // 1692
    var info = mapFromLineView(view, line, lineN);                                                                     // 1693
    return {                                                                                                           // 1694
      line: line, view: view, rect: null,                                                                              // 1695
      map: info.map, cache: info.cache, before: info.before,                                                           // 1696
      hasHeights: false                                                                                                // 1697
    };                                                                                                                 // 1698
  }                                                                                                                    // 1699
                                                                                                                       // 1700
  // Given a prepared measurement object, measures the position of an                                                  // 1701
  // actual character (or fetches it from the cache).                                                                  // 1702
  function measureCharPrepared(cm, prepared, ch, bias, varHeight) {                                                    // 1703
    if (prepared.before) ch = -1;                                                                                      // 1704
    var key = ch + (bias || ""), found;                                                                                // 1705
    if (prepared.cache.hasOwnProperty(key)) {                                                                          // 1706
      found = prepared.cache[key];                                                                                     // 1707
    } else {                                                                                                           // 1708
      if (!prepared.rect)                                                                                              // 1709
        prepared.rect = prepared.view.text.getBoundingClientRect();                                                    // 1710
      if (!prepared.hasHeights) {                                                                                      // 1711
        ensureLineHeights(cm, prepared.view, prepared.rect);                                                           // 1712
        prepared.hasHeights = true;                                                                                    // 1713
      }                                                                                                                // 1714
      found = measureCharInner(cm, prepared, ch, bias);                                                                // 1715
      if (!found.bogus) prepared.cache[key] = found;                                                                   // 1716
    }                                                                                                                  // 1717
    return {left: found.left, right: found.right,                                                                      // 1718
            top: varHeight ? found.rtop : found.top,                                                                   // 1719
            bottom: varHeight ? found.rbottom : found.bottom};                                                         // 1720
  }                                                                                                                    // 1721
                                                                                                                       // 1722
  var nullRect = {left: 0, right: 0, top: 0, bottom: 0};                                                               // 1723
                                                                                                                       // 1724
  function measureCharInner(cm, prepared, ch, bias) {                                                                  // 1725
    var map = prepared.map;                                                                                            // 1726
                                                                                                                       // 1727
    var node, start, end, collapse;                                                                                    // 1728
    // First, search the line map for the text node corresponding to,                                                  // 1729
    // or closest to, the target character.                                                                            // 1730
    for (var i = 0; i < map.length; i += 3) {                                                                          // 1731
      var mStart = map[i], mEnd = map[i + 1];                                                                          // 1732
      if (ch < mStart) {                                                                                               // 1733
        start = 0; end = 1;                                                                                            // 1734
        collapse = "left";                                                                                             // 1735
      } else if (ch < mEnd) {                                                                                          // 1736
        start = ch - mStart;                                                                                           // 1737
        end = start + 1;                                                                                               // 1738
      } else if (i == map.length - 3 || ch == mEnd && map[i + 3] > ch) {                                               // 1739
        end = mEnd - mStart;                                                                                           // 1740
        start = end - 1;                                                                                               // 1741
        if (ch >= mEnd) collapse = "right";                                                                            // 1742
      }                                                                                                                // 1743
      if (start != null) {                                                                                             // 1744
        node = map[i + 2];                                                                                             // 1745
        if (mStart == mEnd && bias == (node.insertLeft ? "left" : "right"))                                            // 1746
          collapse = bias;                                                                                             // 1747
        if (bias == "left" && start == 0)                                                                              // 1748
          while (i && map[i - 2] == map[i - 3] && map[i - 1].insertLeft) {                                             // 1749
            node = map[(i -= 3) + 2];                                                                                  // 1750
            collapse = "left";                                                                                         // 1751
          }                                                                                                            // 1752
        if (bias == "right" && start == mEnd - mStart)                                                                 // 1753
          while (i < map.length - 3 && map[i + 3] == map[i + 4] && !map[i + 5].insertLeft) {                           // 1754
            node = map[(i += 3) + 2];                                                                                  // 1755
            collapse = "right";                                                                                        // 1756
          }                                                                                                            // 1757
        break;                                                                                                         // 1758
      }                                                                                                                // 1759
    }                                                                                                                  // 1760
                                                                                                                       // 1761
    var rect;                                                                                                          // 1762
    if (node.nodeType == 3) { // If it is a text node, use a range to retrieve the coordinates.                        // 1763
      for (var i = 0; i < 4; i++) { // Retry a maximum of 4 times when nonsense rectangles are returned                // 1764
        while (start && isExtendingChar(prepared.line.text.charAt(mStart + start))) --start;                           // 1765
        while (mStart + end < mEnd && isExtendingChar(prepared.line.text.charAt(mStart + end))) ++end;                 // 1766
        if (ie && ie_version < 9 && start == 0 && end == mEnd - mStart) {                                              // 1767
          rect = node.parentNode.getBoundingClientRect();                                                              // 1768
        } else if (ie && cm.options.lineWrapping) {                                                                    // 1769
          var rects = range(node, start, end).getClientRects();                                                        // 1770
          if (rects.length)                                                                                            // 1771
            rect = rects[bias == "right" ? rects.length - 1 : 0];                                                      // 1772
          else                                                                                                         // 1773
            rect = nullRect;                                                                                           // 1774
        } else {                                                                                                       // 1775
          rect = range(node, start, end).getBoundingClientRect() || nullRect;                                          // 1776
        }                                                                                                              // 1777
        if (rect.left || rect.right || start == 0) break;                                                              // 1778
        end = start;                                                                                                   // 1779
        start = start - 1;                                                                                             // 1780
        collapse = "right";                                                                                            // 1781
      }                                                                                                                // 1782
      if (ie && ie_version < 11) rect = maybeUpdateRectForZooming(cm.display.measure, rect);                           // 1783
    } else { // If it is a widget, simply get the box for the whole widget.                                            // 1784
      if (start > 0) collapse = bias = "right";                                                                        // 1785
      var rects;                                                                                                       // 1786
      if (cm.options.lineWrapping && (rects = node.getClientRects()).length > 1)                                       // 1787
        rect = rects[bias == "right" ? rects.length - 1 : 0];                                                          // 1788
      else                                                                                                             // 1789
        rect = node.getBoundingClientRect();                                                                           // 1790
    }                                                                                                                  // 1791
    if (ie && ie_version < 9 && !start && (!rect || !rect.left && !rect.right)) {                                      // 1792
      var rSpan = node.parentNode.getClientRects()[0];                                                                 // 1793
      if (rSpan)                                                                                                       // 1794
        rect = {left: rSpan.left, right: rSpan.left + charWidth(cm.display), top: rSpan.top, bottom: rSpan.bottom};    // 1795
      else                                                                                                             // 1796
        rect = nullRect;                                                                                               // 1797
    }                                                                                                                  // 1798
                                                                                                                       // 1799
    var rtop = rect.top - prepared.rect.top, rbot = rect.bottom - prepared.rect.top;                                   // 1800
    var mid = (rtop + rbot) / 2;                                                                                       // 1801
    var heights = prepared.view.measure.heights;                                                                       // 1802
    for (var i = 0; i < heights.length - 1; i++)                                                                       // 1803
      if (mid < heights[i]) break;                                                                                     // 1804
    var top = i ? heights[i - 1] : 0, bot = heights[i];                                                                // 1805
    var result = {left: (collapse == "right" ? rect.right : rect.left) - prepared.rect.left,                           // 1806
                  right: (collapse == "left" ? rect.left : rect.right) - prepared.rect.left,                           // 1807
                  top: top, bottom: bot};                                                                              // 1808
    if (!rect.left && !rect.right) result.bogus = true;                                                                // 1809
    if (!cm.options.singleCursorHeightPerLine) { result.rtop = rtop; result.rbottom = rbot; }                          // 1810
                                                                                                                       // 1811
    return result;                                                                                                     // 1812
  }                                                                                                                    // 1813
                                                                                                                       // 1814
  // Work around problem with bounding client rects on ranges being                                                    // 1815
  // returned incorrectly when zoomed on IE10 and below.                                                               // 1816
  function maybeUpdateRectForZooming(measure, rect) {                                                                  // 1817
    if (!window.screen || screen.logicalXDPI == null ||                                                                // 1818
        screen.logicalXDPI == screen.deviceXDPI || !hasBadZoomedRects(measure))                                        // 1819
      return rect;                                                                                                     // 1820
    var scaleX = screen.logicalXDPI / screen.deviceXDPI;                                                               // 1821
    var scaleY = screen.logicalYDPI / screen.deviceYDPI;                                                               // 1822
    return {left: rect.left * scaleX, right: rect.right * scaleX,                                                      // 1823
            top: rect.top * scaleY, bottom: rect.bottom * scaleY};                                                     // 1824
  }                                                                                                                    // 1825
                                                                                                                       // 1826
  function clearLineMeasurementCacheFor(lineView) {                                                                    // 1827
    if (lineView.measure) {                                                                                            // 1828
      lineView.measure.cache = {};                                                                                     // 1829
      lineView.measure.heights = null;                                                                                 // 1830
      if (lineView.rest) for (var i = 0; i < lineView.rest.length; i++)                                                // 1831
        lineView.measure.caches[i] = {};                                                                               // 1832
    }                                                                                                                  // 1833
  }                                                                                                                    // 1834
                                                                                                                       // 1835
  function clearLineMeasurementCache(cm) {                                                                             // 1836
    cm.display.externalMeasure = null;                                                                                 // 1837
    removeChildren(cm.display.lineMeasure);                                                                            // 1838
    for (var i = 0; i < cm.display.view.length; i++)                                                                   // 1839
      clearLineMeasurementCacheFor(cm.display.view[i]);                                                                // 1840
  }                                                                                                                    // 1841
                                                                                                                       // 1842
  function clearCaches(cm) {                                                                                           // 1843
    clearLineMeasurementCache(cm);                                                                                     // 1844
    cm.display.cachedCharWidth = cm.display.cachedTextHeight = cm.display.cachedPaddingH = null;                       // 1845
    if (!cm.options.lineWrapping) cm.display.maxLineChanged = true;                                                    // 1846
    cm.display.lineNumChars = null;                                                                                    // 1847
  }                                                                                                                    // 1848
                                                                                                                       // 1849
  function pageScrollX() { return window.pageXOffset || (document.documentElement || document.body).scrollLeft; }      // 1850
  function pageScrollY() { return window.pageYOffset || (document.documentElement || document.body).scrollTop; }       // 1851
                                                                                                                       // 1852
  // Converts a {top, bottom, left, right} box from line-local                                                         // 1853
  // coordinates into another coordinate system. Context may be one of                                                 // 1854
  // "line", "div" (display.lineDiv), "local"/null (editor), "window",                                                 // 1855
  // or "page".                                                                                                        // 1856
  function intoCoordSystem(cm, lineObj, rect, context) {                                                               // 1857
    if (lineObj.widgets) for (var i = 0; i < lineObj.widgets.length; ++i) if (lineObj.widgets[i].above) {              // 1858
      var size = widgetHeight(lineObj.widgets[i]);                                                                     // 1859
      rect.top += size; rect.bottom += size;                                                                           // 1860
    }                                                                                                                  // 1861
    if (context == "line") return rect;                                                                                // 1862
    if (!context) context = "local";                                                                                   // 1863
    var yOff = heightAtLine(lineObj);                                                                                  // 1864
    if (context == "local") yOff += paddingTop(cm.display);                                                            // 1865
    else yOff -= cm.display.viewOffset;                                                                                // 1866
    if (context == "page" || context == "window") {                                                                    // 1867
      var lOff = cm.display.lineSpace.getBoundingClientRect();                                                         // 1868
      yOff += lOff.top + (context == "window" ? 0 : pageScrollY());                                                    // 1869
      var xOff = lOff.left + (context == "window" ? 0 : pageScrollX());                                                // 1870
      rect.left += xOff; rect.right += xOff;                                                                           // 1871
    }                                                                                                                  // 1872
    rect.top += yOff; rect.bottom += yOff;                                                                             // 1873
    return rect;                                                                                                       // 1874
  }                                                                                                                    // 1875
                                                                                                                       // 1876
  // Coverts a box from "div" coords to another coordinate system.                                                     // 1877
  // Context may be "window", "page", "div", or "local"/null.                                                          // 1878
  function fromCoordSystem(cm, coords, context) {                                                                      // 1879
    if (context == "div") return coords;                                                                               // 1880
    var left = coords.left, top = coords.top;                                                                          // 1881
    // First move into "page" coordinate system                                                                        // 1882
    if (context == "page") {                                                                                           // 1883
      left -= pageScrollX();                                                                                           // 1884
      top -= pageScrollY();                                                                                            // 1885
    } else if (context == "local" || !context) {                                                                       // 1886
      var localBox = cm.display.sizer.getBoundingClientRect();                                                         // 1887
      left += localBox.left;                                                                                           // 1888
      top += localBox.top;                                                                                             // 1889
    }                                                                                                                  // 1890
                                                                                                                       // 1891
    var lineSpaceBox = cm.display.lineSpace.getBoundingClientRect();                                                   // 1892
    return {left: left - lineSpaceBox.left, top: top - lineSpaceBox.top};                                              // 1893
  }                                                                                                                    // 1894
                                                                                                                       // 1895
  function charCoords(cm, pos, context, lineObj, bias) {                                                               // 1896
    if (!lineObj) lineObj = getLine(cm.doc, pos.line);                                                                 // 1897
    return intoCoordSystem(cm, lineObj, measureChar(cm, lineObj, pos.ch, bias), context);                              // 1898
  }                                                                                                                    // 1899
                                                                                                                       // 1900
  // Returns a box for a given cursor position, which may have an                                                      // 1901
  // 'other' property containing the position of the secondary cursor                                                  // 1902
  // on a bidi boundary.                                                                                               // 1903
  function cursorCoords(cm, pos, context, lineObj, preparedMeasure, varHeight) {                                       // 1904
    lineObj = lineObj || getLine(cm.doc, pos.line);                                                                    // 1905
    if (!preparedMeasure) preparedMeasure = prepareMeasureForLine(cm, lineObj);                                        // 1906
    function get(ch, right) {                                                                                          // 1907
      var m = measureCharPrepared(cm, preparedMeasure, ch, right ? "right" : "left", varHeight);                       // 1908
      if (right) m.left = m.right; else m.right = m.left;                                                              // 1909
      return intoCoordSystem(cm, lineObj, m, context);                                                                 // 1910
    }                                                                                                                  // 1911
    function getBidi(ch, partPos) {                                                                                    // 1912
      var part = order[partPos], right = part.level % 2;                                                               // 1913
      if (ch == bidiLeft(part) && partPos && part.level < order[partPos - 1].level) {                                  // 1914
        part = order[--partPos];                                                                                       // 1915
        ch = bidiRight(part) - (part.level % 2 ? 0 : 1);                                                               // 1916
        right = true;                                                                                                  // 1917
      } else if (ch == bidiRight(part) && partPos < order.length - 1 && part.level < order[partPos + 1].level) {       // 1918
        part = order[++partPos];                                                                                       // 1919
        ch = bidiLeft(part) - part.level % 2;                                                                          // 1920
        right = false;                                                                                                 // 1921
      }                                                                                                                // 1922
      if (right && ch == part.to && ch > part.from) return get(ch - 1);                                                // 1923
      return get(ch, right);                                                                                           // 1924
    }                                                                                                                  // 1925
    var order = getOrder(lineObj), ch = pos.ch;                                                                        // 1926
    if (!order) return get(ch);                                                                                        // 1927
    var partPos = getBidiPartAt(order, ch);                                                                            // 1928
    var val = getBidi(ch, partPos);                                                                                    // 1929
    if (bidiOther != null) val.other = getBidi(ch, bidiOther);                                                         // 1930
    return val;                                                                                                        // 1931
  }                                                                                                                    // 1932
                                                                                                                       // 1933
  // Used to cheaply estimate the coordinates for a position. Used for                                                 // 1934
  // intermediate scroll updates.                                                                                      // 1935
  function estimateCoords(cm, pos) {                                                                                   // 1936
    var left = 0, pos = clipPos(cm.doc, pos);                                                                          // 1937
    if (!cm.options.lineWrapping) left = charWidth(cm.display) * pos.ch;                                               // 1938
    var lineObj = getLine(cm.doc, pos.line);                                                                           // 1939
    var top = heightAtLine(lineObj) + paddingTop(cm.display);                                                          // 1940
    return {left: left, right: left, top: top, bottom: top + lineObj.height};                                          // 1941
  }                                                                                                                    // 1942
                                                                                                                       // 1943
  // Positions returned by coordsChar contain some extra information.                                                  // 1944
  // xRel is the relative x position of the input coordinates compared                                                 // 1945
  // to the found position (so xRel > 0 means the coordinates are to                                                   // 1946
  // the right of the character position, for example). When outside                                                   // 1947
  // is true, that means the coordinates lie outside the line's                                                        // 1948
  // vertical range.                                                                                                   // 1949
  function PosWithInfo(line, ch, outside, xRel) {                                                                      // 1950
    var pos = Pos(line, ch);                                                                                           // 1951
    pos.xRel = xRel;                                                                                                   // 1952
    if (outside) pos.outside = true;                                                                                   // 1953
    return pos;                                                                                                        // 1954
  }                                                                                                                    // 1955
                                                                                                                       // 1956
  // Compute the character position closest to the given coordinates.                                                  // 1957
  // Input must be lineSpace-local ("div" coordinate system).                                                          // 1958
  function coordsChar(cm, x, y) {                                                                                      // 1959
    var doc = cm.doc;                                                                                                  // 1960
    y += cm.display.viewOffset;                                                                                        // 1961
    if (y < 0) return PosWithInfo(doc.first, 0, true, -1);                                                             // 1962
    var lineN = lineAtHeight(doc, y), last = doc.first + doc.size - 1;                                                 // 1963
    if (lineN > last)                                                                                                  // 1964
      return PosWithInfo(doc.first + doc.size - 1, getLine(doc, last).text.length, true, 1);                           // 1965
    if (x < 0) x = 0;                                                                                                  // 1966
                                                                                                                       // 1967
    var lineObj = getLine(doc, lineN);                                                                                 // 1968
    for (;;) {                                                                                                         // 1969
      var found = coordsCharInner(cm, lineObj, lineN, x, y);                                                           // 1970
      var merged = collapsedSpanAtEnd(lineObj);                                                                        // 1971
      var mergedPos = merged && merged.find(0, true);                                                                  // 1972
      if (merged && (found.ch > mergedPos.from.ch || found.ch == mergedPos.from.ch && found.xRel > 0))                 // 1973
        lineN = lineNo(lineObj = mergedPos.to.line);                                                                   // 1974
      else                                                                                                             // 1975
        return found;                                                                                                  // 1976
    }                                                                                                                  // 1977
  }                                                                                                                    // 1978
                                                                                                                       // 1979
  function coordsCharInner(cm, lineObj, lineNo, x, y) {                                                                // 1980
    var innerOff = y - heightAtLine(lineObj);                                                                          // 1981
    var wrongLine = false, adjust = 2 * cm.display.wrapper.clientWidth;                                                // 1982
    var preparedMeasure = prepareMeasureForLine(cm, lineObj);                                                          // 1983
                                                                                                                       // 1984
    function getX(ch) {                                                                                                // 1985
      var sp = cursorCoords(cm, Pos(lineNo, ch), "line", lineObj, preparedMeasure);                                    // 1986
      wrongLine = true;                                                                                                // 1987
      if (innerOff > sp.bottom) return sp.left - adjust;                                                               // 1988
      else if (innerOff < sp.top) return sp.left + adjust;                                                             // 1989
      else wrongLine = false;                                                                                          // 1990
      return sp.left;                                                                                                  // 1991
    }                                                                                                                  // 1992
                                                                                                                       // 1993
    var bidi = getOrder(lineObj), dist = lineObj.text.length;                                                          // 1994
    var from = lineLeft(lineObj), to = lineRight(lineObj);                                                             // 1995
    var fromX = getX(from), fromOutside = wrongLine, toX = getX(to), toOutside = wrongLine;                            // 1996
                                                                                                                       // 1997
    if (x > toX) return PosWithInfo(lineNo, to, toOutside, 1);                                                         // 1998
    // Do a binary search between these bounds.                                                                        // 1999
    for (;;) {                                                                                                         // 2000
      if (bidi ? to == from || to == moveVisually(lineObj, from, 1) : to - from <= 1) {                                // 2001
        var ch = x < fromX || x - fromX <= toX - x ? from : to;                                                        // 2002
        var xDiff = x - (ch == from ? fromX : toX);                                                                    // 2003
        while (isExtendingChar(lineObj.text.charAt(ch))) ++ch;                                                         // 2004
        var pos = PosWithInfo(lineNo, ch, ch == from ? fromOutside : toOutside,                                        // 2005
                              xDiff < -1 ? -1 : xDiff > 1 ? 1 : 0);                                                    // 2006
        return pos;                                                                                                    // 2007
      }                                                                                                                // 2008
      var step = Math.ceil(dist / 2), middle = from + step;                                                            // 2009
      if (bidi) {                                                                                                      // 2010
        middle = from;                                                                                                 // 2011
        for (var i = 0; i < step; ++i) middle = moveVisually(lineObj, middle, 1);                                      // 2012
      }                                                                                                                // 2013
      var middleX = getX(middle);                                                                                      // 2014
      if (middleX > x) {to = middle; toX = middleX; if (toOutside = wrongLine) toX += 1000; dist = step;}              // 2015
      else {from = middle; fromX = middleX; fromOutside = wrongLine; dist -= step;}                                    // 2016
    }                                                                                                                  // 2017
  }                                                                                                                    // 2018
                                                                                                                       // 2019
  var measureText;                                                                                                     // 2020
  // Compute the default text height.                                                                                  // 2021
  function textHeight(display) {                                                                                       // 2022
    if (display.cachedTextHeight != null) return display.cachedTextHeight;                                             // 2023
    if (measureText == null) {                                                                                         // 2024
      measureText = elt("pre");                                                                                        // 2025
      // Measure a bunch of lines, for browsers that compute                                                           // 2026
      // fractional heights.                                                                                           // 2027
      for (var i = 0; i < 49; ++i) {                                                                                   // 2028
        measureText.appendChild(document.createTextNode("x"));                                                         // 2029
        measureText.appendChild(elt("br"));                                                                            // 2030
      }                                                                                                                // 2031
      measureText.appendChild(document.createTextNode("x"));                                                           // 2032
    }                                                                                                                  // 2033
    removeChildrenAndAdd(display.measure, measureText);                                                                // 2034
    var height = measureText.offsetHeight / 50;                                                                        // 2035
    if (height > 3) display.cachedTextHeight = height;                                                                 // 2036
    removeChildren(display.measure);                                                                                   // 2037
    return height || 1;                                                                                                // 2038
  }                                                                                                                    // 2039
                                                                                                                       // 2040
  // Compute the default character width.                                                                              // 2041
  function charWidth(display) {                                                                                        // 2042
    if (display.cachedCharWidth != null) return display.cachedCharWidth;                                               // 2043
    var anchor = elt("span", "xxxxxxxxxx");                                                                            // 2044
    var pre = elt("pre", [anchor]);                                                                                    // 2045
    removeChildrenAndAdd(display.measure, pre);                                                                        // 2046
    var rect = anchor.getBoundingClientRect(), width = (rect.right - rect.left) / 10;                                  // 2047
    if (width > 2) display.cachedCharWidth = width;                                                                    // 2048
    return width || 10;                                                                                                // 2049
  }                                                                                                                    // 2050
                                                                                                                       // 2051
  // OPERATIONS                                                                                                        // 2052
                                                                                                                       // 2053
  // Operations are used to wrap a series of changes to the editor                                                     // 2054
  // state in such a way that each change won't have to update the                                                     // 2055
  // cursor and display (which would be awkward, slow, and                                                             // 2056
  // error-prone). Instead, display updates are batched and then all                                                   // 2057
  // combined and executed at once.                                                                                    // 2058
                                                                                                                       // 2059
  var operationGroup = null;                                                                                           // 2060
                                                                                                                       // 2061
  var nextOpId = 0;                                                                                                    // 2062
  // Start a new operation.                                                                                            // 2063
  function startOperation(cm) {                                                                                        // 2064
    cm.curOp = {                                                                                                       // 2065
      cm: cm,                                                                                                          // 2066
      viewChanged: false,      // Flag that indicates that lines might need to be redrawn                              // 2067
      startHeight: cm.doc.height, // Used to detect need to update scrollbar                                           // 2068
      forceUpdate: false,      // Used to force a redraw                                                               // 2069
      updateInput: null,       // Whether to reset the input textarea                                                  // 2070
      typing: false,           // Whether this reset should be careful to leave existing text (for compositing)        // 2071
      changeObjs: null,        // Accumulated changes, for firing change events                                        // 2072
      cursorActivityHandlers: null, // Set of handlers to fire cursorActivity on                                       // 2073
      cursorActivityCalled: 0, // Tracks which cursorActivity handlers have been called already                        // 2074
      selectionChanged: false, // Whether the selection needs to be redrawn                                            // 2075
      updateMaxLine: false,    // Set when the widest line needs to be determined anew                                 // 2076
      scrollLeft: null, scrollTop: null, // Intermediate scroll position, not pushed to DOM yet                        // 2077
      scrollToPos: null,       // Used to scroll to a specific position                                                // 2078
      id: ++nextOpId           // Unique ID                                                                            // 2079
    };                                                                                                                 // 2080
    if (operationGroup) {                                                                                              // 2081
      operationGroup.ops.push(cm.curOp);                                                                               // 2082
    } else {                                                                                                           // 2083
      cm.curOp.ownsGroup = operationGroup = {                                                                          // 2084
        ops: [cm.curOp],                                                                                               // 2085
        delayedCallbacks: []                                                                                           // 2086
      };                                                                                                               // 2087
    }                                                                                                                  // 2088
  }                                                                                                                    // 2089
                                                                                                                       // 2090
  function fireCallbacksForOps(group) {                                                                                // 2091
    // Calls delayed callbacks and cursorActivity handlers until no                                                    // 2092
    // new ones appear                                                                                                 // 2093
    var callbacks = group.delayedCallbacks, i = 0;                                                                     // 2094
    do {                                                                                                               // 2095
      for (; i < callbacks.length; i++)                                                                                // 2096
        callbacks[i]();                                                                                                // 2097
      for (var j = 0; j < group.ops.length; j++) {                                                                     // 2098
        var op = group.ops[j];                                                                                         // 2099
        if (op.cursorActivityHandlers)                                                                                 // 2100
          while (op.cursorActivityCalled < op.cursorActivityHandlers.length)                                           // 2101
            op.cursorActivityHandlers[op.cursorActivityCalled++](op.cm);                                               // 2102
      }                                                                                                                // 2103
    } while (i < callbacks.length);                                                                                    // 2104
  }                                                                                                                    // 2105
                                                                                                                       // 2106
  // Finish an operation, updating the display and signalling delayed events                                           // 2107
  function endOperation(cm) {                                                                                          // 2108
    var op = cm.curOp, group = op.ownsGroup;                                                                           // 2109
    if (!group) return;                                                                                                // 2110
                                                                                                                       // 2111
    try { fireCallbacksForOps(group); }                                                                                // 2112
    finally {                                                                                                          // 2113
      operationGroup = null;                                                                                           // 2114
      for (var i = 0; i < group.ops.length; i++)                                                                       // 2115
        group.ops[i].cm.curOp = null;                                                                                  // 2116
      endOperations(group);                                                                                            // 2117
    }                                                                                                                  // 2118
  }                                                                                                                    // 2119
                                                                                                                       // 2120
  // The DOM updates done when an operation finishes are batched so                                                    // 2121
  // that the minimum number of relayouts are required.                                                                // 2122
  function endOperations(group) {                                                                                      // 2123
    var ops = group.ops;                                                                                               // 2124
    for (var i = 0; i < ops.length; i++) // Read DOM                                                                   // 2125
      endOperation_R1(ops[i]);                                                                                         // 2126
    for (var i = 0; i < ops.length; i++) // Write DOM (maybe)                                                          // 2127
      endOperation_W1(ops[i]);                                                                                         // 2128
    for (var i = 0; i < ops.length; i++) // Read DOM                                                                   // 2129
      endOperation_R2(ops[i]);                                                                                         // 2130
    for (var i = 0; i < ops.length; i++) // Write DOM (maybe)                                                          // 2131
      endOperation_W2(ops[i]);                                                                                         // 2132
    for (var i = 0; i < ops.length; i++) // Read DOM                                                                   // 2133
      endOperation_finish(ops[i]);                                                                                     // 2134
  }                                                                                                                    // 2135
                                                                                                                       // 2136
  function endOperation_R1(op) {                                                                                       // 2137
    var cm = op.cm, display = cm.display;                                                                              // 2138
    maybeClipScrollbars(cm);                                                                                           // 2139
    if (op.updateMaxLine) findMaxLine(cm);                                                                             // 2140
                                                                                                                       // 2141
    op.mustUpdate = op.viewChanged || op.forceUpdate || op.scrollTop != null ||                                        // 2142
      op.scrollToPos && (op.scrollToPos.from.line < display.viewFrom ||                                                // 2143
                         op.scrollToPos.to.line >= display.viewTo) ||                                                  // 2144
      display.maxLineChanged && cm.options.lineWrapping;                                                               // 2145
    op.update = op.mustUpdate &&                                                                                       // 2146
      new DisplayUpdate(cm, op.mustUpdate && {top: op.scrollTop, ensure: op.scrollToPos}, op.forceUpdate);             // 2147
  }                                                                                                                    // 2148
                                                                                                                       // 2149
  function endOperation_W1(op) {                                                                                       // 2150
    op.updatedDisplay = op.mustUpdate && updateDisplayIfNeeded(op.cm, op.update);                                      // 2151
  }                                                                                                                    // 2152
                                                                                                                       // 2153
  function endOperation_R2(op) {                                                                                       // 2154
    var cm = op.cm, display = cm.display;                                                                              // 2155
    if (op.updatedDisplay) updateHeightsInViewport(cm);                                                                // 2156
                                                                                                                       // 2157
    op.barMeasure = measureForScrollbars(cm);                                                                          // 2158
                                                                                                                       // 2159
    // If the max line changed since it was last measured, measure it,                                                 // 2160
    // and ensure the document's width matches it.                                                                     // 2161
    // updateDisplay_W2 will use these properties to do the actual resizing                                            // 2162
    if (display.maxLineChanged && !cm.options.lineWrapping) {                                                          // 2163
      op.adjustWidthTo = measureChar(cm, display.maxLine, display.maxLine.text.length).left + 3;                       // 2164
      cm.display.sizerWidth = op.adjustWidthTo;                                                                        // 2165
      op.barMeasure.scrollWidth =                                                                                      // 2166
        Math.max(display.scroller.clientWidth, display.sizer.offsetLeft + op.adjustWidthTo + scrollGap(cm) + cm.display.barWidth);
      op.maxScrollLeft = Math.max(0, display.sizer.offsetLeft + op.adjustWidthTo - displayWidth(cm));                  // 2168
    }                                                                                                                  // 2169
                                                                                                                       // 2170
    if (op.updatedDisplay || op.selectionChanged)                                                                      // 2171
      op.newSelectionNodes = drawSelection(cm);                                                                        // 2172
  }                                                                                                                    // 2173
                                                                                                                       // 2174
  function endOperation_W2(op) {                                                                                       // 2175
    var cm = op.cm;                                                                                                    // 2176
                                                                                                                       // 2177
    if (op.adjustWidthTo != null) {                                                                                    // 2178
      cm.display.sizer.style.minWidth = op.adjustWidthTo + "px";                                                       // 2179
      if (op.maxScrollLeft < cm.doc.scrollLeft)                                                                        // 2180
        setScrollLeft(cm, Math.min(cm.display.scroller.scrollLeft, op.maxScrollLeft), true);                           // 2181
      cm.display.maxLineChanged = false;                                                                               // 2182
    }                                                                                                                  // 2183
                                                                                                                       // 2184
    if (op.newSelectionNodes)                                                                                          // 2185
      showSelection(cm, op.newSelectionNodes);                                                                         // 2186
    if (op.updatedDisplay)                                                                                             // 2187
      setDocumentHeight(cm, op.barMeasure);                                                                            // 2188
    if (op.updatedDisplay || op.startHeight != cm.doc.height)                                                          // 2189
      updateScrollbars(cm, op.barMeasure);                                                                             // 2190
                                                                                                                       // 2191
    if (op.selectionChanged) restartBlink(cm);                                                                         // 2192
                                                                                                                       // 2193
    if (cm.state.focused && op.updateInput)                                                                            // 2194
      resetInput(cm, op.typing);                                                                                       // 2195
  }                                                                                                                    // 2196
                                                                                                                       // 2197
  function endOperation_finish(op) {                                                                                   // 2198
    var cm = op.cm, display = cm.display, doc = cm.doc;                                                                // 2199
                                                                                                                       // 2200
    if (op.updatedDisplay) postUpdateDisplay(cm, op.update);                                                           // 2201
                                                                                                                       // 2202
    // Abort mouse wheel delta measurement, when scrolling explicitly                                                  // 2203
    if (display.wheelStartX != null && (op.scrollTop != null || op.scrollLeft != null || op.scrollToPos))              // 2204
      display.wheelStartX = display.wheelStartY = null;                                                                // 2205
                                                                                                                       // 2206
    // Propagate the scroll position to the actual DOM scroller                                                        // 2207
    if (op.scrollTop != null && (display.scroller.scrollTop != op.scrollTop || op.forceScroll)) {                      // 2208
      doc.scrollTop = Math.max(0, Math.min(display.scroller.scrollHeight - display.scroller.clientHeight, op.scrollTop));
      display.scrollbars.setScrollTop(doc.scrollTop);                                                                  // 2210
      display.scroller.scrollTop = doc.scrollTop;                                                                      // 2211
    }                                                                                                                  // 2212
    if (op.scrollLeft != null && (display.scroller.scrollLeft != op.scrollLeft || op.forceScroll)) {                   // 2213
      doc.scrollLeft = Math.max(0, Math.min(display.scroller.scrollWidth - displayWidth(cm), op.scrollLeft));          // 2214
      display.scrollbars.setScrollLeft(doc.scrollLeft);                                                                // 2215
      display.scroller.scrollLeft = doc.scrollLeft;                                                                    // 2216
      alignHorizontally(cm);                                                                                           // 2217
    }                                                                                                                  // 2218
    // If we need to scroll a specific position into view, do so.                                                      // 2219
    if (op.scrollToPos) {                                                                                              // 2220
      var coords = scrollPosIntoView(cm, clipPos(doc, op.scrollToPos.from),                                            // 2221
                                     clipPos(doc, op.scrollToPos.to), op.scrollToPos.margin);                          // 2222
      if (op.scrollToPos.isCursor && cm.state.focused) maybeScrollWindow(cm, coords);                                  // 2223
    }                                                                                                                  // 2224
                                                                                                                       // 2225
    // Fire events for markers that are hidden/unidden by editing or                                                   // 2226
    // undoing                                                                                                         // 2227
    var hidden = op.maybeHiddenMarkers, unhidden = op.maybeUnhiddenMarkers;                                            // 2228
    if (hidden) for (var i = 0; i < hidden.length; ++i)                                                                // 2229
      if (!hidden[i].lines.length) signal(hidden[i], "hide");                                                          // 2230
    if (unhidden) for (var i = 0; i < unhidden.length; ++i)                                                            // 2231
      if (unhidden[i].lines.length) signal(unhidden[i], "unhide");                                                     // 2232
                                                                                                                       // 2233
    if (display.wrapper.offsetHeight)                                                                                  // 2234
      doc.scrollTop = cm.display.scroller.scrollTop;                                                                   // 2235
                                                                                                                       // 2236
    // Fire change events, and delayed event handlers                                                                  // 2237
    if (op.changeObjs)                                                                                                 // 2238
      signal(cm, "changes", cm, op.changeObjs);                                                                        // 2239
  }                                                                                                                    // 2240
                                                                                                                       // 2241
  // Run the given function in an operation                                                                            // 2242
  function runInOp(cm, f) {                                                                                            // 2243
    if (cm.curOp) return f();                                                                                          // 2244
    startOperation(cm);                                                                                                // 2245
    try { return f(); }                                                                                                // 2246
    finally { endOperation(cm); }                                                                                      // 2247
  }                                                                                                                    // 2248
  // Wraps a function in an operation. Returns the wrapped function.                                                   // 2249
  function operation(cm, f) {                                                                                          // 2250
    return function() {                                                                                                // 2251
      if (cm.curOp) return f.apply(cm, arguments);                                                                     // 2252
      startOperation(cm);                                                                                              // 2253
      try { return f.apply(cm, arguments); }                                                                           // 2254
      finally { endOperation(cm); }                                                                                    // 2255
    };                                                                                                                 // 2256
  }                                                                                                                    // 2257
  // Used to add methods to editor and doc instances, wrapping them in                                                 // 2258
  // operations.                                                                                                       // 2259
  function methodOp(f) {                                                                                               // 2260
    return function() {                                                                                                // 2261
      if (this.curOp) return f.apply(this, arguments);                                                                 // 2262
      startOperation(this);                                                                                            // 2263
      try { return f.apply(this, arguments); }                                                                         // 2264
      finally { endOperation(this); }                                                                                  // 2265
    };                                                                                                                 // 2266
  }                                                                                                                    // 2267
  function docMethodOp(f) {                                                                                            // 2268
    return function() {                                                                                                // 2269
      var cm = this.cm;                                                                                                // 2270
      if (!cm || cm.curOp) return f.apply(this, arguments);                                                            // 2271
      startOperation(cm);                                                                                              // 2272
      try { return f.apply(this, arguments); }                                                                         // 2273
      finally { endOperation(cm); }                                                                                    // 2274
    };                                                                                                                 // 2275
  }                                                                                                                    // 2276
                                                                                                                       // 2277
  // VIEW TRACKING                                                                                                     // 2278
                                                                                                                       // 2279
  // These objects are used to represent the visible (currently drawn)                                                 // 2280
  // part of the document. A LineView may correspond to multiple                                                       // 2281
  // logical lines, if those are connected by collapsed ranges.                                                        // 2282
  function LineView(doc, line, lineN) {                                                                                // 2283
    // The starting line                                                                                               // 2284
    this.line = line;                                                                                                  // 2285
    // Continuing lines, if any                                                                                        // 2286
    this.rest = visualLineContinued(line);                                                                             // 2287
    // Number of logical lines in this visual line                                                                     // 2288
    this.size = this.rest ? lineNo(lst(this.rest)) - lineN + 1 : 1;                                                    // 2289
    this.node = this.text = null;                                                                                      // 2290
    this.hidden = lineIsHidden(doc, line);                                                                             // 2291
  }                                                                                                                    // 2292
                                                                                                                       // 2293
  // Create a range of LineView objects for the given lines.                                                           // 2294
  function buildViewArray(cm, from, to) {                                                                              // 2295
    var array = [], nextPos;                                                                                           // 2296
    for (var pos = from; pos < to; pos = nextPos) {                                                                    // 2297
      var view = new LineView(cm.doc, getLine(cm.doc, pos), pos);                                                      // 2298
      nextPos = pos + view.size;                                                                                       // 2299
      array.push(view);                                                                                                // 2300
    }                                                                                                                  // 2301
    return array;                                                                                                      // 2302
  }                                                                                                                    // 2303
                                                                                                                       // 2304
  // Updates the display.view data structure for a given change to the                                                 // 2305
  // document. From and to are in pre-change coordinates. Lendiff is                                                   // 2306
  // the amount of lines added or subtracted by the change. This is                                                    // 2307
  // used for changes that span multiple lines, or change the way                                                      // 2308
  // lines are divided into visual lines. regLineChange (below)                                                        // 2309
  // registers single-line changes.                                                                                    // 2310
  function regChange(cm, from, to, lendiff) {                                                                          // 2311
    if (from == null) from = cm.doc.first;                                                                             // 2312
    if (to == null) to = cm.doc.first + cm.doc.size;                                                                   // 2313
    if (!lendiff) lendiff = 0;                                                                                         // 2314
                                                                                                                       // 2315
    var display = cm.display;                                                                                          // 2316
    if (lendiff && to < display.viewTo &&                                                                              // 2317
        (display.updateLineNumbers == null || display.updateLineNumbers > from))                                       // 2318
      display.updateLineNumbers = from;                                                                                // 2319
                                                                                                                       // 2320
    cm.curOp.viewChanged = true;                                                                                       // 2321
                                                                                                                       // 2322
    if (from >= display.viewTo) { // Change after                                                                      // 2323
      if (sawCollapsedSpans && visualLineNo(cm.doc, from) < display.viewTo)                                            // 2324
        resetView(cm);                                                                                                 // 2325
    } else if (to <= display.viewFrom) { // Change before                                                              // 2326
      if (sawCollapsedSpans && visualLineEndNo(cm.doc, to + lendiff) > display.viewFrom) {                             // 2327
        resetView(cm);                                                                                                 // 2328
      } else {                                                                                                         // 2329
        display.viewFrom += lendiff;                                                                                   // 2330
        display.viewTo += lendiff;                                                                                     // 2331
      }                                                                                                                // 2332
    } else if (from <= display.viewFrom && to >= display.viewTo) { // Full overlap                                     // 2333
      resetView(cm);                                                                                                   // 2334
    } else if (from <= display.viewFrom) { // Top overlap                                                              // 2335
      var cut = viewCuttingPoint(cm, to, to + lendiff, 1);                                                             // 2336
      if (cut) {                                                                                                       // 2337
        display.view = display.view.slice(cut.index);                                                                  // 2338
        display.viewFrom = cut.lineN;                                                                                  // 2339
        display.viewTo += lendiff;                                                                                     // 2340
      } else {                                                                                                         // 2341
        resetView(cm);                                                                                                 // 2342
      }                                                                                                                // 2343
    } else if (to >= display.viewTo) { // Bottom overlap                                                               // 2344
      var cut = viewCuttingPoint(cm, from, from, -1);                                                                  // 2345
      if (cut) {                                                                                                       // 2346
        display.view = display.view.slice(0, cut.index);                                                               // 2347
        display.viewTo = cut.lineN;                                                                                    // 2348
      } else {                                                                                                         // 2349
        resetView(cm);                                                                                                 // 2350
      }                                                                                                                // 2351
    } else { // Gap in the middle                                                                                      // 2352
      var cutTop = viewCuttingPoint(cm, from, from, -1);                                                               // 2353
      var cutBot = viewCuttingPoint(cm, to, to + lendiff, 1);                                                          // 2354
      if (cutTop && cutBot) {                                                                                          // 2355
        display.view = display.view.slice(0, cutTop.index)                                                             // 2356
          .concat(buildViewArray(cm, cutTop.lineN, cutBot.lineN))                                                      // 2357
          .concat(display.view.slice(cutBot.index));                                                                   // 2358
        display.viewTo += lendiff;                                                                                     // 2359
      } else {                                                                                                         // 2360
        resetView(cm);                                                                                                 // 2361
      }                                                                                                                // 2362
    }                                                                                                                  // 2363
                                                                                                                       // 2364
    var ext = display.externalMeasured;                                                                                // 2365
    if (ext) {                                                                                                         // 2366
      if (to < ext.lineN)                                                                                              // 2367
        ext.lineN += lendiff;                                                                                          // 2368
      else if (from < ext.lineN + ext.size)                                                                            // 2369
        display.externalMeasured = null;                                                                               // 2370
    }                                                                                                                  // 2371
  }                                                                                                                    // 2372
                                                                                                                       // 2373
  // Register a change to a single line. Type must be one of "text",                                                   // 2374
  // "gutter", "class", "widget"                                                                                       // 2375
  function regLineChange(cm, line, type) {                                                                             // 2376
    cm.curOp.viewChanged = true;                                                                                       // 2377
    var display = cm.display, ext = cm.display.externalMeasured;                                                       // 2378
    if (ext && line >= ext.lineN && line < ext.lineN + ext.size)                                                       // 2379
      display.externalMeasured = null;                                                                                 // 2380
                                                                                                                       // 2381
    if (line < display.viewFrom || line >= display.viewTo) return;                                                     // 2382
    var lineView = display.view[findViewIndex(cm, line)];                                                              // 2383
    if (lineView.node == null) return;                                                                                 // 2384
    var arr = lineView.changes || (lineView.changes = []);                                                             // 2385
    if (indexOf(arr, type) == -1) arr.push(type);                                                                      // 2386
  }                                                                                                                    // 2387
                                                                                                                       // 2388
  // Clear the view.                                                                                                   // 2389
  function resetView(cm) {                                                                                             // 2390
    cm.display.viewFrom = cm.display.viewTo = cm.doc.first;                                                            // 2391
    cm.display.view = [];                                                                                              // 2392
    cm.display.viewOffset = 0;                                                                                         // 2393
  }                                                                                                                    // 2394
                                                                                                                       // 2395
  // Find the view element corresponding to a given line. Return null                                                  // 2396
  // when the line isn't visible.                                                                                      // 2397
  function findViewIndex(cm, n) {                                                                                      // 2398
    if (n >= cm.display.viewTo) return null;                                                                           // 2399
    n -= cm.display.viewFrom;                                                                                          // 2400
    if (n < 0) return null;                                                                                            // 2401
    var view = cm.display.view;                                                                                        // 2402
    for (var i = 0; i < view.length; i++) {                                                                            // 2403
      n -= view[i].size;                                                                                               // 2404
      if (n < 0) return i;                                                                                             // 2405
    }                                                                                                                  // 2406
  }                                                                                                                    // 2407
                                                                                                                       // 2408
  function viewCuttingPoint(cm, oldN, newN, dir) {                                                                     // 2409
    var index = findViewIndex(cm, oldN), diff, view = cm.display.view;                                                 // 2410
    if (!sawCollapsedSpans || newN == cm.doc.first + cm.doc.size)                                                      // 2411
      return {index: index, lineN: newN};                                                                              // 2412
    for (var i = 0, n = cm.display.viewFrom; i < index; i++)                                                           // 2413
      n += view[i].size;                                                                                               // 2414
    if (n != oldN) {                                                                                                   // 2415
      if (dir > 0) {                                                                                                   // 2416
        if (index == view.length - 1) return null;                                                                     // 2417
        diff = (n + view[index].size) - oldN;                                                                          // 2418
        index++;                                                                                                       // 2419
      } else {                                                                                                         // 2420
        diff = n - oldN;                                                                                               // 2421
      }                                                                                                                // 2422
      oldN += diff; newN += diff;                                                                                      // 2423
    }                                                                                                                  // 2424
    while (visualLineNo(cm.doc, newN) != newN) {                                                                       // 2425
      if (index == (dir < 0 ? 0 : view.length - 1)) return null;                                                       // 2426
      newN += dir * view[index - (dir < 0 ? 1 : 0)].size;                                                              // 2427
      index += dir;                                                                                                    // 2428
    }                                                                                                                  // 2429
    return {index: index, lineN: newN};                                                                                // 2430
  }                                                                                                                    // 2431
                                                                                                                       // 2432
  // Force the view to cover a given range, adding empty view element                                                  // 2433
  // or clipping off existing ones as needed.                                                                          // 2434
  function adjustView(cm, from, to) {                                                                                  // 2435
    var display = cm.display, view = display.view;                                                                     // 2436
    if (view.length == 0 || from >= display.viewTo || to <= display.viewFrom) {                                        // 2437
      display.view = buildViewArray(cm, from, to);                                                                     // 2438
      display.viewFrom = from;                                                                                         // 2439
    } else {                                                                                                           // 2440
      if (display.viewFrom > from)                                                                                     // 2441
        display.view = buildViewArray(cm, from, display.viewFrom).concat(display.view);                                // 2442
      else if (display.viewFrom < from)                                                                                // 2443
        display.view = display.view.slice(findViewIndex(cm, from));                                                    // 2444
      display.viewFrom = from;                                                                                         // 2445
      if (display.viewTo < to)                                                                                         // 2446
        display.view = display.view.concat(buildViewArray(cm, display.viewTo, to));                                    // 2447
      else if (display.viewTo > to)                                                                                    // 2448
        display.view = display.view.slice(0, findViewIndex(cm, to));                                                   // 2449
    }                                                                                                                  // 2450
    display.viewTo = to;                                                                                               // 2451
  }                                                                                                                    // 2452
                                                                                                                       // 2453
  // Count the number of lines in the view whose DOM representation is                                                 // 2454
  // out of date (or nonexistent).                                                                                     // 2455
  function countDirtyView(cm) {                                                                                        // 2456
    var view = cm.display.view, dirty = 0;                                                                             // 2457
    for (var i = 0; i < view.length; i++) {                                                                            // 2458
      var lineView = view[i];                                                                                          // 2459
      if (!lineView.hidden && (!lineView.node || lineView.changes)) ++dirty;                                           // 2460
    }                                                                                                                  // 2461
    return dirty;                                                                                                      // 2462
  }                                                                                                                    // 2463
                                                                                                                       // 2464
  // INPUT HANDLING                                                                                                    // 2465
                                                                                                                       // 2466
  // Poll for input changes, using the normal rate of polling. This                                                    // 2467
  // runs as long as the editor is focused.                                                                            // 2468
  function slowPoll(cm) {                                                                                              // 2469
    if (cm.display.pollingFast) return;                                                                                // 2470
    cm.display.poll.set(cm.options.pollInterval, function() {                                                          // 2471
      readInput(cm);                                                                                                   // 2472
      if (cm.state.focused) slowPoll(cm);                                                                              // 2473
    });                                                                                                                // 2474
  }                                                                                                                    // 2475
                                                                                                                       // 2476
  // When an event has just come in that is likely to add or change                                                    // 2477
  // something in the input textarea, we poll faster, to ensure that                                                   // 2478
  // the change appears on the screen quickly.                                                                         // 2479
  function fastPoll(cm) {                                                                                              // 2480
    var missed = false;                                                                                                // 2481
    cm.display.pollingFast = true;                                                                                     // 2482
    function p() {                                                                                                     // 2483
      var changed = readInput(cm);                                                                                     // 2484
      if (!changed && !missed) {missed = true; cm.display.poll.set(60, p);}                                            // 2485
      else {cm.display.pollingFast = false; slowPoll(cm);}                                                             // 2486
    }                                                                                                                  // 2487
    cm.display.poll.set(20, p);                                                                                        // 2488
  }                                                                                                                    // 2489
                                                                                                                       // 2490
  // This will be set to an array of strings when copying, so that,                                                    // 2491
  // when pasting, we know what kind of selections the copied text                                                     // 2492
  // was made out of.                                                                                                  // 2493
  var lastCopied = null;                                                                                               // 2494
                                                                                                                       // 2495
  // Read input from the textarea, and update the document to match.                                                   // 2496
  // When something is selected, it is present in the textarea, and                                                    // 2497
  // selected (unless it is huge, in which case a placeholder is                                                       // 2498
  // used). When nothing is selected, the cursor sits after previously                                                 // 2499
  // seen text (can be empty), which is stored in prevInput (we must                                                   // 2500
  // not reset the textarea when typing, because that breaks IME).                                                     // 2501
  function readInput(cm) {                                                                                             // 2502
    var input = cm.display.input, prevInput = cm.display.prevInput, doc = cm.doc;                                      // 2503
    // Since this is called a *lot*, try to bail out as cheaply as                                                     // 2504
    // possible when it is clear that nothing happened. hasSelection                                                   // 2505
    // will be the case when there is a lot of text in the textarea,                                                   // 2506
    // in which case reading its value would be expensive.                                                             // 2507
    if (!cm.state.focused || (hasSelection(input) && !prevInput) || isReadOnly(cm) || cm.options.disableInput || cm.state.keySeq)
      return false;                                                                                                    // 2509
    // See paste handler for more on the fakedLastChar kludge                                                          // 2510
    if (cm.state.pasteIncoming && cm.state.fakedLastChar) {                                                            // 2511
      input.value = input.value.substring(0, input.value.length - 1);                                                  // 2512
      cm.state.fakedLastChar = false;                                                                                  // 2513
    }                                                                                                                  // 2514
    var text = input.value;                                                                                            // 2515
    // If nothing changed, bail.                                                                                       // 2516
    if (text == prevInput && !cm.somethingSelected()) return false;                                                    // 2517
    // Work around nonsensical selection resetting in IE9/10, and                                                      // 2518
    // inexplicable appearance of private area unicode characters on                                                   // 2519
    // some key combos in Mac (#2689).                                                                                 // 2520
    if (ie && ie_version >= 9 && cm.display.inputHasSelection === text ||                                              // 2521
        mac && /[\uf700-\uf7ff]/.test(text)) {                                                                         // 2522
      resetInput(cm);                                                                                                  // 2523
      return false;                                                                                                    // 2524
    }                                                                                                                  // 2525
                                                                                                                       // 2526
    var withOp = !cm.curOp;                                                                                            // 2527
    if (withOp) startOperation(cm);                                                                                    // 2528
    cm.display.shift = false;                                                                                          // 2529
                                                                                                                       // 2530
    if (text.charCodeAt(0) == 0x200b && doc.sel == cm.display.selForContextMenu && !prevInput)                         // 2531
      prevInput = "\u200b";                                                                                            // 2532
    // Find the part of the input that is actually new                                                                 // 2533
    var same = 0, l = Math.min(prevInput.length, text.length);                                                         // 2534
    while (same < l && prevInput.charCodeAt(same) == text.charCodeAt(same)) ++same;                                    // 2535
    var inserted = text.slice(same), textLines = splitLines(inserted);                                                 // 2536
                                                                                                                       // 2537
    // When pasing N lines into N selections, insert one line per selection                                            // 2538
    var multiPaste = null;                                                                                             // 2539
    if (cm.state.pasteIncoming && doc.sel.ranges.length > 1) {                                                         // 2540
      if (lastCopied && lastCopied.join("\n") == inserted)                                                             // 2541
        multiPaste = doc.sel.ranges.length % lastCopied.length == 0 && map(lastCopied, splitLines);                    // 2542
      else if (textLines.length == doc.sel.ranges.length)                                                              // 2543
        multiPaste = map(textLines, function(l) { return [l]; });                                                      // 2544
    }                                                                                                                  // 2545
                                                                                                                       // 2546
    // Normal behavior is to insert the new text into every selection                                                  // 2547
    for (var i = doc.sel.ranges.length - 1; i >= 0; i--) {                                                             // 2548
      var range = doc.sel.ranges[i];                                                                                   // 2549
      var from = range.from(), to = range.to();                                                                        // 2550
      // Handle deletion                                                                                               // 2551
      if (same < prevInput.length)                                                                                     // 2552
        from = Pos(from.line, from.ch - (prevInput.length - same));                                                    // 2553
      // Handle overwrite                                                                                              // 2554
      else if (cm.state.overwrite && range.empty() && !cm.state.pasteIncoming)                                         // 2555
        to = Pos(to.line, Math.min(getLine(doc, to.line).text.length, to.ch + lst(textLines).length));                 // 2556
      var updateInput = cm.curOp.updateInput;                                                                          // 2557
      var changeEvent = {from: from, to: to, text: multiPaste ? multiPaste[i % multiPaste.length] : textLines,         // 2558
                         origin: cm.state.pasteIncoming ? "paste" : cm.state.cutIncoming ? "cut" : "+input"};          // 2559
      makeChange(cm.doc, changeEvent);                                                                                 // 2560
      signalLater(cm, "inputRead", cm, changeEvent);                                                                   // 2561
      // When an 'electric' character is inserted, immediately trigger a reindent                                      // 2562
      if (inserted && !cm.state.pasteIncoming && cm.options.electricChars &&                                           // 2563
          cm.options.smartIndent && range.head.ch < 100 &&                                                             // 2564
          (!i || doc.sel.ranges[i - 1].head.line != range.head.line)) {                                                // 2565
        var mode = cm.getModeAt(range.head);                                                                           // 2566
        var end = changeEnd(changeEvent);                                                                              // 2567
        if (mode.electricChars) {                                                                                      // 2568
          for (var j = 0; j < mode.electricChars.length; j++)                                                          // 2569
            if (inserted.indexOf(mode.electricChars.charAt(j)) > -1) {                                                 // 2570
              indentLine(cm, end.line, "smart");                                                                       // 2571
              break;                                                                                                   // 2572
            }                                                                                                          // 2573
        } else if (mode.electricInput) {                                                                               // 2574
          if (mode.electricInput.test(getLine(doc, end.line).text.slice(0, end.ch)))                                   // 2575
            indentLine(cm, end.line, "smart");                                                                         // 2576
        }                                                                                                              // 2577
      }                                                                                                                // 2578
    }                                                                                                                  // 2579
    ensureCursorVisible(cm);                                                                                           // 2580
    cm.curOp.updateInput = updateInput;                                                                                // 2581
    cm.curOp.typing = true;                                                                                            // 2582
                                                                                                                       // 2583
    // Don't leave long text in the textarea, since it makes further polling slow                                      // 2584
    if (text.length > 1000 || text.indexOf("\n") > -1) input.value = cm.display.prevInput = "";                        // 2585
    else cm.display.prevInput = text;                                                                                  // 2586
    if (withOp) endOperation(cm);                                                                                      // 2587
    cm.state.pasteIncoming = cm.state.cutIncoming = false;                                                             // 2588
    return true;                                                                                                       // 2589
  }                                                                                                                    // 2590
                                                                                                                       // 2591
  // Reset the input to correspond to the selection (or to be empty,                                                   // 2592
  // when not typing and nothing is selected)                                                                          // 2593
  function resetInput(cm, typing) {                                                                                    // 2594
    if (cm.display.contextMenuPending) return;                                                                         // 2595
    var minimal, selected, doc = cm.doc;                                                                               // 2596
    if (cm.somethingSelected()) {                                                                                      // 2597
      cm.display.prevInput = "";                                                                                       // 2598
      var range = doc.sel.primary();                                                                                   // 2599
      minimal = hasCopyEvent &&                                                                                        // 2600
        (range.to().line - range.from().line > 100 || (selected = cm.getSelection()).length > 1000);                   // 2601
      var content = minimal ? "-" : selected || cm.getSelection();                                                     // 2602
      cm.display.input.value = content;                                                                                // 2603
      if (cm.state.focused) selectInput(cm.display.input);                                                             // 2604
      if (ie && ie_version >= 9) cm.display.inputHasSelection = content;                                               // 2605
    } else if (!typing) {                                                                                              // 2606
      cm.display.prevInput = cm.display.input.value = "";                                                              // 2607
      if (ie && ie_version >= 9) cm.display.inputHasSelection = null;                                                  // 2608
    }                                                                                                                  // 2609
    cm.display.inaccurateSelection = minimal;                                                                          // 2610
  }                                                                                                                    // 2611
                                                                                                                       // 2612
  function focusInput(cm) {                                                                                            // 2613
    if (cm.options.readOnly != "nocursor" && (!mobile || activeElt() != cm.display.input))                             // 2614
      cm.display.input.focus();                                                                                        // 2615
  }                                                                                                                    // 2616
                                                                                                                       // 2617
  function ensureFocus(cm) {                                                                                           // 2618
    if (!cm.state.focused) { focusInput(cm); onFocus(cm); }                                                            // 2619
  }                                                                                                                    // 2620
                                                                                                                       // 2621
  function isReadOnly(cm) {                                                                                            // 2622
    return cm.options.readOnly || cm.doc.cantEdit;                                                                     // 2623
  }                                                                                                                    // 2624
                                                                                                                       // 2625
  // EVENT HANDLERS                                                                                                    // 2626
                                                                                                                       // 2627
  // Attach the necessary event handlers when initializing the editor                                                  // 2628
  function registerEventHandlers(cm) {                                                                                 // 2629
    var d = cm.display;                                                                                                // 2630
    on(d.scroller, "mousedown", operation(cm, onMouseDown));                                                           // 2631
    // Older IE's will not fire a second mousedown for a double click                                                  // 2632
    if (ie && ie_version < 11)                                                                                         // 2633
      on(d.scroller, "dblclick", operation(cm, function(e) {                                                           // 2634
        if (signalDOMEvent(cm, e)) return;                                                                             // 2635
        var pos = posFromMouse(cm, e);                                                                                 // 2636
        if (!pos || clickInGutter(cm, e) || eventInWidget(cm.display, e)) return;                                      // 2637
        e_preventDefault(e);                                                                                           // 2638
        var word = cm.findWordAt(pos);                                                                                 // 2639
        extendSelection(cm.doc, word.anchor, word.head);                                                               // 2640
      }));                                                                                                             // 2641
    else                                                                                                               // 2642
      on(d.scroller, "dblclick", function(e) { signalDOMEvent(cm, e) || e_preventDefault(e); });                       // 2643
    // Prevent normal selection in the editor (we handle our own)                                                      // 2644
    on(d.lineSpace, "selectstart", function(e) {                                                                       // 2645
      if (!eventInWidget(d, e)) e_preventDefault(e);                                                                   // 2646
    });                                                                                                                // 2647
    // Some browsers fire contextmenu *after* opening the menu, at                                                     // 2648
    // which point we can't mess with it anymore. Context menu is                                                      // 2649
    // handled in onMouseDown for these browsers.                                                                      // 2650
    if (!captureRightClick) on(d.scroller, "contextmenu", function(e) {onContextMenu(cm, e);});                        // 2651
                                                                                                                       // 2652
    // Sync scrolling between fake scrollbars and real scrollable                                                      // 2653
    // area, ensure viewport is updated when scrolling.                                                                // 2654
    on(d.scroller, "scroll", function() {                                                                              // 2655
      if (d.scroller.clientHeight) {                                                                                   // 2656
        setScrollTop(cm, d.scroller.scrollTop);                                                                        // 2657
        setScrollLeft(cm, d.scroller.scrollLeft, true);                                                                // 2658
        signal(cm, "scroll", cm);                                                                                      // 2659
      }                                                                                                                // 2660
    });                                                                                                                // 2661
                                                                                                                       // 2662
    // Listen to wheel events in order to try and update the viewport on time.                                         // 2663
    on(d.scroller, "mousewheel", function(e){onScrollWheel(cm, e);});                                                  // 2664
    on(d.scroller, "DOMMouseScroll", function(e){onScrollWheel(cm, e);});                                              // 2665
                                                                                                                       // 2666
    // Prevent wrapper from ever scrolling                                                                             // 2667
    on(d.wrapper, "scroll", function() { d.wrapper.scrollTop = d.wrapper.scrollLeft = 0; });                           // 2668
                                                                                                                       // 2669
    on(d.input, "keyup", function(e) { onKeyUp.call(cm, e); });                                                        // 2670
    on(d.input, "input", function() {                                                                                  // 2671
      if (ie && ie_version >= 9 && cm.display.inputHasSelection) cm.display.inputHasSelection = null;                  // 2672
      readInput(cm);                                                                                                   // 2673
    });                                                                                                                // 2674
    on(d.input, "keydown", operation(cm, onKeyDown));                                                                  // 2675
    on(d.input, "keypress", operation(cm, onKeyPress));                                                                // 2676
    on(d.input, "focus", bind(onFocus, cm));                                                                           // 2677
    on(d.input, "blur", bind(onBlur, cm));                                                                             // 2678
                                                                                                                       // 2679
    function drag_(e) {                                                                                                // 2680
      if (!signalDOMEvent(cm, e)) e_stop(e);                                                                           // 2681
    }                                                                                                                  // 2682
    if (cm.options.dragDrop) {                                                                                         // 2683
      on(d.scroller, "dragstart", function(e){onDragStart(cm, e);});                                                   // 2684
      on(d.scroller, "dragenter", drag_);                                                                              // 2685
      on(d.scroller, "dragover", drag_);                                                                               // 2686
      on(d.scroller, "drop", operation(cm, onDrop));                                                                   // 2687
    }                                                                                                                  // 2688
    on(d.scroller, "paste", function(e) {                                                                              // 2689
      if (eventInWidget(d, e)) return;                                                                                 // 2690
      cm.state.pasteIncoming = true;                                                                                   // 2691
      focusInput(cm);                                                                                                  // 2692
      fastPoll(cm);                                                                                                    // 2693
    });                                                                                                                // 2694
    on(d.input, "paste", function() {                                                                                  // 2695
      // Workaround for webkit bug https://bugs.webkit.org/show_bug.cgi?id=90206                                       // 2696
      // Add a char to the end of textarea before paste occur so that                                                  // 2697
      // selection doesn't span to the end of textarea.                                                                // 2698
      if (webkit && !cm.state.fakedLastChar && !(new Date - cm.state.lastMiddleDown < 200)) {                          // 2699
        var start = d.input.selectionStart, end = d.input.selectionEnd;                                                // 2700
        d.input.value += "$";                                                                                          // 2701
        // The selection end needs to be set before the start, otherwise there                                         // 2702
        // can be an intermediate non-empty selection between the two, which                                           // 2703
        // can override the middle-click paste buffer on linux and cause the                                           // 2704
        // wrong thing to get pasted.                                                                                  // 2705
        d.input.selectionEnd = end;                                                                                    // 2706
        d.input.selectionStart = start;                                                                                // 2707
        cm.state.fakedLastChar = true;                                                                                 // 2708
      }                                                                                                                // 2709
      cm.state.pasteIncoming = true;                                                                                   // 2710
      fastPoll(cm);                                                                                                    // 2711
    });                                                                                                                // 2712
                                                                                                                       // 2713
    function prepareCopyCut(e) {                                                                                       // 2714
      if (cm.somethingSelected()) {                                                                                    // 2715
        lastCopied = cm.getSelections();                                                                               // 2716
        if (d.inaccurateSelection) {                                                                                   // 2717
          d.prevInput = "";                                                                                            // 2718
          d.inaccurateSelection = false;                                                                               // 2719
          d.input.value = lastCopied.join("\n");                                                                       // 2720
          selectInput(d.input);                                                                                        // 2721
        }                                                                                                              // 2722
      } else {                                                                                                         // 2723
        var text = [], ranges = [];                                                                                    // 2724
        for (var i = 0; i < cm.doc.sel.ranges.length; i++) {                                                           // 2725
          var line = cm.doc.sel.ranges[i].head.line;                                                                   // 2726
          var lineRange = {anchor: Pos(line, 0), head: Pos(line + 1, 0)};                                              // 2727
          ranges.push(lineRange);                                                                                      // 2728
          text.push(cm.getRange(lineRange.anchor, lineRange.head));                                                    // 2729
        }                                                                                                              // 2730
        if (e.type == "cut") {                                                                                         // 2731
          cm.setSelections(ranges, null, sel_dontScroll);                                                              // 2732
        } else {                                                                                                       // 2733
          d.prevInput = "";                                                                                            // 2734
          d.input.value = text.join("\n");                                                                             // 2735
          selectInput(d.input);                                                                                        // 2736
        }                                                                                                              // 2737
        lastCopied = text;                                                                                             // 2738
      }                                                                                                                // 2739
      if (e.type == "cut") cm.state.cutIncoming = true;                                                                // 2740
    }                                                                                                                  // 2741
    on(d.input, "cut", prepareCopyCut);                                                                                // 2742
    on(d.input, "copy", prepareCopyCut);                                                                               // 2743
                                                                                                                       // 2744
    // Needed to handle Tab key in KHTML                                                                               // 2745
    if (khtml) on(d.sizer, "mouseup", function() {                                                                     // 2746
      if (activeElt() == d.input) d.input.blur();                                                                      // 2747
      focusInput(cm);                                                                                                  // 2748
    });                                                                                                                // 2749
  }                                                                                                                    // 2750
                                                                                                                       // 2751
  // Called when the window resizes                                                                                    // 2752
  function onResize(cm) {                                                                                              // 2753
    var d = cm.display;                                                                                                // 2754
    if (d.lastWrapHeight == d.wrapper.clientHeight && d.lastWrapWidth == d.wrapper.clientWidth)                        // 2755
      return;                                                                                                          // 2756
    // Might be a text scaling operation, clear size caches.                                                           // 2757
    d.cachedCharWidth = d.cachedTextHeight = d.cachedPaddingH = null;                                                  // 2758
    d.scrollbarsClipped = false;                                                                                       // 2759
    cm.setSize();                                                                                                      // 2760
  }                                                                                                                    // 2761
                                                                                                                       // 2762
  // MOUSE EVENTS                                                                                                      // 2763
                                                                                                                       // 2764
  // Return true when the given mouse event happened in a widget                                                       // 2765
  function eventInWidget(display, e) {                                                                                 // 2766
    for (var n = e_target(e); n != display.wrapper; n = n.parentNode) {                                                // 2767
      if (!n || (n.nodeType == 1 && n.getAttribute("cm-ignore-events") == "true") ||                                   // 2768
          (n.parentNode == display.sizer && n != display.mover))                                                       // 2769
        return true;                                                                                                   // 2770
    }                                                                                                                  // 2771
  }                                                                                                                    // 2772
                                                                                                                       // 2773
  // Given a mouse event, find the corresponding position. If liberal                                                  // 2774
  // is false, it checks whether a gutter or scrollbar was clicked,                                                    // 2775
  // and returns null if it was. forRect is used by rectangular                                                        // 2776
  // selections, and tries to estimate a character position even for                                                   // 2777
  // coordinates beyond the right of the text.                                                                         // 2778
  function posFromMouse(cm, e, liberal, forRect) {                                                                     // 2779
    var display = cm.display;                                                                                          // 2780
    if (!liberal && e_target(e).getAttribute("not-content") == "true") return null;                                    // 2781
                                                                                                                       // 2782
    var x, y, space = display.lineSpace.getBoundingClientRect();                                                       // 2783
    // Fails unpredictably on IE[67] when mouse is dragged around quickly.                                             // 2784
    try { x = e.clientX - space.left; y = e.clientY - space.top; }                                                     // 2785
    catch (e) { return null; }                                                                                         // 2786
    var coords = coordsChar(cm, x, y), line;                                                                           // 2787
    if (forRect && coords.xRel == 1 && (line = getLine(cm.doc, coords.line).text).length == coords.ch) {               // 2788
      var colDiff = countColumn(line, line.length, cm.options.tabSize) - line.length;                                  // 2789
      coords = Pos(coords.line, Math.max(0, Math.round((x - paddingH(cm.display).left) / charWidth(cm.display)) - colDiff));
    }                                                                                                                  // 2791
    return coords;                                                                                                     // 2792
  }                                                                                                                    // 2793
                                                                                                                       // 2794
  // A mouse down can be a single click, double click, triple click,                                                   // 2795
  // start of selection drag, start of text drag, new cursor                                                           // 2796
  // (ctrl-click), rectangle drag (alt-drag), or xwin                                                                  // 2797
  // middle-click-paste. Or it might be a click on something we should                                                 // 2798
  // not interfere with, such as a scrollbar or widget.                                                                // 2799
  function onMouseDown(e) {                                                                                            // 2800
    if (signalDOMEvent(this, e)) return;                                                                               // 2801
    var cm = this, display = cm.display;                                                                               // 2802
    display.shift = e.shiftKey;                                                                                        // 2803
                                                                                                                       // 2804
    if (eventInWidget(display, e)) {                                                                                   // 2805
      if (!webkit) {                                                                                                   // 2806
        // Briefly turn off draggability, to allow widgets to do                                                       // 2807
        // normal dragging things.                                                                                     // 2808
        display.scroller.draggable = false;                                                                            // 2809
        setTimeout(function(){display.scroller.draggable = true;}, 100);                                               // 2810
      }                                                                                                                // 2811
      return;                                                                                                          // 2812
    }                                                                                                                  // 2813
    if (clickInGutter(cm, e)) return;                                                                                  // 2814
    var start = posFromMouse(cm, e);                                                                                   // 2815
    window.focus();                                                                                                    // 2816
                                                                                                                       // 2817
    switch (e_button(e)) {                                                                                             // 2818
    case 1:                                                                                                            // 2819
      if (start)                                                                                                       // 2820
        leftButtonDown(cm, e, start);                                                                                  // 2821
      else if (e_target(e) == display.scroller)                                                                        // 2822
        e_preventDefault(e);                                                                                           // 2823
      break;                                                                                                           // 2824
    case 2:                                                                                                            // 2825
      if (webkit) cm.state.lastMiddleDown = +new Date;                                                                 // 2826
      if (start) extendSelection(cm.doc, start);                                                                       // 2827
      setTimeout(bind(focusInput, cm), 20);                                                                            // 2828
      e_preventDefault(e);                                                                                             // 2829
      break;                                                                                                           // 2830
    case 3:                                                                                                            // 2831
      if (captureRightClick) onContextMenu(cm, e);                                                                     // 2832
      break;                                                                                                           // 2833
    }                                                                                                                  // 2834
  }                                                                                                                    // 2835
                                                                                                                       // 2836
  var lastClick, lastDoubleClick;                                                                                      // 2837
  function leftButtonDown(cm, e, start) {                                                                              // 2838
    setTimeout(bind(ensureFocus, cm), 0);                                                                              // 2839
                                                                                                                       // 2840
    var now = +new Date, type;                                                                                         // 2841
    if (lastDoubleClick && lastDoubleClick.time > now - 400 && cmp(lastDoubleClick.pos, start) == 0) {                 // 2842
      type = "triple";                                                                                                 // 2843
    } else if (lastClick && lastClick.time > now - 400 && cmp(lastClick.pos, start) == 0) {                            // 2844
      type = "double";                                                                                                 // 2845
      lastDoubleClick = {time: now, pos: start};                                                                       // 2846
    } else {                                                                                                           // 2847
      type = "single";                                                                                                 // 2848
      lastClick = {time: now, pos: start};                                                                             // 2849
    }                                                                                                                  // 2850
                                                                                                                       // 2851
    var sel = cm.doc.sel, modifier = mac ? e.metaKey : e.ctrlKey, contained;                                           // 2852
    if (cm.options.dragDrop && dragAndDrop && !isReadOnly(cm) &&                                                       // 2853
        type == "single" && (contained = sel.contains(start)) > -1 &&                                                  // 2854
        !sel.ranges[contained].empty())                                                                                // 2855
      leftButtonStartDrag(cm, e, start, modifier);                                                                     // 2856
    else                                                                                                               // 2857
      leftButtonSelect(cm, e, start, type, modifier);                                                                  // 2858
  }                                                                                                                    // 2859
                                                                                                                       // 2860
  // Start a text drag. When it ends, see if any dragging actually                                                     // 2861
  // happen, and treat as a click if it didn't.                                                                        // 2862
  function leftButtonStartDrag(cm, e, start, modifier) {                                                               // 2863
    var display = cm.display;                                                                                          // 2864
    var dragEnd = operation(cm, function(e2) {                                                                         // 2865
      if (webkit) display.scroller.draggable = false;                                                                  // 2866
      cm.state.draggingText = false;                                                                                   // 2867
      off(document, "mouseup", dragEnd);                                                                               // 2868
      off(display.scroller, "drop", dragEnd);                                                                          // 2869
      if (Math.abs(e.clientX - e2.clientX) + Math.abs(e.clientY - e2.clientY) < 10) {                                  // 2870
        e_preventDefault(e2);                                                                                          // 2871
        if (!modifier)                                                                                                 // 2872
          extendSelection(cm.doc, start);                                                                              // 2873
        focusInput(cm);                                                                                                // 2874
        // Work around unexplainable focus problem in IE9 (#2127)                                                      // 2875
        if (ie && ie_version == 9)                                                                                     // 2876
          setTimeout(function() {document.body.focus(); focusInput(cm);}, 20);                                         // 2877
      }                                                                                                                // 2878
    });                                                                                                                // 2879
    // Let the drag handler handle this.                                                                               // 2880
    if (webkit) display.scroller.draggable = true;                                                                     // 2881
    cm.state.draggingText = dragEnd;                                                                                   // 2882
    // IE's approach to draggable                                                                                      // 2883
    if (display.scroller.dragDrop) display.scroller.dragDrop();                                                        // 2884
    on(document, "mouseup", dragEnd);                                                                                  // 2885
    on(display.scroller, "drop", dragEnd);                                                                             // 2886
  }                                                                                                                    // 2887
                                                                                                                       // 2888
  // Normal selection, as opposed to text dragging.                                                                    // 2889
  function leftButtonSelect(cm, e, start, type, addNew) {                                                              // 2890
    var display = cm.display, doc = cm.doc;                                                                            // 2891
    e_preventDefault(e);                                                                                               // 2892
                                                                                                                       // 2893
    var ourRange, ourIndex, startSel = doc.sel, ranges = startSel.ranges;                                              // 2894
    if (addNew && !e.shiftKey) {                                                                                       // 2895
      ourIndex = doc.sel.contains(start);                                                                              // 2896
      if (ourIndex > -1)                                                                                               // 2897
        ourRange = ranges[ourIndex];                                                                                   // 2898
      else                                                                                                             // 2899
        ourRange = new Range(start, start);                                                                            // 2900
    } else {                                                                                                           // 2901
      ourRange = doc.sel.primary();                                                                                    // 2902
    }                                                                                                                  // 2903
                                                                                                                       // 2904
    if (e.altKey) {                                                                                                    // 2905
      type = "rect";                                                                                                   // 2906
      if (!addNew) ourRange = new Range(start, start);                                                                 // 2907
      start = posFromMouse(cm, e, true, true);                                                                         // 2908
      ourIndex = -1;                                                                                                   // 2909
    } else if (type == "double") {                                                                                     // 2910
      var word = cm.findWordAt(start);                                                                                 // 2911
      if (cm.display.shift || doc.extend)                                                                              // 2912
        ourRange = extendRange(doc, ourRange, word.anchor, word.head);                                                 // 2913
      else                                                                                                             // 2914
        ourRange = word;                                                                                               // 2915
    } else if (type == "triple") {                                                                                     // 2916
      var line = new Range(Pos(start.line, 0), clipPos(doc, Pos(start.line + 1, 0)));                                  // 2917
      if (cm.display.shift || doc.extend)                                                                              // 2918
        ourRange = extendRange(doc, ourRange, line.anchor, line.head);                                                 // 2919
      else                                                                                                             // 2920
        ourRange = line;                                                                                               // 2921
    } else {                                                                                                           // 2922
      ourRange = extendRange(doc, ourRange, start);                                                                    // 2923
    }                                                                                                                  // 2924
                                                                                                                       // 2925
    if (!addNew) {                                                                                                     // 2926
      ourIndex = 0;                                                                                                    // 2927
      setSelection(doc, new Selection([ourRange], 0), sel_mouse);                                                      // 2928
      startSel = doc.sel;                                                                                              // 2929
    } else if (ourIndex == -1) {                                                                                       // 2930
      ourIndex = ranges.length;                                                                                        // 2931
      setSelection(doc, normalizeSelection(ranges.concat([ourRange]), ourIndex),                                       // 2932
                   {scroll: false, origin: "*mouse"});                                                                 // 2933
    } else if (ranges.length > 1 && ranges[ourIndex].empty() && type == "single") {                                    // 2934
      setSelection(doc, normalizeSelection(ranges.slice(0, ourIndex).concat(ranges.slice(ourIndex + 1)), 0));          // 2935
      startSel = doc.sel;                                                                                              // 2936
    } else {                                                                                                           // 2937
      replaceOneSelection(doc, ourIndex, ourRange, sel_mouse);                                                         // 2938
    }                                                                                                                  // 2939
                                                                                                                       // 2940
    var lastPos = start;                                                                                               // 2941
    function extendTo(pos) {                                                                                           // 2942
      if (cmp(lastPos, pos) == 0) return;                                                                              // 2943
      lastPos = pos;                                                                                                   // 2944
                                                                                                                       // 2945
      if (type == "rect") {                                                                                            // 2946
        var ranges = [], tabSize = cm.options.tabSize;                                                                 // 2947
        var startCol = countColumn(getLine(doc, start.line).text, start.ch, tabSize);                                  // 2948
        var posCol = countColumn(getLine(doc, pos.line).text, pos.ch, tabSize);                                        // 2949
        var left = Math.min(startCol, posCol), right = Math.max(startCol, posCol);                                     // 2950
        for (var line = Math.min(start.line, pos.line), end = Math.min(cm.lastLine(), Math.max(start.line, pos.line)); // 2951
             line <= end; line++) {                                                                                    // 2952
          var text = getLine(doc, line).text, leftPos = findColumn(text, left, tabSize);                               // 2953
          if (left == right)                                                                                           // 2954
            ranges.push(new Range(Pos(line, leftPos), Pos(line, leftPos)));                                            // 2955
          else if (text.length > leftPos)                                                                              // 2956
            ranges.push(new Range(Pos(line, leftPos), Pos(line, findColumn(text, right, tabSize))));                   // 2957
        }                                                                                                              // 2958
        if (!ranges.length) ranges.push(new Range(start, start));                                                      // 2959
        setSelection(doc, normalizeSelection(startSel.ranges.slice(0, ourIndex).concat(ranges), ourIndex),             // 2960
                     {origin: "*mouse", scroll: false});                                                               // 2961
        cm.scrollIntoView(pos);                                                                                        // 2962
      } else {                                                                                                         // 2963
        var oldRange = ourRange;                                                                                       // 2964
        var anchor = oldRange.anchor, head = pos;                                                                      // 2965
        if (type != "single") {                                                                                        // 2966
          if (type == "double")                                                                                        // 2967
            var range = cm.findWordAt(pos);                                                                            // 2968
          else                                                                                                         // 2969
            var range = new Range(Pos(pos.line, 0), clipPos(doc, Pos(pos.line + 1, 0)));                               // 2970
          if (cmp(range.anchor, anchor) > 0) {                                                                         // 2971
            head = range.head;                                                                                         // 2972
            anchor = minPos(oldRange.from(), range.anchor);                                                            // 2973
          } else {                                                                                                     // 2974
            head = range.anchor;                                                                                       // 2975
            anchor = maxPos(oldRange.to(), range.head);                                                                // 2976
          }                                                                                                            // 2977
        }                                                                                                              // 2978
        var ranges = startSel.ranges.slice(0);                                                                         // 2979
        ranges[ourIndex] = new Range(clipPos(doc, anchor), head);                                                      // 2980
        setSelection(doc, normalizeSelection(ranges, ourIndex), sel_mouse);                                            // 2981
      }                                                                                                                // 2982
    }                                                                                                                  // 2983
                                                                                                                       // 2984
    var editorSize = display.wrapper.getBoundingClientRect();                                                          // 2985
    // Used to ensure timeout re-tries don't fire when another extend                                                  // 2986
    // happened in the meantime (clearTimeout isn't reliable -- at                                                     // 2987
    // least on Chrome, the timeouts still happen even when cleared,                                                   // 2988
    // if the clear happens after their scheduled firing time).                                                        // 2989
    var counter = 0;                                                                                                   // 2990
                                                                                                                       // 2991
    function extend(e) {                                                                                               // 2992
      var curCount = ++counter;                                                                                        // 2993
      var cur = posFromMouse(cm, e, true, type == "rect");                                                             // 2994
      if (!cur) return;                                                                                                // 2995
      if (cmp(cur, lastPos) != 0) {                                                                                    // 2996
        ensureFocus(cm);                                                                                               // 2997
        extendTo(cur);                                                                                                 // 2998
        var visible = visibleLines(display, doc);                                                                      // 2999
        if (cur.line >= visible.to || cur.line < visible.from)                                                         // 3000
          setTimeout(operation(cm, function(){if (counter == curCount) extend(e);}), 150);                             // 3001
      } else {                                                                                                         // 3002
        var outside = e.clientY < editorSize.top ? -20 : e.clientY > editorSize.bottom ? 20 : 0;                       // 3003
        if (outside) setTimeout(operation(cm, function() {                                                             // 3004
          if (counter != curCount) return;                                                                             // 3005
          display.scroller.scrollTop += outside;                                                                       // 3006
          extend(e);                                                                                                   // 3007
        }), 50);                                                                                                       // 3008
      }                                                                                                                // 3009
    }                                                                                                                  // 3010
                                                                                                                       // 3011
    function done(e) {                                                                                                 // 3012
      counter = Infinity;                                                                                              // 3013
      e_preventDefault(e);                                                                                             // 3014
      focusInput(cm);                                                                                                  // 3015
      off(document, "mousemove", move);                                                                                // 3016
      off(document, "mouseup", up);                                                                                    // 3017
      doc.history.lastSelOrigin = null;                                                                                // 3018
    }                                                                                                                  // 3019
                                                                                                                       // 3020
    var move = operation(cm, function(e) {                                                                             // 3021
      if (!e_button(e)) done(e);                                                                                       // 3022
      else extend(e);                                                                                                  // 3023
    });                                                                                                                // 3024
    var up = operation(cm, done);                                                                                      // 3025
    on(document, "mousemove", move);                                                                                   // 3026
    on(document, "mouseup", up);                                                                                       // 3027
  }                                                                                                                    // 3028
                                                                                                                       // 3029
  // Determines whether an event happened in the gutter, and fires the                                                 // 3030
  // handlers for the corresponding event.                                                                             // 3031
  function gutterEvent(cm, e, type, prevent, signalfn) {                                                               // 3032
    try { var mX = e.clientX, mY = e.clientY; }                                                                        // 3033
    catch(e) { return false; }                                                                                         // 3034
    if (mX >= Math.floor(cm.display.gutters.getBoundingClientRect().right)) return false;                              // 3035
    if (prevent) e_preventDefault(e);                                                                                  // 3036
                                                                                                                       // 3037
    var display = cm.display;                                                                                          // 3038
    var lineBox = display.lineDiv.getBoundingClientRect();                                                             // 3039
                                                                                                                       // 3040
    if (mY > lineBox.bottom || !hasHandler(cm, type)) return e_defaultPrevented(e);                                    // 3041
    mY -= lineBox.top - display.viewOffset;                                                                            // 3042
                                                                                                                       // 3043
    for (var i = 0; i < cm.options.gutters.length; ++i) {                                                              // 3044
      var g = display.gutters.childNodes[i];                                                                           // 3045
      if (g && g.getBoundingClientRect().right >= mX) {                                                                // 3046
        var line = lineAtHeight(cm.doc, mY);                                                                           // 3047
        var gutter = cm.options.gutters[i];                                                                            // 3048
        signalfn(cm, type, cm, line, gutter, e);                                                                       // 3049
        return e_defaultPrevented(e);                                                                                  // 3050
      }                                                                                                                // 3051
    }                                                                                                                  // 3052
  }                                                                                                                    // 3053
                                                                                                                       // 3054
  function clickInGutter(cm, e) {                                                                                      // 3055
    return gutterEvent(cm, e, "gutterClick", true, signalLater);                                                       // 3056
  }                                                                                                                    // 3057
                                                                                                                       // 3058
  // Kludge to work around strange IE behavior where it'll sometimes                                                   // 3059
  // re-fire a series of drag-related events right after the drop (#1551)                                              // 3060
  var lastDrop = 0;                                                                                                    // 3061
                                                                                                                       // 3062
  function onDrop(e) {                                                                                                 // 3063
    var cm = this;                                                                                                     // 3064
    if (signalDOMEvent(cm, e) || eventInWidget(cm.display, e))                                                         // 3065
      return;                                                                                                          // 3066
    e_preventDefault(e);                                                                                               // 3067
    if (ie) lastDrop = +new Date;                                                                                      // 3068
    var pos = posFromMouse(cm, e, true), files = e.dataTransfer.files;                                                 // 3069
    if (!pos || isReadOnly(cm)) return;                                                                                // 3070
    // Might be a file drop, in which case we simply extract the text                                                  // 3071
    // and insert it.                                                                                                  // 3072
    if (files && files.length && window.FileReader && window.File) {                                                   // 3073
      var n = files.length, text = Array(n), read = 0;                                                                 // 3074
      var loadFile = function(file, i) {                                                                               // 3075
        var reader = new FileReader;                                                                                   // 3076
        reader.onload = operation(cm, function() {                                                                     // 3077
          text[i] = reader.result;                                                                                     // 3078
          if (++read == n) {                                                                                           // 3079
            pos = clipPos(cm.doc, pos);                                                                                // 3080
            var change = {from: pos, to: pos, text: splitLines(text.join("\n")), origin: "paste"};                     // 3081
            makeChange(cm.doc, change);                                                                                // 3082
            setSelectionReplaceHistory(cm.doc, simpleSelection(pos, changeEnd(change)));                               // 3083
          }                                                                                                            // 3084
        });                                                                                                            // 3085
        reader.readAsText(file);                                                                                       // 3086
      };                                                                                                               // 3087
      for (var i = 0; i < n; ++i) loadFile(files[i], i);                                                               // 3088
    } else { // Normal drop                                                                                            // 3089
      // Don't do a replace if the drop happened inside of the selected text.                                          // 3090
      if (cm.state.draggingText && cm.doc.sel.contains(pos) > -1) {                                                    // 3091
        cm.state.draggingText(e);                                                                                      // 3092
        // Ensure the editor is re-focused                                                                             // 3093
        setTimeout(bind(focusInput, cm), 20);                                                                          // 3094
        return;                                                                                                        // 3095
      }                                                                                                                // 3096
      try {                                                                                                            // 3097
        var text = e.dataTransfer.getData("Text");                                                                     // 3098
        if (text) {                                                                                                    // 3099
          if (cm.state.draggingText && !(mac ? e.metaKey : e.ctrlKey))                                                 // 3100
            var selected = cm.listSelections();                                                                        // 3101
          setSelectionNoUndo(cm.doc, simpleSelection(pos, pos));                                                       // 3102
          if (selected) for (var i = 0; i < selected.length; ++i)                                                      // 3103
            replaceRange(cm.doc, "", selected[i].anchor, selected[i].head, "drag");                                    // 3104
          cm.replaceSelection(text, "around", "paste");                                                                // 3105
          focusInput(cm);                                                                                              // 3106
        }                                                                                                              // 3107
      }                                                                                                                // 3108
      catch(e){}                                                                                                       // 3109
    }                                                                                                                  // 3110
  }                                                                                                                    // 3111
                                                                                                                       // 3112
  function onDragStart(cm, e) {                                                                                        // 3113
    if (ie && (!cm.state.draggingText || +new Date - lastDrop < 100)) { e_stop(e); return; }                           // 3114
    if (signalDOMEvent(cm, e) || eventInWidget(cm.display, e)) return;                                                 // 3115
                                                                                                                       // 3116
    e.dataTransfer.setData("Text", cm.getSelection());                                                                 // 3117
                                                                                                                       // 3118
    // Use dummy image instead of default browsers image.                                                              // 3119
    // Recent Safari (~6.0.2) have a tendency to segfault when this happens, so we don't do it there.                  // 3120
    if (e.dataTransfer.setDragImage && !safari) {                                                                      // 3121
      var img = elt("img", null, null, "position: fixed; left: 0; top: 0;");                                           // 3122
      img.src = "data:image/gif;base64,R0lGODlhAQABAAAAACH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==";                          // 3123
      if (presto) {                                                                                                    // 3124
        img.width = img.height = 1;                                                                                    // 3125
        cm.display.wrapper.appendChild(img);                                                                           // 3126
        // Force a relayout, or Opera won't use our image for some obscure reason                                      // 3127
        img._top = img.offsetTop;                                                                                      // 3128
      }                                                                                                                // 3129
      e.dataTransfer.setDragImage(img, 0, 0);                                                                          // 3130
      if (presto) img.parentNode.removeChild(img);                                                                     // 3131
    }                                                                                                                  // 3132
  }                                                                                                                    // 3133
                                                                                                                       // 3134
  // SCROLL EVENTS                                                                                                     // 3135
                                                                                                                       // 3136
  // Sync the scrollable area and scrollbars, ensure the viewport                                                      // 3137
  // covers the visible area.                                                                                          // 3138
  function setScrollTop(cm, val) {                                                                                     // 3139
    if (Math.abs(cm.doc.scrollTop - val) < 2) return;                                                                  // 3140
    cm.doc.scrollTop = val;                                                                                            // 3141
    if (!gecko) updateDisplaySimple(cm, {top: val});                                                                   // 3142
    if (cm.display.scroller.scrollTop != val) cm.display.scroller.scrollTop = val;                                     // 3143
    cm.display.scrollbars.setScrollTop(val);                                                                           // 3144
    if (gecko) updateDisplaySimple(cm);                                                                                // 3145
    startWorker(cm, 100);                                                                                              // 3146
  }                                                                                                                    // 3147
  // Sync scroller and scrollbar, ensure the gutter elements are                                                       // 3148
  // aligned.                                                                                                          // 3149
  function setScrollLeft(cm, val, isScroller) {                                                                        // 3150
    if (isScroller ? val == cm.doc.scrollLeft : Math.abs(cm.doc.scrollLeft - val) < 2) return;                         // 3151
    val = Math.min(val, cm.display.scroller.scrollWidth - cm.display.scroller.clientWidth);                            // 3152
    cm.doc.scrollLeft = val;                                                                                           // 3153
    alignHorizontally(cm);                                                                                             // 3154
    if (cm.display.scroller.scrollLeft != val) cm.display.scroller.scrollLeft = val;                                   // 3155
    cm.display.scrollbars.setScrollLeft(val);                                                                          // 3156
  }                                                                                                                    // 3157
                                                                                                                       // 3158
  // Since the delta values reported on mouse wheel events are                                                         // 3159
  // unstandardized between browsers and even browser versions, and                                                    // 3160
  // generally horribly unpredictable, this code starts by measuring                                                   // 3161
  // the scroll effect that the first few mouse wheel events have,                                                     // 3162
  // and, from that, detects the way it can convert deltas to pixel                                                    // 3163
  // offsets afterwards.                                                                                               // 3164
  //                                                                                                                   // 3165
  // The reason we want to know the amount a wheel event will scroll                                                   // 3166
  // is that it gives us a chance to update the display before the                                                     // 3167
  // actual scrolling happens, reducing flickering.                                                                    // 3168
                                                                                                                       // 3169
  var wheelSamples = 0, wheelPixelsPerUnit = null;                                                                     // 3170
  // Fill in a browser-detected starting value on browsers where we                                                    // 3171
  // know one. These don't have to be accurate -- the result of them                                                   // 3172
  // being wrong would just be a slight flicker on the first wheel                                                     // 3173
  // scroll (if it is large enough).                                                                                   // 3174
  if (ie) wheelPixelsPerUnit = -.53;                                                                                   // 3175
  else if (gecko) wheelPixelsPerUnit = 15;                                                                             // 3176
  else if (chrome) wheelPixelsPerUnit = -.7;                                                                           // 3177
  else if (safari) wheelPixelsPerUnit = -1/3;                                                                          // 3178
                                                                                                                       // 3179
  var wheelEventDelta = function(e) {                                                                                  // 3180
    var dx = e.wheelDeltaX, dy = e.wheelDeltaY;                                                                        // 3181
    if (dx == null && e.detail && e.axis == e.HORIZONTAL_AXIS) dx = e.detail;                                          // 3182
    if (dy == null && e.detail && e.axis == e.VERTICAL_AXIS) dy = e.detail;                                            // 3183
    else if (dy == null) dy = e.wheelDelta;                                                                            // 3184
    return {x: dx, y: dy};                                                                                             // 3185
  };                                                                                                                   // 3186
  CodeMirror.wheelEventPixels = function(e) {                                                                          // 3187
    var delta = wheelEventDelta(e);                                                                                    // 3188
    delta.x *= wheelPixelsPerUnit;                                                                                     // 3189
    delta.y *= wheelPixelsPerUnit;                                                                                     // 3190
    return delta;                                                                                                      // 3191
  };                                                                                                                   // 3192
                                                                                                                       // 3193
  function onScrollWheel(cm, e) {                                                                                      // 3194
    var delta = wheelEventDelta(e), dx = delta.x, dy = delta.y;                                                        // 3195
                                                                                                                       // 3196
    var display = cm.display, scroll = display.scroller;                                                               // 3197
    // Quit if there's nothing to scroll here                                                                          // 3198
    if (!(dx && scroll.scrollWidth > scroll.clientWidth ||                                                             // 3199
          dy && scroll.scrollHeight > scroll.clientHeight)) return;                                                    // 3200
                                                                                                                       // 3201
    // Webkit browsers on OS X abort momentum scrolls when the target                                                  // 3202
    // of the scroll event is removed from the scrollable element.                                                     // 3203
    // This hack (see related code in patchDisplay) makes sure the                                                     // 3204
    // element is kept around.                                                                                         // 3205
    if (dy && mac && webkit) {                                                                                         // 3206
      outer: for (var cur = e.target, view = display.view; cur != scroll; cur = cur.parentNode) {                      // 3207
        for (var i = 0; i < view.length; i++) {                                                                        // 3208
          if (view[i].node == cur) {                                                                                   // 3209
            cm.display.currentWheelTarget = cur;                                                                       // 3210
            break outer;                                                                                               // 3211
          }                                                                                                            // 3212
        }                                                                                                              // 3213
      }                                                                                                                // 3214
    }                                                                                                                  // 3215
                                                                                                                       // 3216
    // On some browsers, horizontal scrolling will cause redraws to                                                    // 3217
    // happen before the gutter has been realigned, causing it to                                                      // 3218
    // wriggle around in a most unseemly way. When we have an                                                          // 3219
    // estimated pixels/delta value, we just handle horizontal                                                         // 3220
    // scrolling entirely here. It'll be slightly off from native, but                                                 // 3221
    // better than glitching out.                                                                                      // 3222
    if (dx && !gecko && !presto && wheelPixelsPerUnit != null) {                                                       // 3223
      if (dy)                                                                                                          // 3224
        setScrollTop(cm, Math.max(0, Math.min(scroll.scrollTop + dy * wheelPixelsPerUnit, scroll.scrollHeight - scroll.clientHeight)));
      setScrollLeft(cm, Math.max(0, Math.min(scroll.scrollLeft + dx * wheelPixelsPerUnit, scroll.scrollWidth - scroll.clientWidth)));
      e_preventDefault(e);                                                                                             // 3227
      display.wheelStartX = null; // Abort measurement, if in progress                                                 // 3228
      return;                                                                                                          // 3229
    }                                                                                                                  // 3230
                                                                                                                       // 3231
    // 'Project' the visible viewport to cover the area that is being                                                  // 3232
    // scrolled into view (if we know enough to estimate it).                                                          // 3233
    if (dy && wheelPixelsPerUnit != null) {                                                                            // 3234
      var pixels = dy * wheelPixelsPerUnit;                                                                            // 3235
      var top = cm.doc.scrollTop, bot = top + display.wrapper.clientHeight;                                            // 3236
      if (pixels < 0) top = Math.max(0, top + pixels - 50);                                                            // 3237
      else bot = Math.min(cm.doc.height, bot + pixels + 50);                                                           // 3238
      updateDisplaySimple(cm, {top: top, bottom: bot});                                                                // 3239
    }                                                                                                                  // 3240
                                                                                                                       // 3241
    if (wheelSamples < 20) {                                                                                           // 3242
      if (display.wheelStartX == null) {                                                                               // 3243
        display.wheelStartX = scroll.scrollLeft; display.wheelStartY = scroll.scrollTop;                               // 3244
        display.wheelDX = dx; display.wheelDY = dy;                                                                    // 3245
        setTimeout(function() {                                                                                        // 3246
          if (display.wheelStartX == null) return;                                                                     // 3247
          var movedX = scroll.scrollLeft - display.wheelStartX;                                                        // 3248
          var movedY = scroll.scrollTop - display.wheelStartY;                                                         // 3249
          var sample = (movedY && display.wheelDY && movedY / display.wheelDY) ||                                      // 3250
            (movedX && display.wheelDX && movedX / display.wheelDX);                                                   // 3251
          display.wheelStartX = display.wheelStartY = null;                                                            // 3252
          if (!sample) return;                                                                                         // 3253
          wheelPixelsPerUnit = (wheelPixelsPerUnit * wheelSamples + sample) / (wheelSamples + 1);                      // 3254
          ++wheelSamples;                                                                                              // 3255
        }, 200);                                                                                                       // 3256
      } else {                                                                                                         // 3257
        display.wheelDX += dx; display.wheelDY += dy;                                                                  // 3258
      }                                                                                                                // 3259
    }                                                                                                                  // 3260
  }                                                                                                                    // 3261
                                                                                                                       // 3262
  // KEY EVENTS                                                                                                        // 3263
                                                                                                                       // 3264
  // Run a handler that was bound to a key.                                                                            // 3265
  function doHandleBinding(cm, bound, dropShift) {                                                                     // 3266
    if (typeof bound == "string") {                                                                                    // 3267
      bound = commands[bound];                                                                                         // 3268
      if (!bound) return false;                                                                                        // 3269
    }                                                                                                                  // 3270
    // Ensure previous input has been read, so that the handler sees a                                                 // 3271
    // consistent view of the document                                                                                 // 3272
    if (cm.display.pollingFast && readInput(cm)) cm.display.pollingFast = false;                                       // 3273
    var prevShift = cm.display.shift, done = false;                                                                    // 3274
    try {                                                                                                              // 3275
      if (isReadOnly(cm)) cm.state.suppressEdits = true;                                                               // 3276
      if (dropShift) cm.display.shift = false;                                                                         // 3277
      done = bound(cm) != Pass;                                                                                        // 3278
    } finally {                                                                                                        // 3279
      cm.display.shift = prevShift;                                                                                    // 3280
      cm.state.suppressEdits = false;                                                                                  // 3281
    }                                                                                                                  // 3282
    return done;                                                                                                       // 3283
  }                                                                                                                    // 3284
                                                                                                                       // 3285
  function lookupKeyForEditor(cm, name, handle) {                                                                      // 3286
    for (var i = 0; i < cm.state.keyMaps.length; i++) {                                                                // 3287
      var result = lookupKey(name, cm.state.keyMaps[i], handle, cm);                                                   // 3288
      if (result) return result;                                                                                       // 3289
    }                                                                                                                  // 3290
    return (cm.options.extraKeys && lookupKey(name, cm.options.extraKeys, handle, cm))                                 // 3291
      || lookupKey(name, cm.options.keyMap, handle, cm);                                                               // 3292
  }                                                                                                                    // 3293
                                                                                                                       // 3294
  var stopSeq = new Delayed;                                                                                           // 3295
  function dispatchKey(cm, name, e, handle) {                                                                          // 3296
    var seq = cm.state.keySeq;                                                                                         // 3297
    if (seq) {                                                                                                         // 3298
      if (isModifierKey(name)) return "handled";                                                                       // 3299
      stopSeq.set(50, function() {                                                                                     // 3300
        if (cm.state.keySeq == seq) {                                                                                  // 3301
          cm.state.keySeq = null;                                                                                      // 3302
          resetInput(cm);                                                                                              // 3303
        }                                                                                                              // 3304
      });                                                                                                              // 3305
      name = seq + " " + name;                                                                                         // 3306
    }                                                                                                                  // 3307
    var result = lookupKeyForEditor(cm, name, handle);                                                                 // 3308
                                                                                                                       // 3309
    if (result == "multi")                                                                                             // 3310
      cm.state.keySeq = name;                                                                                          // 3311
    if (result == "handled")                                                                                           // 3312
      signalLater(cm, "keyHandled", cm, name, e);                                                                      // 3313
                                                                                                                       // 3314
    if (result == "handled" || result == "multi") {                                                                    // 3315
      e_preventDefault(e);                                                                                             // 3316
      restartBlink(cm);                                                                                                // 3317
    }                                                                                                                  // 3318
                                                                                                                       // 3319
    if (seq && !result && /\'$/.test(name)) {                                                                          // 3320
      e_preventDefault(e);                                                                                             // 3321
      return true;                                                                                                     // 3322
    }                                                                                                                  // 3323
    return !!result;                                                                                                   // 3324
  }                                                                                                                    // 3325
                                                                                                                       // 3326
  // Handle a key from the keydown event.                                                                              // 3327
  function handleKeyBinding(cm, e) {                                                                                   // 3328
    var name = keyName(e, true);                                                                                       // 3329
    if (!name) return false;                                                                                           // 3330
                                                                                                                       // 3331
    if (e.shiftKey && !cm.state.keySeq) {                                                                              // 3332
      // First try to resolve full name (including 'Shift-'). Failing                                                  // 3333
      // that, see if there is a cursor-motion command (starting with                                                  // 3334
      // 'go') bound to the keyname without 'Shift-'.                                                                  // 3335
      return dispatchKey(cm, "Shift-" + name, e, function(b) {return doHandleBinding(cm, b, true);})                   // 3336
          || dispatchKey(cm, name, e, function(b) {                                                                    // 3337
               if (typeof b == "string" ? /^go[A-Z]/.test(b) : b.motion)                                               // 3338
                 return doHandleBinding(cm, b);                                                                        // 3339
             });                                                                                                       // 3340
    } else {                                                                                                           // 3341
      return dispatchKey(cm, name, e, function(b) { return doHandleBinding(cm, b); });                                 // 3342
    }                                                                                                                  // 3343
  }                                                                                                                    // 3344
                                                                                                                       // 3345
  // Handle a key from the keypress event                                                                              // 3346
  function handleCharBinding(cm, e, ch) {                                                                              // 3347
    return dispatchKey(cm, "'" + ch + "'", e,                                                                          // 3348
                       function(b) { return doHandleBinding(cm, b, true); });                                          // 3349
  }                                                                                                                    // 3350
                                                                                                                       // 3351
  var lastStoppedKey = null;                                                                                           // 3352
  function onKeyDown(e) {                                                                                              // 3353
    var cm = this;                                                                                                     // 3354
    ensureFocus(cm);                                                                                                   // 3355
    if (signalDOMEvent(cm, e)) return;                                                                                 // 3356
    // IE does strange things with escape.                                                                             // 3357
    if (ie && ie_version < 11 && e.keyCode == 27) e.returnValue = false;                                               // 3358
    var code = e.keyCode;                                                                                              // 3359
    cm.display.shift = code == 16 || e.shiftKey;                                                                       // 3360
    var handled = handleKeyBinding(cm, e);                                                                             // 3361
    if (presto) {                                                                                                      // 3362
      lastStoppedKey = handled ? code : null;                                                                          // 3363
      // Opera has no cut event... we try to at least catch the key combo                                              // 3364
      if (!handled && code == 88 && !hasCopyEvent && (mac ? e.metaKey : e.ctrlKey))                                    // 3365
        cm.replaceSelection("", null, "cut");                                                                          // 3366
    }                                                                                                                  // 3367
                                                                                                                       // 3368
    // Turn mouse into crosshair when Alt is held on Mac.                                                              // 3369
    if (code == 18 && !/\bCodeMirror-crosshair\b/.test(cm.display.lineDiv.className))                                  // 3370
      showCrossHair(cm);                                                                                               // 3371
  }                                                                                                                    // 3372
                                                                                                                       // 3373
  function showCrossHair(cm) {                                                                                         // 3374
    var lineDiv = cm.display.lineDiv;                                                                                  // 3375
    addClass(lineDiv, "CodeMirror-crosshair");                                                                         // 3376
                                                                                                                       // 3377
    function up(e) {                                                                                                   // 3378
      if (e.keyCode == 18 || !e.altKey) {                                                                              // 3379
        rmClass(lineDiv, "CodeMirror-crosshair");                                                                      // 3380
        off(document, "keyup", up);                                                                                    // 3381
        off(document, "mouseover", up);                                                                                // 3382
      }                                                                                                                // 3383
    }                                                                                                                  // 3384
    on(document, "keyup", up);                                                                                         // 3385
    on(document, "mouseover", up);                                                                                     // 3386
  }                                                                                                                    // 3387
                                                                                                                       // 3388
  function onKeyUp(e) {                                                                                                // 3389
    if (e.keyCode == 16) this.doc.sel.shift = false;                                                                   // 3390
    signalDOMEvent(this, e);                                                                                           // 3391
  }                                                                                                                    // 3392
                                                                                                                       // 3393
  function onKeyPress(e) {                                                                                             // 3394
    var cm = this;                                                                                                     // 3395
    if (signalDOMEvent(cm, e) || e.ctrlKey && !e.altKey || mac && e.metaKey) return;                                   // 3396
    var keyCode = e.keyCode, charCode = e.charCode;                                                                    // 3397
    if (presto && keyCode == lastStoppedKey) {lastStoppedKey = null; e_preventDefault(e); return;}                     // 3398
    if (((presto && (!e.which || e.which < 10)) || khtml) && handleKeyBinding(cm, e)) return;                          // 3399
    var ch = String.fromCharCode(charCode == null ? keyCode : charCode);                                               // 3400
    if (handleCharBinding(cm, e, ch)) return;                                                                          // 3401
    if (ie && ie_version >= 9) cm.display.inputHasSelection = null;                                                    // 3402
    fastPoll(cm);                                                                                                      // 3403
  }                                                                                                                    // 3404
                                                                                                                       // 3405
  // FOCUS/BLUR EVENTS                                                                                                 // 3406
                                                                                                                       // 3407
  function onFocus(cm) {                                                                                               // 3408
    if (cm.options.readOnly == "nocursor") return;                                                                     // 3409
    if (!cm.state.focused) {                                                                                           // 3410
      signal(cm, "focus", cm);                                                                                         // 3411
      cm.state.focused = true;                                                                                         // 3412
      addClass(cm.display.wrapper, "CodeMirror-focused");                                                              // 3413
      // The prevInput test prevents this from firing when a context                                                   // 3414
      // menu is closed (since the resetInput would kill the                                                           // 3415
      // select-all detection hack)                                                                                    // 3416
      if (!cm.curOp && cm.display.selForContextMenu != cm.doc.sel) {                                                   // 3417
        resetInput(cm);                                                                                                // 3418
        if (webkit) setTimeout(bind(resetInput, cm, true), 0); // Issue #1730                                          // 3419
      }                                                                                                                // 3420
    }                                                                                                                  // 3421
    slowPoll(cm);                                                                                                      // 3422
    restartBlink(cm);                                                                                                  // 3423
  }                                                                                                                    // 3424
  function onBlur(cm) {                                                                                                // 3425
    if (cm.state.focused) {                                                                                            // 3426
      signal(cm, "blur", cm);                                                                                          // 3427
      cm.state.focused = false;                                                                                        // 3428
      rmClass(cm.display.wrapper, "CodeMirror-focused");                                                               // 3429
    }                                                                                                                  // 3430
    clearInterval(cm.display.blinker);                                                                                 // 3431
    setTimeout(function() {if (!cm.state.focused) cm.display.shift = false;}, 150);                                    // 3432
  }                                                                                                                    // 3433
                                                                                                                       // 3434
  // CONTEXT MENU HANDLING                                                                                             // 3435
                                                                                                                       // 3436
  // To make the context menu work, we need to briefly unhide the                                                      // 3437
  // textarea (making it as unobtrusive as possible) to let the                                                        // 3438
  // right-click take effect on it.                                                                                    // 3439
  function onContextMenu(cm, e) {                                                                                      // 3440
    if (signalDOMEvent(cm, e, "contextmenu")) return;                                                                  // 3441
    var display = cm.display;                                                                                          // 3442
    if (eventInWidget(display, e) || contextMenuInGutter(cm, e)) return;                                               // 3443
                                                                                                                       // 3444
    var pos = posFromMouse(cm, e), scrollPos = display.scroller.scrollTop;                                             // 3445
    if (!pos || presto) return; // Opera is difficult.                                                                 // 3446
                                                                                                                       // 3447
    // Reset the current text selection only if the click is done outside of the selection                             // 3448
    // and 'resetSelectionOnContextMenu' option is true.                                                               // 3449
    var reset = cm.options.resetSelectionOnContextMenu;                                                                // 3450
    if (reset && cm.doc.sel.contains(pos) == -1)                                                                       // 3451
      operation(cm, setSelection)(cm.doc, simpleSelection(pos), sel_dontScroll);                                       // 3452
                                                                                                                       // 3453
    var oldCSS = display.input.style.cssText;                                                                          // 3454
    display.inputDiv.style.position = "absolute";                                                                      // 3455
    display.input.style.cssText = "position: fixed; width: 30px; height: 30px; top: " + (e.clientY - 5) +              // 3456
      "px; left: " + (e.clientX - 5) + "px; z-index: 1000; background: " +                                             // 3457
      (ie ? "rgba(255, 255, 255, .05)" : "transparent") +                                                              // 3458
      "; outline: none; border-width: 0; outline: none; overflow: hidden; opacity: .05; filter: alpha(opacity=5);";    // 3459
    if (webkit) var oldScrollY = window.scrollY; // Work around Chrome issue (#2712)                                   // 3460
    focusInput(cm);                                                                                                    // 3461
    if (webkit) window.scrollTo(null, oldScrollY);                                                                     // 3462
    resetInput(cm);                                                                                                    // 3463
    // Adds "Select all" to context menu in FF                                                                         // 3464
    if (!cm.somethingSelected()) display.input.value = display.prevInput = " ";                                        // 3465
    display.contextMenuPending = true;                                                                                 // 3466
    display.selForContextMenu = cm.doc.sel;                                                                            // 3467
    clearTimeout(display.detectingSelectAll);                                                                          // 3468
                                                                                                                       // 3469
    // Select-all will be greyed out if there's nothing to select, so                                                  // 3470
    // this adds a zero-width space so that we can later check whether                                                 // 3471
    // it got selected.                                                                                                // 3472
    function prepareSelectAllHack() {                                                                                  // 3473
      if (display.input.selectionStart != null) {                                                                      // 3474
        var selected = cm.somethingSelected();                                                                         // 3475
        var extval = display.input.value = "\u200b" + (selected ? display.input.value : "");                           // 3476
        display.prevInput = selected ? "" : "\u200b";                                                                  // 3477
        display.input.selectionStart = 1; display.input.selectionEnd = extval.length;                                  // 3478
        // Re-set this, in case some other handler touched the                                                         // 3479
        // selection in the meantime.                                                                                  // 3480
        display.selForContextMenu = cm.doc.sel;                                                                        // 3481
      }                                                                                                                // 3482
    }                                                                                                                  // 3483
    function rehide() {                                                                                                // 3484
      display.contextMenuPending = false;                                                                              // 3485
      display.inputDiv.style.position = "relative";                                                                    // 3486
      display.input.style.cssText = oldCSS;                                                                            // 3487
      if (ie && ie_version < 9) display.scrollbars.setScrollTop(display.scroller.scrollTop = scrollPos);               // 3488
      slowPoll(cm);                                                                                                    // 3489
                                                                                                                       // 3490
      // Try to detect the user choosing select-all                                                                    // 3491
      if (display.input.selectionStart != null) {                                                                      // 3492
        if (!ie || (ie && ie_version < 9)) prepareSelectAllHack();                                                     // 3493
        var i = 0, poll = function() {                                                                                 // 3494
          if (display.selForContextMenu == cm.doc.sel && display.input.selectionStart == 0)                            // 3495
            operation(cm, commands.selectAll)(cm);                                                                     // 3496
          else if (i++ < 10) display.detectingSelectAll = setTimeout(poll, 500);                                       // 3497
          else resetInput(cm);                                                                                         // 3498
        };                                                                                                             // 3499
        display.detectingSelectAll = setTimeout(poll, 200);                                                            // 3500
      }                                                                                                                // 3501
    }                                                                                                                  // 3502
                                                                                                                       // 3503
    if (ie && ie_version >= 9) prepareSelectAllHack();                                                                 // 3504
    if (captureRightClick) {                                                                                           // 3505
      e_stop(e);                                                                                                       // 3506
      var mouseup = function() {                                                                                       // 3507
        off(window, "mouseup", mouseup);                                                                               // 3508
        setTimeout(rehide, 20);                                                                                        // 3509
      };                                                                                                               // 3510
      on(window, "mouseup", mouseup);                                                                                  // 3511
    } else {                                                                                                           // 3512
      setTimeout(rehide, 50);                                                                                          // 3513
    }                                                                                                                  // 3514
  }                                                                                                                    // 3515
                                                                                                                       // 3516
  function contextMenuInGutter(cm, e) {                                                                                // 3517
    if (!hasHandler(cm, "gutterContextMenu")) return false;                                                            // 3518
    return gutterEvent(cm, e, "gutterContextMenu", false, signal);                                                     // 3519
  }                                                                                                                    // 3520
                                                                                                                       // 3521
  // UPDATING                                                                                                          // 3522
                                                                                                                       // 3523
  // Compute the position of the end of a change (its 'to' property                                                    // 3524
  // refers to the pre-change end).                                                                                    // 3525
  var changeEnd = CodeMirror.changeEnd = function(change) {                                                            // 3526
    if (!change.text) return change.to;                                                                                // 3527
    return Pos(change.from.line + change.text.length - 1,                                                              // 3528
               lst(change.text).length + (change.text.length == 1 ? change.from.ch : 0));                              // 3529
  };                                                                                                                   // 3530
                                                                                                                       // 3531
  // Adjust a position to refer to the post-change position of the                                                     // 3532
  // same text, or the end of the change if the change covers it.                                                      // 3533
  function adjustForChange(pos, change) {                                                                              // 3534
    if (cmp(pos, change.from) < 0) return pos;                                                                         // 3535
    if (cmp(pos, change.to) <= 0) return changeEnd(change);                                                            // 3536
                                                                                                                       // 3537
    var line = pos.line + change.text.length - (change.to.line - change.from.line) - 1, ch = pos.ch;                   // 3538
    if (pos.line == change.to.line) ch += changeEnd(change).ch - change.to.ch;                                         // 3539
    return Pos(line, ch);                                                                                              // 3540
  }                                                                                                                    // 3541
                                                                                                                       // 3542
  function computeSelAfterChange(doc, change) {                                                                        // 3543
    var out = [];                                                                                                      // 3544
    for (var i = 0; i < doc.sel.ranges.length; i++) {                                                                  // 3545
      var range = doc.sel.ranges[i];                                                                                   // 3546
      out.push(new Range(adjustForChange(range.anchor, change),                                                        // 3547
                         adjustForChange(range.head, change)));                                                        // 3548
    }                                                                                                                  // 3549
    return normalizeSelection(out, doc.sel.primIndex);                                                                 // 3550
  }                                                                                                                    // 3551
                                                                                                                       // 3552
  function offsetPos(pos, old, nw) {                                                                                   // 3553
    if (pos.line == old.line)                                                                                          // 3554
      return Pos(nw.line, pos.ch - old.ch + nw.ch);                                                                    // 3555
    else                                                                                                               // 3556
      return Pos(nw.line + (pos.line - old.line), pos.ch);                                                             // 3557
  }                                                                                                                    // 3558
                                                                                                                       // 3559
  // Used by replaceSelections to allow moving the selection to the                                                    // 3560
  // start or around the replaced test. Hint may be "start" or "around".                                               // 3561
  function computeReplacedSel(doc, changes, hint) {                                                                    // 3562
    var out = [];                                                                                                      // 3563
    var oldPrev = Pos(doc.first, 0), newPrev = oldPrev;                                                                // 3564
    for (var i = 0; i < changes.length; i++) {                                                                         // 3565
      var change = changes[i];                                                                                         // 3566
      var from = offsetPos(change.from, oldPrev, newPrev);                                                             // 3567
      var to = offsetPos(changeEnd(change), oldPrev, newPrev);                                                         // 3568
      oldPrev = change.to;                                                                                             // 3569
      newPrev = to;                                                                                                    // 3570
      if (hint == "around") {                                                                                          // 3571
        var range = doc.sel.ranges[i], inv = cmp(range.head, range.anchor) < 0;                                        // 3572
        out[i] = new Range(inv ? to : from, inv ? from : to);                                                          // 3573
      } else {                                                                                                         // 3574
        out[i] = new Range(from, from);                                                                                // 3575
      }                                                                                                                // 3576
    }                                                                                                                  // 3577
    return new Selection(out, doc.sel.primIndex);                                                                      // 3578
  }                                                                                                                    // 3579
                                                                                                                       // 3580
  // Allow "beforeChange" event handlers to influence a change                                                         // 3581
  function filterChange(doc, change, update) {                                                                         // 3582
    var obj = {                                                                                                        // 3583
      canceled: false,                                                                                                 // 3584
      from: change.from,                                                                                               // 3585
      to: change.to,                                                                                                   // 3586
      text: change.text,                                                                                               // 3587
      origin: change.origin,                                                                                           // 3588
      cancel: function() { this.canceled = true; }                                                                     // 3589
    };                                                                                                                 // 3590
    if (update) obj.update = function(from, to, text, origin) {                                                        // 3591
      if (from) this.from = clipPos(doc, from);                                                                        // 3592
      if (to) this.to = clipPos(doc, to);                                                                              // 3593
      if (text) this.text = text;                                                                                      // 3594
      if (origin !== undefined) this.origin = origin;                                                                  // 3595
    };                                                                                                                 // 3596
    signal(doc, "beforeChange", doc, obj);                                                                             // 3597
    if (doc.cm) signal(doc.cm, "beforeChange", doc.cm, obj);                                                           // 3598
                                                                                                                       // 3599
    if (obj.canceled) return null;                                                                                     // 3600
    return {from: obj.from, to: obj.to, text: obj.text, origin: obj.origin};                                           // 3601
  }                                                                                                                    // 3602
                                                                                                                       // 3603
  // Apply a change to a document, and add it to the document's                                                        // 3604
  // history, and propagating it to all linked documents.                                                              // 3605
  function makeChange(doc, change, ignoreReadOnly) {                                                                   // 3606
    if (doc.cm) {                                                                                                      // 3607
      if (!doc.cm.curOp) return operation(doc.cm, makeChange)(doc, change, ignoreReadOnly);                            // 3608
      if (doc.cm.state.suppressEdits) return;                                                                          // 3609
    }                                                                                                                  // 3610
                                                                                                                       // 3611
    if (hasHandler(doc, "beforeChange") || doc.cm && hasHandler(doc.cm, "beforeChange")) {                             // 3612
      change = filterChange(doc, change, true);                                                                        // 3613
      if (!change) return;                                                                                             // 3614
    }                                                                                                                  // 3615
                                                                                                                       // 3616
    // Possibly split or suppress the update based on the presence                                                     // 3617
    // of read-only spans in its range.                                                                                // 3618
    var split = sawReadOnlySpans && !ignoreReadOnly && removeReadOnlyRanges(doc, change.from, change.to);              // 3619
    if (split) {                                                                                                       // 3620
      for (var i = split.length - 1; i >= 0; --i)                                                                      // 3621
        makeChangeInner(doc, {from: split[i].from, to: split[i].to, text: i ? [""] : change.text});                    // 3622
    } else {                                                                                                           // 3623
      makeChangeInner(doc, change);                                                                                    // 3624
    }                                                                                                                  // 3625
  }                                                                                                                    // 3626
                                                                                                                       // 3627
  function makeChangeInner(doc, change) {                                                                              // 3628
    if (change.text.length == 1 && change.text[0] == "" && cmp(change.from, change.to) == 0) return;                   // 3629
    var selAfter = computeSelAfterChange(doc, change);                                                                 // 3630
    addChangeToHistory(doc, change, selAfter, doc.cm ? doc.cm.curOp.id : NaN);                                         // 3631
                                                                                                                       // 3632
    makeChangeSingleDoc(doc, change, selAfter, stretchSpansOverChange(doc, change));                                   // 3633
    var rebased = [];                                                                                                  // 3634
                                                                                                                       // 3635
    linkedDocs(doc, function(doc, sharedHist) {                                                                        // 3636
      if (!sharedHist && indexOf(rebased, doc.history) == -1) {                                                        // 3637
        rebaseHist(doc.history, change);                                                                               // 3638
        rebased.push(doc.history);                                                                                     // 3639
      }                                                                                                                // 3640
      makeChangeSingleDoc(doc, change, null, stretchSpansOverChange(doc, change));                                     // 3641
    });                                                                                                                // 3642
  }                                                                                                                    // 3643
                                                                                                                       // 3644
  // Revert a change stored in a document's history.                                                                   // 3645
  function makeChangeFromHistory(doc, type, allowSelectionOnly) {                                                      // 3646
    if (doc.cm && doc.cm.state.suppressEdits) return;                                                                  // 3647
                                                                                                                       // 3648
    var hist = doc.history, event, selAfter = doc.sel;                                                                 // 3649
    var source = type == "undo" ? hist.done : hist.undone, dest = type == "undo" ? hist.undone : hist.done;            // 3650
                                                                                                                       // 3651
    // Verify that there is a useable event (so that ctrl-z won't                                                      // 3652
    // needlessly clear selection events)                                                                              // 3653
    for (var i = 0; i < source.length; i++) {                                                                          // 3654
      event = source[i];                                                                                               // 3655
      if (allowSelectionOnly ? event.ranges && !event.equals(doc.sel) : !event.ranges)                                 // 3656
        break;                                                                                                         // 3657
    }                                                                                                                  // 3658
    if (i == source.length) return;                                                                                    // 3659
    hist.lastOrigin = hist.lastSelOrigin = null;                                                                       // 3660
                                                                                                                       // 3661
    for (;;) {                                                                                                         // 3662
      event = source.pop();                                                                                            // 3663
      if (event.ranges) {                                                                                              // 3664
        pushSelectionToHistory(event, dest);                                                                           // 3665
        if (allowSelectionOnly && !event.equals(doc.sel)) {                                                            // 3666
          setSelection(doc, event, {clearRedo: false});                                                                // 3667
          return;                                                                                                      // 3668
        }                                                                                                              // 3669
        selAfter = event;                                                                                              // 3670
      }                                                                                                                // 3671
      else break;                                                                                                      // 3672
    }                                                                                                                  // 3673
                                                                                                                       // 3674
    // Build up a reverse change object to add to the opposite history                                                 // 3675
    // stack (redo when undoing, and vice versa).                                                                      // 3676
    var antiChanges = [];                                                                                              // 3677
    pushSelectionToHistory(selAfter, dest);                                                                            // 3678
    dest.push({changes: antiChanges, generation: hist.generation});                                                    // 3679
    hist.generation = event.generation || ++hist.maxGeneration;                                                        // 3680
                                                                                                                       // 3681
    var filter = hasHandler(doc, "beforeChange") || doc.cm && hasHandler(doc.cm, "beforeChange");                      // 3682
                                                                                                                       // 3683
    for (var i = event.changes.length - 1; i >= 0; --i) {                                                              // 3684
      var change = event.changes[i];                                                                                   // 3685
      change.origin = type;                                                                                            // 3686
      if (filter && !filterChange(doc, change, false)) {                                                               // 3687
        source.length = 0;                                                                                             // 3688
        return;                                                                                                        // 3689
      }                                                                                                                // 3690
                                                                                                                       // 3691
      antiChanges.push(historyChangeFromChange(doc, change));                                                          // 3692
                                                                                                                       // 3693
      var after = i ? computeSelAfterChange(doc, change) : lst(source);                                                // 3694
      makeChangeSingleDoc(doc, change, after, mergeOldSpans(doc, change));                                             // 3695
      if (!i && doc.cm) doc.cm.scrollIntoView({from: change.from, to: changeEnd(change)});                             // 3696
      var rebased = [];                                                                                                // 3697
                                                                                                                       // 3698
      // Propagate to the linked documents                                                                             // 3699
      linkedDocs(doc, function(doc, sharedHist) {                                                                      // 3700
        if (!sharedHist && indexOf(rebased, doc.history) == -1) {                                                      // 3701
          rebaseHist(doc.history, change);                                                                             // 3702
          rebased.push(doc.history);                                                                                   // 3703
        }                                                                                                              // 3704
        makeChangeSingleDoc(doc, change, null, mergeOldSpans(doc, change));                                            // 3705
      });                                                                                                              // 3706
    }                                                                                                                  // 3707
  }                                                                                                                    // 3708
                                                                                                                       // 3709
  // Sub-views need their line numbers shifted when text is added                                                      // 3710
  // above or below them in the parent document.                                                                       // 3711
  function shiftDoc(doc, distance) {                                                                                   // 3712
    if (distance == 0) return;                                                                                         // 3713
    doc.first += distance;                                                                                             // 3714
    doc.sel = new Selection(map(doc.sel.ranges, function(range) {                                                      // 3715
      return new Range(Pos(range.anchor.line + distance, range.anchor.ch),                                             // 3716
                       Pos(range.head.line + distance, range.head.ch));                                                // 3717
    }), doc.sel.primIndex);                                                                                            // 3718
    if (doc.cm) {                                                                                                      // 3719
      regChange(doc.cm, doc.first, doc.first - distance, distance);                                                    // 3720
      for (var d = doc.cm.display, l = d.viewFrom; l < d.viewTo; l++)                                                  // 3721
        regLineChange(doc.cm, l, "gutter");                                                                            // 3722
    }                                                                                                                  // 3723
  }                                                                                                                    // 3724
                                                                                                                       // 3725
  // More lower-level change function, handling only a single document                                                 // 3726
  // (not linked ones).                                                                                                // 3727
  function makeChangeSingleDoc(doc, change, selAfter, spans) {                                                         // 3728
    if (doc.cm && !doc.cm.curOp)                                                                                       // 3729
      return operation(doc.cm, makeChangeSingleDoc)(doc, change, selAfter, spans);                                     // 3730
                                                                                                                       // 3731
    if (change.to.line < doc.first) {                                                                                  // 3732
      shiftDoc(doc, change.text.length - 1 - (change.to.line - change.from.line));                                     // 3733
      return;                                                                                                          // 3734
    }                                                                                                                  // 3735
    if (change.from.line > doc.lastLine()) return;                                                                     // 3736
                                                                                                                       // 3737
    // Clip the change to the size of this doc                                                                         // 3738
    if (change.from.line < doc.first) {                                                                                // 3739
      var shift = change.text.length - 1 - (doc.first - change.from.line);                                             // 3740
      shiftDoc(doc, shift);                                                                                            // 3741
      change = {from: Pos(doc.first, 0), to: Pos(change.to.line + shift, change.to.ch),                                // 3742
                text: [lst(change.text)], origin: change.origin};                                                      // 3743
    }                                                                                                                  // 3744
    var last = doc.lastLine();                                                                                         // 3745
    if (change.to.line > last) {                                                                                       // 3746
      change = {from: change.from, to: Pos(last, getLine(doc, last).text.length),                                      // 3747
                text: [change.text[0]], origin: change.origin};                                                        // 3748
    }                                                                                                                  // 3749
                                                                                                                       // 3750
    change.removed = getBetween(doc, change.from, change.to);                                                          // 3751
                                                                                                                       // 3752
    if (!selAfter) selAfter = computeSelAfterChange(doc, change);                                                      // 3753
    if (doc.cm) makeChangeSingleDocInEditor(doc.cm, change, spans);                                                    // 3754
    else updateDoc(doc, change, spans);                                                                                // 3755
    setSelectionNoUndo(doc, selAfter, sel_dontScroll);                                                                 // 3756
  }                                                                                                                    // 3757
                                                                                                                       // 3758
  // Handle the interaction of a change to a document with the editor                                                  // 3759
  // that this document is part of.                                                                                    // 3760
  function makeChangeSingleDocInEditor(cm, change, spans) {                                                            // 3761
    var doc = cm.doc, display = cm.display, from = change.from, to = change.to;                                        // 3762
                                                                                                                       // 3763
    var recomputeMaxLength = false, checkWidthStart = from.line;                                                       // 3764
    if (!cm.options.lineWrapping) {                                                                                    // 3765
      checkWidthStart = lineNo(visualLine(getLine(doc, from.line)));                                                   // 3766
      doc.iter(checkWidthStart, to.line + 1, function(line) {                                                          // 3767
        if (line == display.maxLine) {                                                                                 // 3768
          recomputeMaxLength = true;                                                                                   // 3769
          return true;                                                                                                 // 3770
        }                                                                                                              // 3771
      });                                                                                                              // 3772
    }                                                                                                                  // 3773
                                                                                                                       // 3774
    if (doc.sel.contains(change.from, change.to) > -1)                                                                 // 3775
      signalCursorActivity(cm);                                                                                        // 3776
                                                                                                                       // 3777
    updateDoc(doc, change, spans, estimateHeight(cm));                                                                 // 3778
                                                                                                                       // 3779
    if (!cm.options.lineWrapping) {                                                                                    // 3780
      doc.iter(checkWidthStart, from.line + change.text.length, function(line) {                                       // 3781
        var len = lineLength(line);                                                                                    // 3782
        if (len > display.maxLineLength) {                                                                             // 3783
          display.maxLine = line;                                                                                      // 3784
          display.maxLineLength = len;                                                                                 // 3785
          display.maxLineChanged = true;                                                                               // 3786
          recomputeMaxLength = false;                                                                                  // 3787
        }                                                                                                              // 3788
      });                                                                                                              // 3789
      if (recomputeMaxLength) cm.curOp.updateMaxLine = true;                                                           // 3790
    }                                                                                                                  // 3791
                                                                                                                       // 3792
    // Adjust frontier, schedule worker                                                                                // 3793
    doc.frontier = Math.min(doc.frontier, from.line);                                                                  // 3794
    startWorker(cm, 400);                                                                                              // 3795
                                                                                                                       // 3796
    var lendiff = change.text.length - (to.line - from.line) - 1;                                                      // 3797
    // Remember that these lines changed, for updating the display                                                     // 3798
    if (change.full)                                                                                                   // 3799
      regChange(cm);                                                                                                   // 3800
    else if (from.line == to.line && change.text.length == 1 && !isWholeLineUpdate(cm.doc, change))                    // 3801
      regLineChange(cm, from.line, "text");                                                                            // 3802
    else                                                                                                               // 3803
      regChange(cm, from.line, to.line + 1, lendiff);                                                                  // 3804
                                                                                                                       // 3805
    var changesHandler = hasHandler(cm, "changes"), changeHandler = hasHandler(cm, "change");                          // 3806
    if (changeHandler || changesHandler) {                                                                             // 3807
      var obj = {                                                                                                      // 3808
        from: from, to: to,                                                                                            // 3809
        text: change.text,                                                                                             // 3810
        removed: change.removed,                                                                                       // 3811
        origin: change.origin                                                                                          // 3812
      };                                                                                                               // 3813
      if (changeHandler) signalLater(cm, "change", cm, obj);                                                           // 3814
      if (changesHandler) (cm.curOp.changeObjs || (cm.curOp.changeObjs = [])).push(obj);                               // 3815
    }                                                                                                                  // 3816
    cm.display.selForContextMenu = null;                                                                               // 3817
  }                                                                                                                    // 3818
                                                                                                                       // 3819
  function replaceRange(doc, code, from, to, origin) {                                                                 // 3820
    if (!to) to = from;                                                                                                // 3821
    if (cmp(to, from) < 0) { var tmp = to; to = from; from = tmp; }                                                    // 3822
    if (typeof code == "string") code = splitLines(code);                                                              // 3823
    makeChange(doc, {from: from, to: to, text: code, origin: origin});                                                 // 3824
  }                                                                                                                    // 3825
                                                                                                                       // 3826
  // SCROLLING THINGS INTO VIEW                                                                                        // 3827
                                                                                                                       // 3828
  // If an editor sits on the top or bottom of the window, partially                                                   // 3829
  // scrolled out of view, this ensures that the cursor is visible.                                                    // 3830
  function maybeScrollWindow(cm, coords) {                                                                             // 3831
    if (signalDOMEvent(cm, "scrollCursorIntoView")) return;                                                            // 3832
                                                                                                                       // 3833
    var display = cm.display, box = display.sizer.getBoundingClientRect(), doScroll = null;                            // 3834
    if (coords.top + box.top < 0) doScroll = true;                                                                     // 3835
    else if (coords.bottom + box.top > (window.innerHeight || document.documentElement.clientHeight)) doScroll = false;
    if (doScroll != null && !phantom) {                                                                                // 3837
      var scrollNode = elt("div", "\u200b", null, "position: absolute; top: " +                                        // 3838
                           (coords.top - display.viewOffset - paddingTop(cm.display)) + "px; height: " +               // 3839
                           (coords.bottom - coords.top + scrollGap(cm) + display.barHeight) + "px; left: " +           // 3840
                           coords.left + "px; width: 2px;");                                                           // 3841
      cm.display.lineSpace.appendChild(scrollNode);                                                                    // 3842
      scrollNode.scrollIntoView(doScroll);                                                                             // 3843
      cm.display.lineSpace.removeChild(scrollNode);                                                                    // 3844
    }                                                                                                                  // 3845
  }                                                                                                                    // 3846
                                                                                                                       // 3847
  // Scroll a given position into view (immediately), verifying that                                                   // 3848
  // it actually became visible (as line heights are accurately                                                        // 3849
  // measured, the position of something may 'drift' during drawing).                                                  // 3850
  function scrollPosIntoView(cm, pos, end, margin) {                                                                   // 3851
    if (margin == null) margin = 0;                                                                                    // 3852
    for (var limit = 0; limit < 5; limit++) {                                                                          // 3853
      var changed = false, coords = cursorCoords(cm, pos);                                                             // 3854
      var endCoords = !end || end == pos ? coords : cursorCoords(cm, end);                                             // 3855
      var scrollPos = calculateScrollPos(cm, Math.min(coords.left, endCoords.left),                                    // 3856
                                         Math.min(coords.top, endCoords.top) - margin,                                 // 3857
                                         Math.max(coords.left, endCoords.left),                                        // 3858
                                         Math.max(coords.bottom, endCoords.bottom) + margin);                          // 3859
      var startTop = cm.doc.scrollTop, startLeft = cm.doc.scrollLeft;                                                  // 3860
      if (scrollPos.scrollTop != null) {                                                                               // 3861
        setScrollTop(cm, scrollPos.scrollTop);                                                                         // 3862
        if (Math.abs(cm.doc.scrollTop - startTop) > 1) changed = true;                                                 // 3863
      }                                                                                                                // 3864
      if (scrollPos.scrollLeft != null) {                                                                              // 3865
        setScrollLeft(cm, scrollPos.scrollLeft);                                                                       // 3866
        if (Math.abs(cm.doc.scrollLeft - startLeft) > 1) changed = true;                                               // 3867
      }                                                                                                                // 3868
      if (!changed) break;                                                                                             // 3869
    }                                                                                                                  // 3870
    return coords;                                                                                                     // 3871
  }                                                                                                                    // 3872
                                                                                                                       // 3873
  // Scroll a given set of coordinates into view (immediately).                                                        // 3874
  function scrollIntoView(cm, x1, y1, x2, y2) {                                                                        // 3875
    var scrollPos = calculateScrollPos(cm, x1, y1, x2, y2);                                                            // 3876
    if (scrollPos.scrollTop != null) setScrollTop(cm, scrollPos.scrollTop);                                            // 3877
    if (scrollPos.scrollLeft != null) setScrollLeft(cm, scrollPos.scrollLeft);                                         // 3878
  }                                                                                                                    // 3879
                                                                                                                       // 3880
  // Calculate a new scroll position needed to scroll the given                                                        // 3881
  // rectangle into view. Returns an object with scrollTop and                                                         // 3882
  // scrollLeft properties. When these are undefined, the                                                              // 3883
  // vertical/horizontal position does not need to be adjusted.                                                        // 3884
  function calculateScrollPos(cm, x1, y1, x2, y2) {                                                                    // 3885
    var display = cm.display, snapMargin = textHeight(cm.display);                                                     // 3886
    if (y1 < 0) y1 = 0;                                                                                                // 3887
    var screentop = cm.curOp && cm.curOp.scrollTop != null ? cm.curOp.scrollTop : display.scroller.scrollTop;          // 3888
    var screen = displayHeight(cm), result = {};                                                                       // 3889
    if (y2 - y1 > screen) y2 = y1 + screen;                                                                            // 3890
    var docBottom = cm.doc.height + paddingVert(display);                                                              // 3891
    var atTop = y1 < snapMargin, atBottom = y2 > docBottom - snapMargin;                                               // 3892
    if (y1 < screentop) {                                                                                              // 3893
      result.scrollTop = atTop ? 0 : y1;                                                                               // 3894
    } else if (y2 > screentop + screen) {                                                                              // 3895
      var newTop = Math.min(y1, (atBottom ? docBottom : y2) - screen);                                                 // 3896
      if (newTop != screentop) result.scrollTop = newTop;                                                              // 3897
    }                                                                                                                  // 3898
                                                                                                                       // 3899
    var screenleft = cm.curOp && cm.curOp.scrollLeft != null ? cm.curOp.scrollLeft : display.scroller.scrollLeft;      // 3900
    var screenw = displayWidth(cm) - (cm.options.fixedGutter ? display.gutters.offsetWidth : 0);                       // 3901
    var tooWide = x2 - x1 > screenw;                                                                                   // 3902
    if (tooWide) x2 = x1 + screenw;                                                                                    // 3903
    if (x1 < 10)                                                                                                       // 3904
      result.scrollLeft = 0;                                                                                           // 3905
    else if (x1 < screenleft)                                                                                          // 3906
      result.scrollLeft = Math.max(0, x1 - (tooWide ? 0 : 10));                                                        // 3907
    else if (x2 > screenw + screenleft - 3)                                                                            // 3908
      result.scrollLeft = x2 + (tooWide ? 0 : 10) - screenw;                                                           // 3909
    return result;                                                                                                     // 3910
  }                                                                                                                    // 3911
                                                                                                                       // 3912
  // Store a relative adjustment to the scroll position in the current                                                 // 3913
  // operation (to be applied when the operation finishes).                                                            // 3914
  function addToScrollPos(cm, left, top) {                                                                             // 3915
    if (left != null || top != null) resolveScrollToPos(cm);                                                           // 3916
    if (left != null)                                                                                                  // 3917
      cm.curOp.scrollLeft = (cm.curOp.scrollLeft == null ? cm.doc.scrollLeft : cm.curOp.scrollLeft) + left;            // 3918
    if (top != null)                                                                                                   // 3919
      cm.curOp.scrollTop = (cm.curOp.scrollTop == null ? cm.doc.scrollTop : cm.curOp.scrollTop) + top;                 // 3920
  }                                                                                                                    // 3921
                                                                                                                       // 3922
  // Make sure that at the end of the operation the current cursor is                                                  // 3923
  // shown.                                                                                                            // 3924
  function ensureCursorVisible(cm) {                                                                                   // 3925
    resolveScrollToPos(cm);                                                                                            // 3926
    var cur = cm.getCursor(), from = cur, to = cur;                                                                    // 3927
    if (!cm.options.lineWrapping) {                                                                                    // 3928
      from = cur.ch ? Pos(cur.line, cur.ch - 1) : cur;                                                                 // 3929
      to = Pos(cur.line, cur.ch + 1);                                                                                  // 3930
    }                                                                                                                  // 3931
    cm.curOp.scrollToPos = {from: from, to: to, margin: cm.options.cursorScrollMargin, isCursor: true};                // 3932
  }                                                                                                                    // 3933
                                                                                                                       // 3934
  // When an operation has its scrollToPos property set, and another                                                   // 3935
  // scroll action is applied before the end of the operation, this                                                    // 3936
  // 'simulates' scrolling that position into view in a cheap way, so                                                  // 3937
  // that the effect of intermediate scroll commands is not ignored.                                                   // 3938
  function resolveScrollToPos(cm) {                                                                                    // 3939
    var range = cm.curOp.scrollToPos;                                                                                  // 3940
    if (range) {                                                                                                       // 3941
      cm.curOp.scrollToPos = null;                                                                                     // 3942
      var from = estimateCoords(cm, range.from), to = estimateCoords(cm, range.to);                                    // 3943
      var sPos = calculateScrollPos(cm, Math.min(from.left, to.left),                                                  // 3944
                                    Math.min(from.top, to.top) - range.margin,                                         // 3945
                                    Math.max(from.right, to.right),                                                    // 3946
                                    Math.max(from.bottom, to.bottom) + range.margin);                                  // 3947
      cm.scrollTo(sPos.scrollLeft, sPos.scrollTop);                                                                    // 3948
    }                                                                                                                  // 3949
  }                                                                                                                    // 3950
                                                                                                                       // 3951
  // API UTILITIES                                                                                                     // 3952
                                                                                                                       // 3953
  // Indent the given line. The how parameter can be "smart",                                                          // 3954
  // "add"/null, "subtract", or "prev". When aggressive is false                                                       // 3955
  // (typically set to true for forced single-line indents), empty                                                     // 3956
  // lines are not indented, and places where the mode returns Pass                                                    // 3957
  // are left alone.                                                                                                   // 3958
  function indentLine(cm, n, how, aggressive) {                                                                        // 3959
    var doc = cm.doc, state;                                                                                           // 3960
    if (how == null) how = "add";                                                                                      // 3961
    if (how == "smart") {                                                                                              // 3962
      // Fall back to "prev" when the mode doesn't have an indentation                                                 // 3963
      // method.                                                                                                       // 3964
      if (!doc.mode.indent) how = "prev";                                                                              // 3965
      else state = getStateBefore(cm, n);                                                                              // 3966
    }                                                                                                                  // 3967
                                                                                                                       // 3968
    var tabSize = cm.options.tabSize;                                                                                  // 3969
    var line = getLine(doc, n), curSpace = countColumn(line.text, null, tabSize);                                      // 3970
    if (line.stateAfter) line.stateAfter = null;                                                                       // 3971
    var curSpaceString = line.text.match(/^\s*/)[0], indentation;                                                      // 3972
    if (!aggressive && !/\S/.test(line.text)) {                                                                        // 3973
      indentation = 0;                                                                                                 // 3974
      how = "not";                                                                                                     // 3975
    } else if (how == "smart") {                                                                                       // 3976
      indentation = doc.mode.indent(state, line.text.slice(curSpaceString.length), line.text);                         // 3977
      if (indentation == Pass || indentation > 150) {                                                                  // 3978
        if (!aggressive) return;                                                                                       // 3979
        how = "prev";                                                                                                  // 3980
      }                                                                                                                // 3981
    }                                                                                                                  // 3982
    if (how == "prev") {                                                                                               // 3983
      if (n > doc.first) indentation = countColumn(getLine(doc, n-1).text, null, tabSize);                             // 3984
      else indentation = 0;                                                                                            // 3985
    } else if (how == "add") {                                                                                         // 3986
      indentation = curSpace + cm.options.indentUnit;                                                                  // 3987
    } else if (how == "subtract") {                                                                                    // 3988
      indentation = curSpace - cm.options.indentUnit;                                                                  // 3989
    } else if (typeof how == "number") {                                                                               // 3990
      indentation = curSpace + how;                                                                                    // 3991
    }                                                                                                                  // 3992
    indentation = Math.max(0, indentation);                                                                            // 3993
                                                                                                                       // 3994
    var indentString = "", pos = 0;                                                                                    // 3995
    if (cm.options.indentWithTabs)                                                                                     // 3996
      for (var i = Math.floor(indentation / tabSize); i; --i) {pos += tabSize; indentString += "\t";}                  // 3997
    if (pos < indentation) indentString += spaceStr(indentation - pos);                                                // 3998
                                                                                                                       // 3999
    if (indentString != curSpaceString) {                                                                              // 4000
      replaceRange(doc, indentString, Pos(n, 0), Pos(n, curSpaceString.length), "+input");                             // 4001
    } else {                                                                                                           // 4002
      // Ensure that, if the cursor was in the whitespace at the start                                                 // 4003
      // of the line, it is moved to the end of that space.                                                            // 4004
      for (var i = 0; i < doc.sel.ranges.length; i++) {                                                                // 4005
        var range = doc.sel.ranges[i];                                                                                 // 4006
        if (range.head.line == n && range.head.ch < curSpaceString.length) {                                           // 4007
          var pos = Pos(n, curSpaceString.length);                                                                     // 4008
          replaceOneSelection(doc, i, new Range(pos, pos));                                                            // 4009
          break;                                                                                                       // 4010
        }                                                                                                              // 4011
      }                                                                                                                // 4012
    }                                                                                                                  // 4013
    line.stateAfter = null;                                                                                            // 4014
  }                                                                                                                    // 4015
                                                                                                                       // 4016
  // Utility for applying a change to a line by handle or number,                                                      // 4017
  // returning the number and optionally registering the line as                                                       // 4018
  // changed.                                                                                                          // 4019
  function changeLine(doc, handle, changeType, op) {                                                                   // 4020
    var no = handle, line = handle;                                                                                    // 4021
    if (typeof handle == "number") line = getLine(doc, clipLine(doc, handle));                                         // 4022
    else no = lineNo(handle);                                                                                          // 4023
    if (no == null) return null;                                                                                       // 4024
    if (op(line, no) && doc.cm) regLineChange(doc.cm, no, changeType);                                                 // 4025
    return line;                                                                                                       // 4026
  }                                                                                                                    // 4027
                                                                                                                       // 4028
  // Helper for deleting text near the selection(s), used to implement                                                 // 4029
  // backspace, delete, and similar functionality.                                                                     // 4030
  function deleteNearSelection(cm, compute) {                                                                          // 4031
    var ranges = cm.doc.sel.ranges, kill = [];                                                                         // 4032
    // Build up a set of ranges to kill first, merging overlapping                                                     // 4033
    // ranges.                                                                                                         // 4034
    for (var i = 0; i < ranges.length; i++) {                                                                          // 4035
      var toKill = compute(ranges[i]);                                                                                 // 4036
      while (kill.length && cmp(toKill.from, lst(kill).to) <= 0) {                                                     // 4037
        var replaced = kill.pop();                                                                                     // 4038
        if (cmp(replaced.from, toKill.from) < 0) {                                                                     // 4039
          toKill.from = replaced.from;                                                                                 // 4040
          break;                                                                                                       // 4041
        }                                                                                                              // 4042
      }                                                                                                                // 4043
      kill.push(toKill);                                                                                               // 4044
    }                                                                                                                  // 4045
    // Next, remove those actual ranges.                                                                               // 4046
    runInOp(cm, function() {                                                                                           // 4047
      for (var i = kill.length - 1; i >= 0; i--)                                                                       // 4048
        replaceRange(cm.doc, "", kill[i].from, kill[i].to, "+delete");                                                 // 4049
      ensureCursorVisible(cm);                                                                                         // 4050
    });                                                                                                                // 4051
  }                                                                                                                    // 4052
                                                                                                                       // 4053
  // Used for horizontal relative motion. Dir is -1 or 1 (left or                                                      // 4054
  // right), unit can be "char", "column" (like char, but doesn't                                                      // 4055
  // cross line boundaries), "word" (across next word), or "group" (to                                                 // 4056
  // the start of next group of word or non-word-non-whitespace                                                        // 4057
  // chars). The visually param controls whether, in right-to-left                                                     // 4058
  // text, direction 1 means to move towards the next index in the                                                     // 4059
  // string, or towards the character to the right of the current                                                      // 4060
  // position. The resulting position will have a hitSide=true                                                         // 4061
  // property if it reached the end of the document.                                                                   // 4062
  function findPosH(doc, pos, dir, unit, visually) {                                                                   // 4063
    var line = pos.line, ch = pos.ch, origDir = dir;                                                                   // 4064
    var lineObj = getLine(doc, line);                                                                                  // 4065
    var possible = true;                                                                                               // 4066
    function findNextLine() {                                                                                          // 4067
      var l = line + dir;                                                                                              // 4068
      if (l < doc.first || l >= doc.first + doc.size) return (possible = false);                                       // 4069
      line = l;                                                                                                        // 4070
      return lineObj = getLine(doc, l);                                                                                // 4071
    }                                                                                                                  // 4072
    function moveOnce(boundToLine) {                                                                                   // 4073
      var next = (visually ? moveVisually : moveLogically)(lineObj, ch, dir, true);                                    // 4074
      if (next == null) {                                                                                              // 4075
        if (!boundToLine && findNextLine()) {                                                                          // 4076
          if (visually) ch = (dir < 0 ? lineRight : lineLeft)(lineObj);                                                // 4077
          else ch = dir < 0 ? lineObj.text.length : 0;                                                                 // 4078
        } else return (possible = false);                                                                              // 4079
      } else ch = next;                                                                                                // 4080
      return true;                                                                                                     // 4081
    }                                                                                                                  // 4082
                                                                                                                       // 4083
    if (unit == "char") moveOnce();                                                                                    // 4084
    else if (unit == "column") moveOnce(true);                                                                         // 4085
    else if (unit == "word" || unit == "group") {                                                                      // 4086
      var sawType = null, group = unit == "group";                                                                     // 4087
      var helper = doc.cm && doc.cm.getHelper(pos, "wordChars");                                                       // 4088
      for (var first = true;; first = false) {                                                                         // 4089
        if (dir < 0 && !moveOnce(!first)) break;                                                                       // 4090
        var cur = lineObj.text.charAt(ch) || "\n";                                                                     // 4091
        var type = isWordChar(cur, helper) ? "w"                                                                       // 4092
          : group && cur == "\n" ? "n"                                                                                 // 4093
          : !group || /\s/.test(cur) ? null                                                                            // 4094
          : "p";                                                                                                       // 4095
        if (group && !first && !type) type = "s";                                                                      // 4096
        if (sawType && sawType != type) {                                                                              // 4097
          if (dir < 0) {dir = 1; moveOnce();}                                                                          // 4098
          break;                                                                                                       // 4099
        }                                                                                                              // 4100
                                                                                                                       // 4101
        if (type) sawType = type;                                                                                      // 4102
        if (dir > 0 && !moveOnce(!first)) break;                                                                       // 4103
      }                                                                                                                // 4104
    }                                                                                                                  // 4105
    var result = skipAtomic(doc, Pos(line, ch), origDir, true);                                                        // 4106
    if (!possible) result.hitSide = true;                                                                              // 4107
    return result;                                                                                                     // 4108
  }                                                                                                                    // 4109
                                                                                                                       // 4110
  // For relative vertical movement. Dir may be -1 or 1. Unit can be                                                   // 4111
  // "page" or "line". The resulting position will have a hitSide=true                                                 // 4112
  // property if it reached the end of the document.                                                                   // 4113
  function findPosV(cm, pos, dir, unit) {                                                                              // 4114
    var doc = cm.doc, x = pos.left, y;                                                                                 // 4115
    if (unit == "page") {                                                                                              // 4116
      var pageSize = Math.min(cm.display.wrapper.clientHeight, window.innerHeight || document.documentElement.clientHeight);
      y = pos.top + dir * (pageSize - (dir < 0 ? 1.5 : .5) * textHeight(cm.display));                                  // 4118
    } else if (unit == "line") {                                                                                       // 4119
      y = dir > 0 ? pos.bottom + 3 : pos.top - 3;                                                                      // 4120
    }                                                                                                                  // 4121
    for (;;) {                                                                                                         // 4122
      var target = coordsChar(cm, x, y);                                                                               // 4123
      if (!target.outside) break;                                                                                      // 4124
      if (dir < 0 ? y <= 0 : y >= doc.height) { target.hitSide = true; break; }                                        // 4125
      y += dir * 5;                                                                                                    // 4126
    }                                                                                                                  // 4127
    return target;                                                                                                     // 4128
  }                                                                                                                    // 4129
                                                                                                                       // 4130
  // EDITOR METHODS                                                                                                    // 4131
                                                                                                                       // 4132
  // The publicly visible API. Note that methodOp(f) means                                                             // 4133
  // 'wrap f in an operation, performed on its `this` parameter'.                                                      // 4134
                                                                                                                       // 4135
  // This is not the complete set of editor methods. Most of the                                                       // 4136
  // methods defined on the Doc type are also injected into                                                            // 4137
  // CodeMirror.prototype, for backwards compatibility and                                                             // 4138
  // convenience.                                                                                                      // 4139
                                                                                                                       // 4140
  CodeMirror.prototype = {                                                                                             // 4141
    constructor: CodeMirror,                                                                                           // 4142
    focus: function(){window.focus(); focusInput(this); fastPoll(this);},                                              // 4143
                                                                                                                       // 4144
    setOption: function(option, value) {                                                                               // 4145
      var options = this.options, old = options[option];                                                               // 4146
      if (options[option] == value && option != "mode") return;                                                        // 4147
      options[option] = value;                                                                                         // 4148
      if (optionHandlers.hasOwnProperty(option))                                                                       // 4149
        operation(this, optionHandlers[option])(this, value, old);                                                     // 4150
    },                                                                                                                 // 4151
                                                                                                                       // 4152
    getOption: function(option) {return this.options[option];},                                                        // 4153
    getDoc: function() {return this.doc;},                                                                             // 4154
                                                                                                                       // 4155
    addKeyMap: function(map, bottom) {                                                                                 // 4156
      this.state.keyMaps[bottom ? "push" : "unshift"](getKeyMap(map));                                                 // 4157
    },                                                                                                                 // 4158
    removeKeyMap: function(map) {                                                                                      // 4159
      var maps = this.state.keyMaps;                                                                                   // 4160
      for (var i = 0; i < maps.length; ++i)                                                                            // 4161
        if (maps[i] == map || maps[i].name == map) {                                                                   // 4162
          maps.splice(i, 1);                                                                                           // 4163
          return true;                                                                                                 // 4164
        }                                                                                                              // 4165
    },                                                                                                                 // 4166
                                                                                                                       // 4167
    addOverlay: methodOp(function(spec, options) {                                                                     // 4168
      var mode = spec.token ? spec : CodeMirror.getMode(this.options, spec);                                           // 4169
      if (mode.startState) throw new Error("Overlays may not be stateful.");                                           // 4170
      this.state.overlays.push({mode: mode, modeSpec: spec, opaque: options && options.opaque});                       // 4171
      this.state.modeGen++;                                                                                            // 4172
      regChange(this);                                                                                                 // 4173
    }),                                                                                                                // 4174
    removeOverlay: methodOp(function(spec) {                                                                           // 4175
      var overlays = this.state.overlays;                                                                              // 4176
      for (var i = 0; i < overlays.length; ++i) {                                                                      // 4177
        var cur = overlays[i].modeSpec;                                                                                // 4178
        if (cur == spec || typeof spec == "string" && cur.name == spec) {                                              // 4179
          overlays.splice(i, 1);                                                                                       // 4180
          this.state.modeGen++;                                                                                        // 4181
          regChange(this);                                                                                             // 4182
          return;                                                                                                      // 4183
        }                                                                                                              // 4184
      }                                                                                                                // 4185
    }),                                                                                                                // 4186
                                                                                                                       // 4187
    indentLine: methodOp(function(n, dir, aggressive) {                                                                // 4188
      if (typeof dir != "string" && typeof dir != "number") {                                                          // 4189
        if (dir == null) dir = this.options.smartIndent ? "smart" : "prev";                                            // 4190
        else dir = dir ? "add" : "subtract";                                                                           // 4191
      }                                                                                                                // 4192
      if (isLine(this.doc, n)) indentLine(this, n, dir, aggressive);                                                   // 4193
    }),                                                                                                                // 4194
    indentSelection: methodOp(function(how) {                                                                          // 4195
      var ranges = this.doc.sel.ranges, end = -1;                                                                      // 4196
      for (var i = 0; i < ranges.length; i++) {                                                                        // 4197
        var range = ranges[i];                                                                                         // 4198
        if (!range.empty()) {                                                                                          // 4199
          var from = range.from(), to = range.to();                                                                    // 4200
          var start = Math.max(end, from.line);                                                                        // 4201
          end = Math.min(this.lastLine(), to.line - (to.ch ? 0 : 1)) + 1;                                              // 4202
          for (var j = start; j < end; ++j)                                                                            // 4203
            indentLine(this, j, how);                                                                                  // 4204
          var newRanges = this.doc.sel.ranges;                                                                         // 4205
          if (from.ch == 0 && ranges.length == newRanges.length && newRanges[i].from().ch > 0)                         // 4206
            replaceOneSelection(this.doc, i, new Range(from, newRanges[i].to()), sel_dontScroll);                      // 4207
        } else if (range.head.line > end) {                                                                            // 4208
          indentLine(this, range.head.line, how, true);                                                                // 4209
          end = range.head.line;                                                                                       // 4210
          if (i == this.doc.sel.primIndex) ensureCursorVisible(this);                                                  // 4211
        }                                                                                                              // 4212
      }                                                                                                                // 4213
    }),                                                                                                                // 4214
                                                                                                                       // 4215
    // Fetch the parser token for a given character. Useful for hacks                                                  // 4216
    // that want to inspect the mode state (say, for completion).                                                      // 4217
    getTokenAt: function(pos, precise) {                                                                               // 4218
      return takeToken(this, pos, precise);                                                                            // 4219
    },                                                                                                                 // 4220
                                                                                                                       // 4221
    getLineTokens: function(line, precise) {                                                                           // 4222
      return takeToken(this, Pos(line), precise, true);                                                                // 4223
    },                                                                                                                 // 4224
                                                                                                                       // 4225
    getTokenTypeAt: function(pos) {                                                                                    // 4226
      pos = clipPos(this.doc, pos);                                                                                    // 4227
      var styles = getLineStyles(this, getLine(this.doc, pos.line));                                                   // 4228
      var before = 0, after = (styles.length - 1) / 2, ch = pos.ch;                                                    // 4229
      var type;                                                                                                        // 4230
      if (ch == 0) type = styles[2];                                                                                   // 4231
      else for (;;) {                                                                                                  // 4232
        var mid = (before + after) >> 1;                                                                               // 4233
        if ((mid ? styles[mid * 2 - 1] : 0) >= ch) after = mid;                                                        // 4234
        else if (styles[mid * 2 + 1] < ch) before = mid + 1;                                                           // 4235
        else { type = styles[mid * 2 + 2]; break; }                                                                    // 4236
      }                                                                                                                // 4237
      var cut = type ? type.indexOf("cm-overlay ") : -1;                                                               // 4238
      return cut < 0 ? type : cut == 0 ? null : type.slice(0, cut - 1);                                                // 4239
    },                                                                                                                 // 4240
                                                                                                                       // 4241
    getModeAt: function(pos) {                                                                                         // 4242
      var mode = this.doc.mode;                                                                                        // 4243
      if (!mode.innerMode) return mode;                                                                                // 4244
      return CodeMirror.innerMode(mode, this.getTokenAt(pos).state).mode;                                              // 4245
    },                                                                                                                 // 4246
                                                                                                                       // 4247
    getHelper: function(pos, type) {                                                                                   // 4248
      return this.getHelpers(pos, type)[0];                                                                            // 4249
    },                                                                                                                 // 4250
                                                                                                                       // 4251
    getHelpers: function(pos, type) {                                                                                  // 4252
      var found = [];                                                                                                  // 4253
      if (!helpers.hasOwnProperty(type)) return helpers;                                                               // 4254
      var help = helpers[type], mode = this.getModeAt(pos);                                                            // 4255
      if (typeof mode[type] == "string") {                                                                             // 4256
        if (help[mode[type]]) found.push(help[mode[type]]);                                                            // 4257
      } else if (mode[type]) {                                                                                         // 4258
        for (var i = 0; i < mode[type].length; i++) {                                                                  // 4259
          var val = help[mode[type][i]];                                                                               // 4260
          if (val) found.push(val);                                                                                    // 4261
        }                                                                                                              // 4262
      } else if (mode.helperType && help[mode.helperType]) {                                                           // 4263
        found.push(help[mode.helperType]);                                                                             // 4264
      } else if (help[mode.name]) {                                                                                    // 4265
        found.push(help[mode.name]);                                                                                   // 4266
      }                                                                                                                // 4267
      for (var i = 0; i < help._global.length; i++) {                                                                  // 4268
        var cur = help._global[i];                                                                                     // 4269
        if (cur.pred(mode, this) && indexOf(found, cur.val) == -1)                                                     // 4270
          found.push(cur.val);                                                                                         // 4271
      }                                                                                                                // 4272
      return found;                                                                                                    // 4273
    },                                                                                                                 // 4274
                                                                                                                       // 4275
    getStateAfter: function(line, precise) {                                                                           // 4276
      var doc = this.doc;                                                                                              // 4277
      line = clipLine(doc, line == null ? doc.first + doc.size - 1: line);                                             // 4278
      return getStateBefore(this, line + 1, precise);                                                                  // 4279
    },                                                                                                                 // 4280
                                                                                                                       // 4281
    cursorCoords: function(start, mode) {                                                                              // 4282
      var pos, range = this.doc.sel.primary();                                                                         // 4283
      if (start == null) pos = range.head;                                                                             // 4284
      else if (typeof start == "object") pos = clipPos(this.doc, start);                                               // 4285
      else pos = start ? range.from() : range.to();                                                                    // 4286
      return cursorCoords(this, pos, mode || "page");                                                                  // 4287
    },                                                                                                                 // 4288
                                                                                                                       // 4289
    charCoords: function(pos, mode) {                                                                                  // 4290
      return charCoords(this, clipPos(this.doc, pos), mode || "page");                                                 // 4291
    },                                                                                                                 // 4292
                                                                                                                       // 4293
    coordsChar: function(coords, mode) {                                                                               // 4294
      coords = fromCoordSystem(this, coords, mode || "page");                                                          // 4295
      return coordsChar(this, coords.left, coords.top);                                                                // 4296
    },                                                                                                                 // 4297
                                                                                                                       // 4298
    lineAtHeight: function(height, mode) {                                                                             // 4299
      height = fromCoordSystem(this, {top: height, left: 0}, mode || "page").top;                                      // 4300
      return lineAtHeight(this.doc, height + this.display.viewOffset);                                                 // 4301
    },                                                                                                                 // 4302
    heightAtLine: function(line, mode) {                                                                               // 4303
      var end = false, last = this.doc.first + this.doc.size - 1;                                                      // 4304
      if (line < this.doc.first) line = this.doc.first;                                                                // 4305
      else if (line > last) { line = last; end = true; }                                                               // 4306
      var lineObj = getLine(this.doc, line);                                                                           // 4307
      return intoCoordSystem(this, lineObj, {top: 0, left: 0}, mode || "page").top +                                   // 4308
        (end ? this.doc.height - heightAtLine(lineObj) : 0);                                                           // 4309
    },                                                                                                                 // 4310
                                                                                                                       // 4311
    defaultTextHeight: function() { return textHeight(this.display); },                                                // 4312
    defaultCharWidth: function() { return charWidth(this.display); },                                                  // 4313
                                                                                                                       // 4314
    setGutterMarker: methodOp(function(line, gutterID, value) {                                                        // 4315
      return changeLine(this.doc, line, "gutter", function(line) {                                                     // 4316
        var markers = line.gutterMarkers || (line.gutterMarkers = {});                                                 // 4317
        markers[gutterID] = value;                                                                                     // 4318
        if (!value && isEmpty(markers)) line.gutterMarkers = null;                                                     // 4319
        return true;                                                                                                   // 4320
      });                                                                                                              // 4321
    }),                                                                                                                // 4322
                                                                                                                       // 4323
    clearGutter: methodOp(function(gutterID) {                                                                         // 4324
      var cm = this, doc = cm.doc, i = doc.first;                                                                      // 4325
      doc.iter(function(line) {                                                                                        // 4326
        if (line.gutterMarkers && line.gutterMarkers[gutterID]) {                                                      // 4327
          line.gutterMarkers[gutterID] = null;                                                                         // 4328
          regLineChange(cm, i, "gutter");                                                                              // 4329
          if (isEmpty(line.gutterMarkers)) line.gutterMarkers = null;                                                  // 4330
        }                                                                                                              // 4331
        ++i;                                                                                                           // 4332
      });                                                                                                              // 4333
    }),                                                                                                                // 4334
                                                                                                                       // 4335
    addLineWidget: methodOp(function(handle, node, options) {                                                          // 4336
      return addLineWidget(this, handle, node, options);                                                               // 4337
    }),                                                                                                                // 4338
                                                                                                                       // 4339
    removeLineWidget: function(widget) { widget.clear(); },                                                            // 4340
                                                                                                                       // 4341
    lineInfo: function(line) {                                                                                         // 4342
      if (typeof line == "number") {                                                                                   // 4343
        if (!isLine(this.doc, line)) return null;                                                                      // 4344
        var n = line;                                                                                                  // 4345
        line = getLine(this.doc, line);                                                                                // 4346
        if (!line) return null;                                                                                        // 4347
      } else {                                                                                                         // 4348
        var n = lineNo(line);                                                                                          // 4349
        if (n == null) return null;                                                                                    // 4350
      }                                                                                                                // 4351
      return {line: n, handle: line, text: line.text, gutterMarkers: line.gutterMarkers,                               // 4352
              textClass: line.textClass, bgClass: line.bgClass, wrapClass: line.wrapClass,                             // 4353
              widgets: line.widgets};                                                                                  // 4354
    },                                                                                                                 // 4355
                                                                                                                       // 4356
    getViewport: function() { return {from: this.display.viewFrom, to: this.display.viewTo};},                         // 4357
                                                                                                                       // 4358
    addWidget: function(pos, node, scroll, vert, horiz) {                                                              // 4359
      var display = this.display;                                                                                      // 4360
      pos = cursorCoords(this, clipPos(this.doc, pos));                                                                // 4361
      var top = pos.bottom, left = pos.left;                                                                           // 4362
      node.style.position = "absolute";                                                                                // 4363
      node.setAttribute("cm-ignore-events", "true");                                                                   // 4364
      display.sizer.appendChild(node);                                                                                 // 4365
      if (vert == "over") {                                                                                            // 4366
        top = pos.top;                                                                                                 // 4367
      } else if (vert == "above" || vert == "near") {                                                                  // 4368
        var vspace = Math.max(display.wrapper.clientHeight, this.doc.height),                                          // 4369
        hspace = Math.max(display.sizer.clientWidth, display.lineSpace.clientWidth);                                   // 4370
        // Default to positioning above (if specified and possible); otherwise default to positioning below            // 4371
        if ((vert == 'above' || pos.bottom + node.offsetHeight > vspace) && pos.top > node.offsetHeight)               // 4372
          top = pos.top - node.offsetHeight;                                                                           // 4373
        else if (pos.bottom + node.offsetHeight <= vspace)                                                             // 4374
          top = pos.bottom;                                                                                            // 4375
        if (left + node.offsetWidth > hspace)                                                                          // 4376
          left = hspace - node.offsetWidth;                                                                            // 4377
      }                                                                                                                // 4378
      node.style.top = top + "px";                                                                                     // 4379
      node.style.left = node.style.right = "";                                                                         // 4380
      if (horiz == "right") {                                                                                          // 4381
        left = display.sizer.clientWidth - node.offsetWidth;                                                           // 4382
        node.style.right = "0px";                                                                                      // 4383
      } else {                                                                                                         // 4384
        if (horiz == "left") left = 0;                                                                                 // 4385
        else if (horiz == "middle") left = (display.sizer.clientWidth - node.offsetWidth) / 2;                         // 4386
        node.style.left = left + "px";                                                                                 // 4387
      }                                                                                                                // 4388
      if (scroll)                                                                                                      // 4389
        scrollIntoView(this, left, top, left + node.offsetWidth, top + node.offsetHeight);                             // 4390
    },                                                                                                                 // 4391
                                                                                                                       // 4392
    triggerOnKeyDown: methodOp(onKeyDown),                                                                             // 4393
    triggerOnKeyPress: methodOp(onKeyPress),                                                                           // 4394
    triggerOnKeyUp: onKeyUp,                                                                                           // 4395
                                                                                                                       // 4396
    execCommand: function(cmd) {                                                                                       // 4397
      if (commands.hasOwnProperty(cmd))                                                                                // 4398
        return commands[cmd](this);                                                                                    // 4399
    },                                                                                                                 // 4400
                                                                                                                       // 4401
    findPosH: function(from, amount, unit, visually) {                                                                 // 4402
      var dir = 1;                                                                                                     // 4403
      if (amount < 0) { dir = -1; amount = -amount; }                                                                  // 4404
      for (var i = 0, cur = clipPos(this.doc, from); i < amount; ++i) {                                                // 4405
        cur = findPosH(this.doc, cur, dir, unit, visually);                                                            // 4406
        if (cur.hitSide) break;                                                                                        // 4407
      }                                                                                                                // 4408
      return cur;                                                                                                      // 4409
    },                                                                                                                 // 4410
                                                                                                                       // 4411
    moveH: methodOp(function(dir, unit) {                                                                              // 4412
      var cm = this;                                                                                                   // 4413
      cm.extendSelectionsBy(function(range) {                                                                          // 4414
        if (cm.display.shift || cm.doc.extend || range.empty())                                                        // 4415
          return findPosH(cm.doc, range.head, dir, unit, cm.options.rtlMoveVisually);                                  // 4416
        else                                                                                                           // 4417
          return dir < 0 ? range.from() : range.to();                                                                  // 4418
      }, sel_move);                                                                                                    // 4419
    }),                                                                                                                // 4420
                                                                                                                       // 4421
    deleteH: methodOp(function(dir, unit) {                                                                            // 4422
      var sel = this.doc.sel, doc = this.doc;                                                                          // 4423
      if (sel.somethingSelected())                                                                                     // 4424
        doc.replaceSelection("", null, "+delete");                                                                     // 4425
      else                                                                                                             // 4426
        deleteNearSelection(this, function(range) {                                                                    // 4427
          var other = findPosH(doc, range.head, dir, unit, false);                                                     // 4428
          return dir < 0 ? {from: other, to: range.head} : {from: range.head, to: other};                              // 4429
        });                                                                                                            // 4430
    }),                                                                                                                // 4431
                                                                                                                       // 4432
    findPosV: function(from, amount, unit, goalColumn) {                                                               // 4433
      var dir = 1, x = goalColumn;                                                                                     // 4434
      if (amount < 0) { dir = -1; amount = -amount; }                                                                  // 4435
      for (var i = 0, cur = clipPos(this.doc, from); i < amount; ++i) {                                                // 4436
        var coords = cursorCoords(this, cur, "div");                                                                   // 4437
        if (x == null) x = coords.left;                                                                                // 4438
        else coords.left = x;                                                                                          // 4439
        cur = findPosV(this, coords, dir, unit);                                                                       // 4440
        if (cur.hitSide) break;                                                                                        // 4441
      }                                                                                                                // 4442
      return cur;                                                                                                      // 4443
    },                                                                                                                 // 4444
                                                                                                                       // 4445
    moveV: methodOp(function(dir, unit) {                                                                              // 4446
      var cm = this, doc = this.doc, goals = [];                                                                       // 4447
      var collapse = !cm.display.shift && !doc.extend && doc.sel.somethingSelected();                                  // 4448
      doc.extendSelectionsBy(function(range) {                                                                         // 4449
        if (collapse)                                                                                                  // 4450
          return dir < 0 ? range.from() : range.to();                                                                  // 4451
        var headPos = cursorCoords(cm, range.head, "div");                                                             // 4452
        if (range.goalColumn != null) headPos.left = range.goalColumn;                                                 // 4453
        goals.push(headPos.left);                                                                                      // 4454
        var pos = findPosV(cm, headPos, dir, unit);                                                                    // 4455
        if (unit == "page" && range == doc.sel.primary())                                                              // 4456
          addToScrollPos(cm, null, charCoords(cm, pos, "div").top - headPos.top);                                      // 4457
        return pos;                                                                                                    // 4458
      }, sel_move);                                                                                                    // 4459
      if (goals.length) for (var i = 0; i < doc.sel.ranges.length; i++)                                                // 4460
        doc.sel.ranges[i].goalColumn = goals[i];                                                                       // 4461
    }),                                                                                                                // 4462
                                                                                                                       // 4463
    // Find the word at the given position (as returned by coordsChar).                                                // 4464
    findWordAt: function(pos) {                                                                                        // 4465
      var doc = this.doc, line = getLine(doc, pos.line).text;                                                          // 4466
      var start = pos.ch, end = pos.ch;                                                                                // 4467
      if (line) {                                                                                                      // 4468
        var helper = this.getHelper(pos, "wordChars");                                                                 // 4469
        if ((pos.xRel < 0 || end == line.length) && start) --start; else ++end;                                        // 4470
        var startChar = line.charAt(start);                                                                            // 4471
        var check = isWordChar(startChar, helper)                                                                      // 4472
          ? function(ch) { return isWordChar(ch, helper); }                                                            // 4473
          : /\s/.test(startChar) ? function(ch) {return /\s/.test(ch);}                                                // 4474
          : function(ch) {return !/\s/.test(ch) && !isWordChar(ch);};                                                  // 4475
        while (start > 0 && check(line.charAt(start - 1))) --start;                                                    // 4476
        while (end < line.length && check(line.charAt(end))) ++end;                                                    // 4477
      }                                                                                                                // 4478
      return new Range(Pos(pos.line, start), Pos(pos.line, end));                                                      // 4479
    },                                                                                                                 // 4480
                                                                                                                       // 4481
    toggleOverwrite: function(value) {                                                                                 // 4482
      if (value != null && value == this.state.overwrite) return;                                                      // 4483
      if (this.state.overwrite = !this.state.overwrite)                                                                // 4484
        addClass(this.display.cursorDiv, "CodeMirror-overwrite");                                                      // 4485
      else                                                                                                             // 4486
        rmClass(this.display.cursorDiv, "CodeMirror-overwrite");                                                       // 4487
                                                                                                                       // 4488
      signal(this, "overwriteToggle", this, this.state.overwrite);                                                     // 4489
    },                                                                                                                 // 4490
    hasFocus: function() { return activeElt() == this.display.input; },                                                // 4491
                                                                                                                       // 4492
    scrollTo: methodOp(function(x, y) {                                                                                // 4493
      if (x != null || y != null) resolveScrollToPos(this);                                                            // 4494
      if (x != null) this.curOp.scrollLeft = x;                                                                        // 4495
      if (y != null) this.curOp.scrollTop = y;                                                                         // 4496
    }),                                                                                                                // 4497
    getScrollInfo: function() {                                                                                        // 4498
      var scroller = this.display.scroller;                                                                            // 4499
      return {left: scroller.scrollLeft, top: scroller.scrollTop,                                                      // 4500
              height: scroller.scrollHeight - scrollGap(this) - this.display.barHeight,                                // 4501
              width: scroller.scrollWidth - scrollGap(this) - this.display.barWidth,                                   // 4502
              clientHeight: displayHeight(this), clientWidth: displayWidth(this)};                                     // 4503
    },                                                                                                                 // 4504
                                                                                                                       // 4505
    scrollIntoView: methodOp(function(range, margin) {                                                                 // 4506
      if (range == null) {                                                                                             // 4507
        range = {from: this.doc.sel.primary().head, to: null};                                                         // 4508
        if (margin == null) margin = this.options.cursorScrollMargin;                                                  // 4509
      } else if (typeof range == "number") {                                                                           // 4510
        range = {from: Pos(range, 0), to: null};                                                                       // 4511
      } else if (range.from == null) {                                                                                 // 4512
        range = {from: range, to: null};                                                                               // 4513
      }                                                                                                                // 4514
      if (!range.to) range.to = range.from;                                                                            // 4515
      range.margin = margin || 0;                                                                                      // 4516
                                                                                                                       // 4517
      if (range.from.line != null) {                                                                                   // 4518
        resolveScrollToPos(this);                                                                                      // 4519
        this.curOp.scrollToPos = range;                                                                                // 4520
      } else {                                                                                                         // 4521
        var sPos = calculateScrollPos(this, Math.min(range.from.left, range.to.left),                                  // 4522
                                      Math.min(range.from.top, range.to.top) - range.margin,                           // 4523
                                      Math.max(range.from.right, range.to.right),                                      // 4524
                                      Math.max(range.from.bottom, range.to.bottom) + range.margin);                    // 4525
        this.scrollTo(sPos.scrollLeft, sPos.scrollTop);                                                                // 4526
      }                                                                                                                // 4527
    }),                                                                                                                // 4528
                                                                                                                       // 4529
    setSize: methodOp(function(width, height) {                                                                        // 4530
      var cm = this;                                                                                                   // 4531
      function interpret(val) {                                                                                        // 4532
        return typeof val == "number" || /^\d+$/.test(String(val)) ? val + "px" : val;                                 // 4533
      }                                                                                                                // 4534
      if (width != null) cm.display.wrapper.style.width = interpret(width);                                            // 4535
      if (height != null) cm.display.wrapper.style.height = interpret(height);                                         // 4536
      if (cm.options.lineWrapping) clearLineMeasurementCache(this);                                                    // 4537
      var lineNo = cm.display.viewFrom;                                                                                // 4538
      cm.doc.iter(lineNo, cm.display.viewTo, function(line) {                                                          // 4539
        if (line.widgets) for (var i = 0; i < line.widgets.length; i++)                                                // 4540
          if (line.widgets[i].noHScroll) { regLineChange(cm, lineNo, "widget"); break; }                               // 4541
        ++lineNo;                                                                                                      // 4542
      });                                                                                                              // 4543
      cm.curOp.forceUpdate = true;                                                                                     // 4544
      signal(cm, "refresh", this);                                                                                     // 4545
    }),                                                                                                                // 4546
                                                                                                                       // 4547
    operation: function(f){return runInOp(this, f);},                                                                  // 4548
                                                                                                                       // 4549
    refresh: methodOp(function() {                                                                                     // 4550
      var oldHeight = this.display.cachedTextHeight;                                                                   // 4551
      regChange(this);                                                                                                 // 4552
      this.curOp.forceUpdate = true;                                                                                   // 4553
      clearCaches(this);                                                                                               // 4554
      this.scrollTo(this.doc.scrollLeft, this.doc.scrollTop);                                                          // 4555
      updateGutterSpace(this);                                                                                         // 4556
      if (oldHeight == null || Math.abs(oldHeight - textHeight(this.display)) > .5)                                    // 4557
        estimateLineHeights(this);                                                                                     // 4558
      signal(this, "refresh", this);                                                                                   // 4559
    }),                                                                                                                // 4560
                                                                                                                       // 4561
    swapDoc: methodOp(function(doc) {                                                                                  // 4562
      var old = this.doc;                                                                                              // 4563
      old.cm = null;                                                                                                   // 4564
      attachDoc(this, doc);                                                                                            // 4565
      clearCaches(this);                                                                                               // 4566
      resetInput(this);                                                                                                // 4567
      this.scrollTo(doc.scrollLeft, doc.scrollTop);                                                                    // 4568
      this.curOp.forceScroll = true;                                                                                   // 4569
      signalLater(this, "swapDoc", this, old);                                                                         // 4570
      return old;                                                                                                      // 4571
    }),                                                                                                                // 4572
                                                                                                                       // 4573
    getInputField: function(){return this.display.input;},                                                             // 4574
    getWrapperElement: function(){return this.display.wrapper;},                                                       // 4575
    getScrollerElement: function(){return this.display.scroller;},                                                     // 4576
    getGutterElement: function(){return this.display.gutters;}                                                         // 4577
  };                                                                                                                   // 4578
  eventMixin(CodeMirror);                                                                                              // 4579
                                                                                                                       // 4580
  // OPTION DEFAULTS                                                                                                   // 4581
                                                                                                                       // 4582
  // The default configuration options.                                                                                // 4583
  var defaults = CodeMirror.defaults = {};                                                                             // 4584
  // Functions to run when options are changed.                                                                        // 4585
  var optionHandlers = CodeMirror.optionHandlers = {};                                                                 // 4586
                                                                                                                       // 4587
  function option(name, deflt, handle, notOnInit) {                                                                    // 4588
    CodeMirror.defaults[name] = deflt;                                                                                 // 4589
    if (handle) optionHandlers[name] =                                                                                 // 4590
      notOnInit ? function(cm, val, old) {if (old != Init) handle(cm, val, old);} : handle;                            // 4591
  }                                                                                                                    // 4592
                                                                                                                       // 4593
  // Passed to option handlers when there is no old value.                                                             // 4594
  var Init = CodeMirror.Init = {toString: function(){return "CodeMirror.Init";}};                                      // 4595
                                                                                                                       // 4596
  // These two are, on init, called from the constructor because they                                                  // 4597
  // have to be initialized before the editor can start at all.                                                        // 4598
  option("value", "", function(cm, val) {                                                                              // 4599
    cm.setValue(val);                                                                                                  // 4600
  }, true);                                                                                                            // 4601
  option("mode", null, function(cm, val) {                                                                             // 4602
    cm.doc.modeOption = val;                                                                                           // 4603
    loadMode(cm);                                                                                                      // 4604
  }, true);                                                                                                            // 4605
                                                                                                                       // 4606
  option("indentUnit", 2, loadMode, true);                                                                             // 4607
  option("indentWithTabs", false);                                                                                     // 4608
  option("smartIndent", true);                                                                                         // 4609
  option("tabSize", 4, function(cm) {                                                                                  // 4610
    resetModeState(cm);                                                                                                // 4611
    clearCaches(cm);                                                                                                   // 4612
    regChange(cm);                                                                                                     // 4613
  }, true);                                                                                                            // 4614
  option("specialChars", /[\t\u0000-\u0019\u00ad\u200b-\u200f\u2028\u2029\ufeff]/g, function(cm, val) {                // 4615
    cm.options.specialChars = new RegExp(val.source + (val.test("\t") ? "" : "|\t"), "g");                             // 4616
    cm.refresh();                                                                                                      // 4617
  }, true);                                                                                                            // 4618
  option("specialCharPlaceholder", defaultSpecialCharPlaceholder, function(cm) {cm.refresh();}, true);                 // 4619
  option("electricChars", true);                                                                                       // 4620
  option("rtlMoveVisually", !windows);                                                                                 // 4621
  option("wholeLineUpdateBefore", true);                                                                               // 4622
                                                                                                                       // 4623
  option("theme", "default", function(cm) {                                                                            // 4624
    themeChanged(cm);                                                                                                  // 4625
    guttersChanged(cm);                                                                                                // 4626
  }, true);                                                                                                            // 4627
  option("keyMap", "default", function(cm, val, old) {                                                                 // 4628
    var next = getKeyMap(val);                                                                                         // 4629
    var prev = old != CodeMirror.Init && getKeyMap(old);                                                               // 4630
    if (prev && prev.detach) prev.detach(cm, next);                                                                    // 4631
    if (next.attach) next.attach(cm, prev || null);                                                                    // 4632
  });                                                                                                                  // 4633
  option("extraKeys", null);                                                                                           // 4634
                                                                                                                       // 4635
  option("lineWrapping", false, wrappingChanged, true);                                                                // 4636
  option("gutters", [], function(cm) {                                                                                 // 4637
    setGuttersForLineNumbers(cm.options);                                                                              // 4638
    guttersChanged(cm);                                                                                                // 4639
  }, true);                                                                                                            // 4640
  option("fixedGutter", true, function(cm, val) {                                                                      // 4641
    cm.display.gutters.style.left = val ? compensateForHScroll(cm.display) + "px" : "0";                               // 4642
    cm.refresh();                                                                                                      // 4643
  }, true);                                                                                                            // 4644
  option("coverGutterNextToScrollbar", false, function(cm) {updateScrollbars(cm);}, true);                             // 4645
  option("scrollbarStyle", "native", function(cm) {                                                                    // 4646
    initScrollbars(cm);                                                                                                // 4647
    updateScrollbars(cm);                                                                                              // 4648
    cm.display.scrollbars.setScrollTop(cm.doc.scrollTop);                                                              // 4649
    cm.display.scrollbars.setScrollLeft(cm.doc.scrollLeft);                                                            // 4650
  }, true);                                                                                                            // 4651
  option("lineNumbers", false, function(cm) {                                                                          // 4652
    setGuttersForLineNumbers(cm.options);                                                                              // 4653
    guttersChanged(cm);                                                                                                // 4654
  }, true);                                                                                                            // 4655
  option("firstLineNumber", 1, guttersChanged, true);                                                                  // 4656
  option("lineNumberFormatter", function(integer) {return integer;}, guttersChanged, true);                            // 4657
  option("showCursorWhenSelecting", false, updateSelection, true);                                                     // 4658
                                                                                                                       // 4659
  option("resetSelectionOnContextMenu", true);                                                                         // 4660
                                                                                                                       // 4661
  option("readOnly", false, function(cm, val) {                                                                        // 4662
    if (val == "nocursor") {                                                                                           // 4663
      onBlur(cm);                                                                                                      // 4664
      cm.display.input.blur();                                                                                         // 4665
      cm.display.disabled = true;                                                                                      // 4666
    } else {                                                                                                           // 4667
      cm.display.disabled = false;                                                                                     // 4668
      if (!val) resetInput(cm);                                                                                        // 4669
    }                                                                                                                  // 4670
  });                                                                                                                  // 4671
  option("disableInput", false, function(cm, val) {if (!val) resetInput(cm);}, true);                                  // 4672
  option("dragDrop", true);                                                                                            // 4673
                                                                                                                       // 4674
  option("cursorBlinkRate", 530);                                                                                      // 4675
  option("cursorScrollMargin", 0);                                                                                     // 4676
  option("cursorHeight", 1, updateSelection, true);                                                                    // 4677
  option("singleCursorHeightPerLine", true, updateSelection, true);                                                    // 4678
  option("workTime", 100);                                                                                             // 4679
  option("workDelay", 100);                                                                                            // 4680
  option("flattenSpans", true, resetModeState, true);                                                                  // 4681
  option("addModeClass", false, resetModeState, true);                                                                 // 4682
  option("pollInterval", 100);                                                                                         // 4683
  option("undoDepth", 200, function(cm, val){cm.doc.history.undoDepth = val;});                                        // 4684
  option("historyEventDelay", 1250);                                                                                   // 4685
  option("viewportMargin", 10, function(cm){cm.refresh();}, true);                                                     // 4686
  option("maxHighlightLength", 10000, resetModeState, true);                                                           // 4687
  option("moveInputWithCursor", true, function(cm, val) {                                                              // 4688
    if (!val) cm.display.inputDiv.style.top = cm.display.inputDiv.style.left = 0;                                      // 4689
  });                                                                                                                  // 4690
                                                                                                                       // 4691
  option("tabindex", null, function(cm, val) {                                                                         // 4692
    cm.display.input.tabIndex = val || "";                                                                             // 4693
  });                                                                                                                  // 4694
  option("autofocus", null);                                                                                           // 4695
                                                                                                                       // 4696
  // MODE DEFINITION AND QUERYING                                                                                      // 4697
                                                                                                                       // 4698
  // Known modes, by name and by MIME                                                                                  // 4699
  var modes = CodeMirror.modes = {}, mimeModes = CodeMirror.mimeModes = {};                                            // 4700
                                                                                                                       // 4701
  // Extra arguments are stored as the mode's dependencies, which is                                                   // 4702
  // used by (legacy) mechanisms like loadmode.js to automatically                                                     // 4703
  // load a mode. (Preferred mechanism is the require/define calls.)                                                   // 4704
  CodeMirror.defineMode = function(name, mode) {                                                                       // 4705
    if (!CodeMirror.defaults.mode && name != "null") CodeMirror.defaults.mode = name;                                  // 4706
    if (arguments.length > 2)                                                                                          // 4707
      mode.dependencies = Array.prototype.slice.call(arguments, 2);                                                    // 4708
    modes[name] = mode;                                                                                                // 4709
  };                                                                                                                   // 4710
                                                                                                                       // 4711
  CodeMirror.defineMIME = function(mime, spec) {                                                                       // 4712
    mimeModes[mime] = spec;                                                                                            // 4713
  };                                                                                                                   // 4714
                                                                                                                       // 4715
  // Given a MIME type, a {name, ...options} config object, or a name                                                  // 4716
  // string, return a mode config object.                                                                              // 4717
  CodeMirror.resolveMode = function(spec) {                                                                            // 4718
    if (typeof spec == "string" && mimeModes.hasOwnProperty(spec)) {                                                   // 4719
      spec = mimeModes[spec];                                                                                          // 4720
    } else if (spec && typeof spec.name == "string" && mimeModes.hasOwnProperty(spec.name)) {                          // 4721
      var found = mimeModes[spec.name];                                                                                // 4722
      if (typeof found == "string") found = {name: found};                                                             // 4723
      spec = createObj(found, spec);                                                                                   // 4724
      spec.name = found.name;                                                                                          // 4725
    } else if (typeof spec == "string" && /^[\w\-]+\/[\w\-]+\+xml$/.test(spec)) {                                      // 4726
      return CodeMirror.resolveMode("application/xml");                                                                // 4727
    }                                                                                                                  // 4728
    if (typeof spec == "string") return {name: spec};                                                                  // 4729
    else return spec || {name: "null"};                                                                                // 4730
  };                                                                                                                   // 4731
                                                                                                                       // 4732
  // Given a mode spec (anything that resolveMode accepts), find and                                                   // 4733
  // initialize an actual mode object.                                                                                 // 4734
  CodeMirror.getMode = function(options, spec) {                                                                       // 4735
    var spec = CodeMirror.resolveMode(spec);                                                                           // 4736
    var mfactory = modes[spec.name];                                                                                   // 4737
    if (!mfactory) return CodeMirror.getMode(options, "text/plain");                                                   // 4738
    var modeObj = mfactory(options, spec);                                                                             // 4739
    if (modeExtensions.hasOwnProperty(spec.name)) {                                                                    // 4740
      var exts = modeExtensions[spec.name];                                                                            // 4741
      for (var prop in exts) {                                                                                         // 4742
        if (!exts.hasOwnProperty(prop)) continue;                                                                      // 4743
        if (modeObj.hasOwnProperty(prop)) modeObj["_" + prop] = modeObj[prop];                                         // 4744
        modeObj[prop] = exts[prop];                                                                                    // 4745
      }                                                                                                                // 4746
    }                                                                                                                  // 4747
    modeObj.name = spec.name;                                                                                          // 4748
    if (spec.helperType) modeObj.helperType = spec.helperType;                                                         // 4749
    if (spec.modeProps) for (var prop in spec.modeProps)                                                               // 4750
      modeObj[prop] = spec.modeProps[prop];                                                                            // 4751
                                                                                                                       // 4752
    return modeObj;                                                                                                    // 4753
  };                                                                                                                   // 4754
                                                                                                                       // 4755
  // Minimal default mode.                                                                                             // 4756
  CodeMirror.defineMode("null", function() {                                                                           // 4757
    return {token: function(stream) {stream.skipToEnd();}};                                                            // 4758
  });                                                                                                                  // 4759
  CodeMirror.defineMIME("text/plain", "null");                                                                         // 4760
                                                                                                                       // 4761
  // This can be used to attach properties to mode objects from                                                        // 4762
  // outside the actual mode definition.                                                                               // 4763
  var modeExtensions = CodeMirror.modeExtensions = {};                                                                 // 4764
  CodeMirror.extendMode = function(mode, properties) {                                                                 // 4765
    var exts = modeExtensions.hasOwnProperty(mode) ? modeExtensions[mode] : (modeExtensions[mode] = {});               // 4766
    copyObj(properties, exts);                                                                                         // 4767
  };                                                                                                                   // 4768
                                                                                                                       // 4769
  // EXTENSIONS                                                                                                        // 4770
                                                                                                                       // 4771
  CodeMirror.defineExtension = function(name, func) {                                                                  // 4772
    CodeMirror.prototype[name] = func;                                                                                 // 4773
  };                                                                                                                   // 4774
  CodeMirror.defineDocExtension = function(name, func) {                                                               // 4775
    Doc.prototype[name] = func;                                                                                        // 4776
  };                                                                                                                   // 4777
  CodeMirror.defineOption = option;                                                                                    // 4778
                                                                                                                       // 4779
  var initHooks = [];                                                                                                  // 4780
  CodeMirror.defineInitHook = function(f) {initHooks.push(f);};                                                        // 4781
                                                                                                                       // 4782
  var helpers = CodeMirror.helpers = {};                                                                               // 4783
  CodeMirror.registerHelper = function(type, name, value) {                                                            // 4784
    if (!helpers.hasOwnProperty(type)) helpers[type] = CodeMirror[type] = {_global: []};                               // 4785
    helpers[type][name] = value;                                                                                       // 4786
  };                                                                                                                   // 4787
  CodeMirror.registerGlobalHelper = function(type, name, predicate, value) {                                           // 4788
    CodeMirror.registerHelper(type, name, value);                                                                      // 4789
    helpers[type]._global.push({pred: predicate, val: value});                                                         // 4790
  };                                                                                                                   // 4791
                                                                                                                       // 4792
  // MODE STATE HANDLING                                                                                               // 4793
                                                                                                                       // 4794
  // Utility functions for working with state. Exported because nested                                                 // 4795
  // modes need to do this for their inner modes.                                                                      // 4796
                                                                                                                       // 4797
  var copyState = CodeMirror.copyState = function(mode, state) {                                                       // 4798
    if (state === true) return state;                                                                                  // 4799
    if (mode.copyState) return mode.copyState(state);                                                                  // 4800
    var nstate = {};                                                                                                   // 4801
    for (var n in state) {                                                                                             // 4802
      var val = state[n];                                                                                              // 4803
      if (val instanceof Array) val = val.concat([]);                                                                  // 4804
      nstate[n] = val;                                                                                                 // 4805
    }                                                                                                                  // 4806
    return nstate;                                                                                                     // 4807
  };                                                                                                                   // 4808
                                                                                                                       // 4809
  var startState = CodeMirror.startState = function(mode, a1, a2) {                                                    // 4810
    return mode.startState ? mode.startState(a1, a2) : true;                                                           // 4811
  };                                                                                                                   // 4812
                                                                                                                       // 4813
  // Given a mode and a state (for that mode), find the inner mode and                                                 // 4814
  // state at the position that the state refers to.                                                                   // 4815
  CodeMirror.innerMode = function(mode, state) {                                                                       // 4816
    while (mode.innerMode) {                                                                                           // 4817
      var info = mode.innerMode(state);                                                                                // 4818
      if (!info || info.mode == mode) break;                                                                           // 4819
      state = info.state;                                                                                              // 4820
      mode = info.mode;                                                                                                // 4821
    }                                                                                                                  // 4822
    return info || {mode: mode, state: state};                                                                         // 4823
  };                                                                                                                   // 4824
                                                                                                                       // 4825
  // STANDARD COMMANDS                                                                                                 // 4826
                                                                                                                       // 4827
  // Commands are parameter-less actions that can be performed on an                                                   // 4828
  // editor, mostly used for keybindings.                                                                              // 4829
  var commands = CodeMirror.commands = {                                                                               // 4830
    selectAll: function(cm) {cm.setSelection(Pos(cm.firstLine(), 0), Pos(cm.lastLine()), sel_dontScroll);},            // 4831
    singleSelection: function(cm) {                                                                                    // 4832
      cm.setSelection(cm.getCursor("anchor"), cm.getCursor("head"), sel_dontScroll);                                   // 4833
    },                                                                                                                 // 4834
    killLine: function(cm) {                                                                                           // 4835
      deleteNearSelection(cm, function(range) {                                                                        // 4836
        if (range.empty()) {                                                                                           // 4837
          var len = getLine(cm.doc, range.head.line).text.length;                                                      // 4838
          if (range.head.ch == len && range.head.line < cm.lastLine())                                                 // 4839
            return {from: range.head, to: Pos(range.head.line + 1, 0)};                                                // 4840
          else                                                                                                         // 4841
            return {from: range.head, to: Pos(range.head.line, len)};                                                  // 4842
        } else {                                                                                                       // 4843
          return {from: range.from(), to: range.to()};                                                                 // 4844
        }                                                                                                              // 4845
      });                                                                                                              // 4846
    },                                                                                                                 // 4847
    deleteLine: function(cm) {                                                                                         // 4848
      deleteNearSelection(cm, function(range) {                                                                        // 4849
        return {from: Pos(range.from().line, 0),                                                                       // 4850
                to: clipPos(cm.doc, Pos(range.to().line + 1, 0))};                                                     // 4851
      });                                                                                                              // 4852
    },                                                                                                                 // 4853
    delLineLeft: function(cm) {                                                                                        // 4854
      deleteNearSelection(cm, function(range) {                                                                        // 4855
        return {from: Pos(range.from().line, 0), to: range.from()};                                                    // 4856
      });                                                                                                              // 4857
    },                                                                                                                 // 4858
    delWrappedLineLeft: function(cm) {                                                                                 // 4859
      deleteNearSelection(cm, function(range) {                                                                        // 4860
        var top = cm.charCoords(range.head, "div").top + 5;                                                            // 4861
        var leftPos = cm.coordsChar({left: 0, top: top}, "div");                                                       // 4862
        return {from: leftPos, to: range.from()};                                                                      // 4863
      });                                                                                                              // 4864
    },                                                                                                                 // 4865
    delWrappedLineRight: function(cm) {                                                                                // 4866
      deleteNearSelection(cm, function(range) {                                                                        // 4867
        var top = cm.charCoords(range.head, "div").top + 5;                                                            // 4868
        var rightPos = cm.coordsChar({left: cm.display.lineDiv.offsetWidth + 100, top: top}, "div");                   // 4869
        return {from: range.from(), to: rightPos };                                                                    // 4870
      });                                                                                                              // 4871
    },                                                                                                                 // 4872
    undo: function(cm) {cm.undo();},                                                                                   // 4873
    redo: function(cm) {cm.redo();},                                                                                   // 4874
    undoSelection: function(cm) {cm.undoSelection();},                                                                 // 4875
    redoSelection: function(cm) {cm.redoSelection();},                                                                 // 4876
    goDocStart: function(cm) {cm.extendSelection(Pos(cm.firstLine(), 0));},                                            // 4877
    goDocEnd: function(cm) {cm.extendSelection(Pos(cm.lastLine()));},                                                  // 4878
    goLineStart: function(cm) {                                                                                        // 4879
      cm.extendSelectionsBy(function(range) { return lineStart(cm, range.head.line); },                                // 4880
                            {origin: "+move", bias: 1});                                                               // 4881
    },                                                                                                                 // 4882
    goLineStartSmart: function(cm) {                                                                                   // 4883
      cm.extendSelectionsBy(function(range) {                                                                          // 4884
        return lineStartSmart(cm, range.head);                                                                         // 4885
      }, {origin: "+move", bias: 1});                                                                                  // 4886
    },                                                                                                                 // 4887
    goLineEnd: function(cm) {                                                                                          // 4888
      cm.extendSelectionsBy(function(range) { return lineEnd(cm, range.head.line); },                                  // 4889
                            {origin: "+move", bias: -1});                                                              // 4890
    },                                                                                                                 // 4891
    goLineRight: function(cm) {                                                                                        // 4892
      cm.extendSelectionsBy(function(range) {                                                                          // 4893
        var top = cm.charCoords(range.head, "div").top + 5;                                                            // 4894
        return cm.coordsChar({left: cm.display.lineDiv.offsetWidth + 100, top: top}, "div");                           // 4895
      }, sel_move);                                                                                                    // 4896
    },                                                                                                                 // 4897
    goLineLeft: function(cm) {                                                                                         // 4898
      cm.extendSelectionsBy(function(range) {                                                                          // 4899
        var top = cm.charCoords(range.head, "div").top + 5;                                                            // 4900
        return cm.coordsChar({left: 0, top: top}, "div");                                                              // 4901
      }, sel_move);                                                                                                    // 4902
    },                                                                                                                 // 4903
    goLineLeftSmart: function(cm) {                                                                                    // 4904
      cm.extendSelectionsBy(function(range) {                                                                          // 4905
        var top = cm.charCoords(range.head, "div").top + 5;                                                            // 4906
        var pos = cm.coordsChar({left: 0, top: top}, "div");                                                           // 4907
        if (pos.ch < cm.getLine(pos.line).search(/\S/)) return lineStartSmart(cm, range.head);                         // 4908
        return pos;                                                                                                    // 4909
      }, sel_move);                                                                                                    // 4910
    },                                                                                                                 // 4911
    goLineUp: function(cm) {cm.moveV(-1, "line");},                                                                    // 4912
    goLineDown: function(cm) {cm.moveV(1, "line");},                                                                   // 4913
    goPageUp: function(cm) {cm.moveV(-1, "page");},                                                                    // 4914
    goPageDown: function(cm) {cm.moveV(1, "page");},                                                                   // 4915
    goCharLeft: function(cm) {cm.moveH(-1, "char");},                                                                  // 4916
    goCharRight: function(cm) {cm.moveH(1, "char");},                                                                  // 4917
    goColumnLeft: function(cm) {cm.moveH(-1, "column");},                                                              // 4918
    goColumnRight: function(cm) {cm.moveH(1, "column");},                                                              // 4919
    goWordLeft: function(cm) {cm.moveH(-1, "word");},                                                                  // 4920
    goGroupRight: function(cm) {cm.moveH(1, "group");},                                                                // 4921
    goGroupLeft: function(cm) {cm.moveH(-1, "group");},                                                                // 4922
    goWordRight: function(cm) {cm.moveH(1, "word");},                                                                  // 4923
    delCharBefore: function(cm) {cm.deleteH(-1, "char");},                                                             // 4924
    delCharAfter: function(cm) {cm.deleteH(1, "char");},                                                               // 4925
    delWordBefore: function(cm) {cm.deleteH(-1, "word");},                                                             // 4926
    delWordAfter: function(cm) {cm.deleteH(1, "word");},                                                               // 4927
    delGroupBefore: function(cm) {cm.deleteH(-1, "group");},                                                           // 4928
    delGroupAfter: function(cm) {cm.deleteH(1, "group");},                                                             // 4929
    indentAuto: function(cm) {cm.indentSelection("smart");},                                                           // 4930
    indentMore: function(cm) {cm.indentSelection("add");},                                                             // 4931
    indentLess: function(cm) {cm.indentSelection("subtract");},                                                        // 4932
    insertTab: function(cm) {cm.replaceSelection("\t");},                                                              // 4933
    insertSoftTab: function(cm) {                                                                                      // 4934
      var spaces = [], ranges = cm.listSelections(), tabSize = cm.options.tabSize;                                     // 4935
      for (var i = 0; i < ranges.length; i++) {                                                                        // 4936
        var pos = ranges[i].from();                                                                                    // 4937
        var col = countColumn(cm.getLine(pos.line), pos.ch, tabSize);                                                  // 4938
        spaces.push(new Array(tabSize - col % tabSize + 1).join(" "));                                                 // 4939
      }                                                                                                                // 4940
      cm.replaceSelections(spaces);                                                                                    // 4941
    },                                                                                                                 // 4942
    defaultTab: function(cm) {                                                                                         // 4943
      if (cm.somethingSelected()) cm.indentSelection("add");                                                           // 4944
      else cm.execCommand("insertTab");                                                                                // 4945
    },                                                                                                                 // 4946
    transposeChars: function(cm) {                                                                                     // 4947
      runInOp(cm, function() {                                                                                         // 4948
        var ranges = cm.listSelections(), newSel = [];                                                                 // 4949
        for (var i = 0; i < ranges.length; i++) {                                                                      // 4950
          var cur = ranges[i].head, line = getLine(cm.doc, cur.line).text;                                             // 4951
          if (line) {                                                                                                  // 4952
            if (cur.ch == line.length) cur = new Pos(cur.line, cur.ch - 1);                                            // 4953
            if (cur.ch > 0) {                                                                                          // 4954
              cur = new Pos(cur.line, cur.ch + 1);                                                                     // 4955
              cm.replaceRange(line.charAt(cur.ch - 1) + line.charAt(cur.ch - 2),                                       // 4956
                              Pos(cur.line, cur.ch - 2), cur, "+transpose");                                           // 4957
            } else if (cur.line > cm.doc.first) {                                                                      // 4958
              var prev = getLine(cm.doc, cur.line - 1).text;                                                           // 4959
              if (prev)                                                                                                // 4960
                cm.replaceRange(line.charAt(0) + "\n" + prev.charAt(prev.length - 1),                                  // 4961
                                Pos(cur.line - 1, prev.length - 1), Pos(cur.line, 1), "+transpose");                   // 4962
            }                                                                                                          // 4963
          }                                                                                                            // 4964
          newSel.push(new Range(cur, cur));                                                                            // 4965
        }                                                                                                              // 4966
        cm.setSelections(newSel);                                                                                      // 4967
      });                                                                                                              // 4968
    },                                                                                                                 // 4969
    newlineAndIndent: function(cm) {                                                                                   // 4970
      runInOp(cm, function() {                                                                                         // 4971
        var len = cm.listSelections().length;                                                                          // 4972
        for (var i = 0; i < len; i++) {                                                                                // 4973
          var range = cm.listSelections()[i];                                                                          // 4974
          cm.replaceRange("\n", range.anchor, range.head, "+input");                                                   // 4975
          cm.indentLine(range.from().line + 1, null, true);                                                            // 4976
          ensureCursorVisible(cm);                                                                                     // 4977
        }                                                                                                              // 4978
      });                                                                                                              // 4979
    },                                                                                                                 // 4980
    toggleOverwrite: function(cm) {cm.toggleOverwrite();}                                                              // 4981
  };                                                                                                                   // 4982
                                                                                                                       // 4983
                                                                                                                       // 4984
  // STANDARD KEYMAPS                                                                                                  // 4985
                                                                                                                       // 4986
  var keyMap = CodeMirror.keyMap = {};                                                                                 // 4987
                                                                                                                       // 4988
  keyMap.basic = {                                                                                                     // 4989
    "Left": "goCharLeft", "Right": "goCharRight", "Up": "goLineUp", "Down": "goLineDown",                              // 4990
    "End": "goLineEnd", "Home": "goLineStartSmart", "PageUp": "goPageUp", "PageDown": "goPageDown",                    // 4991
    "Delete": "delCharAfter", "Backspace": "delCharBefore", "Shift-Backspace": "delCharBefore",                        // 4992
    "Tab": "defaultTab", "Shift-Tab": "indentAuto",                                                                    // 4993
    "Enter": "newlineAndIndent", "Insert": "toggleOverwrite",                                                          // 4994
    "Esc": "singleSelection"                                                                                           // 4995
  };                                                                                                                   // 4996
  // Note that the save and find-related commands aren't defined by                                                    // 4997
  // default. User code or addons can define them. Unknown commands                                                    // 4998
  // are simply ignored.                                                                                               // 4999
  keyMap.pcDefault = {                                                                                                 // 5000
    "Ctrl-A": "selectAll", "Ctrl-D": "deleteLine", "Ctrl-Z": "undo", "Shift-Ctrl-Z": "redo", "Ctrl-Y": "redo",         // 5001
    "Ctrl-Home": "goDocStart", "Ctrl-End": "goDocEnd", "Ctrl-Up": "goLineUp", "Ctrl-Down": "goLineDown",               // 5002
    "Ctrl-Left": "goGroupLeft", "Ctrl-Right": "goGroupRight", "Alt-Left": "goLineStart", "Alt-Right": "goLineEnd",     // 5003
    "Ctrl-Backspace": "delGroupBefore", "Ctrl-Delete": "delGroupAfter", "Ctrl-S": "save", "Ctrl-F": "find",            // 5004
    "Ctrl-G": "findNext", "Shift-Ctrl-G": "findPrev", "Shift-Ctrl-F": "replace", "Shift-Ctrl-R": "replaceAll",         // 5005
    "Ctrl-[": "indentLess", "Ctrl-]": "indentMore",                                                                    // 5006
    "Ctrl-U": "undoSelection", "Shift-Ctrl-U": "redoSelection", "Alt-U": "redoSelection",                              // 5007
    fallthrough: "basic"                                                                                               // 5008
  };                                                                                                                   // 5009
  // Very basic readline/emacs-style bindings, which are standard on Mac.                                              // 5010
  keyMap.emacsy = {                                                                                                    // 5011
    "Ctrl-F": "goCharRight", "Ctrl-B": "goCharLeft", "Ctrl-P": "goLineUp", "Ctrl-N": "goLineDown",                     // 5012
    "Alt-F": "goWordRight", "Alt-B": "goWordLeft", "Ctrl-A": "goLineStart", "Ctrl-E": "goLineEnd",                     // 5013
    "Ctrl-V": "goPageDown", "Shift-Ctrl-V": "goPageUp", "Ctrl-D": "delCharAfter", "Ctrl-H": "delCharBefore",           // 5014
    "Alt-D": "delWordAfter", "Alt-Backspace": "delWordBefore", "Ctrl-K": "killLine", "Ctrl-T": "transposeChars"        // 5015
  };                                                                                                                   // 5016
  keyMap.macDefault = {                                                                                                // 5017
    "Cmd-A": "selectAll", "Cmd-D": "deleteLine", "Cmd-Z": "undo", "Shift-Cmd-Z": "redo", "Cmd-Y": "redo",              // 5018
    "Cmd-Home": "goDocStart", "Cmd-Up": "goDocStart", "Cmd-End": "goDocEnd", "Cmd-Down": "goDocEnd", "Alt-Left": "goGroupLeft",
    "Alt-Right": "goGroupRight", "Cmd-Left": "goLineLeft", "Cmd-Right": "goLineRight", "Alt-Backspace": "delGroupBefore",
    "Ctrl-Alt-Backspace": "delGroupAfter", "Alt-Delete": "delGroupAfter", "Cmd-S": "save", "Cmd-F": "find",            // 5021
    "Cmd-G": "findNext", "Shift-Cmd-G": "findPrev", "Cmd-Alt-F": "replace", "Shift-Cmd-Alt-F": "replaceAll",           // 5022
    "Cmd-[": "indentLess", "Cmd-]": "indentMore", "Cmd-Backspace": "delWrappedLineLeft", "Cmd-Delete": "delWrappedLineRight",
    "Cmd-U": "undoSelection", "Shift-Cmd-U": "redoSelection", "Ctrl-Up": "goDocStart", "Ctrl-Down": "goDocEnd",        // 5024
    fallthrough: ["basic", "emacsy"]                                                                                   // 5025
  };                                                                                                                   // 5026
  keyMap["default"] = mac ? keyMap.macDefault : keyMap.pcDefault;                                                      // 5027
                                                                                                                       // 5028
  // KEYMAP DISPATCH                                                                                                   // 5029
                                                                                                                       // 5030
  function normalizeKeyName(name) {                                                                                    // 5031
    var parts = name.split(/-(?!$)/), name = parts[parts.length - 1];                                                  // 5032
    var alt, ctrl, shift, cmd;                                                                                         // 5033
    for (var i = 0; i < parts.length - 1; i++) {                                                                       // 5034
      var mod = parts[i];                                                                                              // 5035
      if (/^(cmd|meta|m)$/i.test(mod)) cmd = true;                                                                     // 5036
      else if (/^a(lt)?$/i.test(mod)) alt = true;                                                                      // 5037
      else if (/^(c|ctrl|control)$/i.test(mod)) ctrl = true;                                                           // 5038
      else if (/^s(hift)$/i.test(mod)) shift = true;                                                                   // 5039
      else throw new Error("Unrecognized modifier name: " + mod);                                                      // 5040
    }                                                                                                                  // 5041
    if (alt) name = "Alt-" + name;                                                                                     // 5042
    if (ctrl) name = "Ctrl-" + name;                                                                                   // 5043
    if (cmd) name = "Cmd-" + name;                                                                                     // 5044
    if (shift) name = "Shift-" + name;                                                                                 // 5045
    return name;                                                                                                       // 5046
  }                                                                                                                    // 5047
                                                                                                                       // 5048
  // This is a kludge to keep keymaps mostly working as raw objects                                                    // 5049
  // (backwards compatibility) while at the same time support features                                                 // 5050
  // like normalization and multi-stroke key bindings. It compiles a                                                   // 5051
  // new normalized keymap, and then updates the old object to reflect                                                 // 5052
  // this.                                                                                                             // 5053
  CodeMirror.normalizeKeyMap = function(keymap) {                                                                      // 5054
    var copy = {};                                                                                                     // 5055
    for (var keyname in keymap) if (keymap.hasOwnProperty(keyname)) {                                                  // 5056
      var value = keymap[keyname];                                                                                     // 5057
      if (/^(name|fallthrough|(de|at)tach)$/.test(keyname)) continue;                                                  // 5058
      if (value == "...") { delete keymap[keyname]; continue; }                                                        // 5059
                                                                                                                       // 5060
      var keys = map(keyname.split(" "), normalizeKeyName);                                                            // 5061
      for (var i = 0; i < keys.length; i++) {                                                                          // 5062
        var val, name;                                                                                                 // 5063
        if (i == keys.length - 1) {                                                                                    // 5064
          name = keyname;                                                                                              // 5065
          val = value;                                                                                                 // 5066
        } else {                                                                                                       // 5067
          name = keys.slice(0, i + 1).join(" ");                                                                       // 5068
          val = "...";                                                                                                 // 5069
        }                                                                                                              // 5070
        var prev = copy[name];                                                                                         // 5071
        if (!prev) copy[name] = val;                                                                                   // 5072
        else if (prev != val) throw new Error("Inconsistent bindings for " + name);                                    // 5073
      }                                                                                                                // 5074
      delete keymap[keyname];                                                                                          // 5075
    }                                                                                                                  // 5076
    for (var prop in copy) keymap[prop] = copy[prop];                                                                  // 5077
    return keymap;                                                                                                     // 5078
  };                                                                                                                   // 5079
                                                                                                                       // 5080
  var lookupKey = CodeMirror.lookupKey = function(key, map, handle, context) {                                         // 5081
    map = getKeyMap(map);                                                                                              // 5082
    var found = map.call ? map.call(key, context) : map[key];                                                          // 5083
    if (found === false) return "nothing";                                                                             // 5084
    if (found === "...") return "multi";                                                                               // 5085
    if (found != null && handle(found)) return "handled";                                                              // 5086
                                                                                                                       // 5087
    if (map.fallthrough) {                                                                                             // 5088
      if (Object.prototype.toString.call(map.fallthrough) != "[object Array]")                                         // 5089
        return lookupKey(key, map.fallthrough, handle, context);                                                       // 5090
      for (var i = 0; i < map.fallthrough.length; i++) {                                                               // 5091
        var result = lookupKey(key, map.fallthrough[i], handle, context);                                              // 5092
        if (result) return result;                                                                                     // 5093
      }                                                                                                                // 5094
    }                                                                                                                  // 5095
  };                                                                                                                   // 5096
                                                                                                                       // 5097
  // Modifier key presses don't count as 'real' key presses for the                                                    // 5098
  // purpose of keymap fallthrough.                                                                                    // 5099
  var isModifierKey = CodeMirror.isModifierKey = function(value) {                                                     // 5100
    var name = typeof value == "string" ? value : keyNames[value.keyCode];                                             // 5101
    return name == "Ctrl" || name == "Alt" || name == "Shift" || name == "Mod";                                        // 5102
  };                                                                                                                   // 5103
                                                                                                                       // 5104
  // Look up the name of a key as indicated by an event object.                                                        // 5105
  var keyName = CodeMirror.keyName = function(event, noShift) {                                                        // 5106
    if (presto && event.keyCode == 34 && event["char"]) return false;                                                  // 5107
    var base = keyNames[event.keyCode], name = base;                                                                   // 5108
    if (name == null || event.altGraphKey) return false;                                                               // 5109
    if (event.altKey && base != "Alt") name = "Alt-" + name;                                                           // 5110
    if ((flipCtrlCmd ? event.metaKey : event.ctrlKey) && base != "Ctrl") name = "Ctrl-" + name;                        // 5111
    if ((flipCtrlCmd ? event.ctrlKey : event.metaKey) && base != "Cmd") name = "Cmd-" + name;                          // 5112
    if (!noShift && event.shiftKey && base != "Shift") name = "Shift-" + name;                                         // 5113
    return name;                                                                                                       // 5114
  };                                                                                                                   // 5115
                                                                                                                       // 5116
  function getKeyMap(val) {                                                                                            // 5117
    return typeof val == "string" ? keyMap[val] : val;                                                                 // 5118
  }                                                                                                                    // 5119
                                                                                                                       // 5120
  // FROMTEXTAREA                                                                                                      // 5121
                                                                                                                       // 5122
  CodeMirror.fromTextArea = function(textarea, options) {                                                              // 5123
    if (!options) options = {};                                                                                        // 5124
    options.value = textarea.value;                                                                                    // 5125
    if (!options.tabindex && textarea.tabindex)                                                                        // 5126
      options.tabindex = textarea.tabindex;                                                                            // 5127
    if (!options.placeholder && textarea.placeholder)                                                                  // 5128
      options.placeholder = textarea.placeholder;                                                                      // 5129
    // Set autofocus to true if this textarea is focused, or if it has                                                 // 5130
    // autofocus and no other element is focused.                                                                      // 5131
    if (options.autofocus == null) {                                                                                   // 5132
      var hasFocus = activeElt();                                                                                      // 5133
      options.autofocus = hasFocus == textarea ||                                                                      // 5134
        textarea.getAttribute("autofocus") != null && hasFocus == document.body;                                       // 5135
    }                                                                                                                  // 5136
                                                                                                                       // 5137
    function save() {textarea.value = cm.getValue();}                                                                  // 5138
    if (textarea.form) {                                                                                               // 5139
      on(textarea.form, "submit", save);                                                                               // 5140
      // Deplorable hack to make the submit method do the right thing.                                                 // 5141
      if (!options.leaveSubmitMethodAlone) {                                                                           // 5142
        var form = textarea.form, realSubmit = form.submit;                                                            // 5143
        try {                                                                                                          // 5144
          var wrappedSubmit = form.submit = function() {                                                               // 5145
            save();                                                                                                    // 5146
            form.submit = realSubmit;                                                                                  // 5147
            form.submit();                                                                                             // 5148
            form.submit = wrappedSubmit;                                                                               // 5149
          };                                                                                                           // 5150
        } catch(e) {}                                                                                                  // 5151
      }                                                                                                                // 5152
    }                                                                                                                  // 5153
                                                                                                                       // 5154
    textarea.style.display = "none";                                                                                   // 5155
    var cm = CodeMirror(function(node) {                                                                               // 5156
      textarea.parentNode.insertBefore(node, textarea.nextSibling);                                                    // 5157
    }, options);                                                                                                       // 5158
    cm.save = save;                                                                                                    // 5159
    cm.getTextArea = function() { return textarea; };                                                                  // 5160
    cm.toTextArea = function() {                                                                                       // 5161
      cm.toTextArea = isNaN; // Prevent this from being ran twice                                                      // 5162
      save();                                                                                                          // 5163
      textarea.parentNode.removeChild(cm.getWrapperElement());                                                         // 5164
      textarea.style.display = "";                                                                                     // 5165
      if (textarea.form) {                                                                                             // 5166
        off(textarea.form, "submit", save);                                                                            // 5167
        if (typeof textarea.form.submit == "function")                                                                 // 5168
          textarea.form.submit = realSubmit;                                                                           // 5169
      }                                                                                                                // 5170
    };                                                                                                                 // 5171
    return cm;                                                                                                         // 5172
  };                                                                                                                   // 5173
                                                                                                                       // 5174
  // STRING STREAM                                                                                                     // 5175
                                                                                                                       // 5176
  // Fed to the mode parsers, provides helper functions to make                                                        // 5177
  // parsers more succinct.                                                                                            // 5178
                                                                                                                       // 5179
  var StringStream = CodeMirror.StringStream = function(string, tabSize) {                                             // 5180
    this.pos = this.start = 0;                                                                                         // 5181
    this.string = string;                                                                                              // 5182
    this.tabSize = tabSize || 8;                                                                                       // 5183
    this.lastColumnPos = this.lastColumnValue = 0;                                                                     // 5184
    this.lineStart = 0;                                                                                                // 5185
  };                                                                                                                   // 5186
                                                                                                                       // 5187
  StringStream.prototype = {                                                                                           // 5188
    eol: function() {return this.pos >= this.string.length;},                                                          // 5189
    sol: function() {return this.pos == this.lineStart;},                                                              // 5190
    peek: function() {return this.string.charAt(this.pos) || undefined;},                                              // 5191
    next: function() {                                                                                                 // 5192
      if (this.pos < this.string.length)                                                                               // 5193
        return this.string.charAt(this.pos++);                                                                         // 5194
    },                                                                                                                 // 5195
    eat: function(match) {                                                                                             // 5196
      var ch = this.string.charAt(this.pos);                                                                           // 5197
      if (typeof match == "string") var ok = ch == match;                                                              // 5198
      else var ok = ch && (match.test ? match.test(ch) : match(ch));                                                   // 5199
      if (ok) {++this.pos; return ch;}                                                                                 // 5200
    },                                                                                                                 // 5201
    eatWhile: function(match) {                                                                                        // 5202
      var start = this.pos;                                                                                            // 5203
      while (this.eat(match)){}                                                                                        // 5204
      return this.pos > start;                                                                                         // 5205
    },                                                                                                                 // 5206
    eatSpace: function() {                                                                                             // 5207
      var start = this.pos;                                                                                            // 5208
      while (/[\s\u00a0]/.test(this.string.charAt(this.pos))) ++this.pos;                                              // 5209
      return this.pos > start;                                                                                         // 5210
    },                                                                                                                 // 5211
    skipToEnd: function() {this.pos = this.string.length;},                                                            // 5212
    skipTo: function(ch) {                                                                                             // 5213
      var found = this.string.indexOf(ch, this.pos);                                                                   // 5214
      if (found > -1) {this.pos = found; return true;}                                                                 // 5215
    },                                                                                                                 // 5216
    backUp: function(n) {this.pos -= n;},                                                                              // 5217
    column: function() {                                                                                               // 5218
      if (this.lastColumnPos < this.start) {                                                                           // 5219
        this.lastColumnValue = countColumn(this.string, this.start, this.tabSize, this.lastColumnPos, this.lastColumnValue);
        this.lastColumnPos = this.start;                                                                               // 5221
      }                                                                                                                // 5222
      return this.lastColumnValue - (this.lineStart ? countColumn(this.string, this.lineStart, this.tabSize) : 0);     // 5223
    },                                                                                                                 // 5224
    indentation: function() {                                                                                          // 5225
      return countColumn(this.string, null, this.tabSize) -                                                            // 5226
        (this.lineStart ? countColumn(this.string, this.lineStart, this.tabSize) : 0);                                 // 5227
    },                                                                                                                 // 5228
    match: function(pattern, consume, caseInsensitive) {                                                               // 5229
      if (typeof pattern == "string") {                                                                                // 5230
        var cased = function(str) {return caseInsensitive ? str.toLowerCase() : str;};                                 // 5231
        var substr = this.string.substr(this.pos, pattern.length);                                                     // 5232
        if (cased(substr) == cased(pattern)) {                                                                         // 5233
          if (consume !== false) this.pos += pattern.length;                                                           // 5234
          return true;                                                                                                 // 5235
        }                                                                                                              // 5236
      } else {                                                                                                         // 5237
        var match = this.string.slice(this.pos).match(pattern);                                                        // 5238
        if (match && match.index > 0) return null;                                                                     // 5239
        if (match && consume !== false) this.pos += match[0].length;                                                   // 5240
        return match;                                                                                                  // 5241
      }                                                                                                                // 5242
    },                                                                                                                 // 5243
    current: function(){return this.string.slice(this.start, this.pos);},                                              // 5244
    hideFirstChars: function(n, inner) {                                                                               // 5245
      this.lineStart += n;                                                                                             // 5246
      try { return inner(); }                                                                                          // 5247
      finally { this.lineStart -= n; }                                                                                 // 5248
    }                                                                                                                  // 5249
  };                                                                                                                   // 5250
                                                                                                                       // 5251
  // TEXTMARKERS                                                                                                       // 5252
                                                                                                                       // 5253
  // Created with markText and setBookmark methods. A TextMarker is a                                                  // 5254
  // handle that can be used to clear or find a marked position in the                                                 // 5255
  // document. Line objects hold arrays (markedSpans) containing                                                       // 5256
  // {from, to, marker} object pointing to such marker objects, and                                                    // 5257
  // indicating that such a marker is present on that line. Multiple                                                   // 5258
  // lines may point to the same marker when it spans across lines.                                                    // 5259
  // The spans will have null for their from/to properties when the                                                    // 5260
  // marker continues beyond the start/end of the line. Markers have                                                   // 5261
  // links back to the lines they currently touch.                                                                     // 5262
                                                                                                                       // 5263
  var TextMarker = CodeMirror.TextMarker = function(doc, type) {                                                       // 5264
    this.lines = [];                                                                                                   // 5265
    this.type = type;                                                                                                  // 5266
    this.doc = doc;                                                                                                    // 5267
  };                                                                                                                   // 5268
  eventMixin(TextMarker);                                                                                              // 5269
                                                                                                                       // 5270
  // Clear the marker.                                                                                                 // 5271
  TextMarker.prototype.clear = function() {                                                                            // 5272
    if (this.explicitlyCleared) return;                                                                                // 5273
    var cm = this.doc.cm, withOp = cm && !cm.curOp;                                                                    // 5274
    if (withOp) startOperation(cm);                                                                                    // 5275
    if (hasHandler(this, "clear")) {                                                                                   // 5276
      var found = this.find();                                                                                         // 5277
      if (found) signalLater(this, "clear", found.from, found.to);                                                     // 5278
    }                                                                                                                  // 5279
    var min = null, max = null;                                                                                        // 5280
    for (var i = 0; i < this.lines.length; ++i) {                                                                      // 5281
      var line = this.lines[i];                                                                                        // 5282
      var span = getMarkedSpanFor(line.markedSpans, this);                                                             // 5283
      if (cm && !this.collapsed) regLineChange(cm, lineNo(line), "text");                                              // 5284
      else if (cm) {                                                                                                   // 5285
        if (span.to != null) max = lineNo(line);                                                                       // 5286
        if (span.from != null) min = lineNo(line);                                                                     // 5287
      }                                                                                                                // 5288
      line.markedSpans = removeMarkedSpan(line.markedSpans, span);                                                     // 5289
      if (span.from == null && this.collapsed && !lineIsHidden(this.doc, line) && cm)                                  // 5290
        updateLineHeight(line, textHeight(cm.display));                                                                // 5291
    }                                                                                                                  // 5292
    if (cm && this.collapsed && !cm.options.lineWrapping) for (var i = 0; i < this.lines.length; ++i) {                // 5293
      var visual = visualLine(this.lines[i]), len = lineLength(visual);                                                // 5294
      if (len > cm.display.maxLineLength) {                                                                            // 5295
        cm.display.maxLine = visual;                                                                                   // 5296
        cm.display.maxLineLength = len;                                                                                // 5297
        cm.display.maxLineChanged = true;                                                                              // 5298
      }                                                                                                                // 5299
    }                                                                                                                  // 5300
                                                                                                                       // 5301
    if (min != null && cm && this.collapsed) regChange(cm, min, max + 1);                                              // 5302
    this.lines.length = 0;                                                                                             // 5303
    this.explicitlyCleared = true;                                                                                     // 5304
    if (this.atomic && this.doc.cantEdit) {                                                                            // 5305
      this.doc.cantEdit = false;                                                                                       // 5306
      if (cm) reCheckSelection(cm.doc);                                                                                // 5307
    }                                                                                                                  // 5308
    if (cm) signalLater(cm, "markerCleared", cm, this);                                                                // 5309
    if (withOp) endOperation(cm);                                                                                      // 5310
    if (this.parent) this.parent.clear();                                                                              // 5311
  };                                                                                                                   // 5312
                                                                                                                       // 5313
  // Find the position of the marker in the document. Returns a {from,                                                 // 5314
  // to} object by default. Side can be passed to get a specific side                                                  // 5315
  // -- 0 (both), -1 (left), or 1 (right). When lineObj is true, the                                                   // 5316
  // Pos objects returned contain a line object, rather than a line                                                    // 5317
  // number (used to prevent looking up the same line twice).                                                          // 5318
  TextMarker.prototype.find = function(side, lineObj) {                                                                // 5319
    if (side == null && this.type == "bookmark") side = 1;                                                             // 5320
    var from, to;                                                                                                      // 5321
    for (var i = 0; i < this.lines.length; ++i) {                                                                      // 5322
      var line = this.lines[i];                                                                                        // 5323
      var span = getMarkedSpanFor(line.markedSpans, this);                                                             // 5324
      if (span.from != null) {                                                                                         // 5325
        from = Pos(lineObj ? line : lineNo(line), span.from);                                                          // 5326
        if (side == -1) return from;                                                                                   // 5327
      }                                                                                                                // 5328
      if (span.to != null) {                                                                                           // 5329
        to = Pos(lineObj ? line : lineNo(line), span.to);                                                              // 5330
        if (side == 1) return to;                                                                                      // 5331
      }                                                                                                                // 5332
    }                                                                                                                  // 5333
    return from && {from: from, to: to};                                                                               // 5334
  };                                                                                                                   // 5335
                                                                                                                       // 5336
  // Signals that the marker's widget changed, and surrounding layout                                                  // 5337
  // should be recomputed.                                                                                             // 5338
  TextMarker.prototype.changed = function() {                                                                          // 5339
    var pos = this.find(-1, true), widget = this, cm = this.doc.cm;                                                    // 5340
    if (!pos || !cm) return;                                                                                           // 5341
    runInOp(cm, function() {                                                                                           // 5342
      var line = pos.line, lineN = lineNo(pos.line);                                                                   // 5343
      var view = findViewForLine(cm, lineN);                                                                           // 5344
      if (view) {                                                                                                      // 5345
        clearLineMeasurementCacheFor(view);                                                                            // 5346
        cm.curOp.selectionChanged = cm.curOp.forceUpdate = true;                                                       // 5347
      }                                                                                                                // 5348
      cm.curOp.updateMaxLine = true;                                                                                   // 5349
      if (!lineIsHidden(widget.doc, line) && widget.height != null) {                                                  // 5350
        var oldHeight = widget.height;                                                                                 // 5351
        widget.height = null;                                                                                          // 5352
        var dHeight = widgetHeight(widget) - oldHeight;                                                                // 5353
        if (dHeight)                                                                                                   // 5354
          updateLineHeight(line, line.height + dHeight);                                                               // 5355
      }                                                                                                                // 5356
    });                                                                                                                // 5357
  };                                                                                                                   // 5358
                                                                                                                       // 5359
  TextMarker.prototype.attachLine = function(line) {                                                                   // 5360
    if (!this.lines.length && this.doc.cm) {                                                                           // 5361
      var op = this.doc.cm.curOp;                                                                                      // 5362
      if (!op.maybeHiddenMarkers || indexOf(op.maybeHiddenMarkers, this) == -1)                                        // 5363
        (op.maybeUnhiddenMarkers || (op.maybeUnhiddenMarkers = [])).push(this);                                        // 5364
    }                                                                                                                  // 5365
    this.lines.push(line);                                                                                             // 5366
  };                                                                                                                   // 5367
  TextMarker.prototype.detachLine = function(line) {                                                                   // 5368
    this.lines.splice(indexOf(this.lines, line), 1);                                                                   // 5369
    if (!this.lines.length && this.doc.cm) {                                                                           // 5370
      var op = this.doc.cm.curOp;                                                                                      // 5371
      (op.maybeHiddenMarkers || (op.maybeHiddenMarkers = [])).push(this);                                              // 5372
    }                                                                                                                  // 5373
  };                                                                                                                   // 5374
                                                                                                                       // 5375
  // Collapsed markers have unique ids, in order to be able to order                                                   // 5376
  // them, which is needed for uniquely determining an outer marker                                                    // 5377
  // when they overlap (they may nest, but not partially overlap).                                                     // 5378
  var nextMarkerId = 0;                                                                                                // 5379
                                                                                                                       // 5380
  // Create a marker, wire it up to the right lines, and                                                               // 5381
  function markText(doc, from, to, options, type) {                                                                    // 5382
    // Shared markers (across linked documents) are handled separately                                                 // 5383
    // (markTextShared will call out to this again, once per                                                           // 5384
    // document).                                                                                                      // 5385
    if (options && options.shared) return markTextShared(doc, from, to, options, type);                                // 5386
    // Ensure we are in an operation.                                                                                  // 5387
    if (doc.cm && !doc.cm.curOp) return operation(doc.cm, markText)(doc, from, to, options, type);                     // 5388
                                                                                                                       // 5389
    var marker = new TextMarker(doc, type), diff = cmp(from, to);                                                      // 5390
    if (options) copyObj(options, marker, false);                                                                      // 5391
    // Don't connect empty markers unless clearWhenEmpty is false                                                      // 5392
    if (diff > 0 || diff == 0 && marker.clearWhenEmpty !== false)                                                      // 5393
      return marker;                                                                                                   // 5394
    if (marker.replacedWith) {                                                                                         // 5395
      // Showing up as a widget implies collapsed (widget replaces text)                                               // 5396
      marker.collapsed = true;                                                                                         // 5397
      marker.widgetNode = elt("span", [marker.replacedWith], "CodeMirror-widget");                                     // 5398
      if (!options.handleMouseEvents) marker.widgetNode.setAttribute("cm-ignore-events", "true");                      // 5399
      if (options.insertLeft) marker.widgetNode.insertLeft = true;                                                     // 5400
    }                                                                                                                  // 5401
    if (marker.collapsed) {                                                                                            // 5402
      if (conflictingCollapsedRange(doc, from.line, from, to, marker) ||                                               // 5403
          from.line != to.line && conflictingCollapsedRange(doc, to.line, from, to, marker))                           // 5404
        throw new Error("Inserting collapsed marker partially overlapping an existing one");                           // 5405
      sawCollapsedSpans = true;                                                                                        // 5406
    }                                                                                                                  // 5407
                                                                                                                       // 5408
    if (marker.addToHistory)                                                                                           // 5409
      addChangeToHistory(doc, {from: from, to: to, origin: "markText"}, doc.sel, NaN);                                 // 5410
                                                                                                                       // 5411
    var curLine = from.line, cm = doc.cm, updateMaxLine;                                                               // 5412
    doc.iter(curLine, to.line + 1, function(line) {                                                                    // 5413
      if (cm && marker.collapsed && !cm.options.lineWrapping && visualLine(line) == cm.display.maxLine)                // 5414
        updateMaxLine = true;                                                                                          // 5415
      if (marker.collapsed && curLine != from.line) updateLineHeight(line, 0);                                         // 5416
      addMarkedSpan(line, new MarkedSpan(marker,                                                                       // 5417
                                         curLine == from.line ? from.ch : null,                                        // 5418
                                         curLine == to.line ? to.ch : null));                                          // 5419
      ++curLine;                                                                                                       // 5420
    });                                                                                                                // 5421
    // lineIsHidden depends on the presence of the spans, so needs a second pass                                       // 5422
    if (marker.collapsed) doc.iter(from.line, to.line + 1, function(line) {                                            // 5423
      if (lineIsHidden(doc, line)) updateLineHeight(line, 0);                                                          // 5424
    });                                                                                                                // 5425
                                                                                                                       // 5426
    if (marker.clearOnEnter) on(marker, "beforeCursorEnter", function() { marker.clear(); });                          // 5427
                                                                                                                       // 5428
    if (marker.readOnly) {                                                                                             // 5429
      sawReadOnlySpans = true;                                                                                         // 5430
      if (doc.history.done.length || doc.history.undone.length)                                                        // 5431
        doc.clearHistory();                                                                                            // 5432
    }                                                                                                                  // 5433
    if (marker.collapsed) {                                                                                            // 5434
      marker.id = ++nextMarkerId;                                                                                      // 5435
      marker.atomic = true;                                                                                            // 5436
    }                                                                                                                  // 5437
    if (cm) {                                                                                                          // 5438
      // Sync editor state                                                                                             // 5439
      if (updateMaxLine) cm.curOp.updateMaxLine = true;                                                                // 5440
      if (marker.collapsed)                                                                                            // 5441
        regChange(cm, from.line, to.line + 1);                                                                         // 5442
      else if (marker.className || marker.title || marker.startStyle || marker.endStyle || marker.css)                 // 5443
        for (var i = from.line; i <= to.line; i++) regLineChange(cm, i, "text");                                       // 5444
      if (marker.atomic) reCheckSelection(cm.doc);                                                                     // 5445
      signalLater(cm, "markerAdded", cm, marker);                                                                      // 5446
    }                                                                                                                  // 5447
    return marker;                                                                                                     // 5448
  }                                                                                                                    // 5449
                                                                                                                       // 5450
  // SHARED TEXTMARKERS                                                                                                // 5451
                                                                                                                       // 5452
  // A shared marker spans multiple linked documents. It is                                                            // 5453
  // implemented as a meta-marker-object controlling multiple normal                                                   // 5454
  // markers.                                                                                                          // 5455
  var SharedTextMarker = CodeMirror.SharedTextMarker = function(markers, primary) {                                    // 5456
    this.markers = markers;                                                                                            // 5457
    this.primary = primary;                                                                                            // 5458
    for (var i = 0; i < markers.length; ++i)                                                                           // 5459
      markers[i].parent = this;                                                                                        // 5460
  };                                                                                                                   // 5461
  eventMixin(SharedTextMarker);                                                                                        // 5462
                                                                                                                       // 5463
  SharedTextMarker.prototype.clear = function() {                                                                      // 5464
    if (this.explicitlyCleared) return;                                                                                // 5465
    this.explicitlyCleared = true;                                                                                     // 5466
    for (var i = 0; i < this.markers.length; ++i)                                                                      // 5467
      this.markers[i].clear();                                                                                         // 5468
    signalLater(this, "clear");                                                                                        // 5469
  };                                                                                                                   // 5470
  SharedTextMarker.prototype.find = function(side, lineObj) {                                                          // 5471
    return this.primary.find(side, lineObj);                                                                           // 5472
  };                                                                                                                   // 5473
                                                                                                                       // 5474
  function markTextShared(doc, from, to, options, type) {                                                              // 5475
    options = copyObj(options);                                                                                        // 5476
    options.shared = false;                                                                                            // 5477
    var markers = [markText(doc, from, to, options, type)], primary = markers[0];                                      // 5478
    var widget = options.widgetNode;                                                                                   // 5479
    linkedDocs(doc, function(doc) {                                                                                    // 5480
      if (widget) options.widgetNode = widget.cloneNode(true);                                                         // 5481
      markers.push(markText(doc, clipPos(doc, from), clipPos(doc, to), options, type));                                // 5482
      for (var i = 0; i < doc.linked.length; ++i)                                                                      // 5483
        if (doc.linked[i].isParent) return;                                                                            // 5484
      primary = lst(markers);                                                                                          // 5485
    });                                                                                                                // 5486
    return new SharedTextMarker(markers, primary);                                                                     // 5487
  }                                                                                                                    // 5488
                                                                                                                       // 5489
  function findSharedMarkers(doc) {                                                                                    // 5490
    return doc.findMarks(Pos(doc.first, 0), doc.clipPos(Pos(doc.lastLine())),                                          // 5491
                         function(m) { return m.parent; });                                                            // 5492
  }                                                                                                                    // 5493
                                                                                                                       // 5494
  function copySharedMarkers(doc, markers) {                                                                           // 5495
    for (var i = 0; i < markers.length; i++) {                                                                         // 5496
      var marker = markers[i], pos = marker.find();                                                                    // 5497
      var mFrom = doc.clipPos(pos.from), mTo = doc.clipPos(pos.to);                                                    // 5498
      if (cmp(mFrom, mTo)) {                                                                                           // 5499
        var subMark = markText(doc, mFrom, mTo, marker.primary, marker.primary.type);                                  // 5500
        marker.markers.push(subMark);                                                                                  // 5501
        subMark.parent = marker;                                                                                       // 5502
      }                                                                                                                // 5503
    }                                                                                                                  // 5504
  }                                                                                                                    // 5505
                                                                                                                       // 5506
  function detachSharedMarkers(markers) {                                                                              // 5507
    for (var i = 0; i < markers.length; i++) {                                                                         // 5508
      var marker = markers[i], linked = [marker.primary.doc];;                                                         // 5509
      linkedDocs(marker.primary.doc, function(d) { linked.push(d); });                                                 // 5510
      for (var j = 0; j < marker.markers.length; j++) {                                                                // 5511
        var subMarker = marker.markers[j];                                                                             // 5512
        if (indexOf(linked, subMarker.doc) == -1) {                                                                    // 5513
          subMarker.parent = null;                                                                                     // 5514
          marker.markers.splice(j--, 1);                                                                               // 5515
        }                                                                                                              // 5516
      }                                                                                                                // 5517
    }                                                                                                                  // 5518
  }                                                                                                                    // 5519
                                                                                                                       // 5520
  // TEXTMARKER SPANS                                                                                                  // 5521
                                                                                                                       // 5522
  function MarkedSpan(marker, from, to) {                                                                              // 5523
    this.marker = marker;                                                                                              // 5524
    this.from = from; this.to = to;                                                                                    // 5525
  }                                                                                                                    // 5526
                                                                                                                       // 5527
  // Search an array of spans for a span matching the given marker.                                                    // 5528
  function getMarkedSpanFor(spans, marker) {                                                                           // 5529
    if (spans) for (var i = 0; i < spans.length; ++i) {                                                                // 5530
      var span = spans[i];                                                                                             // 5531
      if (span.marker == marker) return span;                                                                          // 5532
    }                                                                                                                  // 5533
  }                                                                                                                    // 5534
  // Remove a span from an array, returning undefined if no spans are                                                  // 5535
  // left (we don't store arrays for lines without spans).                                                             // 5536
  function removeMarkedSpan(spans, span) {                                                                             // 5537
    for (var r, i = 0; i < spans.length; ++i)                                                                          // 5538
      if (spans[i] != span) (r || (r = [])).push(spans[i]);                                                            // 5539
    return r;                                                                                                          // 5540
  }                                                                                                                    // 5541
  // Add a span to a line.                                                                                             // 5542
  function addMarkedSpan(line, span) {                                                                                 // 5543
    line.markedSpans = line.markedSpans ? line.markedSpans.concat([span]) : [span];                                    // 5544
    span.marker.attachLine(line);                                                                                      // 5545
  }                                                                                                                    // 5546
                                                                                                                       // 5547
  // Used for the algorithm that adjusts markers for a change in the                                                   // 5548
  // document. These functions cut an array of spans at a given                                                        // 5549
  // character position, returning an array of remaining chunks (or                                                    // 5550
  // undefined if nothing remains).                                                                                    // 5551
  function markedSpansBefore(old, startCh, isInsert) {                                                                 // 5552
    if (old) for (var i = 0, nw; i < old.length; ++i) {                                                                // 5553
      var span = old[i], marker = span.marker;                                                                         // 5554
      var startsBefore = span.from == null || (marker.inclusiveLeft ? span.from <= startCh : span.from < startCh);     // 5555
      if (startsBefore || span.from == startCh && marker.type == "bookmark" && (!isInsert || !span.marker.insertLeft)) {
        var endsAfter = span.to == null || (marker.inclusiveRight ? span.to >= startCh : span.to > startCh);           // 5557
        (nw || (nw = [])).push(new MarkedSpan(marker, span.from, endsAfter ? null : span.to));                         // 5558
      }                                                                                                                // 5559
    }                                                                                                                  // 5560
    return nw;                                                                                                         // 5561
  }                                                                                                                    // 5562
  function markedSpansAfter(old, endCh, isInsert) {                                                                    // 5563
    if (old) for (var i = 0, nw; i < old.length; ++i) {                                                                // 5564
      var span = old[i], marker = span.marker;                                                                         // 5565
      var endsAfter = span.to == null || (marker.inclusiveRight ? span.to >= endCh : span.to > endCh);                 // 5566
      if (endsAfter || span.from == endCh && marker.type == "bookmark" && (!isInsert || span.marker.insertLeft)) {     // 5567
        var startsBefore = span.from == null || (marker.inclusiveLeft ? span.from <= endCh : span.from < endCh);       // 5568
        (nw || (nw = [])).push(new MarkedSpan(marker, startsBefore ? null : span.from - endCh,                         // 5569
                                              span.to == null ? null : span.to - endCh));                              // 5570
      }                                                                                                                // 5571
    }                                                                                                                  // 5572
    return nw;                                                                                                         // 5573
  }                                                                                                                    // 5574
                                                                                                                       // 5575
  // Given a change object, compute the new set of marker spans that                                                   // 5576
  // cover the line in which the change took place. Removes spans                                                      // 5577
  // entirely within the change, reconnects spans belonging to the                                                     // 5578
  // same marker that appear on both sides of the change, and cuts off                                                 // 5579
  // spans partially within the change. Returns an array of span                                                       // 5580
  // arrays with one element for each line in (after) the change.                                                      // 5581
  function stretchSpansOverChange(doc, change) {                                                                       // 5582
    if (change.full) return null;                                                                                      // 5583
    var oldFirst = isLine(doc, change.from.line) && getLine(doc, change.from.line).markedSpans;                        // 5584
    var oldLast = isLine(doc, change.to.line) && getLine(doc, change.to.line).markedSpans;                             // 5585
    if (!oldFirst && !oldLast) return null;                                                                            // 5586
                                                                                                                       // 5587
    var startCh = change.from.ch, endCh = change.to.ch, isInsert = cmp(change.from, change.to) == 0;                   // 5588
    // Get the spans that 'stick out' on both sides                                                                    // 5589
    var first = markedSpansBefore(oldFirst, startCh, isInsert);                                                        // 5590
    var last = markedSpansAfter(oldLast, endCh, isInsert);                                                             // 5591
                                                                                                                       // 5592
    // Next, merge those two ends                                                                                      // 5593
    var sameLine = change.text.length == 1, offset = lst(change.text).length + (sameLine ? startCh : 0);               // 5594
    if (first) {                                                                                                       // 5595
      // Fix up .to properties of first                                                                                // 5596
      for (var i = 0; i < first.length; ++i) {                                                                         // 5597
        var span = first[i];                                                                                           // 5598
        if (span.to == null) {                                                                                         // 5599
          var found = getMarkedSpanFor(last, span.marker);                                                             // 5600
          if (!found) span.to = startCh;                                                                               // 5601
          else if (sameLine) span.to = found.to == null ? null : found.to + offset;                                    // 5602
        }                                                                                                              // 5603
      }                                                                                                                // 5604
    }                                                                                                                  // 5605
    if (last) {                                                                                                        // 5606
      // Fix up .from in last (or move them into first in case of sameLine)                                            // 5607
      for (var i = 0; i < last.length; ++i) {                                                                          // 5608
        var span = last[i];                                                                                            // 5609
        if (span.to != null) span.to += offset;                                                                        // 5610
        if (span.from == null) {                                                                                       // 5611
          var found = getMarkedSpanFor(first, span.marker);                                                            // 5612
          if (!found) {                                                                                                // 5613
            span.from = offset;                                                                                        // 5614
            if (sameLine) (first || (first = [])).push(span);                                                          // 5615
          }                                                                                                            // 5616
        } else {                                                                                                       // 5617
          span.from += offset;                                                                                         // 5618
          if (sameLine) (first || (first = [])).push(span);                                                            // 5619
        }                                                                                                              // 5620
      }                                                                                                                // 5621
    }                                                                                                                  // 5622
    // Make sure we didn't create any zero-length spans                                                                // 5623
    if (first) first = clearEmptySpans(first);                                                                         // 5624
    if (last && last != first) last = clearEmptySpans(last);                                                           // 5625
                                                                                                                       // 5626
    var newMarkers = [first];                                                                                          // 5627
    if (!sameLine) {                                                                                                   // 5628
      // Fill gap with whole-line-spans                                                                                // 5629
      var gap = change.text.length - 2, gapMarkers;                                                                    // 5630
      if (gap > 0 && first)                                                                                            // 5631
        for (var i = 0; i < first.length; ++i)                                                                         // 5632
          if (first[i].to == null)                                                                                     // 5633
            (gapMarkers || (gapMarkers = [])).push(new MarkedSpan(first[i].marker, null, null));                       // 5634
      for (var i = 0; i < gap; ++i)                                                                                    // 5635
        newMarkers.push(gapMarkers);                                                                                   // 5636
      newMarkers.push(last);                                                                                           // 5637
    }                                                                                                                  // 5638
    return newMarkers;                                                                                                 // 5639
  }                                                                                                                    // 5640
                                                                                                                       // 5641
  // Remove spans that are empty and don't have a clearWhenEmpty                                                       // 5642
  // option of false.                                                                                                  // 5643
  function clearEmptySpans(spans) {                                                                                    // 5644
    for (var i = 0; i < spans.length; ++i) {                                                                           // 5645
      var span = spans[i];                                                                                             // 5646
      if (span.from != null && span.from == span.to && span.marker.clearWhenEmpty !== false)                           // 5647
        spans.splice(i--, 1);                                                                                          // 5648
    }                                                                                                                  // 5649
    if (!spans.length) return null;                                                                                    // 5650
    return spans;                                                                                                      // 5651
  }                                                                                                                    // 5652
                                                                                                                       // 5653
  // Used for un/re-doing changes from the history. Combines the                                                       // 5654
  // result of computing the existing spans with the set of spans that                                                 // 5655
  // existed in the history (so that deleting around a span and then                                                   // 5656
  // undoing brings back the span).                                                                                    // 5657
  function mergeOldSpans(doc, change) {                                                                                // 5658
    var old = getOldSpans(doc, change);                                                                                // 5659
    var stretched = stretchSpansOverChange(doc, change);                                                               // 5660
    if (!old) return stretched;                                                                                        // 5661
    if (!stretched) return old;                                                                                        // 5662
                                                                                                                       // 5663
    for (var i = 0; i < old.length; ++i) {                                                                             // 5664
      var oldCur = old[i], stretchCur = stretched[i];                                                                  // 5665
      if (oldCur && stretchCur) {                                                                                      // 5666
        spans: for (var j = 0; j < stretchCur.length; ++j) {                                                           // 5667
          var span = stretchCur[j];                                                                                    // 5668
          for (var k = 0; k < oldCur.length; ++k)                                                                      // 5669
            if (oldCur[k].marker == span.marker) continue spans;                                                       // 5670
          oldCur.push(span);                                                                                           // 5671
        }                                                                                                              // 5672
      } else if (stretchCur) {                                                                                         // 5673
        old[i] = stretchCur;                                                                                           // 5674
      }                                                                                                                // 5675
    }                                                                                                                  // 5676
    return old;                                                                                                        // 5677
  }                                                                                                                    // 5678
                                                                                                                       // 5679
  // Used to 'clip' out readOnly ranges when making a change.                                                          // 5680
  function removeReadOnlyRanges(doc, from, to) {                                                                       // 5681
    var markers = null;                                                                                                // 5682
    doc.iter(from.line, to.line + 1, function(line) {                                                                  // 5683
      if (line.markedSpans) for (var i = 0; i < line.markedSpans.length; ++i) {                                        // 5684
        var mark = line.markedSpans[i].marker;                                                                         // 5685
        if (mark.readOnly && (!markers || indexOf(markers, mark) == -1))                                               // 5686
          (markers || (markers = [])).push(mark);                                                                      // 5687
      }                                                                                                                // 5688
    });                                                                                                                // 5689
    if (!markers) return null;                                                                                         // 5690
    var parts = [{from: from, to: to}];                                                                                // 5691
    for (var i = 0; i < markers.length; ++i) {                                                                         // 5692
      var mk = markers[i], m = mk.find(0);                                                                             // 5693
      for (var j = 0; j < parts.length; ++j) {                                                                         // 5694
        var p = parts[j];                                                                                              // 5695
        if (cmp(p.to, m.from) < 0 || cmp(p.from, m.to) > 0) continue;                                                  // 5696
        var newParts = [j, 1], dfrom = cmp(p.from, m.from), dto = cmp(p.to, m.to);                                     // 5697
        if (dfrom < 0 || !mk.inclusiveLeft && !dfrom)                                                                  // 5698
          newParts.push({from: p.from, to: m.from});                                                                   // 5699
        if (dto > 0 || !mk.inclusiveRight && !dto)                                                                     // 5700
          newParts.push({from: m.to, to: p.to});                                                                       // 5701
        parts.splice.apply(parts, newParts);                                                                           // 5702
        j += newParts.length - 1;                                                                                      // 5703
      }                                                                                                                // 5704
    }                                                                                                                  // 5705
    return parts;                                                                                                      // 5706
  }                                                                                                                    // 5707
                                                                                                                       // 5708
  // Connect or disconnect spans from a line.                                                                          // 5709
  function detachMarkedSpans(line) {                                                                                   // 5710
    var spans = line.markedSpans;                                                                                      // 5711
    if (!spans) return;                                                                                                // 5712
    for (var i = 0; i < spans.length; ++i)                                                                             // 5713
      spans[i].marker.detachLine(line);                                                                                // 5714
    line.markedSpans = null;                                                                                           // 5715
  }                                                                                                                    // 5716
  function attachMarkedSpans(line, spans) {                                                                            // 5717
    if (!spans) return;                                                                                                // 5718
    for (var i = 0; i < spans.length; ++i)                                                                             // 5719
      spans[i].marker.attachLine(line);                                                                                // 5720
    line.markedSpans = spans;                                                                                          // 5721
  }                                                                                                                    // 5722
                                                                                                                       // 5723
  // Helpers used when computing which overlapping collapsed span                                                      // 5724
  // counts as the larger one.                                                                                         // 5725
  function extraLeft(marker) { return marker.inclusiveLeft ? -1 : 0; }                                                 // 5726
  function extraRight(marker) { return marker.inclusiveRight ? 1 : 0; }                                                // 5727
                                                                                                                       // 5728
  // Returns a number indicating which of two overlapping collapsed                                                    // 5729
  // spans is larger (and thus includes the other). Falls back to                                                      // 5730
  // comparing ids when the spans cover exactly the same range.                                                        // 5731
  function compareCollapsedMarkers(a, b) {                                                                             // 5732
    var lenDiff = a.lines.length - b.lines.length;                                                                     // 5733
    if (lenDiff != 0) return lenDiff;                                                                                  // 5734
    var aPos = a.find(), bPos = b.find();                                                                              // 5735
    var fromCmp = cmp(aPos.from, bPos.from) || extraLeft(a) - extraLeft(b);                                            // 5736
    if (fromCmp) return -fromCmp;                                                                                      // 5737
    var toCmp = cmp(aPos.to, bPos.to) || extraRight(a) - extraRight(b);                                                // 5738
    if (toCmp) return toCmp;                                                                                           // 5739
    return b.id - a.id;                                                                                                // 5740
  }                                                                                                                    // 5741
                                                                                                                       // 5742
  // Find out whether a line ends or starts in a collapsed span. If                                                    // 5743
  // so, return the marker for that span.                                                                              // 5744
  function collapsedSpanAtSide(line, start) {                                                                          // 5745
    var sps = sawCollapsedSpans && line.markedSpans, found;                                                            // 5746
    if (sps) for (var sp, i = 0; i < sps.length; ++i) {                                                                // 5747
      sp = sps[i];                                                                                                     // 5748
      if (sp.marker.collapsed && (start ? sp.from : sp.to) == null &&                                                  // 5749
          (!found || compareCollapsedMarkers(found, sp.marker) < 0))                                                   // 5750
        found = sp.marker;                                                                                             // 5751
    }                                                                                                                  // 5752
    return found;                                                                                                      // 5753
  }                                                                                                                    // 5754
  function collapsedSpanAtStart(line) { return collapsedSpanAtSide(line, true); }                                      // 5755
  function collapsedSpanAtEnd(line) { return collapsedSpanAtSide(line, false); }                                       // 5756
                                                                                                                       // 5757
  // Test whether there exists a collapsed span that partially                                                         // 5758
  // overlaps (covers the start or end, but not both) of a new span.                                                   // 5759
  // Such overlap is not allowed.                                                                                      // 5760
  function conflictingCollapsedRange(doc, lineNo, from, to, marker) {                                                  // 5761
    var line = getLine(doc, lineNo);                                                                                   // 5762
    var sps = sawCollapsedSpans && line.markedSpans;                                                                   // 5763
    if (sps) for (var i = 0; i < sps.length; ++i) {                                                                    // 5764
      var sp = sps[i];                                                                                                 // 5765
      if (!sp.marker.collapsed) continue;                                                                              // 5766
      var found = sp.marker.find(0);                                                                                   // 5767
      var fromCmp = cmp(found.from, from) || extraLeft(sp.marker) - extraLeft(marker);                                 // 5768
      var toCmp = cmp(found.to, to) || extraRight(sp.marker) - extraRight(marker);                                     // 5769
      if (fromCmp >= 0 && toCmp <= 0 || fromCmp <= 0 && toCmp >= 0) continue;                                          // 5770
      if (fromCmp <= 0 && (cmp(found.to, from) > 0 || (sp.marker.inclusiveRight && marker.inclusiveLeft)) ||           // 5771
          fromCmp >= 0 && (cmp(found.from, to) < 0 || (sp.marker.inclusiveLeft && marker.inclusiveRight)))             // 5772
        return true;                                                                                                   // 5773
    }                                                                                                                  // 5774
  }                                                                                                                    // 5775
                                                                                                                       // 5776
  // A visual line is a line as drawn on the screen. Folding, for                                                      // 5777
  // example, can cause multiple logical lines to appear on the same                                                   // 5778
  // visual line. This finds the start of the visual line that the                                                     // 5779
  // given line is part of (usually that is the line itself).                                                          // 5780
  function visualLine(line) {                                                                                          // 5781
    var merged;                                                                                                        // 5782
    while (merged = collapsedSpanAtStart(line))                                                                        // 5783
      line = merged.find(-1, true).line;                                                                               // 5784
    return line;                                                                                                       // 5785
  }                                                                                                                    // 5786
                                                                                                                       // 5787
  // Returns an array of logical lines that continue the visual line                                                   // 5788
  // started by the argument, or undefined if there are no such lines.                                                 // 5789
  function visualLineContinued(line) {                                                                                 // 5790
    var merged, lines;                                                                                                 // 5791
    while (merged = collapsedSpanAtEnd(line)) {                                                                        // 5792
      line = merged.find(1, true).line;                                                                                // 5793
      (lines || (lines = [])).push(line);                                                                              // 5794
    }                                                                                                                  // 5795
    return lines;                                                                                                      // 5796
  }                                                                                                                    // 5797
                                                                                                                       // 5798
  // Get the line number of the start of the visual line that the                                                      // 5799
  // given line number is part of.                                                                                     // 5800
  function visualLineNo(doc, lineN) {                                                                                  // 5801
    var line = getLine(doc, lineN), vis = visualLine(line);                                                            // 5802
    if (line == vis) return lineN;                                                                                     // 5803
    return lineNo(vis);                                                                                                // 5804
  }                                                                                                                    // 5805
  // Get the line number of the start of the next visual line after                                                    // 5806
  // the given line.                                                                                                   // 5807
  function visualLineEndNo(doc, lineN) {                                                                               // 5808
    if (lineN > doc.lastLine()) return lineN;                                                                          // 5809
    var line = getLine(doc, lineN), merged;                                                                            // 5810
    if (!lineIsHidden(doc, line)) return lineN;                                                                        // 5811
    while (merged = collapsedSpanAtEnd(line))                                                                          // 5812
      line = merged.find(1, true).line;                                                                                // 5813
    return lineNo(line) + 1;                                                                                           // 5814
  }                                                                                                                    // 5815
                                                                                                                       // 5816
  // Compute whether a line is hidden. Lines count as hidden when they                                                 // 5817
  // are part of a visual line that starts with another line, or when                                                  // 5818
  // they are entirely covered by collapsed, non-widget span.                                                          // 5819
  function lineIsHidden(doc, line) {                                                                                   // 5820
    var sps = sawCollapsedSpans && line.markedSpans;                                                                   // 5821
    if (sps) for (var sp, i = 0; i < sps.length; ++i) {                                                                // 5822
      sp = sps[i];                                                                                                     // 5823
      if (!sp.marker.collapsed) continue;                                                                              // 5824
      if (sp.from == null) return true;                                                                                // 5825
      if (sp.marker.widgetNode) continue;                                                                              // 5826
      if (sp.from == 0 && sp.marker.inclusiveLeft && lineIsHiddenInner(doc, line, sp))                                 // 5827
        return true;                                                                                                   // 5828
    }                                                                                                                  // 5829
  }                                                                                                                    // 5830
  function lineIsHiddenInner(doc, line, span) {                                                                        // 5831
    if (span.to == null) {                                                                                             // 5832
      var end = span.marker.find(1, true);                                                                             // 5833
      return lineIsHiddenInner(doc, end.line, getMarkedSpanFor(end.line.markedSpans, span.marker));                    // 5834
    }                                                                                                                  // 5835
    if (span.marker.inclusiveRight && span.to == line.text.length)                                                     // 5836
      return true;                                                                                                     // 5837
    for (var sp, i = 0; i < line.markedSpans.length; ++i) {                                                            // 5838
      sp = line.markedSpans[i];                                                                                        // 5839
      if (sp.marker.collapsed && !sp.marker.widgetNode && sp.from == span.to &&                                        // 5840
          (sp.to == null || sp.to != span.from) &&                                                                     // 5841
          (sp.marker.inclusiveLeft || span.marker.inclusiveRight) &&                                                   // 5842
          lineIsHiddenInner(doc, line, sp)) return true;                                                               // 5843
    }                                                                                                                  // 5844
  }                                                                                                                    // 5845
                                                                                                                       // 5846
  // LINE WIDGETS                                                                                                      // 5847
                                                                                                                       // 5848
  // Line widgets are block elements displayed above or below a line.                                                  // 5849
                                                                                                                       // 5850
  var LineWidget = CodeMirror.LineWidget = function(cm, node, options) {                                               // 5851
    if (options) for (var opt in options) if (options.hasOwnProperty(opt))                                             // 5852
      this[opt] = options[opt];                                                                                        // 5853
    this.cm = cm;                                                                                                      // 5854
    this.node = node;                                                                                                  // 5855
  };                                                                                                                   // 5856
  eventMixin(LineWidget);                                                                                              // 5857
                                                                                                                       // 5858
  function adjustScrollWhenAboveVisible(cm, line, diff) {                                                              // 5859
    if (heightAtLine(line) < ((cm.curOp && cm.curOp.scrollTop) || cm.doc.scrollTop))                                   // 5860
      addToScrollPos(cm, null, diff);                                                                                  // 5861
  }                                                                                                                    // 5862
                                                                                                                       // 5863
  LineWidget.prototype.clear = function() {                                                                            // 5864
    var cm = this.cm, ws = this.line.widgets, line = this.line, no = lineNo(line);                                     // 5865
    if (no == null || !ws) return;                                                                                     // 5866
    for (var i = 0; i < ws.length; ++i) if (ws[i] == this) ws.splice(i--, 1);                                          // 5867
    if (!ws.length) line.widgets = null;                                                                               // 5868
    var height = widgetHeight(this);                                                                                   // 5869
    runInOp(cm, function() {                                                                                           // 5870
      adjustScrollWhenAboveVisible(cm, line, -height);                                                                 // 5871
      regLineChange(cm, no, "widget");                                                                                 // 5872
      updateLineHeight(line, Math.max(0, line.height - height));                                                       // 5873
    });                                                                                                                // 5874
  };                                                                                                                   // 5875
  LineWidget.prototype.changed = function() {                                                                          // 5876
    var oldH = this.height, cm = this.cm, line = this.line;                                                            // 5877
    this.height = null;                                                                                                // 5878
    var diff = widgetHeight(this) - oldH;                                                                              // 5879
    if (!diff) return;                                                                                                 // 5880
    runInOp(cm, function() {                                                                                           // 5881
      cm.curOp.forceUpdate = true;                                                                                     // 5882
      adjustScrollWhenAboveVisible(cm, line, diff);                                                                    // 5883
      updateLineHeight(line, line.height + diff);                                                                      // 5884
    });                                                                                                                // 5885
  };                                                                                                                   // 5886
                                                                                                                       // 5887
  function widgetHeight(widget) {                                                                                      // 5888
    if (widget.height != null) return widget.height;                                                                   // 5889
    if (!contains(document.body, widget.node)) {                                                                       // 5890
      var parentStyle = "position: relative;";                                                                         // 5891
      if (widget.coverGutter)                                                                                          // 5892
        parentStyle += "margin-left: -" + widget.cm.display.gutters.offsetWidth + "px;";                               // 5893
      if (widget.noHScroll)                                                                                            // 5894
        parentStyle += "width: " + widget.cm.display.wrapper.clientWidth + "px;";                                      // 5895
      removeChildrenAndAdd(widget.cm.display.measure, elt("div", [widget.node], null, parentStyle));                   // 5896
    }                                                                                                                  // 5897
    return widget.height = widget.node.offsetHeight;                                                                   // 5898
  }                                                                                                                    // 5899
                                                                                                                       // 5900
  function addLineWidget(cm, handle, node, options) {                                                                  // 5901
    var widget = new LineWidget(cm, node, options);                                                                    // 5902
    if (widget.noHScroll) cm.display.alignWidgets = true;                                                              // 5903
    changeLine(cm.doc, handle, "widget", function(line) {                                                              // 5904
      var widgets = line.widgets || (line.widgets = []);                                                               // 5905
      if (widget.insertAt == null) widgets.push(widget);                                                               // 5906
      else widgets.splice(Math.min(widgets.length - 1, Math.max(0, widget.insertAt)), 0, widget);                      // 5907
      widget.line = line;                                                                                              // 5908
      if (!lineIsHidden(cm.doc, line)) {                                                                               // 5909
        var aboveVisible = heightAtLine(line) < cm.doc.scrollTop;                                                      // 5910
        updateLineHeight(line, line.height + widgetHeight(widget));                                                    // 5911
        if (aboveVisible) addToScrollPos(cm, null, widget.height);                                                     // 5912
        cm.curOp.forceUpdate = true;                                                                                   // 5913
      }                                                                                                                // 5914
      return true;                                                                                                     // 5915
    });                                                                                                                // 5916
    return widget;                                                                                                     // 5917
  }                                                                                                                    // 5918
                                                                                                                       // 5919
  // LINE DATA STRUCTURE                                                                                               // 5920
                                                                                                                       // 5921
  // Line objects. These hold state related to a line, including                                                       // 5922
  // highlighting info (the styles array).                                                                             // 5923
  var Line = CodeMirror.Line = function(text, markedSpans, estimateHeight) {                                           // 5924
    this.text = text;                                                                                                  // 5925
    attachMarkedSpans(this, markedSpans);                                                                              // 5926
    this.height = estimateHeight ? estimateHeight(this) : 1;                                                           // 5927
  };                                                                                                                   // 5928
  eventMixin(Line);                                                                                                    // 5929
  Line.prototype.lineNo = function() { return lineNo(this); };                                                         // 5930
                                                                                                                       // 5931
  // Change the content (text, markers) of a line. Automatically                                                       // 5932
  // invalidates cached information and tries to re-estimate the                                                       // 5933
  // line's height.                                                                                                    // 5934
  function updateLine(line, text, markedSpans, estimateHeight) {                                                       // 5935
    line.text = text;                                                                                                  // 5936
    if (line.stateAfter) line.stateAfter = null;                                                                       // 5937
    if (line.styles) line.styles = null;                                                                               // 5938
    if (line.order != null) line.order = null;                                                                         // 5939
    detachMarkedSpans(line);                                                                                           // 5940
    attachMarkedSpans(line, markedSpans);                                                                              // 5941
    var estHeight = estimateHeight ? estimateHeight(line) : 1;                                                         // 5942
    if (estHeight != line.height) updateLineHeight(line, estHeight);                                                   // 5943
  }                                                                                                                    // 5944
                                                                                                                       // 5945
  // Detach a line from the document tree and its markers.                                                             // 5946
  function cleanUpLine(line) {                                                                                         // 5947
    line.parent = null;                                                                                                // 5948
    detachMarkedSpans(line);                                                                                           // 5949
  }                                                                                                                    // 5950
                                                                                                                       // 5951
  function extractLineClasses(type, output) {                                                                          // 5952
    if (type) for (;;) {                                                                                               // 5953
      var lineClass = type.match(/(?:^|\s+)line-(background-)?(\S+)/);                                                 // 5954
      if (!lineClass) break;                                                                                           // 5955
      type = type.slice(0, lineClass.index) + type.slice(lineClass.index + lineClass[0].length);                       // 5956
      var prop = lineClass[1] ? "bgClass" : "textClass";                                                               // 5957
      if (output[prop] == null)                                                                                        // 5958
        output[prop] = lineClass[2];                                                                                   // 5959
      else if (!(new RegExp("(?:^|\s)" + lineClass[2] + "(?:$|\s)")).test(output[prop]))                               // 5960
        output[prop] += " " + lineClass[2];                                                                            // 5961
    }                                                                                                                  // 5962
    return type;                                                                                                       // 5963
  }                                                                                                                    // 5964
                                                                                                                       // 5965
  function callBlankLine(mode, state) {                                                                                // 5966
    if (mode.blankLine) return mode.blankLine(state);                                                                  // 5967
    if (!mode.innerMode) return;                                                                                       // 5968
    var inner = CodeMirror.innerMode(mode, state);                                                                     // 5969
    if (inner.mode.blankLine) return inner.mode.blankLine(inner.state);                                                // 5970
  }                                                                                                                    // 5971
                                                                                                                       // 5972
  function readToken(mode, stream, state, inner) {                                                                     // 5973
    for (var i = 0; i < 10; i++) {                                                                                     // 5974
      if (inner) inner[0] = CodeMirror.innerMode(mode, state).mode;                                                    // 5975
      var style = mode.token(stream, state);                                                                           // 5976
      if (stream.pos > stream.start) return style;                                                                     // 5977
    }                                                                                                                  // 5978
    throw new Error("Mode " + mode.name + " failed to advance stream.");                                               // 5979
  }                                                                                                                    // 5980
                                                                                                                       // 5981
  // Utility for getTokenAt and getLineTokens                                                                          // 5982
  function takeToken(cm, pos, precise, asArray) {                                                                      // 5983
    function getObj(copy) {                                                                                            // 5984
      return {start: stream.start, end: stream.pos,                                                                    // 5985
              string: stream.current(),                                                                                // 5986
              type: style || null,                                                                                     // 5987
              state: copy ? copyState(doc.mode, state) : state};                                                       // 5988
    }                                                                                                                  // 5989
                                                                                                                       // 5990
    var doc = cm.doc, mode = doc.mode, style;                                                                          // 5991
    pos = clipPos(doc, pos);                                                                                           // 5992
    var line = getLine(doc, pos.line), state = getStateBefore(cm, pos.line, precise);                                  // 5993
    var stream = new StringStream(line.text, cm.options.tabSize), tokens;                                              // 5994
    if (asArray) tokens = [];                                                                                          // 5995
    while ((asArray || stream.pos < pos.ch) && !stream.eol()) {                                                        // 5996
      stream.start = stream.pos;                                                                                       // 5997
      style = readToken(mode, stream, state);                                                                          // 5998
      if (asArray) tokens.push(getObj(true));                                                                          // 5999
    }                                                                                                                  // 6000
    return asArray ? tokens : getObj();                                                                                // 6001
  }                                                                                                                    // 6002
                                                                                                                       // 6003
  // Run the given mode's parser over a line, calling f for each token.                                                // 6004
  function runMode(cm, text, mode, state, f, lineClasses, forceToEnd) {                                                // 6005
    var flattenSpans = mode.flattenSpans;                                                                              // 6006
    if (flattenSpans == null) flattenSpans = cm.options.flattenSpans;                                                  // 6007
    var curStart = 0, curStyle = null;                                                                                 // 6008
    var stream = new StringStream(text, cm.options.tabSize), style;                                                    // 6009
    var inner = cm.options.addModeClass && [null];                                                                     // 6010
    if (text == "") extractLineClasses(callBlankLine(mode, state), lineClasses);                                       // 6011
    while (!stream.eol()) {                                                                                            // 6012
      if (stream.pos > cm.options.maxHighlightLength) {                                                                // 6013
        flattenSpans = false;                                                                                          // 6014
        if (forceToEnd) processLine(cm, text, state, stream.pos);                                                      // 6015
        stream.pos = text.length;                                                                                      // 6016
        style = null;                                                                                                  // 6017
      } else {                                                                                                         // 6018
        style = extractLineClasses(readToken(mode, stream, state, inner), lineClasses);                                // 6019
      }                                                                                                                // 6020
      if (inner) {                                                                                                     // 6021
        var mName = inner[0].name;                                                                                     // 6022
        if (mName) style = "m-" + (style ? mName + " " + style : mName);                                               // 6023
      }                                                                                                                // 6024
      if (!flattenSpans || curStyle != style) {                                                                        // 6025
        while (curStart < stream.start) {                                                                              // 6026
          curStart = Math.min(stream.start, curStart + 50000);                                                         // 6027
          f(curStart, curStyle);                                                                                       // 6028
        }                                                                                                              // 6029
        curStyle = style;                                                                                              // 6030
      }                                                                                                                // 6031
      stream.start = stream.pos;                                                                                       // 6032
    }                                                                                                                  // 6033
    while (curStart < stream.pos) {                                                                                    // 6034
      // Webkit seems to refuse to render text nodes longer than 57444 characters                                      // 6035
      var pos = Math.min(stream.pos, curStart + 50000);                                                                // 6036
      f(pos, curStyle);                                                                                                // 6037
      curStart = pos;                                                                                                  // 6038
    }                                                                                                                  // 6039
  }                                                                                                                    // 6040
                                                                                                                       // 6041
  // Compute a style array (an array starting with a mode generation                                                   // 6042
  // -- for invalidation -- followed by pairs of end positions and                                                     // 6043
  // style strings), which is used to highlight the tokens on the                                                      // 6044
  // line.                                                                                                             // 6045
  function highlightLine(cm, line, state, forceToEnd) {                                                                // 6046
    // A styles array always starts with a number identifying the                                                      // 6047
    // mode/overlays that it is based on (for easy invalidation).                                                      // 6048
    var st = [cm.state.modeGen], lineClasses = {};                                                                     // 6049
    // Compute the base array of styles                                                                                // 6050
    runMode(cm, line.text, cm.doc.mode, state, function(end, style) {                                                  // 6051
      st.push(end, style);                                                                                             // 6052
    }, lineClasses, forceToEnd);                                                                                       // 6053
                                                                                                                       // 6054
    // Run overlays, adjust style array.                                                                               // 6055
    for (var o = 0; o < cm.state.overlays.length; ++o) {                                                               // 6056
      var overlay = cm.state.overlays[o], i = 1, at = 0;                                                               // 6057
      runMode(cm, line.text, overlay.mode, true, function(end, style) {                                                // 6058
        var start = i;                                                                                                 // 6059
        // Ensure there's a token end at the current position, and that i points at it                                 // 6060
        while (at < end) {                                                                                             // 6061
          var i_end = st[i];                                                                                           // 6062
          if (i_end > end)                                                                                             // 6063
            st.splice(i, 1, end, st[i+1], i_end);                                                                      // 6064
          i += 2;                                                                                                      // 6065
          at = Math.min(end, i_end);                                                                                   // 6066
        }                                                                                                              // 6067
        if (!style) return;                                                                                            // 6068
        if (overlay.opaque) {                                                                                          // 6069
          st.splice(start, i - start, end, "cm-overlay " + style);                                                     // 6070
          i = start + 2;                                                                                               // 6071
        } else {                                                                                                       // 6072
          for (; start < i; start += 2) {                                                                              // 6073
            var cur = st[start+1];                                                                                     // 6074
            st[start+1] = (cur ? cur + " " : "") + "cm-overlay " + style;                                              // 6075
          }                                                                                                            // 6076
        }                                                                                                              // 6077
      }, lineClasses);                                                                                                 // 6078
    }                                                                                                                  // 6079
                                                                                                                       // 6080
    return {styles: st, classes: lineClasses.bgClass || lineClasses.textClass ? lineClasses : null};                   // 6081
  }                                                                                                                    // 6082
                                                                                                                       // 6083
  function getLineStyles(cm, line, updateFrontier) {                                                                   // 6084
    if (!line.styles || line.styles[0] != cm.state.modeGen) {                                                          // 6085
      var result = highlightLine(cm, line, line.stateAfter = getStateBefore(cm, lineNo(line)));                        // 6086
      line.styles = result.styles;                                                                                     // 6087
      if (result.classes) line.styleClasses = result.classes;                                                          // 6088
      else if (line.styleClasses) line.styleClasses = null;                                                            // 6089
      if (updateFrontier === cm.doc.frontier) cm.doc.frontier++;                                                       // 6090
    }                                                                                                                  // 6091
    return line.styles;                                                                                                // 6092
  }                                                                                                                    // 6093
                                                                                                                       // 6094
  // Lightweight form of highlight -- proceed over this line and                                                       // 6095
  // update state, but don't save a style array. Used for lines that                                                   // 6096
  // aren't currently visible.                                                                                         // 6097
  function processLine(cm, text, state, startAt) {                                                                     // 6098
    var mode = cm.doc.mode;                                                                                            // 6099
    var stream = new StringStream(text, cm.options.tabSize);                                                           // 6100
    stream.start = stream.pos = startAt || 0;                                                                          // 6101
    if (text == "") callBlankLine(mode, state);                                                                        // 6102
    while (!stream.eol() && stream.pos <= cm.options.maxHighlightLength) {                                             // 6103
      readToken(mode, stream, state);                                                                                  // 6104
      stream.start = stream.pos;                                                                                       // 6105
    }                                                                                                                  // 6106
  }                                                                                                                    // 6107
                                                                                                                       // 6108
  // Convert a style as returned by a mode (either null, or a string                                                   // 6109
  // containing one or more styles) to a CSS style. This is cached,                                                    // 6110
  // and also looks for line-wide styles.                                                                              // 6111
  var styleToClassCache = {}, styleToClassCacheWithMode = {};                                                          // 6112
  function interpretTokenStyle(style, options) {                                                                       // 6113
    if (!style || /^\s*$/.test(style)) return null;                                                                    // 6114
    var cache = options.addModeClass ? styleToClassCacheWithMode : styleToClassCache;                                  // 6115
    return cache[style] ||                                                                                             // 6116
      (cache[style] = style.replace(/\S+/g, "cm-$&"));                                                                 // 6117
  }                                                                                                                    // 6118
                                                                                                                       // 6119
  // Render the DOM representation of the text of a line. Also builds                                                  // 6120
  // up a 'line map', which points at the DOM nodes that represent                                                     // 6121
  // specific stretches of text, and is used by the measuring code.                                                    // 6122
  // The returned object contains the DOM node, this map, and                                                          // 6123
  // information about line-wide styles that were set by the mode.                                                     // 6124
  function buildLineContent(cm, lineView) {                                                                            // 6125
    // The padding-right forces the element to have a 'border', which                                                  // 6126
    // is needed on Webkit to be able to get line-level bounding                                                       // 6127
    // rectangles for it (in measureChar).                                                                             // 6128
    var content = elt("span", null, null, webkit ? "padding-right: .1px" : null);                                      // 6129
    var builder = {pre: elt("pre", [content]), content: content, col: 0, pos: 0, cm: cm};                              // 6130
    lineView.measure = {};                                                                                             // 6131
                                                                                                                       // 6132
    // Iterate over the logical lines that make up this visual line.                                                   // 6133
    for (var i = 0; i <= (lineView.rest ? lineView.rest.length : 0); i++) {                                            // 6134
      var line = i ? lineView.rest[i - 1] : lineView.line, order;                                                      // 6135
      builder.pos = 0;                                                                                                 // 6136
      builder.addToken = buildToken;                                                                                   // 6137
      // Optionally wire in some hacks into the token-rendering                                                        // 6138
      // algorithm, to deal with browser quirks.                                                                       // 6139
      if ((ie || webkit) && cm.getOption("lineWrapping"))                                                              // 6140
        builder.addToken = buildTokenSplitSpaces(builder.addToken);                                                    // 6141
      if (hasBadBidiRects(cm.display.measure) && (order = getOrder(line)))                                             // 6142
        builder.addToken = buildTokenBadBidi(builder.addToken, order);                                                 // 6143
      builder.map = [];                                                                                                // 6144
      var allowFrontierUpdate = lineView != cm.display.externalMeasured && lineNo(line);                               // 6145
      insertLineContent(line, builder, getLineStyles(cm, line, allowFrontierUpdate));                                  // 6146
      if (line.styleClasses) {                                                                                         // 6147
        if (line.styleClasses.bgClass)                                                                                 // 6148
          builder.bgClass = joinClasses(line.styleClasses.bgClass, builder.bgClass || "");                             // 6149
        if (line.styleClasses.textClass)                                                                               // 6150
          builder.textClass = joinClasses(line.styleClasses.textClass, builder.textClass || "");                       // 6151
      }                                                                                                                // 6152
                                                                                                                       // 6153
      // Ensure at least a single node is present, for measuring.                                                      // 6154
      if (builder.map.length == 0)                                                                                     // 6155
        builder.map.push(0, 0, builder.content.appendChild(zeroWidthElement(cm.display.measure)));                     // 6156
                                                                                                                       // 6157
      // Store the map and a cache object for the current logical line                                                 // 6158
      if (i == 0) {                                                                                                    // 6159
        lineView.measure.map = builder.map;                                                                            // 6160
        lineView.measure.cache = {};                                                                                   // 6161
      } else {                                                                                                         // 6162
        (lineView.measure.maps || (lineView.measure.maps = [])).push(builder.map);                                     // 6163
        (lineView.measure.caches || (lineView.measure.caches = [])).push({});                                          // 6164
      }                                                                                                                // 6165
    }                                                                                                                  // 6166
                                                                                                                       // 6167
    // See issue #2901                                                                                                 // 6168
    if (webkit && /\bcm-tab\b/.test(builder.content.lastChild.className))                                              // 6169
      builder.content.className = "cm-tab-wrap-hack";                                                                  // 6170
                                                                                                                       // 6171
    signal(cm, "renderLine", cm, lineView.line, builder.pre);                                                          // 6172
    if (builder.pre.className)                                                                                         // 6173
      builder.textClass = joinClasses(builder.pre.className, builder.textClass || "");                                 // 6174
                                                                                                                       // 6175
    return builder;                                                                                                    // 6176
  }                                                                                                                    // 6177
                                                                                                                       // 6178
  function defaultSpecialCharPlaceholder(ch) {                                                                         // 6179
    var token = elt("span", "\u2022", "cm-invalidchar");                                                               // 6180
    token.title = "\\u" + ch.charCodeAt(0).toString(16);                                                               // 6181
    return token;                                                                                                      // 6182
  }                                                                                                                    // 6183
                                                                                                                       // 6184
  // Build up the DOM representation for a single token, and add it to                                                 // 6185
  // the line map. Takes care to render special characters separately.                                                 // 6186
  function buildToken(builder, text, style, startStyle, endStyle, title, css) {                                        // 6187
    if (!text) return;                                                                                                 // 6188
    var special = builder.cm.options.specialChars, mustWrap = false;                                                   // 6189
    if (!special.test(text)) {                                                                                         // 6190
      builder.col += text.length;                                                                                      // 6191
      var content = document.createTextNode(text);                                                                     // 6192
      builder.map.push(builder.pos, builder.pos + text.length, content);                                               // 6193
      if (ie && ie_version < 9) mustWrap = true;                                                                       // 6194
      builder.pos += text.length;                                                                                      // 6195
    } else {                                                                                                           // 6196
      var content = document.createDocumentFragment(), pos = 0;                                                        // 6197
      while (true) {                                                                                                   // 6198
        special.lastIndex = pos;                                                                                       // 6199
        var m = special.exec(text);                                                                                    // 6200
        var skipped = m ? m.index - pos : text.length - pos;                                                           // 6201
        if (skipped) {                                                                                                 // 6202
          var txt = document.createTextNode(text.slice(pos, pos + skipped));                                           // 6203
          if (ie && ie_version < 9) content.appendChild(elt("span", [txt]));                                           // 6204
          else content.appendChild(txt);                                                                               // 6205
          builder.map.push(builder.pos, builder.pos + skipped, txt);                                                   // 6206
          builder.col += skipped;                                                                                      // 6207
          builder.pos += skipped;                                                                                      // 6208
        }                                                                                                              // 6209
        if (!m) break;                                                                                                 // 6210
        pos += skipped + 1;                                                                                            // 6211
        if (m[0] == "\t") {                                                                                            // 6212
          var tabSize = builder.cm.options.tabSize, tabWidth = tabSize - builder.col % tabSize;                        // 6213
          var txt = content.appendChild(elt("span", spaceStr(tabWidth), "cm-tab"));                                    // 6214
          builder.col += tabWidth;                                                                                     // 6215
        } else {                                                                                                       // 6216
          var txt = builder.cm.options.specialCharPlaceholder(m[0]);                                                   // 6217
          if (ie && ie_version < 9) content.appendChild(elt("span", [txt]));                                           // 6218
          else content.appendChild(txt);                                                                               // 6219
          builder.col += 1;                                                                                            // 6220
        }                                                                                                              // 6221
        builder.map.push(builder.pos, builder.pos + 1, txt);                                                           // 6222
        builder.pos++;                                                                                                 // 6223
      }                                                                                                                // 6224
    }                                                                                                                  // 6225
    if (style || startStyle || endStyle || mustWrap || css) {                                                          // 6226
      var fullStyle = style || "";                                                                                     // 6227
      if (startStyle) fullStyle += startStyle;                                                                         // 6228
      if (endStyle) fullStyle += endStyle;                                                                             // 6229
      var token = elt("span", [content], fullStyle, css);                                                              // 6230
      if (title) token.title = title;                                                                                  // 6231
      return builder.content.appendChild(token);                                                                       // 6232
    }                                                                                                                  // 6233
    builder.content.appendChild(content);                                                                              // 6234
  }                                                                                                                    // 6235
                                                                                                                       // 6236
  function buildTokenSplitSpaces(inner) {                                                                              // 6237
    function split(old) {                                                                                              // 6238
      var out = " ";                                                                                                   // 6239
      for (var i = 0; i < old.length - 2; ++i) out += i % 2 ? " " : "\u00a0";                                          // 6240
      out += " ";                                                                                                      // 6241
      return out;                                                                                                      // 6242
    }                                                                                                                  // 6243
    return function(builder, text, style, startStyle, endStyle, title) {                                               // 6244
      inner(builder, text.replace(/ {3,}/g, split), style, startStyle, endStyle, title);                               // 6245
    };                                                                                                                 // 6246
  }                                                                                                                    // 6247
                                                                                                                       // 6248
  // Work around nonsense dimensions being reported for stretches of                                                   // 6249
  // right-to-left text.                                                                                               // 6250
  function buildTokenBadBidi(inner, order) {                                                                           // 6251
    return function(builder, text, style, startStyle, endStyle, title) {                                               // 6252
      style = style ? style + " cm-force-border" : "cm-force-border";                                                  // 6253
      var start = builder.pos, end = start + text.length;                                                              // 6254
      for (;;) {                                                                                                       // 6255
        // Find the part that overlaps with the start of this text                                                     // 6256
        for (var i = 0; i < order.length; i++) {                                                                       // 6257
          var part = order[i];                                                                                         // 6258
          if (part.to > start && part.from <= start) break;                                                            // 6259
        }                                                                                                              // 6260
        if (part.to >= end) return inner(builder, text, style, startStyle, endStyle, title);                           // 6261
        inner(builder, text.slice(0, part.to - start), style, startStyle, null, title);                                // 6262
        startStyle = null;                                                                                             // 6263
        text = text.slice(part.to - start);                                                                            // 6264
        start = part.to;                                                                                               // 6265
      }                                                                                                                // 6266
    };                                                                                                                 // 6267
  }                                                                                                                    // 6268
                                                                                                                       // 6269
  function buildCollapsedSpan(builder, size, marker, ignoreWidget) {                                                   // 6270
    var widget = !ignoreWidget && marker.widgetNode;                                                                   // 6271
    if (widget) {                                                                                                      // 6272
      builder.map.push(builder.pos, builder.pos + size, widget);                                                       // 6273
      builder.content.appendChild(widget);                                                                             // 6274
    }                                                                                                                  // 6275
    builder.pos += size;                                                                                               // 6276
  }                                                                                                                    // 6277
                                                                                                                       // 6278
  // Outputs a number of spans to make up a line, taking highlighting                                                  // 6279
  // and marked text into account.                                                                                     // 6280
  function insertLineContent(line, builder, styles) {                                                                  // 6281
    var spans = line.markedSpans, allText = line.text, at = 0;                                                         // 6282
    if (!spans) {                                                                                                      // 6283
      for (var i = 1; i < styles.length; i+=2)                                                                         // 6284
        builder.addToken(builder, allText.slice(at, at = styles[i]), interpretTokenStyle(styles[i+1], builder.cm.options));
      return;                                                                                                          // 6286
    }                                                                                                                  // 6287
                                                                                                                       // 6288
    var len = allText.length, pos = 0, i = 1, text = "", style, css;                                                   // 6289
    var nextChange = 0, spanStyle, spanEndStyle, spanStartStyle, title, collapsed;                                     // 6290
    for (;;) {                                                                                                         // 6291
      if (nextChange == pos) { // Update current marker set                                                            // 6292
        spanStyle = spanEndStyle = spanStartStyle = title = css = "";                                                  // 6293
        collapsed = null; nextChange = Infinity;                                                                       // 6294
        var foundBookmarks = [];                                                                                       // 6295
        for (var j = 0; j < spans.length; ++j) {                                                                       // 6296
          var sp = spans[j], m = sp.marker;                                                                            // 6297
          if (sp.from <= pos && (sp.to == null || sp.to > pos)) {                                                      // 6298
            if (sp.to != null && nextChange > sp.to) { nextChange = sp.to; spanEndStyle = ""; }                        // 6299
            if (m.className) spanStyle += " " + m.className;                                                           // 6300
            if (m.css) css = m.css;                                                                                    // 6301
            if (m.startStyle && sp.from == pos) spanStartStyle += " " + m.startStyle;                                  // 6302
            if (m.endStyle && sp.to == nextChange) spanEndStyle += " " + m.endStyle;                                   // 6303
            if (m.title && !title) title = m.title;                                                                    // 6304
            if (m.collapsed && (!collapsed || compareCollapsedMarkers(collapsed.marker, m) < 0))                       // 6305
              collapsed = sp;                                                                                          // 6306
          } else if (sp.from > pos && nextChange > sp.from) {                                                          // 6307
            nextChange = sp.from;                                                                                      // 6308
          }                                                                                                            // 6309
          if (m.type == "bookmark" && sp.from == pos && m.widgetNode) foundBookmarks.push(m);                          // 6310
        }                                                                                                              // 6311
        if (collapsed && (collapsed.from || 0) == pos) {                                                               // 6312
          buildCollapsedSpan(builder, (collapsed.to == null ? len + 1 : collapsed.to) - pos,                           // 6313
                             collapsed.marker, collapsed.from == null);                                                // 6314
          if (collapsed.to == null) return;                                                                            // 6315
        }                                                                                                              // 6316
        if (!collapsed && foundBookmarks.length) for (var j = 0; j < foundBookmarks.length; ++j)                       // 6317
          buildCollapsedSpan(builder, 0, foundBookmarks[j]);                                                           // 6318
      }                                                                                                                // 6319
      if (pos >= len) break;                                                                                           // 6320
                                                                                                                       // 6321
      var upto = Math.min(len, nextChange);                                                                            // 6322
      while (true) {                                                                                                   // 6323
        if (text) {                                                                                                    // 6324
          var end = pos + text.length;                                                                                 // 6325
          if (!collapsed) {                                                                                            // 6326
            var tokenText = end > upto ? text.slice(0, upto - pos) : text;                                             // 6327
            builder.addToken(builder, tokenText, style ? style + spanStyle : spanStyle,                                // 6328
                             spanStartStyle, pos + tokenText.length == nextChange ? spanEndStyle : "", title, css);    // 6329
          }                                                                                                            // 6330
          if (end >= upto) {text = text.slice(upto - pos); pos = upto; break;}                                         // 6331
          pos = end;                                                                                                   // 6332
          spanStartStyle = "";                                                                                         // 6333
        }                                                                                                              // 6334
        text = allText.slice(at, at = styles[i++]);                                                                    // 6335
        style = interpretTokenStyle(styles[i++], builder.cm.options);                                                  // 6336
      }                                                                                                                // 6337
    }                                                                                                                  // 6338
  }                                                                                                                    // 6339
                                                                                                                       // 6340
  // DOCUMENT DATA STRUCTURE                                                                                           // 6341
                                                                                                                       // 6342
  // By default, updates that start and end at the beginning of a line                                                 // 6343
  // are treated specially, in order to make the association of line                                                   // 6344
  // widgets and marker elements with the text behave more intuitive.                                                  // 6345
  function isWholeLineUpdate(doc, change) {                                                                            // 6346
    return change.from.ch == 0 && change.to.ch == 0 && lst(change.text) == "" &&                                       // 6347
      (!doc.cm || doc.cm.options.wholeLineUpdateBefore);                                                               // 6348
  }                                                                                                                    // 6349
                                                                                                                       // 6350
  // Perform a change on the document data structure.                                                                  // 6351
  function updateDoc(doc, change, markedSpans, estimateHeight) {                                                       // 6352
    function spansFor(n) {return markedSpans ? markedSpans[n] : null;}                                                 // 6353
    function update(line, text, spans) {                                                                               // 6354
      updateLine(line, text, spans, estimateHeight);                                                                   // 6355
      signalLater(line, "change", line, change);                                                                       // 6356
    }                                                                                                                  // 6357
    function linesFor(start, end) {                                                                                    // 6358
      for (var i = start, result = []; i < end; ++i)                                                                   // 6359
        result.push(new Line(text[i], spansFor(i), estimateHeight));                                                   // 6360
      return result;                                                                                                   // 6361
    }                                                                                                                  // 6362
                                                                                                                       // 6363
    var from = change.from, to = change.to, text = change.text;                                                        // 6364
    var firstLine = getLine(doc, from.line), lastLine = getLine(doc, to.line);                                         // 6365
    var lastText = lst(text), lastSpans = spansFor(text.length - 1), nlines = to.line - from.line;                     // 6366
                                                                                                                       // 6367
    // Adjust the line structure                                                                                       // 6368
    if (change.full) {                                                                                                 // 6369
      doc.insert(0, linesFor(0, text.length));                                                                         // 6370
      doc.remove(text.length, doc.size - text.length);                                                                 // 6371
    } else if (isWholeLineUpdate(doc, change)) {                                                                       // 6372
      // This is a whole-line replace. Treated specially to make                                                       // 6373
      // sure line objects move the way they are supposed to.                                                          // 6374
      var added = linesFor(0, text.length - 1);                                                                        // 6375
      update(lastLine, lastLine.text, lastSpans);                                                                      // 6376
      if (nlines) doc.remove(from.line, nlines);                                                                       // 6377
      if (added.length) doc.insert(from.line, added);                                                                  // 6378
    } else if (firstLine == lastLine) {                                                                                // 6379
      if (text.length == 1) {                                                                                          // 6380
        update(firstLine, firstLine.text.slice(0, from.ch) + lastText + firstLine.text.slice(to.ch), lastSpans);       // 6381
      } else {                                                                                                         // 6382
        var added = linesFor(1, text.length - 1);                                                                      // 6383
        added.push(new Line(lastText + firstLine.text.slice(to.ch), lastSpans, estimateHeight));                       // 6384
        update(firstLine, firstLine.text.slice(0, from.ch) + text[0], spansFor(0));                                    // 6385
        doc.insert(from.line + 1, added);                                                                              // 6386
      }                                                                                                                // 6387
    } else if (text.length == 1) {                                                                                     // 6388
      update(firstLine, firstLine.text.slice(0, from.ch) + text[0] + lastLine.text.slice(to.ch), spansFor(0));         // 6389
      doc.remove(from.line + 1, nlines);                                                                               // 6390
    } else {                                                                                                           // 6391
      update(firstLine, firstLine.text.slice(0, from.ch) + text[0], spansFor(0));                                      // 6392
      update(lastLine, lastText + lastLine.text.slice(to.ch), lastSpans);                                              // 6393
      var added = linesFor(1, text.length - 1);                                                                        // 6394
      if (nlines > 1) doc.remove(from.line + 1, nlines - 1);                                                           // 6395
      doc.insert(from.line + 1, added);                                                                                // 6396
    }                                                                                                                  // 6397
                                                                                                                       // 6398
    signalLater(doc, "change", doc, change);                                                                           // 6399
  }                                                                                                                    // 6400
                                                                                                                       // 6401
  // The document is represented as a BTree consisting of leaves, with                                                 // 6402
  // chunk of lines in them, and branches, with up to ten leaves or                                                    // 6403
  // other branch nodes below them. The top node is always a branch                                                    // 6404
  // node, and is the document object itself (meaning it has                                                           // 6405
  // additional methods and properties).                                                                               // 6406
  //                                                                                                                   // 6407
  // All nodes have parent links. The tree is used both to go from                                                     // 6408
  // line numbers to line objects, and to go from objects to numbers.                                                  // 6409
  // It also indexes by height, and is used to convert between height                                                  // 6410
  // and line object, and to find the total height of the document.                                                    // 6411
  //                                                                                                                   // 6412
  // See also http://marijnhaverbeke.nl/blog/codemirror-line-tree.html                                                 // 6413
                                                                                                                       // 6414
  function LeafChunk(lines) {                                                                                          // 6415
    this.lines = lines;                                                                                                // 6416
    this.parent = null;                                                                                                // 6417
    for (var i = 0, height = 0; i < lines.length; ++i) {                                                               // 6418
      lines[i].parent = this;                                                                                          // 6419
      height += lines[i].height;                                                                                       // 6420
    }                                                                                                                  // 6421
    this.height = height;                                                                                              // 6422
  }                                                                                                                    // 6423
                                                                                                                       // 6424
  LeafChunk.prototype = {                                                                                              // 6425
    chunkSize: function() { return this.lines.length; },                                                               // 6426
    // Remove the n lines at offset 'at'.                                                                              // 6427
    removeInner: function(at, n) {                                                                                     // 6428
      for (var i = at, e = at + n; i < e; ++i) {                                                                       // 6429
        var line = this.lines[i];                                                                                      // 6430
        this.height -= line.height;                                                                                    // 6431
        cleanUpLine(line);                                                                                             // 6432
        signalLater(line, "delete");                                                                                   // 6433
      }                                                                                                                // 6434
      this.lines.splice(at, n);                                                                                        // 6435
    },                                                                                                                 // 6436
    // Helper used to collapse a small branch into a single leaf.                                                      // 6437
    collapse: function(lines) {                                                                                        // 6438
      lines.push.apply(lines, this.lines);                                                                             // 6439
    },                                                                                                                 // 6440
    // Insert the given array of lines at offset 'at', count them as                                                   // 6441
    // having the given height.                                                                                        // 6442
    insertInner: function(at, lines, height) {                                                                         // 6443
      this.height += height;                                                                                           // 6444
      this.lines = this.lines.slice(0, at).concat(lines).concat(this.lines.slice(at));                                 // 6445
      for (var i = 0; i < lines.length; ++i) lines[i].parent = this;                                                   // 6446
    },                                                                                                                 // 6447
    // Used to iterate over a part of the tree.                                                                        // 6448
    iterN: function(at, n, op) {                                                                                       // 6449
      for (var e = at + n; at < e; ++at)                                                                               // 6450
        if (op(this.lines[at])) return true;                                                                           // 6451
    }                                                                                                                  // 6452
  };                                                                                                                   // 6453
                                                                                                                       // 6454
  function BranchChunk(children) {                                                                                     // 6455
    this.children = children;                                                                                          // 6456
    var size = 0, height = 0;                                                                                          // 6457
    for (var i = 0; i < children.length; ++i) {                                                                        // 6458
      var ch = children[i];                                                                                            // 6459
      size += ch.chunkSize(); height += ch.height;                                                                     // 6460
      ch.parent = this;                                                                                                // 6461
    }                                                                                                                  // 6462
    this.size = size;                                                                                                  // 6463
    this.height = height;                                                                                              // 6464
    this.parent = null;                                                                                                // 6465
  }                                                                                                                    // 6466
                                                                                                                       // 6467
  BranchChunk.prototype = {                                                                                            // 6468
    chunkSize: function() { return this.size; },                                                                       // 6469
    removeInner: function(at, n) {                                                                                     // 6470
      this.size -= n;                                                                                                  // 6471
      for (var i = 0; i < this.children.length; ++i) {                                                                 // 6472
        var child = this.children[i], sz = child.chunkSize();                                                          // 6473
        if (at < sz) {                                                                                                 // 6474
          var rm = Math.min(n, sz - at), oldHeight = child.height;                                                     // 6475
          child.removeInner(at, rm);                                                                                   // 6476
          this.height -= oldHeight - child.height;                                                                     // 6477
          if (sz == rm) { this.children.splice(i--, 1); child.parent = null; }                                         // 6478
          if ((n -= rm) == 0) break;                                                                                   // 6479
          at = 0;                                                                                                      // 6480
        } else at -= sz;                                                                                               // 6481
      }                                                                                                                // 6482
      // If the result is smaller than 25 lines, ensure that it is a                                                   // 6483
      // single leaf node.                                                                                             // 6484
      if (this.size - n < 25 &&                                                                                        // 6485
          (this.children.length > 1 || !(this.children[0] instanceof LeafChunk))) {                                    // 6486
        var lines = [];                                                                                                // 6487
        this.collapse(lines);                                                                                          // 6488
        this.children = [new LeafChunk(lines)];                                                                        // 6489
        this.children[0].parent = this;                                                                                // 6490
      }                                                                                                                // 6491
    },                                                                                                                 // 6492
    collapse: function(lines) {                                                                                        // 6493
      for (var i = 0; i < this.children.length; ++i) this.children[i].collapse(lines);                                 // 6494
    },                                                                                                                 // 6495
    insertInner: function(at, lines, height) {                                                                         // 6496
      this.size += lines.length;                                                                                       // 6497
      this.height += height;                                                                                           // 6498
      for (var i = 0; i < this.children.length; ++i) {                                                                 // 6499
        var child = this.children[i], sz = child.chunkSize();                                                          // 6500
        if (at <= sz) {                                                                                                // 6501
          child.insertInner(at, lines, height);                                                                        // 6502
          if (child.lines && child.lines.length > 50) {                                                                // 6503
            while (child.lines.length > 50) {                                                                          // 6504
              var spilled = child.lines.splice(child.lines.length - 25, 25);                                           // 6505
              var newleaf = new LeafChunk(spilled);                                                                    // 6506
              child.height -= newleaf.height;                                                                          // 6507
              this.children.splice(i + 1, 0, newleaf);                                                                 // 6508
              newleaf.parent = this;                                                                                   // 6509
            }                                                                                                          // 6510
            this.maybeSpill();                                                                                         // 6511
          }                                                                                                            // 6512
          break;                                                                                                       // 6513
        }                                                                                                              // 6514
        at -= sz;                                                                                                      // 6515
      }                                                                                                                // 6516
    },                                                                                                                 // 6517
    // When a node has grown, check whether it should be split.                                                        // 6518
    maybeSpill: function() {                                                                                           // 6519
      if (this.children.length <= 10) return;                                                                          // 6520
      var me = this;                                                                                                   // 6521
      do {                                                                                                             // 6522
        var spilled = me.children.splice(me.children.length - 5, 5);                                                   // 6523
        var sibling = new BranchChunk(spilled);                                                                        // 6524
        if (!me.parent) { // Become the parent node                                                                    // 6525
          var copy = new BranchChunk(me.children);                                                                     // 6526
          copy.parent = me;                                                                                            // 6527
          me.children = [copy, sibling];                                                                               // 6528
          me = copy;                                                                                                   // 6529
        } else {                                                                                                       // 6530
          me.size -= sibling.size;                                                                                     // 6531
          me.height -= sibling.height;                                                                                 // 6532
          var myIndex = indexOf(me.parent.children, me);                                                               // 6533
          me.parent.children.splice(myIndex + 1, 0, sibling);                                                          // 6534
        }                                                                                                              // 6535
        sibling.parent = me.parent;                                                                                    // 6536
      } while (me.children.length > 10);                                                                               // 6537
      me.parent.maybeSpill();                                                                                          // 6538
    },                                                                                                                 // 6539
    iterN: function(at, n, op) {                                                                                       // 6540
      for (var i = 0; i < this.children.length; ++i) {                                                                 // 6541
        var child = this.children[i], sz = child.chunkSize();                                                          // 6542
        if (at < sz) {                                                                                                 // 6543
          var used = Math.min(n, sz - at);                                                                             // 6544
          if (child.iterN(at, used, op)) return true;                                                                  // 6545
          if ((n -= used) == 0) break;                                                                                 // 6546
          at = 0;                                                                                                      // 6547
        } else at -= sz;                                                                                               // 6548
      }                                                                                                                // 6549
    }                                                                                                                  // 6550
  };                                                                                                                   // 6551
                                                                                                                       // 6552
  var nextDocId = 0;                                                                                                   // 6553
  var Doc = CodeMirror.Doc = function(text, mode, firstLine) {                                                         // 6554
    if (!(this instanceof Doc)) return new Doc(text, mode, firstLine);                                                 // 6555
    if (firstLine == null) firstLine = 0;                                                                              // 6556
                                                                                                                       // 6557
    BranchChunk.call(this, [new LeafChunk([new Line("", null)])]);                                                     // 6558
    this.first = firstLine;                                                                                            // 6559
    this.scrollTop = this.scrollLeft = 0;                                                                              // 6560
    this.cantEdit = false;                                                                                             // 6561
    this.cleanGeneration = 1;                                                                                          // 6562
    this.frontier = firstLine;                                                                                         // 6563
    var start = Pos(firstLine, 0);                                                                                     // 6564
    this.sel = simpleSelection(start);                                                                                 // 6565
    this.history = new History(null);                                                                                  // 6566
    this.id = ++nextDocId;                                                                                             // 6567
    this.modeOption = mode;                                                                                            // 6568
                                                                                                                       // 6569
    if (typeof text == "string") text = splitLines(text);                                                              // 6570
    updateDoc(this, {from: start, to: start, text: text});                                                             // 6571
    setSelection(this, simpleSelection(start), sel_dontScroll);                                                        // 6572
  };                                                                                                                   // 6573
                                                                                                                       // 6574
  Doc.prototype = createObj(BranchChunk.prototype, {                                                                   // 6575
    constructor: Doc,                                                                                                  // 6576
    // Iterate over the document. Supports two forms -- with only one                                                  // 6577
    // argument, it calls that for each line in the document. With                                                     // 6578
    // three, it iterates over the range given by the first two (with                                                  // 6579
    // the second being non-inclusive).                                                                                // 6580
    iter: function(from, to, op) {                                                                                     // 6581
      if (op) this.iterN(from - this.first, to - from, op);                                                            // 6582
      else this.iterN(this.first, this.first + this.size, from);                                                       // 6583
    },                                                                                                                 // 6584
                                                                                                                       // 6585
    // Non-public interface for adding and removing lines.                                                             // 6586
    insert: function(at, lines) {                                                                                      // 6587
      var height = 0;                                                                                                  // 6588
      for (var i = 0; i < lines.length; ++i) height += lines[i].height;                                                // 6589
      this.insertInner(at - this.first, lines, height);                                                                // 6590
    },                                                                                                                 // 6591
    remove: function(at, n) { this.removeInner(at - this.first, n); },                                                 // 6592
                                                                                                                       // 6593
    // From here, the methods are part of the public interface. Most                                                   // 6594
    // are also available from CodeMirror (editor) instances.                                                          // 6595
                                                                                                                       // 6596
    getValue: function(lineSep) {                                                                                      // 6597
      var lines = getLines(this, this.first, this.first + this.size);                                                  // 6598
      if (lineSep === false) return lines;                                                                             // 6599
      return lines.join(lineSep || "\n");                                                                              // 6600
    },                                                                                                                 // 6601
    setValue: docMethodOp(function(code) {                                                                             // 6602
      var top = Pos(this.first, 0), last = this.first + this.size - 1;                                                 // 6603
      makeChange(this, {from: top, to: Pos(last, getLine(this, last).text.length),                                     // 6604
                        text: splitLines(code), origin: "setValue", full: true}, true);                                // 6605
      setSelection(this, simpleSelection(top));                                                                        // 6606
    }),                                                                                                                // 6607
    replaceRange: function(code, from, to, origin) {                                                                   // 6608
      from = clipPos(this, from);                                                                                      // 6609
      to = to ? clipPos(this, to) : from;                                                                              // 6610
      replaceRange(this, code, from, to, origin);                                                                      // 6611
    },                                                                                                                 // 6612
    getRange: function(from, to, lineSep) {                                                                            // 6613
      var lines = getBetween(this, clipPos(this, from), clipPos(this, to));                                            // 6614
      if (lineSep === false) return lines;                                                                             // 6615
      return lines.join(lineSep || "\n");                                                                              // 6616
    },                                                                                                                 // 6617
                                                                                                                       // 6618
    getLine: function(line) {var l = this.getLineHandle(line); return l && l.text;},                                   // 6619
                                                                                                                       // 6620
    getLineHandle: function(line) {if (isLine(this, line)) return getLine(this, line);},                               // 6621
    getLineNumber: function(line) {return lineNo(line);},                                                              // 6622
                                                                                                                       // 6623
    getLineHandleVisualStart: function(line) {                                                                         // 6624
      if (typeof line == "number") line = getLine(this, line);                                                         // 6625
      return visualLine(line);                                                                                         // 6626
    },                                                                                                                 // 6627
                                                                                                                       // 6628
    lineCount: function() {return this.size;},                                                                         // 6629
    firstLine: function() {return this.first;},                                                                        // 6630
    lastLine: function() {return this.first + this.size - 1;},                                                         // 6631
                                                                                                                       // 6632
    clipPos: function(pos) {return clipPos(this, pos);},                                                               // 6633
                                                                                                                       // 6634
    getCursor: function(start) {                                                                                       // 6635
      var range = this.sel.primary(), pos;                                                                             // 6636
      if (start == null || start == "head") pos = range.head;                                                          // 6637
      else if (start == "anchor") pos = range.anchor;                                                                  // 6638
      else if (start == "end" || start == "to" || start === false) pos = range.to();                                   // 6639
      else pos = range.from();                                                                                         // 6640
      return pos;                                                                                                      // 6641
    },                                                                                                                 // 6642
    listSelections: function() { return this.sel.ranges; },                                                            // 6643
    somethingSelected: function() {return this.sel.somethingSelected();},                                              // 6644
                                                                                                                       // 6645
    setCursor: docMethodOp(function(line, ch, options) {                                                               // 6646
      setSimpleSelection(this, clipPos(this, typeof line == "number" ? Pos(line, ch || 0) : line), null, options);     // 6647
    }),                                                                                                                // 6648
    setSelection: docMethodOp(function(anchor, head, options) {                                                        // 6649
      setSimpleSelection(this, clipPos(this, anchor), clipPos(this, head || anchor), options);                         // 6650
    }),                                                                                                                // 6651
    extendSelection: docMethodOp(function(head, other, options) {                                                      // 6652
      extendSelection(this, clipPos(this, head), other && clipPos(this, other), options);                              // 6653
    }),                                                                                                                // 6654
    extendSelections: docMethodOp(function(heads, options) {                                                           // 6655
      extendSelections(this, clipPosArray(this, heads, options));                                                      // 6656
    }),                                                                                                                // 6657
    extendSelectionsBy: docMethodOp(function(f, options) {                                                             // 6658
      extendSelections(this, map(this.sel.ranges, f), options);                                                        // 6659
    }),                                                                                                                // 6660
    setSelections: docMethodOp(function(ranges, primary, options) {                                                    // 6661
      if (!ranges.length) return;                                                                                      // 6662
      for (var i = 0, out = []; i < ranges.length; i++)                                                                // 6663
        out[i] = new Range(clipPos(this, ranges[i].anchor),                                                            // 6664
                           clipPos(this, ranges[i].head));                                                             // 6665
      if (primary == null) primary = Math.min(ranges.length - 1, this.sel.primIndex);                                  // 6666
      setSelection(this, normalizeSelection(out, primary), options);                                                   // 6667
    }),                                                                                                                // 6668
    addSelection: docMethodOp(function(anchor, head, options) {                                                        // 6669
      var ranges = this.sel.ranges.slice(0);                                                                           // 6670
      ranges.push(new Range(clipPos(this, anchor), clipPos(this, head || anchor)));                                    // 6671
      setSelection(this, normalizeSelection(ranges, ranges.length - 1), options);                                      // 6672
    }),                                                                                                                // 6673
                                                                                                                       // 6674
    getSelection: function(lineSep) {                                                                                  // 6675
      var ranges = this.sel.ranges, lines;                                                                             // 6676
      for (var i = 0; i < ranges.length; i++) {                                                                        // 6677
        var sel = getBetween(this, ranges[i].from(), ranges[i].to());                                                  // 6678
        lines = lines ? lines.concat(sel) : sel;                                                                       // 6679
      }                                                                                                                // 6680
      if (lineSep === false) return lines;                                                                             // 6681
      else return lines.join(lineSep || "\n");                                                                         // 6682
    },                                                                                                                 // 6683
    getSelections: function(lineSep) {                                                                                 // 6684
      var parts = [], ranges = this.sel.ranges;                                                                        // 6685
      for (var i = 0; i < ranges.length; i++) {                                                                        // 6686
        var sel = getBetween(this, ranges[i].from(), ranges[i].to());                                                  // 6687
        if (lineSep !== false) sel = sel.join(lineSep || "\n");                                                        // 6688
        parts[i] = sel;                                                                                                // 6689
      }                                                                                                                // 6690
      return parts;                                                                                                    // 6691
    },                                                                                                                 // 6692
    replaceSelection: function(code, collapse, origin) {                                                               // 6693
      var dup = [];                                                                                                    // 6694
      for (var i = 0; i < this.sel.ranges.length; i++)                                                                 // 6695
        dup[i] = code;                                                                                                 // 6696
      this.replaceSelections(dup, collapse, origin || "+input");                                                       // 6697
    },                                                                                                                 // 6698
    replaceSelections: docMethodOp(function(code, collapse, origin) {                                                  // 6699
      var changes = [], sel = this.sel;                                                                                // 6700
      for (var i = 0; i < sel.ranges.length; i++) {                                                                    // 6701
        var range = sel.ranges[i];                                                                                     // 6702
        changes[i] = {from: range.from(), to: range.to(), text: splitLines(code[i]), origin: origin};                  // 6703
      }                                                                                                                // 6704
      var newSel = collapse && collapse != "end" && computeReplacedSel(this, changes, collapse);                       // 6705
      for (var i = changes.length - 1; i >= 0; i--)                                                                    // 6706
        makeChange(this, changes[i]);                                                                                  // 6707
      if (newSel) setSelectionReplaceHistory(this, newSel);                                                            // 6708
      else if (this.cm) ensureCursorVisible(this.cm);                                                                  // 6709
    }),                                                                                                                // 6710
    undo: docMethodOp(function() {makeChangeFromHistory(this, "undo");}),                                              // 6711
    redo: docMethodOp(function() {makeChangeFromHistory(this, "redo");}),                                              // 6712
    undoSelection: docMethodOp(function() {makeChangeFromHistory(this, "undo", true);}),                               // 6713
    redoSelection: docMethodOp(function() {makeChangeFromHistory(this, "redo", true);}),                               // 6714
                                                                                                                       // 6715
    setExtending: function(val) {this.extend = val;},                                                                  // 6716
    getExtending: function() {return this.extend;},                                                                    // 6717
                                                                                                                       // 6718
    historySize: function() {                                                                                          // 6719
      var hist = this.history, done = 0, undone = 0;                                                                   // 6720
      for (var i = 0; i < hist.done.length; i++) if (!hist.done[i].ranges) ++done;                                     // 6721
      for (var i = 0; i < hist.undone.length; i++) if (!hist.undone[i].ranges) ++undone;                               // 6722
      return {undo: done, redo: undone};                                                                               // 6723
    },                                                                                                                 // 6724
    clearHistory: function() {this.history = new History(this.history.maxGeneration);},                                // 6725
                                                                                                                       // 6726
    markClean: function() {                                                                                            // 6727
      this.cleanGeneration = this.changeGeneration(true);                                                              // 6728
    },                                                                                                                 // 6729
    changeGeneration: function(forceSplit) {                                                                           // 6730
      if (forceSplit)                                                                                                  // 6731
        this.history.lastOp = this.history.lastSelOp = this.history.lastOrigin = null;                                 // 6732
      return this.history.generation;                                                                                  // 6733
    },                                                                                                                 // 6734
    isClean: function (gen) {                                                                                          // 6735
      return this.history.generation == (gen || this.cleanGeneration);                                                 // 6736
    },                                                                                                                 // 6737
                                                                                                                       // 6738
    getHistory: function() {                                                                                           // 6739
      return {done: copyHistoryArray(this.history.done),                                                               // 6740
              undone: copyHistoryArray(this.history.undone)};                                                          // 6741
    },                                                                                                                 // 6742
    setHistory: function(histData) {                                                                                   // 6743
      var hist = this.history = new History(this.history.maxGeneration);                                               // 6744
      hist.done = copyHistoryArray(histData.done.slice(0), null, true);                                                // 6745
      hist.undone = copyHistoryArray(histData.undone.slice(0), null, true);                                            // 6746
    },                                                                                                                 // 6747
                                                                                                                       // 6748
    addLineClass: docMethodOp(function(handle, where, cls) {                                                           // 6749
      return changeLine(this, handle, where == "gutter" ? "gutter" : "class", function(line) {                         // 6750
        var prop = where == "text" ? "textClass"                                                                       // 6751
                 : where == "background" ? "bgClass"                                                                   // 6752
                 : where == "gutter" ? "gutterClass" : "wrapClass";                                                    // 6753
        if (!line[prop]) line[prop] = cls;                                                                             // 6754
        else if (classTest(cls).test(line[prop])) return false;                                                        // 6755
        else line[prop] += " " + cls;                                                                                  // 6756
        return true;                                                                                                   // 6757
      });                                                                                                              // 6758
    }),                                                                                                                // 6759
    removeLineClass: docMethodOp(function(handle, where, cls) {                                                        // 6760
      return changeLine(this, handle, where == "gutter" ? "gutter" : "class", function(line) {                         // 6761
        var prop = where == "text" ? "textClass"                                                                       // 6762
                 : where == "background" ? "bgClass"                                                                   // 6763
                 : where == "gutter" ? "gutterClass" : "wrapClass";                                                    // 6764
        var cur = line[prop];                                                                                          // 6765
        if (!cur) return false;                                                                                        // 6766
        else if (cls == null) line[prop] = null;                                                                       // 6767
        else {                                                                                                         // 6768
          var found = cur.match(classTest(cls));                                                                       // 6769
          if (!found) return false;                                                                                    // 6770
          var end = found.index + found[0].length;                                                                     // 6771
          line[prop] = cur.slice(0, found.index) + (!found.index || end == cur.length ? "" : " ") + cur.slice(end) || null;
        }                                                                                                              // 6773
        return true;                                                                                                   // 6774
      });                                                                                                              // 6775
    }),                                                                                                                // 6776
                                                                                                                       // 6777
    markText: function(from, to, options) {                                                                            // 6778
      return markText(this, clipPos(this, from), clipPos(this, to), options, "range");                                 // 6779
    },                                                                                                                 // 6780
    setBookmark: function(pos, options) {                                                                              // 6781
      var realOpts = {replacedWith: options && (options.nodeType == null ? options.widget : options),                  // 6782
                      insertLeft: options && options.insertLeft,                                                       // 6783
                      clearWhenEmpty: false, shared: options && options.shared};                                       // 6784
      pos = clipPos(this, pos);                                                                                        // 6785
      return markText(this, pos, pos, realOpts, "bookmark");                                                           // 6786
    },                                                                                                                 // 6787
    findMarksAt: function(pos) {                                                                                       // 6788
      pos = clipPos(this, pos);                                                                                        // 6789
      var markers = [], spans = getLine(this, pos.line).markedSpans;                                                   // 6790
      if (spans) for (var i = 0; i < spans.length; ++i) {                                                              // 6791
        var span = spans[i];                                                                                           // 6792
        if ((span.from == null || span.from <= pos.ch) &&                                                              // 6793
            (span.to == null || span.to >= pos.ch))                                                                    // 6794
          markers.push(span.marker.parent || span.marker);                                                             // 6795
      }                                                                                                                // 6796
      return markers;                                                                                                  // 6797
    },                                                                                                                 // 6798
    findMarks: function(from, to, filter) {                                                                            // 6799
      from = clipPos(this, from); to = clipPos(this, to);                                                              // 6800
      var found = [], lineNo = from.line;                                                                              // 6801
      this.iter(from.line, to.line + 1, function(line) {                                                               // 6802
        var spans = line.markedSpans;                                                                                  // 6803
        if (spans) for (var i = 0; i < spans.length; i++) {                                                            // 6804
          var span = spans[i];                                                                                         // 6805
          if (!(lineNo == from.line && from.ch > span.to ||                                                            // 6806
                span.from == null && lineNo != from.line||                                                             // 6807
                lineNo == to.line && span.from > to.ch) &&                                                             // 6808
              (!filter || filter(span.marker)))                                                                        // 6809
            found.push(span.marker.parent || span.marker);                                                             // 6810
        }                                                                                                              // 6811
        ++lineNo;                                                                                                      // 6812
      });                                                                                                              // 6813
      return found;                                                                                                    // 6814
    },                                                                                                                 // 6815
    getAllMarks: function() {                                                                                          // 6816
      var markers = [];                                                                                                // 6817
      this.iter(function(line) {                                                                                       // 6818
        var sps = line.markedSpans;                                                                                    // 6819
        if (sps) for (var i = 0; i < sps.length; ++i)                                                                  // 6820
          if (sps[i].from != null) markers.push(sps[i].marker);                                                        // 6821
      });                                                                                                              // 6822
      return markers;                                                                                                  // 6823
    },                                                                                                                 // 6824
                                                                                                                       // 6825
    posFromIndex: function(off) {                                                                                      // 6826
      var ch, lineNo = this.first;                                                                                     // 6827
      this.iter(function(line) {                                                                                       // 6828
        var sz = line.text.length + 1;                                                                                 // 6829
        if (sz > off) { ch = off; return true; }                                                                       // 6830
        off -= sz;                                                                                                     // 6831
        ++lineNo;                                                                                                      // 6832
      });                                                                                                              // 6833
      return clipPos(this, Pos(lineNo, ch));                                                                           // 6834
    },                                                                                                                 // 6835
    indexFromPos: function (coords) {                                                                                  // 6836
      coords = clipPos(this, coords);                                                                                  // 6837
      var index = coords.ch;                                                                                           // 6838
      if (coords.line < this.first || coords.ch < 0) return 0;                                                         // 6839
      this.iter(this.first, coords.line, function (line) {                                                             // 6840
        index += line.text.length + 1;                                                                                 // 6841
      });                                                                                                              // 6842
      return index;                                                                                                    // 6843
    },                                                                                                                 // 6844
                                                                                                                       // 6845
    copy: function(copyHistory) {                                                                                      // 6846
      var doc = new Doc(getLines(this, this.first, this.first + this.size), this.modeOption, this.first);              // 6847
      doc.scrollTop = this.scrollTop; doc.scrollLeft = this.scrollLeft;                                                // 6848
      doc.sel = this.sel;                                                                                              // 6849
      doc.extend = false;                                                                                              // 6850
      if (copyHistory) {                                                                                               // 6851
        doc.history.undoDepth = this.history.undoDepth;                                                                // 6852
        doc.setHistory(this.getHistory());                                                                             // 6853
      }                                                                                                                // 6854
      return doc;                                                                                                      // 6855
    },                                                                                                                 // 6856
                                                                                                                       // 6857
    linkedDoc: function(options) {                                                                                     // 6858
      if (!options) options = {};                                                                                      // 6859
      var from = this.first, to = this.first + this.size;                                                              // 6860
      if (options.from != null && options.from > from) from = options.from;                                            // 6861
      if (options.to != null && options.to < to) to = options.to;                                                      // 6862
      var copy = new Doc(getLines(this, from, to), options.mode || this.modeOption, from);                             // 6863
      if (options.sharedHist) copy.history = this.history;                                                             // 6864
      (this.linked || (this.linked = [])).push({doc: copy, sharedHist: options.sharedHist});                           // 6865
      copy.linked = [{doc: this, isParent: true, sharedHist: options.sharedHist}];                                     // 6866
      copySharedMarkers(copy, findSharedMarkers(this));                                                                // 6867
      return copy;                                                                                                     // 6868
    },                                                                                                                 // 6869
    unlinkDoc: function(other) {                                                                                       // 6870
      if (other instanceof CodeMirror) other = other.doc;                                                              // 6871
      if (this.linked) for (var i = 0; i < this.linked.length; ++i) {                                                  // 6872
        var link = this.linked[i];                                                                                     // 6873
        if (link.doc != other) continue;                                                                               // 6874
        this.linked.splice(i, 1);                                                                                      // 6875
        other.unlinkDoc(this);                                                                                         // 6876
        detachSharedMarkers(findSharedMarkers(this));                                                                  // 6877
        break;                                                                                                         // 6878
      }                                                                                                                // 6879
      // If the histories were shared, split them again                                                                // 6880
      if (other.history == this.history) {                                                                             // 6881
        var splitIds = [other.id];                                                                                     // 6882
        linkedDocs(other, function(doc) {splitIds.push(doc.id);}, true);                                               // 6883
        other.history = new History(null);                                                                             // 6884
        other.history.done = copyHistoryArray(this.history.done, splitIds);                                            // 6885
        other.history.undone = copyHistoryArray(this.history.undone, splitIds);                                        // 6886
      }                                                                                                                // 6887
    },                                                                                                                 // 6888
    iterLinkedDocs: function(f) {linkedDocs(this, f);},                                                                // 6889
                                                                                                                       // 6890
    getMode: function() {return this.mode;},                                                                           // 6891
    getEditor: function() {return this.cm;}                                                                            // 6892
  });                                                                                                                  // 6893
                                                                                                                       // 6894
  // Public alias.                                                                                                     // 6895
  Doc.prototype.eachLine = Doc.prototype.iter;                                                                         // 6896
                                                                                                                       // 6897
  // Set up methods on CodeMirror's prototype to redirect to the editor's document.                                    // 6898
  var dontDelegate = "iter insert remove copy getEditor".split(" ");                                                   // 6899
  for (var prop in Doc.prototype) if (Doc.prototype.hasOwnProperty(prop) && indexOf(dontDelegate, prop) < 0)           // 6900
    CodeMirror.prototype[prop] = (function(method) {                                                                   // 6901
      return function() {return method.apply(this.doc, arguments);};                                                   // 6902
    })(Doc.prototype[prop]);                                                                                           // 6903
                                                                                                                       // 6904
  eventMixin(Doc);                                                                                                     // 6905
                                                                                                                       // 6906
  // Call f for all linked documents.                                                                                  // 6907
  function linkedDocs(doc, f, sharedHistOnly) {                                                                        // 6908
    function propagate(doc, skip, sharedHist) {                                                                        // 6909
      if (doc.linked) for (var i = 0; i < doc.linked.length; ++i) {                                                    // 6910
        var rel = doc.linked[i];                                                                                       // 6911
        if (rel.doc == skip) continue;                                                                                 // 6912
        var shared = sharedHist && rel.sharedHist;                                                                     // 6913
        if (sharedHistOnly && !shared) continue;                                                                       // 6914
        f(rel.doc, shared);                                                                                            // 6915
        propagate(rel.doc, doc, shared);                                                                               // 6916
      }                                                                                                                // 6917
    }                                                                                                                  // 6918
    propagate(doc, null, true);                                                                                        // 6919
  }                                                                                                                    // 6920
                                                                                                                       // 6921
  // Attach a document to an editor.                                                                                   // 6922
  function attachDoc(cm, doc) {                                                                                        // 6923
    if (doc.cm) throw new Error("This document is already in use.");                                                   // 6924
    cm.doc = doc;                                                                                                      // 6925
    doc.cm = cm;                                                                                                       // 6926
    estimateLineHeights(cm);                                                                                           // 6927
    loadMode(cm);                                                                                                      // 6928
    if (!cm.options.lineWrapping) findMaxLine(cm);                                                                     // 6929
    cm.options.mode = doc.modeOption;                                                                                  // 6930
    regChange(cm);                                                                                                     // 6931
  }                                                                                                                    // 6932
                                                                                                                       // 6933
  // LINE UTILITIES                                                                                                    // 6934
                                                                                                                       // 6935
  // Find the line object corresponding to the given line number.                                                      // 6936
  function getLine(doc, n) {                                                                                           // 6937
    n -= doc.first;                                                                                                    // 6938
    if (n < 0 || n >= doc.size) throw new Error("There is no line " + (n + doc.first) + " in the document.");          // 6939
    for (var chunk = doc; !chunk.lines;) {                                                                             // 6940
      for (var i = 0;; ++i) {                                                                                          // 6941
        var child = chunk.children[i], sz = child.chunkSize();                                                         // 6942
        if (n < sz) { chunk = child; break; }                                                                          // 6943
        n -= sz;                                                                                                       // 6944
      }                                                                                                                // 6945
    }                                                                                                                  // 6946
    return chunk.lines[n];                                                                                             // 6947
  }                                                                                                                    // 6948
                                                                                                                       // 6949
  // Get the part of a document between two positions, as an array of                                                  // 6950
  // strings.                                                                                                          // 6951
  function getBetween(doc, start, end) {                                                                               // 6952
    var out = [], n = start.line;                                                                                      // 6953
    doc.iter(start.line, end.line + 1, function(line) {                                                                // 6954
      var text = line.text;                                                                                            // 6955
      if (n == end.line) text = text.slice(0, end.ch);                                                                 // 6956
      if (n == start.line) text = text.slice(start.ch);                                                                // 6957
      out.push(text);                                                                                                  // 6958
      ++n;                                                                                                             // 6959
    });                                                                                                                // 6960
    return out;                                                                                                        // 6961
  }                                                                                                                    // 6962
  // Get the lines between from and to, as array of strings.                                                           // 6963
  function getLines(doc, from, to) {                                                                                   // 6964
    var out = [];                                                                                                      // 6965
    doc.iter(from, to, function(line) { out.push(line.text); });                                                       // 6966
    return out;                                                                                                        // 6967
  }                                                                                                                    // 6968
                                                                                                                       // 6969
  // Update the height of a line, propagating the height change                                                        // 6970
  // upwards to parent nodes.                                                                                          // 6971
  function updateLineHeight(line, height) {                                                                            // 6972
    var diff = height - line.height;                                                                                   // 6973
    if (diff) for (var n = line; n; n = n.parent) n.height += diff;                                                    // 6974
  }                                                                                                                    // 6975
                                                                                                                       // 6976
  // Given a line object, find its line number by walking up through                                                   // 6977
  // its parent links.                                                                                                 // 6978
  function lineNo(line) {                                                                                              // 6979
    if (line.parent == null) return null;                                                                              // 6980
    var cur = line.parent, no = indexOf(cur.lines, line);                                                              // 6981
    for (var chunk = cur.parent; chunk; cur = chunk, chunk = chunk.parent) {                                           // 6982
      for (var i = 0;; ++i) {                                                                                          // 6983
        if (chunk.children[i] == cur) break;                                                                           // 6984
        no += chunk.children[i].chunkSize();                                                                           // 6985
      }                                                                                                                // 6986
    }                                                                                                                  // 6987
    return no + cur.first;                                                                                             // 6988
  }                                                                                                                    // 6989
                                                                                                                       // 6990
  // Find the line at the given vertical position, using the height                                                    // 6991
  // information in the document tree.                                                                                 // 6992
  function lineAtHeight(chunk, h) {                                                                                    // 6993
    var n = chunk.first;                                                                                               // 6994
    outer: do {                                                                                                        // 6995
      for (var i = 0; i < chunk.children.length; ++i) {                                                                // 6996
        var child = chunk.children[i], ch = child.height;                                                              // 6997
        if (h < ch) { chunk = child; continue outer; }                                                                 // 6998
        h -= ch;                                                                                                       // 6999
        n += child.chunkSize();                                                                                        // 7000
      }                                                                                                                // 7001
      return n;                                                                                                        // 7002
    } while (!chunk.lines);                                                                                            // 7003
    for (var i = 0; i < chunk.lines.length; ++i) {                                                                     // 7004
      var line = chunk.lines[i], lh = line.height;                                                                     // 7005
      if (h < lh) break;                                                                                               // 7006
      h -= lh;                                                                                                         // 7007
    }                                                                                                                  // 7008
    return n + i;                                                                                                      // 7009
  }                                                                                                                    // 7010
                                                                                                                       // 7011
                                                                                                                       // 7012
  // Find the height above the given line.                                                                             // 7013
  function heightAtLine(lineObj) {                                                                                     // 7014
    lineObj = visualLine(lineObj);                                                                                     // 7015
                                                                                                                       // 7016
    var h = 0, chunk = lineObj.parent;                                                                                 // 7017
    for (var i = 0; i < chunk.lines.length; ++i) {                                                                     // 7018
      var line = chunk.lines[i];                                                                                       // 7019
      if (line == lineObj) break;                                                                                      // 7020
      else h += line.height;                                                                                           // 7021
    }                                                                                                                  // 7022
    for (var p = chunk.parent; p; chunk = p, p = chunk.parent) {                                                       // 7023
      for (var i = 0; i < p.children.length; ++i) {                                                                    // 7024
        var cur = p.children[i];                                                                                       // 7025
        if (cur == chunk) break;                                                                                       // 7026
        else h += cur.height;                                                                                          // 7027
      }                                                                                                                // 7028
    }                                                                                                                  // 7029
    return h;                                                                                                          // 7030
  }                                                                                                                    // 7031
                                                                                                                       // 7032
  // Get the bidi ordering for the given line (and cache it). Returns                                                  // 7033
  // false for lines that are fully left-to-right, and an array of                                                     // 7034
  // BidiSpan objects otherwise.                                                                                       // 7035
  function getOrder(line) {                                                                                            // 7036
    var order = line.order;                                                                                            // 7037
    if (order == null) order = line.order = bidiOrdering(line.text);                                                   // 7038
    return order;                                                                                                      // 7039
  }                                                                                                                    // 7040
                                                                                                                       // 7041
  // HISTORY                                                                                                           // 7042
                                                                                                                       // 7043
  function History(startGen) {                                                                                         // 7044
    // Arrays of change events and selections. Doing something adds an                                                 // 7045
    // event to done and clears undo. Undoing moves events from done                                                   // 7046
    // to undone, redoing moves them in the other direction.                                                           // 7047
    this.done = []; this.undone = [];                                                                                  // 7048
    this.undoDepth = Infinity;                                                                                         // 7049
    // Used to track when changes can be merged into a single undo                                                     // 7050
    // event                                                                                                           // 7051
    this.lastModTime = this.lastSelTime = 0;                                                                           // 7052
    this.lastOp = this.lastSelOp = null;                                                                               // 7053
    this.lastOrigin = this.lastSelOrigin = null;                                                                       // 7054
    // Used by the isClean() method                                                                                    // 7055
    this.generation = this.maxGeneration = startGen || 1;                                                              // 7056
  }                                                                                                                    // 7057
                                                                                                                       // 7058
  // Create a history change event from an updateDoc-style change                                                      // 7059
  // object.                                                                                                           // 7060
  function historyChangeFromChange(doc, change) {                                                                      // 7061
    var histChange = {from: copyPos(change.from), to: changeEnd(change), text: getBetween(doc, change.from, change.to)};
    attachLocalSpans(doc, histChange, change.from.line, change.to.line + 1);                                           // 7063
    linkedDocs(doc, function(doc) {attachLocalSpans(doc, histChange, change.from.line, change.to.line + 1);}, true);   // 7064
    return histChange;                                                                                                 // 7065
  }                                                                                                                    // 7066
                                                                                                                       // 7067
  // Pop all selection events off the end of a history array. Stop at                                                  // 7068
  // a change event.                                                                                                   // 7069
  function clearSelectionEvents(array) {                                                                               // 7070
    while (array.length) {                                                                                             // 7071
      var last = lst(array);                                                                                           // 7072
      if (last.ranges) array.pop();                                                                                    // 7073
      else break;                                                                                                      // 7074
    }                                                                                                                  // 7075
  }                                                                                                                    // 7076
                                                                                                                       // 7077
  // Find the top change event in the history. Pop off selection                                                       // 7078
  // events that are in the way.                                                                                       // 7079
  function lastChangeEvent(hist, force) {                                                                              // 7080
    if (force) {                                                                                                       // 7081
      clearSelectionEvents(hist.done);                                                                                 // 7082
      return lst(hist.done);                                                                                           // 7083
    } else if (hist.done.length && !lst(hist.done).ranges) {                                                           // 7084
      return lst(hist.done);                                                                                           // 7085
    } else if (hist.done.length > 1 && !hist.done[hist.done.length - 2].ranges) {                                      // 7086
      hist.done.pop();                                                                                                 // 7087
      return lst(hist.done);                                                                                           // 7088
    }                                                                                                                  // 7089
  }                                                                                                                    // 7090
                                                                                                                       // 7091
  // Register a change in the history. Merges changes that are within                                                  // 7092
  // a single operation, ore are close together with an origin that                                                    // 7093
  // allows merging (starting with "+") into a single event.                                                           // 7094
  function addChangeToHistory(doc, change, selAfter, opId) {                                                           // 7095
    var hist = doc.history;                                                                                            // 7096
    hist.undone.length = 0;                                                                                            // 7097
    var time = +new Date, cur;                                                                                         // 7098
                                                                                                                       // 7099
    if ((hist.lastOp == opId ||                                                                                        // 7100
         hist.lastOrigin == change.origin && change.origin &&                                                          // 7101
         ((change.origin.charAt(0) == "+" && doc.cm && hist.lastModTime > time - doc.cm.options.historyEventDelay) ||  // 7102
          change.origin.charAt(0) == "*")) &&                                                                          // 7103
        (cur = lastChangeEvent(hist, hist.lastOp == opId))) {                                                          // 7104
      // Merge this change into the last event                                                                         // 7105
      var last = lst(cur.changes);                                                                                     // 7106
      if (cmp(change.from, change.to) == 0 && cmp(change.from, last.to) == 0) {                                        // 7107
        // Optimized case for simple insertion -- don't want to add                                                    // 7108
        // new changesets for every character typed                                                                    // 7109
        last.to = changeEnd(change);                                                                                   // 7110
      } else {                                                                                                         // 7111
        // Add new sub-event                                                                                           // 7112
        cur.changes.push(historyChangeFromChange(doc, change));                                                        // 7113
      }                                                                                                                // 7114
    } else {                                                                                                           // 7115
      // Can not be merged, start a new event.                                                                         // 7116
      var before = lst(hist.done);                                                                                     // 7117
      if (!before || !before.ranges)                                                                                   // 7118
        pushSelectionToHistory(doc.sel, hist.done);                                                                    // 7119
      cur = {changes: [historyChangeFromChange(doc, change)],                                                          // 7120
             generation: hist.generation};                                                                             // 7121
      hist.done.push(cur);                                                                                             // 7122
      while (hist.done.length > hist.undoDepth) {                                                                      // 7123
        hist.done.shift();                                                                                             // 7124
        if (!hist.done[0].ranges) hist.done.shift();                                                                   // 7125
      }                                                                                                                // 7126
    }                                                                                                                  // 7127
    hist.done.push(selAfter);                                                                                          // 7128
    hist.generation = ++hist.maxGeneration;                                                                            // 7129
    hist.lastModTime = hist.lastSelTime = time;                                                                        // 7130
    hist.lastOp = hist.lastSelOp = opId;                                                                               // 7131
    hist.lastOrigin = hist.lastSelOrigin = change.origin;                                                              // 7132
                                                                                                                       // 7133
    if (!last) signal(doc, "historyAdded");                                                                            // 7134
  }                                                                                                                    // 7135
                                                                                                                       // 7136
  function selectionEventCanBeMerged(doc, origin, prev, sel) {                                                         // 7137
    var ch = origin.charAt(0);                                                                                         // 7138
    return ch == "*" ||                                                                                                // 7139
      ch == "+" &&                                                                                                     // 7140
      prev.ranges.length == sel.ranges.length &&                                                                       // 7141
      prev.somethingSelected() == sel.somethingSelected() &&                                                           // 7142
      new Date - doc.history.lastSelTime <= (doc.cm ? doc.cm.options.historyEventDelay : 500);                         // 7143
  }                                                                                                                    // 7144
                                                                                                                       // 7145
  // Called whenever the selection changes, sets the new selection as                                                  // 7146
  // the pending selection in the history, and pushes the old pending                                                  // 7147
  // selection into the 'done' array when it was significantly                                                         // 7148
  // different (in number of selected ranges, emptiness, or time).                                                     // 7149
  function addSelectionToHistory(doc, sel, opId, options) {                                                            // 7150
    var hist = doc.history, origin = options && options.origin;                                                        // 7151
                                                                                                                       // 7152
    // A new event is started when the previous origin does not match                                                  // 7153
    // the current, or the origins don't allow matching. Origins                                                       // 7154
    // starting with * are always merged, those starting with + are                                                    // 7155
    // merged when similar and close together in time.                                                                 // 7156
    if (opId == hist.lastSelOp ||                                                                                      // 7157
        (origin && hist.lastSelOrigin == origin &&                                                                     // 7158
         (hist.lastModTime == hist.lastSelTime && hist.lastOrigin == origin ||                                         // 7159
          selectionEventCanBeMerged(doc, origin, lst(hist.done), sel))))                                               // 7160
      hist.done[hist.done.length - 1] = sel;                                                                           // 7161
    else                                                                                                               // 7162
      pushSelectionToHistory(sel, hist.done);                                                                          // 7163
                                                                                                                       // 7164
    hist.lastSelTime = +new Date;                                                                                      // 7165
    hist.lastSelOrigin = origin;                                                                                       // 7166
    hist.lastSelOp = opId;                                                                                             // 7167
    if (options && options.clearRedo !== false)                                                                        // 7168
      clearSelectionEvents(hist.undone);                                                                               // 7169
  }                                                                                                                    // 7170
                                                                                                                       // 7171
  function pushSelectionToHistory(sel, dest) {                                                                         // 7172
    var top = lst(dest);                                                                                               // 7173
    if (!(top && top.ranges && top.equals(sel)))                                                                       // 7174
      dest.push(sel);                                                                                                  // 7175
  }                                                                                                                    // 7176
                                                                                                                       // 7177
  // Used to store marked span information in the history.                                                             // 7178
  function attachLocalSpans(doc, change, from, to) {                                                                   // 7179
    var existing = change["spans_" + doc.id], n = 0;                                                                   // 7180
    doc.iter(Math.max(doc.first, from), Math.min(doc.first + doc.size, to), function(line) {                           // 7181
      if (line.markedSpans)                                                                                            // 7182
        (existing || (existing = change["spans_" + doc.id] = {}))[n] = line.markedSpans;                               // 7183
      ++n;                                                                                                             // 7184
    });                                                                                                                // 7185
  }                                                                                                                    // 7186
                                                                                                                       // 7187
  // When un/re-doing restores text containing marked spans, those                                                     // 7188
  // that have been explicitly cleared should not be restored.                                                         // 7189
  function removeClearedSpans(spans) {                                                                                 // 7190
    if (!spans) return null;                                                                                           // 7191
    for (var i = 0, out; i < spans.length; ++i) {                                                                      // 7192
      if (spans[i].marker.explicitlyCleared) { if (!out) out = spans.slice(0, i); }                                    // 7193
      else if (out) out.push(spans[i]);                                                                                // 7194
    }                                                                                                                  // 7195
    return !out ? spans : out.length ? out : null;                                                                     // 7196
  }                                                                                                                    // 7197
                                                                                                                       // 7198
  // Retrieve and filter the old marked spans stored in a change event.                                                // 7199
  function getOldSpans(doc, change) {                                                                                  // 7200
    var found = change["spans_" + doc.id];                                                                             // 7201
    if (!found) return null;                                                                                           // 7202
    for (var i = 0, nw = []; i < change.text.length; ++i)                                                              // 7203
      nw.push(removeClearedSpans(found[i]));                                                                           // 7204
    return nw;                                                                                                         // 7205
  }                                                                                                                    // 7206
                                                                                                                       // 7207
  // Used both to provide a JSON-safe object in .getHistory, and, when                                                 // 7208
  // detaching a document, to split the history in two                                                                 // 7209
  function copyHistoryArray(events, newGroup, instantiateSel) {                                                        // 7210
    for (var i = 0, copy = []; i < events.length; ++i) {                                                               // 7211
      var event = events[i];                                                                                           // 7212
      if (event.ranges) {                                                                                              // 7213
        copy.push(instantiateSel ? Selection.prototype.deepCopy.call(event) : event);                                  // 7214
        continue;                                                                                                      // 7215
      }                                                                                                                // 7216
      var changes = event.changes, newChanges = [];                                                                    // 7217
      copy.push({changes: newChanges});                                                                                // 7218
      for (var j = 0; j < changes.length; ++j) {                                                                       // 7219
        var change = changes[j], m;                                                                                    // 7220
        newChanges.push({from: change.from, to: change.to, text: change.text});                                        // 7221
        if (newGroup) for (var prop in change) if (m = prop.match(/^spans_(\d+)$/)) {                                  // 7222
          if (indexOf(newGroup, Number(m[1])) > -1) {                                                                  // 7223
            lst(newChanges)[prop] = change[prop];                                                                      // 7224
            delete change[prop];                                                                                       // 7225
          }                                                                                                            // 7226
        }                                                                                                              // 7227
      }                                                                                                                // 7228
    }                                                                                                                  // 7229
    return copy;                                                                                                       // 7230
  }                                                                                                                    // 7231
                                                                                                                       // 7232
  // Rebasing/resetting history to deal with externally-sourced changes                                                // 7233
                                                                                                                       // 7234
  function rebaseHistSelSingle(pos, from, to, diff) {                                                                  // 7235
    if (to < pos.line) {                                                                                               // 7236
      pos.line += diff;                                                                                                // 7237
    } else if (from < pos.line) {                                                                                      // 7238
      pos.line = from;                                                                                                 // 7239
      pos.ch = 0;                                                                                                      // 7240
    }                                                                                                                  // 7241
  }                                                                                                                    // 7242
                                                                                                                       // 7243
  // Tries to rebase an array of history events given a change in the                                                  // 7244
  // document. If the change touches the same lines as the event, the                                                  // 7245
  // event, and everything 'behind' it, is discarded. If the change is                                                 // 7246
  // before the event, the event's positions are updated. Uses a                                                       // 7247
  // copy-on-write scheme for the positions, to avoid having to                                                        // 7248
  // reallocate them all on every rebase, but also avoid problems with                                                 // 7249
  // shared position objects being unsafely updated.                                                                   // 7250
  function rebaseHistArray(array, from, to, diff) {                                                                    // 7251
    for (var i = 0; i < array.length; ++i) {                                                                           // 7252
      var sub = array[i], ok = true;                                                                                   // 7253
      if (sub.ranges) {                                                                                                // 7254
        if (!sub.copied) { sub = array[i] = sub.deepCopy(); sub.copied = true; }                                       // 7255
        for (var j = 0; j < sub.ranges.length; j++) {                                                                  // 7256
          rebaseHistSelSingle(sub.ranges[j].anchor, from, to, diff);                                                   // 7257
          rebaseHistSelSingle(sub.ranges[j].head, from, to, diff);                                                     // 7258
        }                                                                                                              // 7259
        continue;                                                                                                      // 7260
      }                                                                                                                // 7261
      for (var j = 0; j < sub.changes.length; ++j) {                                                                   // 7262
        var cur = sub.changes[j];                                                                                      // 7263
        if (to < cur.from.line) {                                                                                      // 7264
          cur.from = Pos(cur.from.line + diff, cur.from.ch);                                                           // 7265
          cur.to = Pos(cur.to.line + diff, cur.to.ch);                                                                 // 7266
        } else if (from <= cur.to.line) {                                                                              // 7267
          ok = false;                                                                                                  // 7268
          break;                                                                                                       // 7269
        }                                                                                                              // 7270
      }                                                                                                                // 7271
      if (!ok) {                                                                                                       // 7272
        array.splice(0, i + 1);                                                                                        // 7273
        i = 0;                                                                                                         // 7274
      }                                                                                                                // 7275
    }                                                                                                                  // 7276
  }                                                                                                                    // 7277
                                                                                                                       // 7278
  function rebaseHist(hist, change) {                                                                                  // 7279
    var from = change.from.line, to = change.to.line, diff = change.text.length - (to - from) - 1;                     // 7280
    rebaseHistArray(hist.done, from, to, diff);                                                                        // 7281
    rebaseHistArray(hist.undone, from, to, diff);                                                                      // 7282
  }                                                                                                                    // 7283
                                                                                                                       // 7284
  // EVENT UTILITIES                                                                                                   // 7285
                                                                                                                       // 7286
  // Due to the fact that we still support jurassic IE versions, some                                                  // 7287
  // compatibility wrappers are needed.                                                                                // 7288
                                                                                                                       // 7289
  var e_preventDefault = CodeMirror.e_preventDefault = function(e) {                                                   // 7290
    if (e.preventDefault) e.preventDefault();                                                                          // 7291
    else e.returnValue = false;                                                                                        // 7292
  };                                                                                                                   // 7293
  var e_stopPropagation = CodeMirror.e_stopPropagation = function(e) {                                                 // 7294
    if (e.stopPropagation) e.stopPropagation();                                                                        // 7295
    else e.cancelBubble = true;                                                                                        // 7296
  };                                                                                                                   // 7297
  function e_defaultPrevented(e) {                                                                                     // 7298
    return e.defaultPrevented != null ? e.defaultPrevented : e.returnValue == false;                                   // 7299
  }                                                                                                                    // 7300
  var e_stop = CodeMirror.e_stop = function(e) {e_preventDefault(e); e_stopPropagation(e);};                           // 7301
                                                                                                                       // 7302
  function e_target(e) {return e.target || e.srcElement;}                                                              // 7303
  function e_button(e) {                                                                                               // 7304
    var b = e.which;                                                                                                   // 7305
    if (b == null) {                                                                                                   // 7306
      if (e.button & 1) b = 1;                                                                                         // 7307
      else if (e.button & 2) b = 3;                                                                                    // 7308
      else if (e.button & 4) b = 2;                                                                                    // 7309
    }                                                                                                                  // 7310
    if (mac && e.ctrlKey && b == 1) b = 3;                                                                             // 7311
    return b;                                                                                                          // 7312
  }                                                                                                                    // 7313
                                                                                                                       // 7314
  // EVENT HANDLING                                                                                                    // 7315
                                                                                                                       // 7316
  // Lightweight event framework. on/off also work on DOM nodes,                                                       // 7317
  // registering native DOM handlers.                                                                                  // 7318
                                                                                                                       // 7319
  var on = CodeMirror.on = function(emitter, type, f) {                                                                // 7320
    if (emitter.addEventListener)                                                                                      // 7321
      emitter.addEventListener(type, f, false);                                                                        // 7322
    else if (emitter.attachEvent)                                                                                      // 7323
      emitter.attachEvent("on" + type, f);                                                                             // 7324
    else {                                                                                                             // 7325
      var map = emitter._handlers || (emitter._handlers = {});                                                         // 7326
      var arr = map[type] || (map[type] = []);                                                                         // 7327
      arr.push(f);                                                                                                     // 7328
    }                                                                                                                  // 7329
  };                                                                                                                   // 7330
                                                                                                                       // 7331
  var off = CodeMirror.off = function(emitter, type, f) {                                                              // 7332
    if (emitter.removeEventListener)                                                                                   // 7333
      emitter.removeEventListener(type, f, false);                                                                     // 7334
    else if (emitter.detachEvent)                                                                                      // 7335
      emitter.detachEvent("on" + type, f);                                                                             // 7336
    else {                                                                                                             // 7337
      var arr = emitter._handlers && emitter._handlers[type];                                                          // 7338
      if (!arr) return;                                                                                                // 7339
      for (var i = 0; i < arr.length; ++i)                                                                             // 7340
        if (arr[i] == f) { arr.splice(i, 1); break; }                                                                  // 7341
    }                                                                                                                  // 7342
  };                                                                                                                   // 7343
                                                                                                                       // 7344
  var signal = CodeMirror.signal = function(emitter, type /*, values...*/) {                                           // 7345
    var arr = emitter._handlers && emitter._handlers[type];                                                            // 7346
    if (!arr) return;                                                                                                  // 7347
    var args = Array.prototype.slice.call(arguments, 2);                                                               // 7348
    for (var i = 0; i < arr.length; ++i) arr[i].apply(null, args);                                                     // 7349
  };                                                                                                                   // 7350
                                                                                                                       // 7351
  var orphanDelayedCallbacks = null;                                                                                   // 7352
                                                                                                                       // 7353
  // Often, we want to signal events at a point where we are in the                                                    // 7354
  // middle of some work, but don't want the handler to start calling                                                  // 7355
  // other methods on the editor, which might be in an inconsistent                                                    // 7356
  // state or simply not expect any other events to happen.                                                            // 7357
  // signalLater looks whether there are any handlers, and schedules                                                   // 7358
  // them to be executed when the last operation ends, or, if no                                                       // 7359
  // operation is active, when a timeout fires.                                                                        // 7360
  function signalLater(emitter, type /*, values...*/) {                                                                // 7361
    var arr = emitter._handlers && emitter._handlers[type];                                                            // 7362
    if (!arr) return;                                                                                                  // 7363
    var args = Array.prototype.slice.call(arguments, 2), list;                                                         // 7364
    if (operationGroup) {                                                                                              // 7365
      list = operationGroup.delayedCallbacks;                                                                          // 7366
    } else if (orphanDelayedCallbacks) {                                                                               // 7367
      list = orphanDelayedCallbacks;                                                                                   // 7368
    } else {                                                                                                           // 7369
      list = orphanDelayedCallbacks = [];                                                                              // 7370
      setTimeout(fireOrphanDelayed, 0);                                                                                // 7371
    }                                                                                                                  // 7372
    function bnd(f) {return function(){f.apply(null, args);};};                                                        // 7373
    for (var i = 0; i < arr.length; ++i)                                                                               // 7374
      list.push(bnd(arr[i]));                                                                                          // 7375
  }                                                                                                                    // 7376
                                                                                                                       // 7377
  function fireOrphanDelayed() {                                                                                       // 7378
    var delayed = orphanDelayedCallbacks;                                                                              // 7379
    orphanDelayedCallbacks = null;                                                                                     // 7380
    for (var i = 0; i < delayed.length; ++i) delayed[i]();                                                             // 7381
  }                                                                                                                    // 7382
                                                                                                                       // 7383
  // The DOM events that CodeMirror handles can be overridden by                                                       // 7384
  // registering a (non-DOM) handler on the editor for the event name,                                                 // 7385
  // and preventDefault-ing the event in that handler.                                                                 // 7386
  function signalDOMEvent(cm, e, override) {                                                                           // 7387
    if (typeof e == "string")                                                                                          // 7388
      e = {type: e, preventDefault: function() { this.defaultPrevented = true; }};                                     // 7389
    signal(cm, override || e.type, cm, e);                                                                             // 7390
    return e_defaultPrevented(e) || e.codemirrorIgnore;                                                                // 7391
  }                                                                                                                    // 7392
                                                                                                                       // 7393
  function signalCursorActivity(cm) {                                                                                  // 7394
    var arr = cm._handlers && cm._handlers.cursorActivity;                                                             // 7395
    if (!arr) return;                                                                                                  // 7396
    var set = cm.curOp.cursorActivityHandlers || (cm.curOp.cursorActivityHandlers = []);                               // 7397
    for (var i = 0; i < arr.length; ++i) if (indexOf(set, arr[i]) == -1)                                               // 7398
      set.push(arr[i]);                                                                                                // 7399
  }                                                                                                                    // 7400
                                                                                                                       // 7401
  function hasHandler(emitter, type) {                                                                                 // 7402
    var arr = emitter._handlers && emitter._handlers[type];                                                            // 7403
    return arr && arr.length > 0;                                                                                      // 7404
  }                                                                                                                    // 7405
                                                                                                                       // 7406
  // Add on and off methods to a constructor's prototype, to make                                                      // 7407
  // registering events on such objects more convenient.                                                               // 7408
  function eventMixin(ctor) {                                                                                          // 7409
    ctor.prototype.on = function(type, f) {on(this, type, f);};                                                        // 7410
    ctor.prototype.off = function(type, f) {off(this, type, f);};                                                      // 7411
  }                                                                                                                    // 7412
                                                                                                                       // 7413
  // MISC UTILITIES                                                                                                    // 7414
                                                                                                                       // 7415
  // Number of pixels added to scroller and sizer to hide scrollbar                                                    // 7416
  var scrollerGap = 30;                                                                                                // 7417
                                                                                                                       // 7418
  // Returned or thrown by various protocols to signal 'I'm not                                                        // 7419
  // handling this'.                                                                                                   // 7420
  var Pass = CodeMirror.Pass = {toString: function(){return "CodeMirror.Pass";}};                                      // 7421
                                                                                                                       // 7422
  // Reused option objects for setSelection & friends                                                                  // 7423
  var sel_dontScroll = {scroll: false}, sel_mouse = {origin: "*mouse"}, sel_move = {origin: "+move"};                  // 7424
                                                                                                                       // 7425
  function Delayed() {this.id = null;}                                                                                 // 7426
  Delayed.prototype.set = function(ms, f) {                                                                            // 7427
    clearTimeout(this.id);                                                                                             // 7428
    this.id = setTimeout(f, ms);                                                                                       // 7429
  };                                                                                                                   // 7430
                                                                                                                       // 7431
  // Counts the column offset in a string, taking tabs into account.                                                   // 7432
  // Used mostly to find indentation.                                                                                  // 7433
  var countColumn = CodeMirror.countColumn = function(string, end, tabSize, startIndex, startValue) {                  // 7434
    if (end == null) {                                                                                                 // 7435
      end = string.search(/[^\s\u00a0]/);                                                                              // 7436
      if (end == -1) end = string.length;                                                                              // 7437
    }                                                                                                                  // 7438
    for (var i = startIndex || 0, n = startValue || 0;;) {                                                             // 7439
      var nextTab = string.indexOf("\t", i);                                                                           // 7440
      if (nextTab < 0 || nextTab >= end)                                                                               // 7441
        return n + (end - i);                                                                                          // 7442
      n += nextTab - i;                                                                                                // 7443
      n += tabSize - (n % tabSize);                                                                                    // 7444
      i = nextTab + 1;                                                                                                 // 7445
    }                                                                                                                  // 7446
  };                                                                                                                   // 7447
                                                                                                                       // 7448
  // The inverse of countColumn -- find the offset that corresponds to                                                 // 7449
  // a particular column.                                                                                              // 7450
  function findColumn(string, goal, tabSize) {                                                                         // 7451
    for (var pos = 0, col = 0;;) {                                                                                     // 7452
      var nextTab = string.indexOf("\t", pos);                                                                         // 7453
      if (nextTab == -1) nextTab = string.length;                                                                      // 7454
      var skipped = nextTab - pos;                                                                                     // 7455
      if (nextTab == string.length || col + skipped >= goal)                                                           // 7456
        return pos + Math.min(skipped, goal - col);                                                                    // 7457
      col += nextTab - pos;                                                                                            // 7458
      col += tabSize - (col % tabSize);                                                                                // 7459
      pos = nextTab + 1;                                                                                               // 7460
      if (col >= goal) return pos;                                                                                     // 7461
    }                                                                                                                  // 7462
  }                                                                                                                    // 7463
                                                                                                                       // 7464
  var spaceStrs = [""];                                                                                                // 7465
  function spaceStr(n) {                                                                                               // 7466
    while (spaceStrs.length <= n)                                                                                      // 7467
      spaceStrs.push(lst(spaceStrs) + " ");                                                                            // 7468
    return spaceStrs[n];                                                                                               // 7469
  }                                                                                                                    // 7470
                                                                                                                       // 7471
  function lst(arr) { return arr[arr.length-1]; }                                                                      // 7472
                                                                                                                       // 7473
  var selectInput = function(node) { node.select(); };                                                                 // 7474
  if (ios) // Mobile Safari apparently has a bug where select() is broken.                                             // 7475
    selectInput = function(node) { node.selectionStart = 0; node.selectionEnd = node.value.length; };                  // 7476
  else if (ie) // Suppress mysterious IE10 errors                                                                      // 7477
    selectInput = function(node) { try { node.select(); } catch(_e) {} };                                              // 7478
                                                                                                                       // 7479
  function indexOf(array, elt) {                                                                                       // 7480
    for (var i = 0; i < array.length; ++i)                                                                             // 7481
      if (array[i] == elt) return i;                                                                                   // 7482
    return -1;                                                                                                         // 7483
  }                                                                                                                    // 7484
  function map(array, f) {                                                                                             // 7485
    var out = [];                                                                                                      // 7486
    for (var i = 0; i < array.length; i++) out[i] = f(array[i], i);                                                    // 7487
    return out;                                                                                                        // 7488
  }                                                                                                                    // 7489
                                                                                                                       // 7490
  function createObj(base, props) {                                                                                    // 7491
    var inst;                                                                                                          // 7492
    if (Object.create) {                                                                                               // 7493
      inst = Object.create(base);                                                                                      // 7494
    } else {                                                                                                           // 7495
      var ctor = function() {};                                                                                        // 7496
      ctor.prototype = base;                                                                                           // 7497
      inst = new ctor();                                                                                               // 7498
    }                                                                                                                  // 7499
    if (props) copyObj(props, inst);                                                                                   // 7500
    return inst;                                                                                                       // 7501
  };                                                                                                                   // 7502
                                                                                                                       // 7503
  function copyObj(obj, target, overwrite) {                                                                           // 7504
    if (!target) target = {};                                                                                          // 7505
    for (var prop in obj)                                                                                              // 7506
      if (obj.hasOwnProperty(prop) && (overwrite !== false || !target.hasOwnProperty(prop)))                           // 7507
        target[prop] = obj[prop];                                                                                      // 7508
    return target;                                                                                                     // 7509
  }                                                                                                                    // 7510
                                                                                                                       // 7511
  function bind(f) {                                                                                                   // 7512
    var args = Array.prototype.slice.call(arguments, 1);                                                               // 7513
    return function(){return f.apply(null, args);};                                                                    // 7514
  }                                                                                                                    // 7515
                                                                                                                       // 7516
  var nonASCIISingleCaseWordChar = /[\u00df\u0590-\u05f4\u0600-\u06ff\u3040-\u309f\u30a0-\u30ff\u3400-\u4db5\u4e00-\u9fcc\uac00-\ud7af]/;
  var isWordCharBasic = CodeMirror.isWordChar = function(ch) {                                                         // 7518
    return /\w/.test(ch) || ch > "\x80" &&                                                                             // 7519
      (ch.toUpperCase() != ch.toLowerCase() || nonASCIISingleCaseWordChar.test(ch));                                   // 7520
  };                                                                                                                   // 7521
  function isWordChar(ch, helper) {                                                                                    // 7522
    if (!helper) return isWordCharBasic(ch);                                                                           // 7523
    if (helper.source.indexOf("\\w") > -1 && isWordCharBasic(ch)) return true;                                         // 7524
    return helper.test(ch);                                                                                            // 7525
  }                                                                                                                    // 7526
                                                                                                                       // 7527
  function isEmpty(obj) {                                                                                              // 7528
    for (var n in obj) if (obj.hasOwnProperty(n) && obj[n]) return false;                                              // 7529
    return true;                                                                                                       // 7530
  }                                                                                                                    // 7531
                                                                                                                       // 7532
  // Extending unicode characters. A series of a non-extending char +                                                  // 7533
  // any number of extending chars is treated as a single unit as far                                                  // 7534
  // as editing and measuring is concerned. This is not fully correct,                                                 // 7535
  // since some scripts/fonts/browsers also treat other configurations                                                 // 7536
  // of code points as a group.                                                                                        // 7537
  var extendingChars = /[\u0300-\u036f\u0483-\u0489\u0591-\u05bd\u05bf\u05c1\u05c2\u05c4\u05c5\u05c7\u0610-\u061a\u064b-\u065e\u0670\u06d6-\u06dc\u06de-\u06e4\u06e7\u06e8\u06ea-\u06ed\u0711\u0730-\u074a\u07a6-\u07b0\u07eb-\u07f3\u0816-\u0819\u081b-\u0823\u0825-\u0827\u0829-\u082d\u0900-\u0902\u093c\u0941-\u0948\u094d\u0951-\u0955\u0962\u0963\u0981\u09bc\u09be\u09c1-\u09c4\u09cd\u09d7\u09e2\u09e3\u0a01\u0a02\u0a3c\u0a41\u0a42\u0a47\u0a48\u0a4b-\u0a4d\u0a51\u0a70\u0a71\u0a75\u0a81\u0a82\u0abc\u0ac1-\u0ac5\u0ac7\u0ac8\u0acd\u0ae2\u0ae3\u0b01\u0b3c\u0b3e\u0b3f\u0b41-\u0b44\u0b4d\u0b56\u0b57\u0b62\u0b63\u0b82\u0bbe\u0bc0\u0bcd\u0bd7\u0c3e-\u0c40\u0c46-\u0c48\u0c4a-\u0c4d\u0c55\u0c56\u0c62\u0c63\u0cbc\u0cbf\u0cc2\u0cc6\u0ccc\u0ccd\u0cd5\u0cd6\u0ce2\u0ce3\u0d3e\u0d41-\u0d44\u0d4d\u0d57\u0d62\u0d63\u0dca\u0dcf\u0dd2-\u0dd4\u0dd6\u0ddf\u0e31\u0e34-\u0e3a\u0e47-\u0e4e\u0eb1\u0eb4-\u0eb9\u0ebb\u0ebc\u0ec8-\u0ecd\u0f18\u0f19\u0f35\u0f37\u0f39\u0f71-\u0f7e\u0f80-\u0f84\u0f86\u0f87\u0f90-\u0f97\u0f99-\u0fbc\u0fc6\u102d-\u1030\u1032-\u1037\u1039\u103a\u103d\u103e\u1058\u1059\u105e-\u1060\u1071-\u1074\u1082\u1085\u1086\u108d\u109d\u135f\u1712-\u1714\u1732-\u1734\u1752\u1753\u1772\u1773\u17b7-\u17bd\u17c6\u17c9-\u17d3\u17dd\u180b-\u180d\u18a9\u1920-\u1922\u1927\u1928\u1932\u1939-\u193b\u1a17\u1a18\u1a56\u1a58-\u1a5e\u1a60\u1a62\u1a65-\u1a6c\u1a73-\u1a7c\u1a7f\u1b00-\u1b03\u1b34\u1b36-\u1b3a\u1b3c\u1b42\u1b6b-\u1b73\u1b80\u1b81\u1ba2-\u1ba5\u1ba8\u1ba9\u1c2c-\u1c33\u1c36\u1c37\u1cd0-\u1cd2\u1cd4-\u1ce0\u1ce2-\u1ce8\u1ced\u1dc0-\u1de6\u1dfd-\u1dff\u200c\u200d\u20d0-\u20f0\u2cef-\u2cf1\u2de0-\u2dff\u302a-\u302f\u3099\u309a\ua66f-\ua672\ua67c\ua67d\ua6f0\ua6f1\ua802\ua806\ua80b\ua825\ua826\ua8c4\ua8e0-\ua8f1\ua926-\ua92d\ua947-\ua951\ua980-\ua982\ua9b3\ua9b6-\ua9b9\ua9bc\uaa29-\uaa2e\uaa31\uaa32\uaa35\uaa36\uaa43\uaa4c\uaab0\uaab2-\uaab4\uaab7\uaab8\uaabe\uaabf\uaac1\uabe5\uabe8\uabed\udc00-\udfff\ufb1e\ufe00-\ufe0f\ufe20-\ufe26\uff9e\uff9f]/;
  function isExtendingChar(ch) { return ch.charCodeAt(0) >= 768 && extendingChars.test(ch); }                          // 7539
                                                                                                                       // 7540
  // DOM UTILITIES                                                                                                     // 7541
                                                                                                                       // 7542
  function elt(tag, content, className, style) {                                                                       // 7543
    var e = document.createElement(tag);                                                                               // 7544
    if (className) e.className = className;                                                                            // 7545
    if (style) e.style.cssText = style;                                                                                // 7546
    if (typeof content == "string") e.appendChild(document.createTextNode(content));                                   // 7547
    else if (content) for (var i = 0; i < content.length; ++i) e.appendChild(content[i]);                              // 7548
    return e;                                                                                                          // 7549
  }                                                                                                                    // 7550
                                                                                                                       // 7551
  var range;                                                                                                           // 7552
  if (document.createRange) range = function(node, start, end) {                                                       // 7553
    var r = document.createRange();                                                                                    // 7554
    r.setEnd(node, end);                                                                                               // 7555
    r.setStart(node, start);                                                                                           // 7556
    return r;                                                                                                          // 7557
  };                                                                                                                   // 7558
  else range = function(node, start, end) {                                                                            // 7559
    var r = document.body.createTextRange();                                                                           // 7560
    try { r.moveToElementText(node.parentNode); }                                                                      // 7561
    catch(e) { return r; }                                                                                             // 7562
    r.collapse(true);                                                                                                  // 7563
    r.moveEnd("character", end);                                                                                       // 7564
    r.moveStart("character", start);                                                                                   // 7565
    return r;                                                                                                          // 7566
  };                                                                                                                   // 7567
                                                                                                                       // 7568
  function removeChildren(e) {                                                                                         // 7569
    for (var count = e.childNodes.length; count > 0; --count)                                                          // 7570
      e.removeChild(e.firstChild);                                                                                     // 7571
    return e;                                                                                                          // 7572
  }                                                                                                                    // 7573
                                                                                                                       // 7574
  function removeChildrenAndAdd(parent, e) {                                                                           // 7575
    return removeChildren(parent).appendChild(e);                                                                      // 7576
  }                                                                                                                    // 7577
                                                                                                                       // 7578
  function contains(parent, child) {                                                                                   // 7579
    if (parent.contains)                                                                                               // 7580
      return parent.contains(child);                                                                                   // 7581
    while (child = child.parentNode)                                                                                   // 7582
      if (child == parent) return true;                                                                                // 7583
  }                                                                                                                    // 7584
                                                                                                                       // 7585
  function activeElt() { return document.activeElement; }                                                              // 7586
  // Older versions of IE throws unspecified error when touching                                                       // 7587
  // document.activeElement in some cases (during loading, in iframe)                                                  // 7588
  if (ie && ie_version < 11) activeElt = function() {                                                                  // 7589
    try { return document.activeElement; }                                                                             // 7590
    catch(e) { return document.body; }                                                                                 // 7591
  };                                                                                                                   // 7592
                                                                                                                       // 7593
  function classTest(cls) { return new RegExp("(^|\\s)" + cls + "(?:$|\\s)\\s*"); }                                    // 7594
  var rmClass = CodeMirror.rmClass = function(node, cls) {                                                             // 7595
    var current = node.className;                                                                                      // 7596
    var match = classTest(cls).exec(current);                                                                          // 7597
    if (match) {                                                                                                       // 7598
      var after = current.slice(match.index + match[0].length);                                                        // 7599
      node.className = current.slice(0, match.index) + (after ? match[1] + after : "");                                // 7600
    }                                                                                                                  // 7601
  };                                                                                                                   // 7602
  var addClass = CodeMirror.addClass = function(node, cls) {                                                           // 7603
    var current = node.className;                                                                                      // 7604
    if (!classTest(cls).test(current)) node.className += (current ? " " : "") + cls;                                   // 7605
  };                                                                                                                   // 7606
  function joinClasses(a, b) {                                                                                         // 7607
    var as = a.split(" ");                                                                                             // 7608
    for (var i = 0; i < as.length; i++)                                                                                // 7609
      if (as[i] && !classTest(as[i]).test(b)) b += " " + as[i];                                                        // 7610
    return b;                                                                                                          // 7611
  }                                                                                                                    // 7612
                                                                                                                       // 7613
  // WINDOW-WIDE EVENTS                                                                                                // 7614
                                                                                                                       // 7615
  // These must be handled carefully, because naively registering a                                                    // 7616
  // handler for each editor will cause the editors to never be                                                        // 7617
  // garbage collected.                                                                                                // 7618
                                                                                                                       // 7619
  function forEachCodeMirror(f) {                                                                                      // 7620
    if (!document.body.getElementsByClassName) return;                                                                 // 7621
    var byClass = document.body.getElementsByClassName("CodeMirror");                                                  // 7622
    for (var i = 0; i < byClass.length; i++) {                                                                         // 7623
      var cm = byClass[i].CodeMirror;                                                                                  // 7624
      if (cm) f(cm);                                                                                                   // 7625
    }                                                                                                                  // 7626
  }                                                                                                                    // 7627
                                                                                                                       // 7628
  var globalsRegistered = false;                                                                                       // 7629
  function ensureGlobalHandlers() {                                                                                    // 7630
    if (globalsRegistered) return;                                                                                     // 7631
    registerGlobalHandlers();                                                                                          // 7632
    globalsRegistered = true;                                                                                          // 7633
  }                                                                                                                    // 7634
  function registerGlobalHandlers() {                                                                                  // 7635
    // When the window resizes, we need to refresh active editors.                                                     // 7636
    var resizeTimer;                                                                                                   // 7637
    on(window, "resize", function() {                                                                                  // 7638
      if (resizeTimer == null) resizeTimer = setTimeout(function() {                                                   // 7639
        resizeTimer = null;                                                                                            // 7640
        forEachCodeMirror(onResize);                                                                                   // 7641
      }, 100);                                                                                                         // 7642
    });                                                                                                                // 7643
    // When the window loses focus, we want to show the editor as blurred                                              // 7644
    on(window, "blur", function() {                                                                                    // 7645
      forEachCodeMirror(onBlur);                                                                                       // 7646
    });                                                                                                                // 7647
  }                                                                                                                    // 7648
                                                                                                                       // 7649
  // FEATURE DETECTION                                                                                                 // 7650
                                                                                                                       // 7651
  // Detect drag-and-drop                                                                                              // 7652
  var dragAndDrop = function() {                                                                                       // 7653
    // There is *some* kind of drag-and-drop support in IE6-8, but I                                                   // 7654
    // couldn't get it to work yet.                                                                                    // 7655
    if (ie && ie_version < 9) return false;                                                                            // 7656
    var div = elt('div');                                                                                              // 7657
    return "draggable" in div || "dragDrop" in div;                                                                    // 7658
  }();                                                                                                                 // 7659
                                                                                                                       // 7660
  var zwspSupported;                                                                                                   // 7661
  function zeroWidthElement(measure) {                                                                                 // 7662
    if (zwspSupported == null) {                                                                                       // 7663
      var test = elt("span", "\u200b");                                                                                // 7664
      removeChildrenAndAdd(measure, elt("span", [test, document.createTextNode("x")]));                                // 7665
      if (measure.firstChild.offsetHeight != 0)                                                                        // 7666
        zwspSupported = test.offsetWidth <= 1 && test.offsetHeight > 2 && !(ie && ie_version < 8);                     // 7667
    }                                                                                                                  // 7668
    if (zwspSupported) return elt("span", "\u200b");                                                                   // 7669
    else return elt("span", "\u00a0", null, "display: inline-block; width: 1px; margin-right: -1px");                  // 7670
  }                                                                                                                    // 7671
                                                                                                                       // 7672
  // Feature-detect IE's crummy client rect reporting for bidi text                                                    // 7673
  var badBidiRects;                                                                                                    // 7674
  function hasBadBidiRects(measure) {                                                                                  // 7675
    if (badBidiRects != null) return badBidiRects;                                                                     // 7676
    var txt = removeChildrenAndAdd(measure, document.createTextNode("A\u062eA"));                                      // 7677
    var r0 = range(txt, 0, 1).getBoundingClientRect();                                                                 // 7678
    if (!r0 || r0.left == r0.right) return false; // Safari returns null in some cases (#2780)                         // 7679
    var r1 = range(txt, 1, 2).getBoundingClientRect();                                                                 // 7680
    return badBidiRects = (r1.right - r0.right < 3);                                                                   // 7681
  }                                                                                                                    // 7682
                                                                                                                       // 7683
  // See if "".split is the broken IE version, if so, provide an                                                       // 7684
  // alternative way to split lines.                                                                                   // 7685
  var splitLines = CodeMirror.splitLines = "\n\nb".split(/\n/).length != 3 ? function(string) {                        // 7686
    var pos = 0, result = [], l = string.length;                                                                       // 7687
    while (pos <= l) {                                                                                                 // 7688
      var nl = string.indexOf("\n", pos);                                                                              // 7689
      if (nl == -1) nl = string.length;                                                                                // 7690
      var line = string.slice(pos, string.charAt(nl - 1) == "\r" ? nl - 1 : nl);                                       // 7691
      var rt = line.indexOf("\r");                                                                                     // 7692
      if (rt != -1) {                                                                                                  // 7693
        result.push(line.slice(0, rt));                                                                                // 7694
        pos += rt + 1;                                                                                                 // 7695
      } else {                                                                                                         // 7696
        result.push(line);                                                                                             // 7697
        pos = nl + 1;                                                                                                  // 7698
      }                                                                                                                // 7699
    }                                                                                                                  // 7700
    return result;                                                                                                     // 7701
  } : function(string){return string.split(/\r\n?|\n/);};                                                              // 7702
                                                                                                                       // 7703
  var hasSelection = window.getSelection ? function(te) {                                                              // 7704
    try { return te.selectionStart != te.selectionEnd; }                                                               // 7705
    catch(e) { return false; }                                                                                         // 7706
  } : function(te) {                                                                                                   // 7707
    try {var range = te.ownerDocument.selection.createRange();}                                                        // 7708
    catch(e) {}                                                                                                        // 7709
    if (!range || range.parentElement() != te) return false;                                                           // 7710
    return range.compareEndPoints("StartToEnd", range) != 0;                                                           // 7711
  };                                                                                                                   // 7712
                                                                                                                       // 7713
  var hasCopyEvent = (function() {                                                                                     // 7714
    var e = elt("div");                                                                                                // 7715
    if ("oncopy" in e) return true;                                                                                    // 7716
    e.setAttribute("oncopy", "return;");                                                                               // 7717
    return typeof e.oncopy == "function";                                                                              // 7718
  })();                                                                                                                // 7719
                                                                                                                       // 7720
  var badZoomedRects = null;                                                                                           // 7721
  function hasBadZoomedRects(measure) {                                                                                // 7722
    if (badZoomedRects != null) return badZoomedRects;                                                                 // 7723
    var node = removeChildrenAndAdd(measure, elt("span", "x"));                                                        // 7724
    var normal = node.getBoundingClientRect();                                                                         // 7725
    var fromRange = range(node, 0, 1).getBoundingClientRect();                                                         // 7726
    return badZoomedRects = Math.abs(normal.left - fromRange.left) > 1;                                                // 7727
  }                                                                                                                    // 7728
                                                                                                                       // 7729
  // KEY NAMES                                                                                                         // 7730
                                                                                                                       // 7731
  var keyNames = {3: "Enter", 8: "Backspace", 9: "Tab", 13: "Enter", 16: "Shift", 17: "Ctrl", 18: "Alt",               // 7732
                  19: "Pause", 20: "CapsLock", 27: "Esc", 32: "Space", 33: "PageUp", 34: "PageDown", 35: "End",        // 7733
                  36: "Home", 37: "Left", 38: "Up", 39: "Right", 40: "Down", 44: "PrintScrn", 45: "Insert",            // 7734
                  46: "Delete", 59: ";", 61: "=", 91: "Mod", 92: "Mod", 93: "Mod", 107: "=", 109: "-", 127: "Delete",  // 7735
                  173: "-", 186: ";", 187: "=", 188: ",", 189: "-", 190: ".", 191: "/", 192: "`", 219: "[", 220: "\\", // 7736
                  221: "]", 222: "'", 63232: "Up", 63233: "Down", 63234: "Left", 63235: "Right", 63272: "Delete",      // 7737
                  63273: "Home", 63275: "End", 63276: "PageUp", 63277: "PageDown", 63302: "Insert"};                   // 7738
  CodeMirror.keyNames = keyNames;                                                                                      // 7739
  (function() {                                                                                                        // 7740
    // Number keys                                                                                                     // 7741
    for (var i = 0; i < 10; i++) keyNames[i + 48] = keyNames[i + 96] = String(i);                                      // 7742
    // Alphabetic keys                                                                                                 // 7743
    for (var i = 65; i <= 90; i++) keyNames[i] = String.fromCharCode(i);                                               // 7744
    // Function keys                                                                                                   // 7745
    for (var i = 1; i <= 12; i++) keyNames[i + 111] = keyNames[i + 63235] = "F" + i;                                   // 7746
  })();                                                                                                                // 7747
                                                                                                                       // 7748
  // BIDI HELPERS                                                                                                      // 7749
                                                                                                                       // 7750
  function iterateBidiSections(order, from, to, f) {                                                                   // 7751
    if (!order) return f(from, to, "ltr");                                                                             // 7752
    var found = false;                                                                                                 // 7753
    for (var i = 0; i < order.length; ++i) {                                                                           // 7754
      var part = order[i];                                                                                             // 7755
      if (part.from < to && part.to > from || from == to && part.to == from) {                                         // 7756
        f(Math.max(part.from, from), Math.min(part.to, to), part.level == 1 ? "rtl" : "ltr");                          // 7757
        found = true;                                                                                                  // 7758
      }                                                                                                                // 7759
    }                                                                                                                  // 7760
    if (!found) f(from, to, "ltr");                                                                                    // 7761
  }                                                                                                                    // 7762
                                                                                                                       // 7763
  function bidiLeft(part) { return part.level % 2 ? part.to : part.from; }                                             // 7764
  function bidiRight(part) { return part.level % 2 ? part.from : part.to; }                                            // 7765
                                                                                                                       // 7766
  function lineLeft(line) { var order = getOrder(line); return order ? bidiLeft(order[0]) : 0; }                       // 7767
  function lineRight(line) {                                                                                           // 7768
    var order = getOrder(line);                                                                                        // 7769
    if (!order) return line.text.length;                                                                               // 7770
    return bidiRight(lst(order));                                                                                      // 7771
  }                                                                                                                    // 7772
                                                                                                                       // 7773
  function lineStart(cm, lineN) {                                                                                      // 7774
    var line = getLine(cm.doc, lineN);                                                                                 // 7775
    var visual = visualLine(line);                                                                                     // 7776
    if (visual != line) lineN = lineNo(visual);                                                                        // 7777
    var order = getOrder(visual);                                                                                      // 7778
    var ch = !order ? 0 : order[0].level % 2 ? lineRight(visual) : lineLeft(visual);                                   // 7779
    return Pos(lineN, ch);                                                                                             // 7780
  }                                                                                                                    // 7781
  function lineEnd(cm, lineN) {                                                                                        // 7782
    var merged, line = getLine(cm.doc, lineN);                                                                         // 7783
    while (merged = collapsedSpanAtEnd(line)) {                                                                        // 7784
      line = merged.find(1, true).line;                                                                                // 7785
      lineN = null;                                                                                                    // 7786
    }                                                                                                                  // 7787
    var order = getOrder(line);                                                                                        // 7788
    var ch = !order ? line.text.length : order[0].level % 2 ? lineLeft(line) : lineRight(line);                        // 7789
    return Pos(lineN == null ? lineNo(line) : lineN, ch);                                                              // 7790
  }                                                                                                                    // 7791
  function lineStartSmart(cm, pos) {                                                                                   // 7792
    var start = lineStart(cm, pos.line);                                                                               // 7793
    var line = getLine(cm.doc, start.line);                                                                            // 7794
    var order = getOrder(line);                                                                                        // 7795
    if (!order || order[0].level == 0) {                                                                               // 7796
      var firstNonWS = Math.max(0, line.text.search(/\S/));                                                            // 7797
      var inWS = pos.line == start.line && pos.ch <= firstNonWS && pos.ch;                                             // 7798
      return Pos(start.line, inWS ? 0 : firstNonWS);                                                                   // 7799
    }                                                                                                                  // 7800
    return start;                                                                                                      // 7801
  }                                                                                                                    // 7802
                                                                                                                       // 7803
  function compareBidiLevel(order, a, b) {                                                                             // 7804
    var linedir = order[0].level;                                                                                      // 7805
    if (a == linedir) return true;                                                                                     // 7806
    if (b == linedir) return false;                                                                                    // 7807
    return a < b;                                                                                                      // 7808
  }                                                                                                                    // 7809
  var bidiOther;                                                                                                       // 7810
  function getBidiPartAt(order, pos) {                                                                                 // 7811
    bidiOther = null;                                                                                                  // 7812
    for (var i = 0, found; i < order.length; ++i) {                                                                    // 7813
      var cur = order[i];                                                                                              // 7814
      if (cur.from < pos && cur.to > pos) return i;                                                                    // 7815
      if ((cur.from == pos || cur.to == pos)) {                                                                        // 7816
        if (found == null) {                                                                                           // 7817
          found = i;                                                                                                   // 7818
        } else if (compareBidiLevel(order, cur.level, order[found].level)) {                                           // 7819
          if (cur.from != cur.to) bidiOther = found;                                                                   // 7820
          return i;                                                                                                    // 7821
        } else {                                                                                                       // 7822
          if (cur.from != cur.to) bidiOther = i;                                                                       // 7823
          return found;                                                                                                // 7824
        }                                                                                                              // 7825
      }                                                                                                                // 7826
    }                                                                                                                  // 7827
    return found;                                                                                                      // 7828
  }                                                                                                                    // 7829
                                                                                                                       // 7830
  function moveInLine(line, pos, dir, byUnit) {                                                                        // 7831
    if (!byUnit) return pos + dir;                                                                                     // 7832
    do pos += dir;                                                                                                     // 7833
    while (pos > 0 && isExtendingChar(line.text.charAt(pos)));                                                         // 7834
    return pos;                                                                                                        // 7835
  }                                                                                                                    // 7836
                                                                                                                       // 7837
  // This is needed in order to move 'visually' through bi-directional                                                 // 7838
  // text -- i.e., pressing left should make the cursor go left, even                                                  // 7839
  // when in RTL text. The tricky part is the 'jumps', where RTL and                                                   // 7840
  // LTR text touch each other. This often requires the cursor offset                                                  // 7841
  // to move more than one unit, in order to visually move one unit.                                                   // 7842
  function moveVisually(line, start, dir, byUnit) {                                                                    // 7843
    var bidi = getOrder(line);                                                                                         // 7844
    if (!bidi) return moveLogically(line, start, dir, byUnit);                                                         // 7845
    var pos = getBidiPartAt(bidi, start), part = bidi[pos];                                                            // 7846
    var target = moveInLine(line, start, part.level % 2 ? -dir : dir, byUnit);                                         // 7847
                                                                                                                       // 7848
    for (;;) {                                                                                                         // 7849
      if (target > part.from && target < part.to) return target;                                                       // 7850
      if (target == part.from || target == part.to) {                                                                  // 7851
        if (getBidiPartAt(bidi, target) == pos) return target;                                                         // 7852
        part = bidi[pos += dir];                                                                                       // 7853
        return (dir > 0) == part.level % 2 ? part.to : part.from;                                                      // 7854
      } else {                                                                                                         // 7855
        part = bidi[pos += dir];                                                                                       // 7856
        if (!part) return null;                                                                                        // 7857
        if ((dir > 0) == part.level % 2)                                                                               // 7858
          target = moveInLine(line, part.to, -1, byUnit);                                                              // 7859
        else                                                                                                           // 7860
          target = moveInLine(line, part.from, 1, byUnit);                                                             // 7861
      }                                                                                                                // 7862
    }                                                                                                                  // 7863
  }                                                                                                                    // 7864
                                                                                                                       // 7865
  function moveLogically(line, start, dir, byUnit) {                                                                   // 7866
    var target = start + dir;                                                                                          // 7867
    if (byUnit) while (target > 0 && isExtendingChar(line.text.charAt(target))) target += dir;                         // 7868
    return target < 0 || target > line.text.length ? null : target;                                                    // 7869
  }                                                                                                                    // 7870
                                                                                                                       // 7871
  // Bidirectional ordering algorithm                                                                                  // 7872
  // See http://unicode.org/reports/tr9/tr9-13.html for the algorithm                                                  // 7873
  // that this (partially) implements.                                                                                 // 7874
                                                                                                                       // 7875
  // One-char codes used for character types:                                                                          // 7876
  // L (L):   Left-to-Right                                                                                            // 7877
  // R (R):   Right-to-Left                                                                                            // 7878
  // r (AL):  Right-to-Left Arabic                                                                                     // 7879
  // 1 (EN):  European Number                                                                                          // 7880
  // + (ES):  European Number Separator                                                                                // 7881
  // % (ET):  European Number Terminator                                                                               // 7882
  // n (AN):  Arabic Number                                                                                            // 7883
  // , (CS):  Common Number Separator                                                                                  // 7884
  // m (NSM): Non-Spacing Mark                                                                                         // 7885
  // b (BN):  Boundary Neutral                                                                                         // 7886
  // s (B):   Paragraph Separator                                                                                      // 7887
  // t (S):   Segment Separator                                                                                        // 7888
  // w (WS):  Whitespace                                                                                               // 7889
  // N (ON):  Other Neutrals                                                                                           // 7890
                                                                                                                       // 7891
  // Returns null if characters are ordered as they appear                                                             // 7892
  // (left-to-right), or an array of sections ({from, to, level}                                                       // 7893
  // objects) in the order in which they occur visually.                                                               // 7894
  var bidiOrdering = (function() {                                                                                     // 7895
    // Character types for codepoints 0 to 0xff                                                                        // 7896
    var lowTypes = "bbbbbbbbbtstwsbbbbbbbbbbbbbbssstwNN%%%NNNNNN,N,N1111111111NNNNNNNLLLLLLLLLLLLLLLLLLLLLLLLLLNNNNNNLLLLLLLLLLLLLLLLLLLLLLLLLLNNNNbbbbbbsbbbbbbbbbbbbbbbbbbbbbbbbbb,N%%%%NNNNLNNNNN%%11NLNNN1LNNNNNLLLLLLLLLLLLLLLLLLLLLLLNLLLLLLLLLLLLLLLLLLLLLLLLLLLLLLLN";
    // Character types for codepoints 0x600 to 0x6ff                                                                   // 7898
    var arabicTypes = "rrrrrrrrrrrr,rNNmmmmmmrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrmmmmmmmmmmmmmmrrrrrrrnnnnnnnnnn%nnrrrmrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrmmmmmmmmmmmmmmmmmmmNmmmm";
    function charType(code) {                                                                                          // 7900
      if (code <= 0xf7) return lowTypes.charAt(code);                                                                  // 7901
      else if (0x590 <= code && code <= 0x5f4) return "R";                                                             // 7902
      else if (0x600 <= code && code <= 0x6ed) return arabicTypes.charAt(code - 0x600);                                // 7903
      else if (0x6ee <= code && code <= 0x8ac) return "r";                                                             // 7904
      else if (0x2000 <= code && code <= 0x200b) return "w";                                                           // 7905
      else if (code == 0x200c) return "b";                                                                             // 7906
      else return "L";                                                                                                 // 7907
    }                                                                                                                  // 7908
                                                                                                                       // 7909
    var bidiRE = /[\u0590-\u05f4\u0600-\u06ff\u0700-\u08ac]/;                                                          // 7910
    var isNeutral = /[stwN]/, isStrong = /[LRr]/, countsAsLeft = /[Lb1n]/, countsAsNum = /[1n]/;                       // 7911
    // Browsers seem to always treat the boundaries of block elements as being L.                                      // 7912
    var outerType = "L";                                                                                               // 7913
                                                                                                                       // 7914
    function BidiSpan(level, from, to) {                                                                               // 7915
      this.level = level;                                                                                              // 7916
      this.from = from; this.to = to;                                                                                  // 7917
    }                                                                                                                  // 7918
                                                                                                                       // 7919
    return function(str) {                                                                                             // 7920
      if (!bidiRE.test(str)) return false;                                                                             // 7921
      var len = str.length, types = [];                                                                                // 7922
      for (var i = 0, type; i < len; ++i)                                                                              // 7923
        types.push(type = charType(str.charCodeAt(i)));                                                                // 7924
                                                                                                                       // 7925
      // W1. Examine each non-spacing mark (NSM) in the level run, and                                                 // 7926
      // change the type of the NSM to the type of the previous                                                        // 7927
      // character. If the NSM is at the start of the level run, it will                                               // 7928
      // get the type of sor.                                                                                          // 7929
      for (var i = 0, prev = outerType; i < len; ++i) {                                                                // 7930
        var type = types[i];                                                                                           // 7931
        if (type == "m") types[i] = prev;                                                                              // 7932
        else prev = type;                                                                                              // 7933
      }                                                                                                                // 7934
                                                                                                                       // 7935
      // W2. Search backwards from each instance of a European number                                                  // 7936
      // until the first strong type (R, L, AL, or sor) is found. If an                                                // 7937
      // AL is found, change the type of the European number to Arabic                                                 // 7938
      // number.                                                                                                       // 7939
      // W3. Change all ALs to R.                                                                                      // 7940
      for (var i = 0, cur = outerType; i < len; ++i) {                                                                 // 7941
        var type = types[i];                                                                                           // 7942
        if (type == "1" && cur == "r") types[i] = "n";                                                                 // 7943
        else if (isStrong.test(type)) { cur = type; if (type == "r") types[i] = "R"; }                                 // 7944
      }                                                                                                                // 7945
                                                                                                                       // 7946
      // W4. A single European separator between two European numbers                                                  // 7947
      // changes to a European number. A single common separator between                                               // 7948
      // two numbers of the same type changes to that type.                                                            // 7949
      for (var i = 1, prev = types[0]; i < len - 1; ++i) {                                                             // 7950
        var type = types[i];                                                                                           // 7951
        if (type == "+" && prev == "1" && types[i+1] == "1") types[i] = "1";                                           // 7952
        else if (type == "," && prev == types[i+1] &&                                                                  // 7953
                 (prev == "1" || prev == "n")) types[i] = prev;                                                        // 7954
        prev = type;                                                                                                   // 7955
      }                                                                                                                // 7956
                                                                                                                       // 7957
      // W5. A sequence of European terminators adjacent to European                                                   // 7958
      // numbers changes to all European numbers.                                                                      // 7959
      // W6. Otherwise, separators and terminators change to Other                                                     // 7960
      // Neutral.                                                                                                      // 7961
      for (var i = 0; i < len; ++i) {                                                                                  // 7962
        var type = types[i];                                                                                           // 7963
        if (type == ",") types[i] = "N";                                                                               // 7964
        else if (type == "%") {                                                                                        // 7965
          for (var end = i + 1; end < len && types[end] == "%"; ++end) {}                                              // 7966
          var replace = (i && types[i-1] == "!") || (end < len && types[end] == "1") ? "1" : "N";                      // 7967
          for (var j = i; j < end; ++j) types[j] = replace;                                                            // 7968
          i = end - 1;                                                                                                 // 7969
        }                                                                                                              // 7970
      }                                                                                                                // 7971
                                                                                                                       // 7972
      // W7. Search backwards from each instance of a European number                                                  // 7973
      // until the first strong type (R, L, or sor) is found. If an L is                                               // 7974
      // found, then change the type of the European number to L.                                                      // 7975
      for (var i = 0, cur = outerType; i < len; ++i) {                                                                 // 7976
        var type = types[i];                                                                                           // 7977
        if (cur == "L" && type == "1") types[i] = "L";                                                                 // 7978
        else if (isStrong.test(type)) cur = type;                                                                      // 7979
      }                                                                                                                // 7980
                                                                                                                       // 7981
      // N1. A sequence of neutrals takes the direction of the                                                         // 7982
      // surrounding strong text if the text on both sides has the same                                                // 7983
      // direction. European and Arabic numbers act as if they were R in                                               // 7984
      // terms of their influence on neutrals. Start-of-level-run (sor)                                                // 7985
      // and end-of-level-run (eor) are used at level run boundaries.                                                  // 7986
      // N2. Any remaining neutrals take the embedding direction.                                                      // 7987
      for (var i = 0; i < len; ++i) {                                                                                  // 7988
        if (isNeutral.test(types[i])) {                                                                                // 7989
          for (var end = i + 1; end < len && isNeutral.test(types[end]); ++end) {}                                     // 7990
          var before = (i ? types[i-1] : outerType) == "L";                                                            // 7991
          var after = (end < len ? types[end] : outerType) == "L";                                                     // 7992
          var replace = before || after ? "L" : "R";                                                                   // 7993
          for (var j = i; j < end; ++j) types[j] = replace;                                                            // 7994
          i = end - 1;                                                                                                 // 7995
        }                                                                                                              // 7996
      }                                                                                                                // 7997
                                                                                                                       // 7998
      // Here we depart from the documented algorithm, in order to avoid                                               // 7999
      // building up an actual levels array. Since there are only three                                                // 8000
      // levels (0, 1, 2) in an implementation that doesn't take                                                       // 8001
      // explicit embedding into account, we can build up the order on                                                 // 8002
      // the fly, without following the level-based algorithm.                                                         // 8003
      var order = [], m;                                                                                               // 8004
      for (var i = 0; i < len;) {                                                                                      // 8005
        if (countsAsLeft.test(types[i])) {                                                                             // 8006
          var start = i;                                                                                               // 8007
          for (++i; i < len && countsAsLeft.test(types[i]); ++i) {}                                                    // 8008
          order.push(new BidiSpan(0, start, i));                                                                       // 8009
        } else {                                                                                                       // 8010
          var pos = i, at = order.length;                                                                              // 8011
          for (++i; i < len && types[i] != "L"; ++i) {}                                                                // 8012
          for (var j = pos; j < i;) {                                                                                  // 8013
            if (countsAsNum.test(types[j])) {                                                                          // 8014
              if (pos < j) order.splice(at, 0, new BidiSpan(1, pos, j));                                               // 8015
              var nstart = j;                                                                                          // 8016
              for (++j; j < i && countsAsNum.test(types[j]); ++j) {}                                                   // 8017
              order.splice(at, 0, new BidiSpan(2, nstart, j));                                                         // 8018
              pos = j;                                                                                                 // 8019
            } else ++j;                                                                                                // 8020
          }                                                                                                            // 8021
          if (pos < i) order.splice(at, 0, new BidiSpan(1, pos, i));                                                   // 8022
        }                                                                                                              // 8023
      }                                                                                                                // 8024
      if (order[0].level == 1 && (m = str.match(/^\s+/))) {                                                            // 8025
        order[0].from = m[0].length;                                                                                   // 8026
        order.unshift(new BidiSpan(0, 0, m[0].length));                                                                // 8027
      }                                                                                                                // 8028
      if (lst(order).level == 1 && (m = str.match(/\s+$/))) {                                                          // 8029
        lst(order).to -= m[0].length;                                                                                  // 8030
        order.push(new BidiSpan(0, len - m[0].length, len));                                                           // 8031
      }                                                                                                                // 8032
      if (order[0].level != lst(order).level)                                                                          // 8033
        order.push(new BidiSpan(order[0].level, len, len));                                                            // 8034
                                                                                                                       // 8035
      return order;                                                                                                    // 8036
    };                                                                                                                 // 8037
  })();                                                                                                                // 8038
                                                                                                                       // 8039
  // THE END                                                                                                           // 8040
                                                                                                                       // 8041
  CodeMirror.version = "4.12.0";                                                                                       // 8042
                                                                                                                       // 8043
  return CodeMirror;                                                                                                   // 8044
});                                                                                                                    // 8045
                                                                                                                       // 8046
                                                                                                // 8054
                                                                                                // 8055
                                                                                                // 8056
                                                                                                // 8057
                                                                                                // 8058
                                                                                                // 8059
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//                                                                                                                     //
// packages/mizzao:sharejs-codemirror/codemirror/addon/fold/foldcode.js                                                //
// This file is in bare mode and is not in its own closure.                                                            //
//                                                                                                                     //
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
                                                                                                                       //
// CodeMirror, copyright (c) by Marijn Haverbeke and others                                                            // 1
// Distributed under an MIT license: http://codemirror.net/LICENSE                                                     // 2
                                                                                                                       // 3
(function(mod) {                                                                                                       // 4
  if (typeof exports == "object" && typeof module == "object") // CommonJS                                             // 5
    mod(require("../../lib/codemirror"));                                                                              // 6
  else if (typeof define == "function" && define.amd) // AMD                                                           // 7
    define(["../../lib/codemirror"], mod);                                                                             // 8
  else // Plain browser env                                                                                            // 9
    mod(CodeMirror);                                                                                                   // 10
})(function(CodeMirror) {                                                                                              // 11
  "use strict";                                                                                                        // 12
                                                                                                                       // 13
  function doFold(cm, pos, options, force) {                                                                           // 14
    if (options && options.call) {                                                                                     // 15
      var finder = options;                                                                                            // 16
      options = null;                                                                                                  // 17
    } else {                                                                                                           // 18
      var finder = getOption(cm, options, "rangeFinder");                                                              // 19
    }                                                                                                                  // 20
    if (typeof pos == "number") pos = CodeMirror.Pos(pos, 0);                                                          // 21
    var minSize = getOption(cm, options, "minFoldSize");                                                               // 22
                                                                                                                       // 23
    function getRange(allowFolded) {                                                                                   // 24
      var range = finder(cm, pos);                                                                                     // 25
      if (!range || range.to.line - range.from.line < minSize) return null;                                            // 26
      var marks = cm.findMarksAt(range.from);                                                                          // 27
      for (var i = 0; i < marks.length; ++i) {                                                                         // 28
        if (marks[i].__isFold && force !== "fold") {                                                                   // 29
          if (!allowFolded) return null;                                                                               // 30
          range.cleared = true;                                                                                        // 31
          marks[i].clear();                                                                                            // 32
        }                                                                                                              // 33
      }                                                                                                                // 34
      return range;                                                                                                    // 35
    }                                                                                                                  // 36
                                                                                                                       // 37
    var range = getRange(true);                                                                                        // 38
    if (getOption(cm, options, "scanUp")) while (!range && pos.line > cm.firstLine()) {                                // 39
      pos = CodeMirror.Pos(pos.line - 1, 0);                                                                           // 40
      range = getRange(false);                                                                                         // 41
    }                                                                                                                  // 42
    if (!range || range.cleared || force === "unfold") return;                                                         // 43
                                                                                                                       // 44
    var myWidget = makeWidget(cm, options);                                                                            // 45
    CodeMirror.on(myWidget, "mousedown", function(e) {                                                                 // 46
      myRange.clear();                                                                                                 // 47
      CodeMirror.e_preventDefault(e);                                                                                  // 48
    });                                                                                                                // 49
    var myRange = cm.markText(range.from, range.to, {                                                                  // 50
      replacedWith: myWidget,                                                                                          // 51
      clearOnEnter: true,                                                                                              // 52
      __isFold: true                                                                                                   // 53
    });                                                                                                                // 54
    myRange.on("clear", function(from, to) {                                                                           // 55
      CodeMirror.signal(cm, "unfold", cm, from, to);                                                                   // 56
    });                                                                                                                // 57
    CodeMirror.signal(cm, "fold", cm, range.from, range.to);                                                           // 58
  }                                                                                                                    // 59
                                                                                                                       // 60
  function makeWidget(cm, options) {                                                                                   // 61
    var widget = getOption(cm, options, "widget");                                                                     // 62
    if (typeof widget == "string") {                                                                                   // 63
      var text = document.createTextNode(widget);                                                                      // 64
      widget = document.createElement("span");                                                                         // 65
      widget.appendChild(text);                                                                                        // 66
      widget.className = "CodeMirror-foldmarker";                                                                      // 67
    }                                                                                                                  // 68
    return widget;                                                                                                     // 69
  }                                                                                                                    // 70
                                                                                                                       // 71
  // Clumsy backwards-compatible interface                                                                             // 72
  CodeMirror.newFoldFunction = function(rangeFinder, widget) {                                                         // 73
    return function(cm, pos) { doFold(cm, pos, {rangeFinder: rangeFinder, widget: widget}); };                         // 74
  };                                                                                                                   // 75
                                                                                                                       // 76
  // New-style interface                                                                                               // 77
  CodeMirror.defineExtension("foldCode", function(pos, options, force) {                                               // 78
    doFold(this, pos, options, force);                                                                                 // 79
  });                                                                                                                  // 80
                                                                                                                       // 81
  CodeMirror.defineExtension("isFolded", function(pos) {                                                               // 82
    var marks = this.findMarksAt(pos);                                                                                 // 83
    for (var i = 0; i < marks.length; ++i)                                                                             // 84
      if (marks[i].__isFold) return true;                                                                              // 85
  });                                                                                                                  // 86
                                                                                                                       // 87
  CodeMirror.commands.toggleFold = function(cm) {                                                                      // 88
    cm.foldCode(cm.getCursor());                                                                                       // 89
  };                                                                                                                   // 90
  CodeMirror.commands.fold = function(cm) {                                                                            // 91
    cm.foldCode(cm.getCursor(), null, "fold");                                                                         // 92
  };                                                                                                                   // 93
  CodeMirror.commands.unfold = function(cm) {                                                                          // 94
    cm.foldCode(cm.getCursor(), null, "unfold");                                                                       // 95
  };                                                                                                                   // 96
  CodeMirror.commands.foldAll = function(cm) {                                                                         // 97
    cm.operation(function() {                                                                                          // 98
      for (var i = cm.firstLine(), e = cm.lastLine(); i <= e; i++)                                                     // 99
        cm.foldCode(CodeMirror.Pos(i, 0), null, "fold");                                                               // 100
    });                                                                                                                // 101
  };                                                                                                                   // 102
  CodeMirror.commands.unfoldAll = function(cm) {                                                                       // 103
    cm.operation(function() {                                                                                          // 104
      for (var i = cm.firstLine(), e = cm.lastLine(); i <= e; i++)                                                     // 105
        cm.foldCode(CodeMirror.Pos(i, 0), null, "unfold");                                                             // 106
    });                                                                                                                // 107
  };                                                                                                                   // 108
                                                                                                                       // 109
  CodeMirror.registerHelper("fold", "combine", function() {                                                            // 110
    var funcs = Array.prototype.slice.call(arguments, 0);                                                              // 111
    return function(cm, start) {                                                                                       // 112
      for (var i = 0; i < funcs.length; ++i) {                                                                         // 113
        var found = funcs[i](cm, start);                                                                               // 114
        if (found) return found;                                                                                       // 115
      }                                                                                                                // 116
    };                                                                                                                 // 117
  });                                                                                                                  // 118
                                                                                                                       // 119
  CodeMirror.registerHelper("fold", "auto", function(cm, start) {                                                      // 120
    var helpers = cm.getHelpers(start, "fold");                                                                        // 121
    for (var i = 0; i < helpers.length; i++) {                                                                         // 122
      var cur = helpers[i](cm, start);                                                                                 // 123
      if (cur) return cur;                                                                                             // 124
    }                                                                                                                  // 125
  });                                                                                                                  // 126
                                                                                                                       // 127
  var defaultOptions = {                                                                                               // 128
    rangeFinder: CodeMirror.fold.auto,                                                                                 // 129
    widget: "\u2194",                                                                                                  // 130
    minFoldSize: 0,                                                                                                    // 131
    scanUp: false                                                                                                      // 132
  };                                                                                                                   // 133
                                                                                                                       // 134
  CodeMirror.defineOption("foldOptions", null);                                                                        // 135
                                                                                                                       // 136
  function getOption(cm, options, name) {                                                                              // 137
    if (options && options[name] !== undefined)                                                                        // 138
      return options[name];                                                                                            // 139
    var editorOptions = cm.options.foldOptions;                                                                        // 140
    if (editorOptions && editorOptions[name] !== undefined)                                                            // 141
      return editorOptions[name];                                                                                      // 142
    return defaultOptions[name];                                                                                       // 143
  }                                                                                                                    // 144
                                                                                                                       // 145
  CodeMirror.defineExtension("foldOption", function(options, name) {                                                   // 146
    return getOption(this, options, name);                                                                             // 147
  });                                                                                                                  // 148
});                                                                                                                    // 149
                                                                                                                       // 150
                                                                                                // 8217
                                                                                                // 8218
                                                                                                // 8219
                                                                                                // 8220
                                                                                                // 8221
                                                                                                // 8222
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//                                                                                                                     //
// packages/mizzao:sharejs-codemirror/codemirror/addon/fold/foldgutter.js                                              //
// This file is in bare mode and is not in its own closure.                                                            //
//                                                                                                                     //
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
                                                                                                                       //
// CodeMirror, copyright (c) by Marijn Haverbeke and others                                                            // 1
// Distributed under an MIT license: http://codemirror.net/LICENSE                                                     // 2
                                                                                                                       // 3
(function(mod) {                                                                                                       // 4
  if (typeof exports == "object" && typeof module == "object") // CommonJS                                             // 5
    mod(require("../../lib/codemirror"), require("./foldcode"));                                                       // 6
  else if (typeof define == "function" && define.amd) // AMD                                                           // 7
    define(["../../lib/codemirror", "./foldcode"], mod);                                                               // 8
  else // Plain browser env                                                                                            // 9
    mod(CodeMirror);                                                                                                   // 10
})(function(CodeMirror) {                                                                                              // 11
  "use strict";                                                                                                        // 12
                                                                                                                       // 13
  CodeMirror.defineOption("foldGutter", false, function(cm, val, old) {                                                // 14
    if (old && old != CodeMirror.Init) {                                                                               // 15
      cm.clearGutter(cm.state.foldGutter.options.gutter);                                                              // 16
      cm.state.foldGutter = null;                                                                                      // 17
      cm.off("gutterClick", onGutterClick);                                                                            // 18
      cm.off("change", onChange);                                                                                      // 19
      cm.off("viewportChange", onViewportChange);                                                                      // 20
      cm.off("fold", onFold);                                                                                          // 21
      cm.off("unfold", onFold);                                                                                        // 22
      cm.off("swapDoc", updateInViewport);                                                                             // 23
    }                                                                                                                  // 24
    if (val) {                                                                                                         // 25
      cm.state.foldGutter = new State(parseOptions(val));                                                              // 26
      updateInViewport(cm);                                                                                            // 27
      cm.on("gutterClick", onGutterClick);                                                                             // 28
      cm.on("change", onChange);                                                                                       // 29
      cm.on("viewportChange", onViewportChange);                                                                       // 30
      cm.on("fold", onFold);                                                                                           // 31
      cm.on("unfold", onFold);                                                                                         // 32
      cm.on("swapDoc", updateInViewport);                                                                              // 33
    }                                                                                                                  // 34
  });                                                                                                                  // 35
                                                                                                                       // 36
  var Pos = CodeMirror.Pos;                                                                                            // 37
                                                                                                                       // 38
  function State(options) {                                                                                            // 39
    this.options = options;                                                                                            // 40
    this.from = this.to = 0;                                                                                           // 41
  }                                                                                                                    // 42
                                                                                                                       // 43
  function parseOptions(opts) {                                                                                        // 44
    if (opts === true) opts = {};                                                                                      // 45
    if (opts.gutter == null) opts.gutter = "CodeMirror-foldgutter";                                                    // 46
    if (opts.indicatorOpen == null) opts.indicatorOpen = "CodeMirror-foldgutter-open";                                 // 47
    if (opts.indicatorFolded == null) opts.indicatorFolded = "CodeMirror-foldgutter-folded";                           // 48
    return opts;                                                                                                       // 49
  }                                                                                                                    // 50
                                                                                                                       // 51
  function isFolded(cm, line) {                                                                                        // 52
    var marks = cm.findMarksAt(Pos(line));                                                                             // 53
    for (var i = 0; i < marks.length; ++i)                                                                             // 54
      if (marks[i].__isFold && marks[i].find().from.line == line) return true;                                         // 55
  }                                                                                                                    // 56
                                                                                                                       // 57
  function marker(spec) {                                                                                              // 58
    if (typeof spec == "string") {                                                                                     // 59
      var elt = document.createElement("div");                                                                         // 60
      elt.className = spec + " CodeMirror-guttermarker-subtle";                                                        // 61
      return elt;                                                                                                      // 62
    } else {                                                                                                           // 63
      return spec.cloneNode(true);                                                                                     // 64
    }                                                                                                                  // 65
  }                                                                                                                    // 66
                                                                                                                       // 67
  function updateFoldInfo(cm, from, to) {                                                                              // 68
    var opts = cm.state.foldGutter.options, cur = from;                                                                // 69
    var minSize = cm.foldOption(opts, "minFoldSize");                                                                  // 70
    var func = cm.foldOption(opts, "rangeFinder");                                                                     // 71
    cm.eachLine(from, to, function(line) {                                                                             // 72
      var mark = null;                                                                                                 // 73
      if (isFolded(cm, cur)) {                                                                                         // 74
        mark = marker(opts.indicatorFolded);                                                                           // 75
      } else {                                                                                                         // 76
        var pos = Pos(cur, 0);                                                                                         // 77
        var range = func && func(cm, pos);                                                                             // 78
        if (range && range.to.line - range.from.line >= minSize)                                                       // 79
          mark = marker(opts.indicatorOpen);                                                                           // 80
      }                                                                                                                // 81
      cm.setGutterMarker(line, opts.gutter, mark);                                                                     // 82
      ++cur;                                                                                                           // 83
    });                                                                                                                // 84
  }                                                                                                                    // 85
                                                                                                                       // 86
  function updateInViewport(cm) {                                                                                      // 87
    var vp = cm.getViewport(), state = cm.state.foldGutter;                                                            // 88
    if (!state) return;                                                                                                // 89
    cm.operation(function() {                                                                                          // 90
      updateFoldInfo(cm, vp.from, vp.to);                                                                              // 91
    });                                                                                                                // 92
    state.from = vp.from; state.to = vp.to;                                                                            // 93
  }                                                                                                                    // 94
                                                                                                                       // 95
  function onGutterClick(cm, line, gutter) {                                                                           // 96
    var opts = cm.state.foldGutter.options;                                                                            // 97
    if (gutter != opts.gutter) return;                                                                                 // 98
    cm.foldCode(Pos(line, 0), opts.rangeFinder);                                                                       // 99
  }                                                                                                                    // 100
                                                                                                                       // 101
  function onChange(cm) {                                                                                              // 102
    var state = cm.state.foldGutter, opts = cm.state.foldGutter.options;                                               // 103
    state.from = state.to = 0;                                                                                         // 104
    clearTimeout(state.changeUpdate);                                                                                  // 105
    state.changeUpdate = setTimeout(function() { updateInViewport(cm); }, opts.foldOnChangeTimeSpan || 600);           // 106
  }                                                                                                                    // 107
                                                                                                                       // 108
  function onViewportChange(cm) {                                                                                      // 109
    var state = cm.state.foldGutter, opts = cm.state.foldGutter.options;                                               // 110
    clearTimeout(state.changeUpdate);                                                                                  // 111
    state.changeUpdate = setTimeout(function() {                                                                       // 112
      var vp = cm.getViewport();                                                                                       // 113
      if (state.from == state.to || vp.from - state.to > 20 || state.from - vp.to > 20) {                              // 114
        updateInViewport(cm);                                                                                          // 115
      } else {                                                                                                         // 116
        cm.operation(function() {                                                                                      // 117
          if (vp.from < state.from) {                                                                                  // 118
            updateFoldInfo(cm, vp.from, state.from);                                                                   // 119
            state.from = vp.from;                                                                                      // 120
          }                                                                                                            // 121
          if (vp.to > state.to) {                                                                                      // 122
            updateFoldInfo(cm, state.to, vp.to);                                                                       // 123
            state.to = vp.to;                                                                                          // 124
          }                                                                                                            // 125
        });                                                                                                            // 126
      }                                                                                                                // 127
    }, opts.updateViewportTimeSpan || 400);                                                                            // 128
  }                                                                                                                    // 129
                                                                                                                       // 130
  function onFold(cm, from) {                                                                                          // 131
    var state = cm.state.foldGutter, line = from.line;                                                                 // 132
    if (line >= state.from && line < state.to)                                                                         // 133
      updateFoldInfo(cm, line, line + 1);                                                                              // 134
  }                                                                                                                    // 135
});                                                                                                                    // 136
                                                                                                                       // 137
                                                                                                // 8367
                                                                                                // 8368
                                                                                                // 8369
                                                                                                // 8370
                                                                                                // 8371
                                                                                                // 8372
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//                                                                                                                     //
// packages/mizzao:sharejs-codemirror/codemirror/addon/fold/indent-fold.js                                             //
// This file is in bare mode and is not in its own closure.                                                            //
//                                                                                                                     //
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
                                                                                                                       //
// CodeMirror, copyright (c) by Marijn Haverbeke and others                                                            // 1
// Distributed under an MIT license: http://codemirror.net/LICENSE                                                     // 2
                                                                                                                       // 3
(function(mod) {                                                                                                       // 4
  if (typeof exports == "object" && typeof module == "object") // CommonJS                                             // 5
    mod(require("../../lib/codemirror"));                                                                              // 6
  else if (typeof define == "function" && define.amd) // AMD                                                           // 7
    define(["../../lib/codemirror"], mod);                                                                             // 8
  else // Plain browser env                                                                                            // 9
    mod(CodeMirror);                                                                                                   // 10
})(function(CodeMirror) {                                                                                              // 11
"use strict";                                                                                                          // 12
                                                                                                                       // 13
CodeMirror.registerHelper("fold", "indent", function(cm, start) {                                                      // 14
  var tabSize = cm.getOption("tabSize"), firstLine = cm.getLine(start.line);                                           // 15
  if (!/\S/.test(firstLine)) return;                                                                                   // 16
  var getIndent = function(line) {                                                                                     // 17
    return CodeMirror.countColumn(line, null, tabSize);                                                                // 18
  };                                                                                                                   // 19
  var myIndent = getIndent(firstLine);                                                                                 // 20
  var lastLineInFold = null;                                                                                           // 21
  // Go through lines until we find a line that definitely doesn't belong in                                           // 22
  // the block we're folding, or to the end.                                                                           // 23
  for (var i = start.line + 1, end = cm.lastLine(); i <= end; ++i) {                                                   // 24
    var curLine = cm.getLine(i);                                                                                       // 25
    var curIndent = getIndent(curLine);                                                                                // 26
    if (curIndent > myIndent) {                                                                                        // 27
      // Lines with a greater indent are considered part of the block.                                                 // 28
      lastLineInFold = i;                                                                                              // 29
    } else if (!/\S/.test(curLine)) {                                                                                  // 30
      // Empty lines might be breaks within the block we're trying to fold.                                            // 31
    } else {                                                                                                           // 32
      // A non-empty line at an indent equal to or less than ours marks the                                            // 33
      // start of another block.                                                                                       // 34
      break;                                                                                                           // 35
    }                                                                                                                  // 36
  }                                                                                                                    // 37
  if (lastLineInFold) return {                                                                                         // 38
    from: CodeMirror.Pos(start.line, firstLine.length),                                                                // 39
    to: CodeMirror.Pos(lastLineInFold, cm.getLine(lastLineInFold).length)                                              // 40
  };                                                                                                                   // 41
});                                                                                                                    // 42
                                                                                                                       // 43
});                                                                                                                    // 44
                                                                                                                       // 45
                                                                                                // 8425
                                                                                                // 8426
                                                                                                // 8427
                                                                                                // 8428
                                                                                                // 8429
                                                                                                // 8430
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//                                                                                                                     //
// packages/mizzao:sharejs-codemirror/codemirror/addon/hint/show-hint.js                                               //
// This file is in bare mode and is not in its own closure.                                                            //
//                                                                                                                     //
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
                                                                                                                       //
// CodeMirror, copyright (c) by Marijn Haverbeke and others                                                            // 1
// Distributed under an MIT license: http://codemirror.net/LICENSE                                                     // 2
                                                                                                                       // 3
(function(mod) {                                                                                                       // 4
  if (typeof exports == "object" && typeof module == "object") // CommonJS                                             // 5
    mod(require("../../lib/codemirror"));                                                                              // 6
  else if (typeof define == "function" && define.amd) // AMD                                                           // 7
    define(["../../lib/codemirror"], mod);                                                                             // 8
  else // Plain browser env                                                                                            // 9
    mod(CodeMirror);                                                                                                   // 10
})(function(CodeMirror) {                                                                                              // 11
  "use strict";                                                                                                        // 12
                                                                                                                       // 13
  var HINT_ELEMENT_CLASS        = "CodeMirror-hint";                                                                   // 14
  var ACTIVE_HINT_ELEMENT_CLASS = "CodeMirror-hint-active";                                                            // 15
                                                                                                                       // 16
  // This is the old interface, kept around for now to stay                                                            // 17
  // backwards-compatible.                                                                                             // 18
  CodeMirror.showHint = function(cm, getHints, options) {                                                              // 19
    if (!getHints) return cm.showHint(options);                                                                        // 20
    if (options && options.async) getHints.async = true;                                                               // 21
    var newOpts = {hint: getHints};                                                                                    // 22
    if (options) for (var prop in options) newOpts[prop] = options[prop];                                              // 23
    return cm.showHint(newOpts);                                                                                       // 24
  };                                                                                                                   // 25
                                                                                                                       // 26
  CodeMirror.defineExtension("showHint", function(options) {                                                           // 27
    // We want a single cursor position.                                                                               // 28
    if (this.listSelections().length > 1 || this.somethingSelected()) return;                                          // 29
                                                                                                                       // 30
    if (this.state.completionActive) this.state.completionActive.close();                                              // 31
    var completion = this.state.completionActive = new Completion(this, options);                                      // 32
    var getHints = completion.options.hint;                                                                            // 33
    if (!getHints) return;                                                                                             // 34
                                                                                                                       // 35
    CodeMirror.signal(this, "startCompletion", this);                                                                  // 36
    if (getHints.async)                                                                                                // 37
      getHints(this, function(hints) { completion.showHints(hints); }, completion.options);                            // 38
    else                                                                                                               // 39
      return completion.showHints(getHints(this, completion.options));                                                 // 40
  });                                                                                                                  // 41
                                                                                                                       // 42
  function Completion(cm, options) {                                                                                   // 43
    this.cm = cm;                                                                                                      // 44
    this.options = this.buildOptions(options);                                                                         // 45
    this.widget = this.onClose = null;                                                                                 // 46
  }                                                                                                                    // 47
                                                                                                                       // 48
  Completion.prototype = {                                                                                             // 49
    close: function() {                                                                                                // 50
      if (!this.active()) return;                                                                                      // 51
      this.cm.state.completionActive = null;                                                                           // 52
                                                                                                                       // 53
      if (this.widget) this.widget.close();                                                                            // 54
      if (this.onClose) this.onClose();                                                                                // 55
      CodeMirror.signal(this.cm, "endCompletion", this.cm);                                                            // 56
    },                                                                                                                 // 57
                                                                                                                       // 58
    active: function() {                                                                                               // 59
      return this.cm.state.completionActive == this;                                                                   // 60
    },                                                                                                                 // 61
                                                                                                                       // 62
    pick: function(data, i) {                                                                                          // 63
      var completion = data.list[i];                                                                                   // 64
      if (completion.hint) completion.hint(this.cm, data, completion);                                                 // 65
      else this.cm.replaceRange(getText(completion), completion.from || data.from,                                     // 66
                                completion.to || data.to, "complete");                                                 // 67
      CodeMirror.signal(data, "pick", completion);                                                                     // 68
      this.close();                                                                                                    // 69
    },                                                                                                                 // 70
                                                                                                                       // 71
    showHints: function(data) {                                                                                        // 72
      if (!data || !data.list.length || !this.active()) return this.close();                                           // 73
                                                                                                                       // 74
      if (this.options.completeSingle && data.list.length == 1)                                                        // 75
        this.pick(data, 0);                                                                                            // 76
      else                                                                                                             // 77
        this.showWidget(data);                                                                                         // 78
    },                                                                                                                 // 79
                                                                                                                       // 80
    showWidget: function(data) {                                                                                       // 81
      this.widget = new Widget(this, data);                                                                            // 82
      CodeMirror.signal(data, "shown");                                                                                // 83
                                                                                                                       // 84
      var debounce = 0, completion = this, finished;                                                                   // 85
      var closeOn = this.options.closeCharacters;                                                                      // 86
      var startPos = this.cm.getCursor(), startLen = this.cm.getLine(startPos.line).length;                            // 87
                                                                                                                       // 88
      var requestAnimationFrame = window.requestAnimationFrame || function(fn) {                                       // 89
        return setTimeout(fn, 1000/60);                                                                                // 90
      };                                                                                                               // 91
      var cancelAnimationFrame = window.cancelAnimationFrame || clearTimeout;                                          // 92
                                                                                                                       // 93
      function done() {                                                                                                // 94
        if (finished) return;                                                                                          // 95
        finished = true;                                                                                               // 96
        completion.close();                                                                                            // 97
        completion.cm.off("cursorActivity", activity);                                                                 // 98
        if (data) CodeMirror.signal(data, "close");                                                                    // 99
      }                                                                                                                // 100
                                                                                                                       // 101
      function update() {                                                                                              // 102
        if (finished) return;                                                                                          // 103
        CodeMirror.signal(data, "update");                                                                             // 104
        var getHints = completion.options.hint;                                                                        // 105
        if (getHints.async)                                                                                            // 106
          getHints(completion.cm, finishUpdate, completion.options);                                                   // 107
        else                                                                                                           // 108
          finishUpdate(getHints(completion.cm, completion.options));                                                   // 109
      }                                                                                                                // 110
      function finishUpdate(data_) {                                                                                   // 111
        data = data_;                                                                                                  // 112
        if (finished) return;                                                                                          // 113
        if (!data || !data.list.length) return done();                                                                 // 114
        if (completion.widget) completion.widget.close();                                                              // 115
        completion.widget = new Widget(completion, data);                                                              // 116
      }                                                                                                                // 117
                                                                                                                       // 118
      function clearDebounce() {                                                                                       // 119
        if (debounce) {                                                                                                // 120
          cancelAnimationFrame(debounce);                                                                              // 121
          debounce = 0;                                                                                                // 122
        }                                                                                                              // 123
      }                                                                                                                // 124
                                                                                                                       // 125
      function activity() {                                                                                            // 126
        clearDebounce();                                                                                               // 127
        var pos = completion.cm.getCursor(), line = completion.cm.getLine(pos.line);                                   // 128
        if (pos.line != startPos.line || line.length - pos.ch != startLen - startPos.ch ||                             // 129
            pos.ch < startPos.ch || completion.cm.somethingSelected() ||                                               // 130
            (pos.ch && closeOn.test(line.charAt(pos.ch - 1)))) {                                                       // 131
          completion.close();                                                                                          // 132
        } else {                                                                                                       // 133
          debounce = requestAnimationFrame(update);                                                                    // 134
          if (completion.widget) completion.widget.close();                                                            // 135
        }                                                                                                              // 136
      }                                                                                                                // 137
      this.cm.on("cursorActivity", activity);                                                                          // 138
      this.onClose = done;                                                                                             // 139
    },                                                                                                                 // 140
                                                                                                                       // 141
    buildOptions: function(options) {                                                                                  // 142
      var editor = this.cm.options.hintOptions;                                                                        // 143
      var out = {};                                                                                                    // 144
      for (var prop in defaultOptions) out[prop] = defaultOptions[prop];                                               // 145
      if (editor) for (var prop in editor)                                                                             // 146
        if (editor[prop] !== undefined) out[prop] = editor[prop];                                                      // 147
      if (options) for (var prop in options)                                                                           // 148
        if (options[prop] !== undefined) out[prop] = options[prop];                                                    // 149
      return out;                                                                                                      // 150
    }                                                                                                                  // 151
  };                                                                                                                   // 152
                                                                                                                       // 153
  function getText(completion) {                                                                                       // 154
    if (typeof completion == "string") return completion;                                                              // 155
    else return completion.text;                                                                                       // 156
  }                                                                                                                    // 157
                                                                                                                       // 158
  function buildKeyMap(completion, handle) {                                                                           // 159
    var baseMap = {                                                                                                    // 160
      Up: function() {handle.moveFocus(-1);},                                                                          // 161
      Down: function() {handle.moveFocus(1);},                                                                         // 162
      PageUp: function() {handle.moveFocus(-handle.menuSize() + 1, true);},                                            // 163
      PageDown: function() {handle.moveFocus(handle.menuSize() - 1, true);},                                           // 164
      Home: function() {handle.setFocus(0);},                                                                          // 165
      End: function() {handle.setFocus(handle.length - 1);},                                                           // 166
      Enter: handle.pick,                                                                                              // 167
      Tab: handle.pick,                                                                                                // 168
      Esc: handle.close                                                                                                // 169
    };                                                                                                                 // 170
    var custom = completion.options.customKeys;                                                                        // 171
    var ourMap = custom ? {} : baseMap;                                                                                // 172
    function addBinding(key, val) {                                                                                    // 173
      var bound;                                                                                                       // 174
      if (typeof val != "string")                                                                                      // 175
        bound = function(cm) { return val(cm, handle); };                                                              // 176
      // This mechanism is deprecated                                                                                  // 177
      else if (baseMap.hasOwnProperty(val))                                                                            // 178
        bound = baseMap[val];                                                                                          // 179
      else                                                                                                             // 180
        bound = val;                                                                                                   // 181
      ourMap[key] = bound;                                                                                             // 182
    }                                                                                                                  // 183
    if (custom)                                                                                                        // 184
      for (var key in custom) if (custom.hasOwnProperty(key))                                                          // 185
        addBinding(key, custom[key]);                                                                                  // 186
    var extra = completion.options.extraKeys;                                                                          // 187
    if (extra)                                                                                                         // 188
      for (var key in extra) if (extra.hasOwnProperty(key))                                                            // 189
        addBinding(key, extra[key]);                                                                                   // 190
    return ourMap;                                                                                                     // 191
  }                                                                                                                    // 192
                                                                                                                       // 193
  function getHintElement(hintsElement, el) {                                                                          // 194
    while (el && el != hintsElement) {                                                                                 // 195
      if (el.nodeName.toUpperCase() === "LI" && el.parentNode == hintsElement) return el;                              // 196
      el = el.parentNode;                                                                                              // 197
    }                                                                                                                  // 198
  }                                                                                                                    // 199
                                                                                                                       // 200
  function Widget(completion, data) {                                                                                  // 201
    this.completion = completion;                                                                                      // 202
    this.data = data;                                                                                                  // 203
    var widget = this, cm = completion.cm;                                                                             // 204
                                                                                                                       // 205
    var hints = this.hints = document.createElement("ul");                                                             // 206
    hints.className = "CodeMirror-hints";                                                                              // 207
    this.selectedHint = data.selectedHint || 0;                                                                        // 208
                                                                                                                       // 209
    var completions = data.list;                                                                                       // 210
    for (var i = 0; i < completions.length; ++i) {                                                                     // 211
      var elt = hints.appendChild(document.createElement("li")), cur = completions[i];                                 // 212
      var className = HINT_ELEMENT_CLASS + (i != this.selectedHint ? "" : " " + ACTIVE_HINT_ELEMENT_CLASS);            // 213
      if (cur.className != null) className = cur.className + " " + className;                                          // 214
      elt.className = className;                                                                                       // 215
      if (cur.render) cur.render(elt, data, cur);                                                                      // 216
      else elt.appendChild(document.createTextNode(cur.displayText || getText(cur)));                                  // 217
      elt.hintId = i;                                                                                                  // 218
    }                                                                                                                  // 219
                                                                                                                       // 220
    var pos = cm.cursorCoords(completion.options.alignWithWord ? data.from : null);                                    // 221
    var left = pos.left, top = pos.bottom, below = true;                                                               // 222
    hints.style.left = left + "px";                                                                                    // 223
    hints.style.top = top + "px";                                                                                      // 224
    // If we're at the edge of the screen, then we want the menu to appear on the left of the cursor.                  // 225
    var winW = window.innerWidth || Math.max(document.body.offsetWidth, document.documentElement.offsetWidth);         // 226
    var winH = window.innerHeight || Math.max(document.body.offsetHeight, document.documentElement.offsetHeight);      // 227
    (completion.options.container || document.body).appendChild(hints);                                                // 228
    var box = hints.getBoundingClientRect(), overlapY = box.bottom - winH;                                             // 229
    if (overlapY > 0) {                                                                                                // 230
      var height = box.bottom - box.top, curTop = pos.top - (pos.bottom - box.top);                                    // 231
      if (curTop - height > 0) { // Fits above cursor                                                                  // 232
        hints.style.top = (top = pos.top - height) + "px";                                                             // 233
        below = false;                                                                                                 // 234
      } else if (height > winH) {                                                                                      // 235
        hints.style.height = (winH - 5) + "px";                                                                        // 236
        hints.style.top = (top = pos.bottom - box.top) + "px";                                                         // 237
        var cursor = cm.getCursor();                                                                                   // 238
        if (data.from.ch != cursor.ch) {                                                                               // 239
          pos = cm.cursorCoords(cursor);                                                                               // 240
          hints.style.left = (left = pos.left) + "px";                                                                 // 241
          box = hints.getBoundingClientRect();                                                                         // 242
        }                                                                                                              // 243
      }                                                                                                                // 244
    }                                                                                                                  // 245
    var overlapX = box.right - winW;                                                                                   // 246
    if (overlapX > 0) {                                                                                                // 247
      if (box.right - box.left > winW) {                                                                               // 248
        hints.style.width = (winW - 5) + "px";                                                                         // 249
        overlapX -= (box.right - box.left) - winW;                                                                     // 250
      }                                                                                                                // 251
      hints.style.left = (left = pos.left - overlapX) + "px";                                                          // 252
    }                                                                                                                  // 253
                                                                                                                       // 254
    cm.addKeyMap(this.keyMap = buildKeyMap(completion, {                                                               // 255
      moveFocus: function(n, avoidWrap) { widget.changeActive(widget.selectedHint + n, avoidWrap); },                  // 256
      setFocus: function(n) { widget.changeActive(n); },                                                               // 257
      menuSize: function() { return widget.screenAmount(); },                                                          // 258
      length: completions.length,                                                                                      // 259
      close: function() { completion.close(); },                                                                       // 260
      pick: function() { widget.pick(); },                                                                             // 261
      data: data                                                                                                       // 262
    }));                                                                                                               // 263
                                                                                                                       // 264
    if (completion.options.closeOnUnfocus) {                                                                           // 265
      var closingOnBlur;                                                                                               // 266
      cm.on("blur", this.onBlur = function() { closingOnBlur = setTimeout(function() { completion.close(); }, 100); });
      cm.on("focus", this.onFocus = function() { clearTimeout(closingOnBlur); });                                      // 268
    }                                                                                                                  // 269
                                                                                                                       // 270
    var startScroll = cm.getScrollInfo();                                                                              // 271
    cm.on("scroll", this.onScroll = function() {                                                                       // 272
      var curScroll = cm.getScrollInfo(), editor = cm.getWrapperElement().getBoundingClientRect();                     // 273
      var newTop = top + startScroll.top - curScroll.top;                                                              // 274
      var point = newTop - (window.pageYOffset || (document.documentElement || document.body).scrollTop);              // 275
      if (!below) point += hints.offsetHeight;                                                                         // 276
      if (point <= editor.top || point >= editor.bottom) return completion.close();                                    // 277
      hints.style.top = newTop + "px";                                                                                 // 278
      hints.style.left = (left + startScroll.left - curScroll.left) + "px";                                            // 279
    });                                                                                                                // 280
                                                                                                                       // 281
    CodeMirror.on(hints, "dblclick", function(e) {                                                                     // 282
      var t = getHintElement(hints, e.target || e.srcElement);                                                         // 283
      if (t && t.hintId != null) {widget.changeActive(t.hintId); widget.pick();}                                       // 284
    });                                                                                                                // 285
                                                                                                                       // 286
    CodeMirror.on(hints, "click", function(e) {                                                                        // 287
      var t = getHintElement(hints, e.target || e.srcElement);                                                         // 288
      if (t && t.hintId != null) {                                                                                     // 289
        widget.changeActive(t.hintId);                                                                                 // 290
        if (completion.options.completeOnSingleClick) widget.pick();                                                   // 291
      }                                                                                                                // 292
    });                                                                                                                // 293
                                                                                                                       // 294
    CodeMirror.on(hints, "mousedown", function() {                                                                     // 295
      setTimeout(function(){cm.focus();}, 20);                                                                         // 296
    });                                                                                                                // 297
                                                                                                                       // 298
    CodeMirror.signal(data, "select", completions[0], hints.firstChild);                                               // 299
    return true;                                                                                                       // 300
  }                                                                                                                    // 301
                                                                                                                       // 302
  Widget.prototype = {                                                                                                 // 303
    close: function() {                                                                                                // 304
      if (this.completion.widget != this) return;                                                                      // 305
      this.completion.widget = null;                                                                                   // 306
      this.hints.parentNode.removeChild(this.hints);                                                                   // 307
      this.completion.cm.removeKeyMap(this.keyMap);                                                                    // 308
                                                                                                                       // 309
      var cm = this.completion.cm;                                                                                     // 310
      if (this.completion.options.closeOnUnfocus) {                                                                    // 311
        cm.off("blur", this.onBlur);                                                                                   // 312
        cm.off("focus", this.onFocus);                                                                                 // 313
      }                                                                                                                // 314
      cm.off("scroll", this.onScroll);                                                                                 // 315
    },                                                                                                                 // 316
                                                                                                                       // 317
    pick: function() {                                                                                                 // 318
      this.completion.pick(this.data, this.selectedHint);                                                              // 319
    },                                                                                                                 // 320
                                                                                                                       // 321
    changeActive: function(i, avoidWrap) {                                                                             // 322
      if (i >= this.data.list.length)                                                                                  // 323
        i = avoidWrap ? this.data.list.length - 1 : 0;                                                                 // 324
      else if (i < 0)                                                                                                  // 325
        i = avoidWrap ? 0  : this.data.list.length - 1;                                                                // 326
      if (this.selectedHint == i) return;                                                                              // 327
      var node = this.hints.childNodes[this.selectedHint];                                                             // 328
      node.className = node.className.replace(" " + ACTIVE_HINT_ELEMENT_CLASS, "");                                    // 329
      node = this.hints.childNodes[this.selectedHint = i];                                                             // 330
      node.className += " " + ACTIVE_HINT_ELEMENT_CLASS;                                                               // 331
      if (node.offsetTop < this.hints.scrollTop)                                                                       // 332
        this.hints.scrollTop = node.offsetTop - 3;                                                                     // 333
      else if (node.offsetTop + node.offsetHeight > this.hints.scrollTop + this.hints.clientHeight)                    // 334
        this.hints.scrollTop = node.offsetTop + node.offsetHeight - this.hints.clientHeight + 3;                       // 335
      CodeMirror.signal(this.data, "select", this.data.list[this.selectedHint], node);                                 // 336
    },                                                                                                                 // 337
                                                                                                                       // 338
    screenAmount: function() {                                                                                         // 339
      return Math.floor(this.hints.clientHeight / this.hints.firstChild.offsetHeight) || 1;                            // 340
    }                                                                                                                  // 341
  };                                                                                                                   // 342
                                                                                                                       // 343
  CodeMirror.registerHelper("hint", "auto", function(cm, options) {                                                    // 344
    var helpers = cm.getHelpers(cm.getCursor(), "hint"), words;                                                        // 345
    if (helpers.length) {                                                                                              // 346
      for (var i = 0; i < helpers.length; i++) {                                                                       // 347
        var cur = helpers[i](cm, options);                                                                             // 348
        if (cur && cur.list.length) return cur;                                                                        // 349
      }                                                                                                                // 350
    } else if (words = cm.getHelper(cm.getCursor(), "hintWords")) {                                                    // 351
      if (words) return CodeMirror.hint.fromList(cm, {words: words});                                                  // 352
    } else if (CodeMirror.hint.anyword) {                                                                              // 353
      return CodeMirror.hint.anyword(cm, options);                                                                     // 354
    }                                                                                                                  // 355
  });                                                                                                                  // 356
                                                                                                                       // 357
  CodeMirror.registerHelper("hint", "fromList", function(cm, options) {                                                // 358
    var cur = cm.getCursor(), token = cm.getTokenAt(cur);                                                              // 359
    var found = [];                                                                                                    // 360
    for (var i = 0; i < options.words.length; i++) {                                                                   // 361
      var word = options.words[i];                                                                                     // 362
      if (word.slice(0, token.string.length) == token.string)                                                          // 363
        found.push(word);                                                                                              // 364
    }                                                                                                                  // 365
                                                                                                                       // 366
    if (found.length) return {                                                                                         // 367
      list: found,                                                                                                     // 368
      from: CodeMirror.Pos(cur.line, token.start),                                                                     // 369
            to: CodeMirror.Pos(cur.line, token.end)                                                                    // 370
    };                                                                                                                 // 371
  });                                                                                                                  // 372
                                                                                                                       // 373
  CodeMirror.commands.autocomplete = CodeMirror.showHint;                                                              // 374
                                                                                                                       // 375
  var defaultOptions = {                                                                                               // 376
    hint: CodeMirror.hint.auto,                                                                                        // 377
    completeSingle: true,                                                                                              // 378
    alignWithWord: true,                                                                                               // 379
    closeCharacters: /[\s()\[\]{};:>,]/,                                                                               // 380
    closeOnUnfocus: true,                                                                                              // 381
    completeOnSingleClick: false,                                                                                      // 382
    container: null,                                                                                                   // 383
    customKeys: null,                                                                                                  // 384
    extraKeys: null                                                                                                    // 385
  };                                                                                                                   // 386
                                                                                                                       // 387
  CodeMirror.defineOption("hintOptions", null);                                                                        // 388
});                                                                                                                    // 389
                                                                                                                       // 390
                                                                                                // 8828
                                                                                                // 8829
                                                                                                // 8830
                                                                                                // 8831
                                                                                                // 8832
                                                                                                // 8833
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//                                                                                                                     //
// packages/mizzao:sharejs-codemirror/codemirror/addon/display/placeholder.js                                          //
// This file is in bare mode and is not in its own closure.                                                            //
//                                                                                                                     //
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
                                                                                                                       //
// CodeMirror, copyright (c) by Marijn Haverbeke and others                                                            // 1
// Distributed under an MIT license: http://codemirror.net/LICENSE                                                     // 2
                                                                                                                       // 3
(function(mod) {                                                                                                       // 4
  if (typeof exports == "object" && typeof module == "object") // CommonJS                                             // 5
    mod(require("../../lib/codemirror"));                                                                              // 6
  else if (typeof define == "function" && define.amd) // AMD                                                           // 7
    define(["../../lib/codemirror"], mod);                                                                             // 8
  else // Plain browser env                                                                                            // 9
    mod(CodeMirror);                                                                                                   // 10
})(function(CodeMirror) {                                                                                              // 11
  CodeMirror.defineOption("placeholder", "", function(cm, val, old) {                                                  // 12
    var prev = old && old != CodeMirror.Init;                                                                          // 13
    if (val && !prev) {                                                                                                // 14
      cm.on("blur", onBlur);                                                                                           // 15
      cm.on("change", onChange);                                                                                       // 16
      onChange(cm);                                                                                                    // 17
    } else if (!val && prev) {                                                                                         // 18
      cm.off("blur", onBlur);                                                                                          // 19
      cm.off("change", onChange);                                                                                      // 20
      clearPlaceholder(cm);                                                                                            // 21
      var wrapper = cm.getWrapperElement();                                                                            // 22
      wrapper.className = wrapper.className.replace(" CodeMirror-empty", "");                                          // 23
    }                                                                                                                  // 24
                                                                                                                       // 25
    if (val && !cm.hasFocus()) onBlur(cm);                                                                             // 26
  });                                                                                                                  // 27
                                                                                                                       // 28
  function clearPlaceholder(cm) {                                                                                      // 29
    if (cm.state.placeholder) {                                                                                        // 30
      cm.state.placeholder.parentNode.removeChild(cm.state.placeholder);                                               // 31
      cm.state.placeholder = null;                                                                                     // 32
    }                                                                                                                  // 33
  }                                                                                                                    // 34
  function setPlaceholder(cm) {                                                                                        // 35
    clearPlaceholder(cm);                                                                                              // 36
    var elt = cm.state.placeholder = document.createElement("pre");                                                    // 37
    elt.style.cssText = "height: 0; overflow: visible";                                                                // 38
    elt.className = "CodeMirror-placeholder";                                                                          // 39
    elt.appendChild(document.createTextNode(cm.getOption("placeholder")));                                             // 40
    cm.display.lineSpace.insertBefore(elt, cm.display.lineSpace.firstChild);                                           // 41
  }                                                                                                                    // 42
                                                                                                                       // 43
  function onBlur(cm) {                                                                                                // 44
    if (isEmpty(cm)) setPlaceholder(cm);                                                                               // 45
  }                                                                                                                    // 46
  function onChange(cm) {                                                                                              // 47
    var wrapper = cm.getWrapperElement(), empty = isEmpty(cm);                                                         // 48
    wrapper.className = wrapper.className.replace(" CodeMirror-empty", "") + (empty ? " CodeMirror-empty" : "");       // 49
                                                                                                                       // 50
    if (empty) setPlaceholder(cm);                                                                                     // 51
    else clearPlaceholder(cm);                                                                                         // 52
  }                                                                                                                    // 53
                                                                                                                       // 54
  function isEmpty(cm) {                                                                                               // 55
    return (cm.lineCount() === 1) && (cm.getLine(0) === "");                                                           // 56
  }                                                                                                                    // 57
});                                                                                                                    // 58
                                                                                                                       // 59
                                                                                                // 8900
                                                                                                // 8901
                                                                                                // 8902
                                                                                                // 8903
                                                                                                // 8904
                                                                                                // 8905
(function () {                                                                                  // 8906
                                                                                                // 8907
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//                                                                                                                     //
// packages/mizzao:sharejs-codemirror/template.templates.js                                                            //
//                                                                                                                     //
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
                                                                                                                       //
                                                                                                                       // 1
Template.__checkName("_sharejsCM");                                                                                    // 2
Template["_sharejsCM"] = new Template("Template._sharejsCM", (function() {                                             // 3
  var view = this;                                                                                                     // 4
  return HTML.DIV({                                                                                                    // 5
    "data-docid": function() {                                                                                         // 6
      return Spacebars.mustache(view.lookup("docid"));                                                                 // 7
    },                                                                                                                 // 8
    "class": "shareJSCM"                                                                                               // 9
  });                                                                                                                  // 10
}));                                                                                                                   // 11
                                                                                                                       // 12
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
                                                                                                // 8927
}).call(this);                                                                                  // 8928
                                                                                                // 8929
                                                                                                // 8930
                                                                                                // 8931
                                                                                                // 8932
                                                                                                // 8933
                                                                                                // 8934
(function () {                                                                                  // 8935
                                                                                                // 8936
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//                                                                                                                     //
// packages/mizzao:sharejs-codemirror/client.coffee.js                                                                 //
//                                                                                                                     //
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
                                                                                                                       //
__coffeescriptShare = typeof __coffeescriptShare === 'object' ? __coffeescriptShare : {}; var share = __coffeescriptShare;
var ShareJSCMConnector,                                                                         // 8944
  __hasProp = {}.hasOwnProperty,                                                                // 8945
  __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };
                                                                                                // 8947
ShareJSCMConnector = (function(_super) {                                                        // 8948
  __extends(ShareJSCMConnector, _super);                                                        // 8949
                                                                                                // 8950
  function ShareJSCMConnector() {                                                               // 8951
    return ShareJSCMConnector.__super__.constructor.apply(this, arguments);                     // 8952
  }                                                                                             // 8953
                                                                                                // 8954
  ShareJSCMConnector.prototype.createView = function() {                                        // 8955
    return Blaze.With(Blaze.getData, function() {                                               // 8956
      return Template._sharejsCM;                                                               // 8957
    });                                                                                         // 8958
  };                                                                                            // 8959
                                                                                                // 8960
  ShareJSCMConnector.prototype.rendered = function(element) {                                   // 8961
    ShareJSCMConnector.__super__.rendered.apply(this, arguments);                               // 8962
    this.cm = new CodeMirror(element);                                                          // 8963
    return typeof this.configCallback === "function" ? this.configCallback(this.cm) : void 0;   // 8964
  };                                                                                            // 8965
                                                                                                // 8966
  ShareJSCMConnector.prototype.connect = function() {                                           // 8967
    this.cm.readOnly = true;                                                                    // 8968
    return ShareJSCMConnector.__super__.connect.apply(this, arguments);                         // 8969
  };                                                                                            // 8970
                                                                                                // 8971
  ShareJSCMConnector.prototype.attach = function(doc) {                                         // 8972
    ShareJSCMConnector.__super__.attach.apply(this, arguments);                                 // 8973
    doc.attach_cm(this.cm);                                                                     // 8974
    this.cm.readOnly = false;                                                                   // 8975
    return typeof this.connectCallback === "function" ? this.connectCallback(this.cm) : void 0;
  };                                                                                            // 8977
                                                                                                // 8978
  ShareJSCMConnector.prototype.disconnect = function() {                                        // 8979
    var _ref;                                                                                   // 8980
    if ((_ref = this.doc) != null) {                                                            // 8981
      if (typeof _ref.detach_cm === "function") {                                               // 8982
        _ref.detach_cm();                                                                       // 8983
      }                                                                                         // 8984
    }                                                                                           // 8985
    return ShareJSCMConnector.__super__.disconnect.apply(this, arguments);                      // 8986
  };                                                                                            // 8987
                                                                                                // 8988
  ShareJSCMConnector.prototype.destroy = function() {                                           // 8989
    ShareJSCMConnector.__super__.destroy.apply(this, arguments);                                // 8990
    return this.cm = null;                                                                      // 8991
  };                                                                                            // 8992
                                                                                                // 8993
  return ShareJSCMConnector;                                                                    // 8994
                                                                                                // 8995
})(ShareJSConnector);                                                                           // 8996
                                                                                                // 8997
UI.registerHelper("sharejsCM", new Template('sharejsCM', function() {                           // 8998
  return new ShareJSCMConnector(this).create();                                                 // 8999
}));                                                                                            // 9000
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
                                                                                                // 9002
}).call(this);                                                                                  // 9003
                                                                                                // 9004
//////////////////////////////////////////////////////////////////////////////////////////////////

}).call(this);


/* Exports */
if (typeof Package === 'undefined') Package = {};
Package['mizzao:sharejs-codemirror'] = {};

})();
